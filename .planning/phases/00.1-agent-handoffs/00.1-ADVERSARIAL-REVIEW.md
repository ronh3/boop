# Phase 00.1 — Adversarial Review

Status: pending independent Claude review. This scaffold contains no findings,
review decision, correction disposition, or phase acceptance.

Claude appends dated findings and re-review evidence here under `AGENTS.md`.

---

# Claude independent adversarial review — 2026-09-04

**Reviewed SHA:** `a9a8e72caf1232ed516eea752229f5de250b978e`
**Branch:** `phase/00.1-agent-handoffs`
**Branch HEAD at review time:** `ae3e7699adf91bd82c356f542bba1912f15a70a5`
**Main baseline:** `0fdce4897c858176af882618ab016621a8221bcb`
**Reviewer:** Claude (independent adversarial reviewer, `AGENTS.md:14-17`)
**Handoff:** `.planning/CLAUDE-NEXT.md` at `ae3e769`, `status: ready`,
`mode: active_phase`, `task_base_sha`/`review_target_sha` `a9a8e72`
**Disposition:** Findings recorded only. Claude records no dispositions, no
resolutions, no corrections, and no phase closure. Phase 00.1 is **not**
declared complete.

## Review scope

Complete `0fdce489..a9a8e72` diff (13 files, +262/-46), plus the full current
text of `AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `.planning/STATE.md`,
`.planning/ROADMAP.md`, `.planning/CODEX-NEXT.md`, `.planning/CLAUDE-NEXT.md`,
the four `00.1-*` phase artifacts, `.planning/phases/00-lightweight-agent-workflow/00-UAT.md`,
`tools/workflow_guard.py`, `tools/check_release_gates.py`,
`tests/test_workflow_gates.py`, and `.github/workflows/main.yml`. The
handoff-delivery commit `ae3e769` was inspected for boundary purposes but is
outside the named review target; see M-01.

Reviewed against the fixed scope in `00.1-CONTEXT.md`: Phase 00 historical and
coordination cleanup (A), a reusable external exact-SHA merge-authorization
policy (B), Codex and Claude handoff mailboxes (C), and documentation and tests
necessary for these workflow changes only (D).

Green CI was explicitly not treated as evidence that the workflow design is
correct.

## Independently reproduced facts

| Check | Result |
|---|---|
| `git rev-parse HEAD` | `ae3e7699…` |
| `git ls-remote origin refs/heads/phase/00.1-agent-handoffs` | `ae3e7699…` — local equals origin |
| `git status --porcelain` | empty — clean worktree |
| `git merge-base --is-ancestor a9a8e72 HEAD` | true — `task_base_sha` is an ancestor |
| `git log --oneline a9a8e72..HEAD` | exactly `ae3e769` (docs(workflow): assign Phase 00.1 Claude review), touching only `.planning/CLAUDE-NEXT.md` — the expected handoff-delivery coordination change |
| `git log --oneline 0fdce489..HEAD` | `a9a8e72`, `ae3e769` — two commits in the phase |
| `main_baseline` ancestry | `0fdce489` is an ancestor of both `refs/remotes/origin/main` and HEAD |
| `git ls-remote origin refs/heads/main` | `0fdce489…` — equals the declared baseline |
| `phase-00-approved` | tag object `9b6fb516…` (local and origin identical); peels to `0fdce489…`, matching `origin/main` and the ROADMAP claim |
| Version chain | `0.1.496.5` → `0.1.496.6` at `a9a8e72` (package-affecting); preserved at `ae3e769` (planning-only). All four checkpoints synchronized at HEAD: `mfile.version`, `mfile.title`, `boop_init.lua:3`, `CODEX.md:123` |
| `python3 tools/check_release_gates.py` at HEAD | `[OK] versions`, `[OK] version-bump`, `[OK] workflow`, `[OK] manifests`, `[OK] state-drift`, `[OK] architecture` |
| Exact-SHA CI for the reviewed SHA | run `33935721064`, attempt `1`, event `push`, headBranch `phase/00.1-agent-handoffs`, headSha `a9a8e72caf1232ed516eea752229f5de250b978e`, conclusion `success`, `https://github.com/ronh3/boop/actions/runs/33935721064` |
| Other CI on this branch | run `33944331398`, event `push`, headSha `ae3e7699…` (current HEAD), conclusion `success` — not cited by the handoff and not recorded anywhere; see M-04 |
| `git diff 0fdce489..HEAD -- tools/ tests/ .github/` | **empty** — no enforcement or test change anywhere in the phase; see H-01 |
| `git diff 0fdce489..HEAD -- src/` | only `src/scripts/boop/boop_init.lua` version string |

The handoff's required CI run is genuine, is bound to the exact reviewed SHA,
and succeeded. That is automated evidence only (`AGENTS.md:264-280`) and is not
treated below as validation of the workflow contract.

**Totals: 0 blocker · 2 high · 4 medium · 2 low.**

---

## High

### H-01 — Every guarantee Phase 00.1 adds is prose-only; the phase ships no enforcement

- **Severity:** High
- **Location:** `AGENTS.md:140-228` (all new text); absence of change in
  `tools/`, `tests/`, `.github/`.
- **Violated invariant:** `00.1-CONTEXT.md` fixed scope D — "Documentation
  **and tests** necessary for these workflow changes only."
- **Problem:** `git diff 0fdce489..HEAD -- tools/ tests/ .github/` is empty. The
  phase adds a large body of new normative claims and mechanically enforces none
  of them:
  - the mailbox frontmatter schema and its enum values (`AGENTS.md:188-201`);
  - the handoff precedence order (`AGENTS.md:171-186`);
  - the pre-execution checks an agent must perform before running a handoff
    (`AGENTS.md:209-219`);
  - the approval-tag name format, its embedded SHA, and its target
    (`AGENTS.md:143-149`);
  - approval-tag immutability (`AGENTS.md:150-159`);
  - the `phase_bootstrap` fork-point proof (`AGENTS.md:221-228`).

  This is the same defect class as Phase 00's M-06 ("The versioning rule is
  prose-only; CI enforces neither classification nor monotonicity"), which was
  accepted and corrected by extending `tools/workflow_guard.py` with
  `check_version_history`. Phase 00.1 reintroduces that class at a larger
  surface area without extending the guard. The existing 30 tests in
  `tests/test_workflow_gates.py` are unchanged and still pass — nothing
  regressed — but they cover none of the above.
- **Failure scenario:** An agent (or a future session of the same agent) writes
  `status: ready` with a `task_base_sha` that is not an ancestor of HEAD, skips
  the local/origin parity check, or names a `mode` its role may not use. Every
  release gate and every CI step passes, because nothing reads the mailbox at
  all. The only detection mechanism is a human noticing.
- **Smallest correction:** Add a `handoff` gate to `tools/workflow_guard.py`
  (invoked from the `GATES` table in `tools/check_release_gates.py:352-353`)
  that parses both mailbox files, validates the frontmatter keys and enums,
  requires `branch`/`task_base_sha`/`review_target_sha` to be null exactly when
  `status: idle`, requires a non-null `task_base_sha` to be a resolvable commit
  and an ancestor of HEAD, and rejects `mode: phase_bootstrap` in
  `CLAUDE-NEXT.md`. Cover it in `tests/test_workflow_gates.py`.

### H-02 — The documented `phase_bootstrap` sequence is rejected by the release gate `AGENTS.md` mandates

- **Severity:** High
- **Location:** `AGENTS.md:221-228` vs `tools/workflow_guard.py:119-127`.
- **Violated invariant:** `AGENTS.md:257-258` — "Before committing or pushing,
  inspect staged paths and run `python3 tools/check_release_gates.py`."
- **Problem:** `AGENTS.md:221-224` states that `phase_bootstrap` "permits a
  human-authorized new branch from the named exact `origin/main`
  `task_base_sha` **to carry its delivery handoff while STATE still describes
  the completed prior phase**," and `:225-227` orders the work: Codex "first
  proves the branch fork point equals that base and later commits are only the
  expected handoff-delivery planning change, **then** creates or updates the new
  context and STATE."

  That sequence requires at least one commit on the new branch whose staged
  `.planning/STATE.md` still names the *prior* phase's branch.
  `check_staged_branch` (`tools/workflow_guard.py:119-127`) reads
  `active_branch` from the **staged** `STATE.md` and fails whenever it differs
  from `git branch --show-current`. It is wired into the mandatory `workflow`
  gate at `tools/check_release_gates.py:353`. So the literal documented flow is
  uncommittable.

  Phase 00.1 did not hit this only because `a9a8e72` collapsed the handoff
  delivery and the STATE update into a single commit — the opposite of the
  ordering the new text prescribes. The prose therefore describes a procedure
  that has never been executed and cannot be, and the one working procedure is
  undocumented.
- **Failure scenario:** Codex is handed a `phase_bootstrap` handoff for Phase
  01. It creates `phase/01-…` from `origin/main`, stages the delivery handoff,
  runs the mandatory gate, and gets `staged work requires the active phase
  branch; no direct main or detached commits`. It is now stuck between a rule
  that says commit the handoff first and a gate that forbids it, with no
  documented resolution — the likeliest outcomes are bypassing the gate or
  improvising an undocumented ordering.
- **Smallest correction:** Either state in `AGENTS.md:221-228` that the
  bootstrap delivery handoff and the new `STATE.md`/context must land in the
  **same** commit (matching what `a9a8e72` actually did and what
  `check_staged_branch` permits), or relax `check_staged_branch` to accept a
  documented bootstrap exception and test it.

---

## Medium

### M-01 — The authorized and merged SHA is never required to be the independently reviewed SHA

- **Severity:** Medium
- **Location:** `AGENTS.md:140-159`; `.planning/CLAUDE-NEXT.md:7-8`.
- **Violated invariant:** `AGENTS.md:22-34` authority order — independent
  adversarial review precedes closure; `AGENTS.md:122-125` — additions to `main`
  require independent review.
- **Problem:** `AGENTS.md:141-146` requires the human to authorize "the final
  immutable full phase HEAD" *after* review, but nothing anywhere requires that
  HEAD to equal, or to be reconciled with, the SHA that was independently
  reviewed. The gap is structural, not hypothetical:
  - Phase 00 closed on accepted reviewed SHA `f244be0` (`00-UAT.md`, Phase
    closure) and merged `0fdce489` — one later commit, tag-verified above.
  - Phase 00.1 already repeats it before review is even recorded: the handoff
    names `review_target_sha: a9a8e72` while HEAD is `ae3e769`.
  - This review commit will itself advance HEAD again.

  Each individual instance here is low-impact (planning/mailbox text). The
  defect is that the rule permits arbitrary content in that gap. `AGENTS.md:158`
  forbids a bookkeeping commit *after* authorization precisely because it would
  invalidate the SHA — but says nothing about the unreviewed commits that
  necessarily accumulate *before* it.
- **Failure scenario:** A phase is reviewed at SHA X with no findings. Two
  further commits land — a mailbox update and a closure-bookkeeping entry that
  also edits a sourced file. The human authorizes HEAD, Codex tags it, and it
  fast-forwards to `main`. Every gate in `AGENTS.md` was satisfied and code that
  no reviewer saw is now on `main`.
- **Smallest correction:** In `AGENTS.md:140-149`, require that the authorized
  SHA either equal the independently reviewed SHA, or be covered by a recorded
  narrow Claude re-review of the exact `reviewed..authorized` delta — the
  mechanism Phase 00 already used for its final narrow re-review.

### M-02 — Approval tags carry no authenticity binding and no name-to-target check

- **Severity:** Medium
- **Location:** `AGENTS.md:143-159`; `AGENTS.md:104`.
- **Violated invariant:** `AGENTS.md:110` — "This is a role contract; Git author
  names do not authenticate a human."
- **Problem:** The tag is now the single terminal artifact of merge
  authorization: `AGENTS.md:104` says "The tag, not a later STATE or UAT entry,
  is final authorization evidence." Three properties it is relied on for are not
  established:
  1. **Name/target binding.** `phase-<phase>-approved-<12-char-commit-sha>`
     embeds a SHA prefix, but nothing verifies the tag actually targets the
     commit that prefix names. A tag named for SHA X pointing at commit Y
     satisfies every check in the repository.
  2. **Authenticity.** `AGENTS.md:110` already concedes Git identity cannot
     authenticate a human — and `AGENTS.md:144-146` makes **Codex** the actor
     that creates the tag. An unsigned annotated tag created by the same agent
     the contract distrusts elsewhere carries no more authority than a commit
     whose author line says "Ron". Moving authorization "outside the commit
     tree" buys ordering and immutability against rebase; it does not buy
     authenticity.
  3. **Immutability.** `AGENTS.md:150-151` asserts tags are "immutable evidence
     once pushed," but Git tags are force-movable by default; no server-side tag
     protection ruleset is evidenced in the repository, and nothing in-tree can
     detect a moved tag.

  Separately, because the tag name is a deterministic function of the SHA, a
  superseded authorization stays permanently valid: `AGENTS.md:155-157` requires
  a *new* tag after branch mutation but never invalidates the old one, so
  re-merging a previously authorized-but-superseded SHA passes every stated
  check.
- **Failure scenario:** After a correction round, an agent finds the earlier
  `phase-01-approved-<old-sha>` tag, confirms it peels to a commit, confirms the
  name matches, and fast-forwards `main` to the pre-correction commit. Every
  documented pre-merge verification in `AGENTS.md:152-157` compares the tag to
  itself and passes.
- **Smallest correction:** Add a guard that, for every `phase-*-approved-*` ref,
  asserts the peeled target SHA begins with the 12-char prefix in the tag name;
  require GPG/SSH-signed tags or a GitHub tag-protection ruleset for authenticity
  and immutability, and state in `AGENTS.md` that a superseded approval tag is
  void for merge once a newer tag exists for the same phase.

### M-03 — Ready handoffs have no completion or expiry semantics and are indefinitely replayable

- **Severity:** Medium
- **Location:** `AGENTS.md:166-219`; `.planning/CLAUDE-NEXT.md:3` and its
  `# Constraints` / `# Completion Boundary` sections.
- **Violated invariant:** `AGENTS.md:184-185` — "Updating a handoff does not
  execute it; the human remains the trigger."
- **Problem:** Nothing defines what a handoff's state becomes after it is
  executed. `CLAUDE-NEXT.md` will still read `status: ready` with
  `review_target_sha: a9a8e72` after this review is recorded, because:
  - its `# Completion Boundary` does not instruct the agent to reset it, and
  - its `# Constraints` explicitly forbid Claude from editing "either NEXT
    mailbox" — correctly, per `AGENTS.md:183-184`.

  So the assigned agent is structurally unable to retire its own handoff, and no
  rule obliges the assigner to. The pre-execution checks at `AGENTS.md:209-215`
  detect a *stale base* (`task_base_sha` not an ancestor, unexpected commits in
  the range) but not a *completed task*: after this review commit, `a9a8e72`
  remains an ancestor and the intervening commits are still "planning
  coordination," so a replay passes every check.
- **Failure scenario:** The human says "run the current handoff" a week later,
  intending new work. Claude re-reads `CLAUDE-NEXT.md`, passes every documented
  check, and re-reviews `a9a8e72` — a SHA that is by then several corrections
  old — appending a duplicate, stale review to the append-only artifact that
  cannot be removed.
- **Smallest correction:** Add a terminal state to the schema
  (`status: idle | ready | completed`) plus a rule at `AGENTS.md:209-219` that an
  agent must stop if its handoff's `review_target_sha` is already cited by a
  recorded entry in the phase's review artifact; or require the assigner to
  reset the mailbox to `idle` as part of accepting the handoff's output.

### M-04 — `00.1-VERIFICATION.md` is still a pending scaffold at the review boundary, and HEAD's CI run is unrecorded

- **Severity:** Medium
- **Location:** `.planning/phases/00.1-agent-handoffs/00.1-VERIFICATION.md`;
  `AGENTS.md:271-278`.
- **Violated invariant:** `AGENTS.md:160-162` — record "CI run ID, attempt, URL,
  event, branch, and head SHA in VERIFICATION"; `AGENTS.md:271-273` — "Commit
  completed run identifiers and summaries in `<NN>-VERIFICATION.md` **at the
  next evidence or review boundary**."
- **Problem:** The handoff explicitly asks whether this is a workflow
  inconsistency. It is. Two successful runs exist on this branch —
  `33935721064` for `a9a8e72` and `33944331398` for HEAD `ae3e769` — and
  neither is recorded anywhere in the repository. `STATE.md` reads
  `implementation_complete_awaiting_independent_review`, so the review boundary
  is *now*; the artifact `AGENTS.md` designates as the authority for automated
  verification instead asserts "This scaffold contains no completed test or CI
  result."

  This is not yet a rule breach — `AGENTS.md:271-272` permits deferral to the next
  boundary, and the Phase 00 precedent (its M-04) was that a missing artifact,
  not an unfilled one, is the defect. But it produces two concrete problems:
  1. The independent review is being asked to assess a phase whose sole
     automated-evidence artifact states, on its face, that no evidence exists.
     The CI identity had to be reconstructed from the GitHub API and the
     handoff, neither of which is a durable repository artifact.
  2. The handoff cites only `33935721064` (for `a9a8e72`) and never mentions
     `33944331398`, the run that actually covers phase HEAD. Per
     `AGENTS.md:277-278`, "Every later mutation requires a new final gate" —
     that gate exists and passed, but a reader of this repository has no way to
     know it.
- **Failure scenario:** Human arbitration or closure reads
  `00.1-VERIFICATION.md`, finds an explicit "no completed CI result," and either
  blocks on evidence that already exists or accepts the phase without any
  in-repository record of which SHA was gated.
- **Smallest correction:** Codex appends both run identities (ID, attempt, URL,
  event, branch, head SHA, conclusion) to `00.1-VERIFICATION.md` at this review
  boundary, as `AGENTS.md:271-273` provides for.

---

## Low

### L-01 — The ROADMAP's Phase 00 reconciliation collapses the reviewed SHA and the merged SHA

- **Severity:** Low
- **Location:** `.planning/ROADMAP.md:7-10`.
- **Violated invariant:** `AGENTS.md:48` — ROADMAP is the authority for
  milestone sequencing and the phase namespace index.
- **Problem:** The entry reads "COMPLETE; merged exact SHA `0fdce489…`,
  historical approval tag `phase-00-approved`." Both claims verify: the tag
  peels to `0fdce489` and `origin/main` is `0fdce489`. But Phase 00's recorded
  acceptance was of a *different* SHA — `00-UAT.md` names accepted
  independently reviewed SHA `f244be0`, and its "Phase 00 exact-SHA
  merge-authorization exception" deliberately documents that the
  closure-bookkeeping commit's resulting SHA becomes the merge candidate. The
  ROADMAP's single-SHA framing erases that distinction from the sequencing
  authority, where a reader is most likely to look for "which commit was
  accepted."
- **Failure scenario:** A future phase cites the Phase 00 precedent for its own
  authorization and reproduces only the merged-SHA half, losing the reviewed-SHA
  reconciliation that M-01 shows is already unenforced.
- **Smallest correction:** Extend `.planning/ROADMAP.md:7-10` to name both:
  accepted reviewed SHA `f244be0`, authorized/merged SHA `0fdce489`.

### L-02 — The mailbox schema does not constrain `mode` by agent, and leaves `mode` undefined when idle

- **Severity:** Low
- **Location:** `AGENTS.md:188-207`; `.planning/CODEX-NEXT.md:5`.
- **Violated invariant:** `AGENTS.md:221` — "`phase_bootstrap` is Codex-only."
- **Problem:** The single shared frontmatter block at `AGENTS.md:190-201`
  presents `mode: active_phase | phase_bootstrap` for both files, so a
  `CLAUDE-NEXT.md` carrying `mode: phase_bootstrap` is schema-valid and only
  prose 30 lines later forbids it — with no gate to catch it (H-01).
  Relatedly, `AGENTS.md:204-205` says "An `idle` handoff has no executable
  task," but the schema still requires `mode`, and `CODEX-NEXT.md:5` accordingly
  declares `mode: active_phase` while idle with every other field null. The
  format does not say what `mode` means, or whether it is honoured, when
  `status: idle`.
- **Failure scenario:** An idle `CLAUDE-NEXT.md` is later flipped to
  `status: ready` without revisiting `mode`, inheriting a stale or
  role-inappropriate value that no check rejects.
- **Smallest correction:** State in `AGENTS.md:194` that `mode` is
  `active_phase | phase_bootstrap` for `CODEX-NEXT.md` and `active_phase` only
  for `CLAUDE-NEXT.md`, and that `mode` is ignored when `status: idle`.

---

## Investigated and concluded NOT defects

- **Version synchronization and bump classification.** `a9a8e72` is
  package-affecting (`AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `mfile`,
  `boop_init.lua` are outside `.planning/`) and correctly bumps all four
  checkpoints `0.1.496.5` → `0.1.496.6`. `ae3e769` is planning-only and
  correctly preserves them. The `version-bump` gate passes at HEAD. That a
  documentation-only phase forces a package version increment is the rule at
  `AGENTS.md:245-256` working as written, not a defect.
- **Workflow-only scope containment.** `git diff 0fdce489..HEAD -- src/` touches
  exactly one file, `src/scripts/boop/boop_init.lua`, and only its version
  string. No alias, trigger, script, manifest, gameplay, or command-surface file
  is modified. The explicit exclusions in `00.1-CONTEXT.md` are honoured; no
  build artifact under `build/` was edited.
- **Baseline and branch integrity.** `main_baseline` `0fdce489` is an ancestor
  of both `refs/remotes/origin/main` and HEAD; local branch equals origin
  branch; the worktree is clean; `active_branch` in `STATE.md` matches the
  checked-out branch and the handoff.
- **Phase 00 tag correctness.** The `phase-00-approved` annotated tag object is
  byte-identical local and remote (`9b6fb516…`), peels to `0fdce489`, carries a
  message naming the human authorization, date, and full SHA, and was preserved
  rather than renamed as `AGENTS.md:147-149` requires.
- **Append-only artifact integrity.** All four `00.1-*` artifacts were created
  in `a9a8e72` and none has been rewritten; the `workflow` gate's
  `check_review_transition` verifies prefix preservation across the phase and
  passes.
- **Existing enforcement is intact.** `tests/test_workflow_gates.py` (30 tests)
  and `tools/workflow_guard.py` are unchanged by this phase and still pass. The
  `active_branch` regex at `tools/workflow_guard.py:124` already accepted the
  decimal `phase/00.1-…` form before this phase; no widening was needed or made.
- **Handoff precedence ordering.** The precedence block at `AGENTS.md:171-178`
  correctly subordinates handoff instructions to review/evidence constraints and
  STATE, and `CLAUDE-NEXT.md`'s constraints do not attempt to widen Claude's
  role. The delivery commit `ae3e769` modifies only the mailbox, which
  `AGENTS.md:168-169` declares is not an authority or evidence artifact.

## Review status

- **Reviewed SHA:** `a9a8e72caf1232ed516eea752229f5de250b978e`
- **Branch HEAD at review time:** `ae3e7699adf91bd82c356f542bba1912f15a70a5`
- **Findings:** 0 blocker, 2 high, 4 medium, 2 low
- **Independent review:** initial pass complete; no re-review performed
- **Human arbitration:** pending
- **Live Mudlet applicability or validation:** pending human determination
- **Phase closure and merge to `main`:** pending explicit human authorization
- **Phase 00.1 is not complete.**

This review commit mutates the repository and therefore invalidates the
exact-SHA CI evidence recorded above for `a9a8e72` and `ae3e769`; per
`AGENTS.md:269-278` the gate must be run again against the new HEAD by Codex.
Per the handoff's Completion Boundary, Claude stops here: no corrections are
proposed, no findings are dispositioned, and no gate other than
`independent_review` is touched.
