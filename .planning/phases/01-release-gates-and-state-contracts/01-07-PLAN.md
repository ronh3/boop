---
phase: 01-release-gates-and-state-contracts
plan: "07"
type: gap_closure
status: planned
gap_closure: true
gap_ids:
  - G-01-8D
files_modified:
  - src/scripts/boop/boop_gag.lua
  - tests/boop_gag_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
requirements:
  - REL-04
---

# Phase 01 Plan 07: Gag Summary Gap Closure

<objective>
Repair the compact gag summary state machine so existing own-attack, proc, crit, balance, prompt, companion, battlerage, kill, and XP fixtures pass without hiding safety-relevant lines.
</objective>

<context>
@.planning/phases/01-release-gates-and-state-contracts/01-UAT.md
@.planning/debug/phase-01-full-mudlet-busted.md
@src/scripts/boop/boop_gag.lua
@src/scripts/boop/boop_stats.lua
@tests/boop_gag_spec.lua
@tests/support/boop_test_helper.lua
@tools/check_release_gates.py
</context>

## Tasks

1. Reproduce and group the gag failures before editing.
   - Replay the failing `boop_gag_spec.lua` cases in small groups: basic own attack, proc aggregation, DSL/dual-hit merge, companion maul, standalone battlerage, alternate wording, and kill/XP ordering.
   - Record which failures share a pending state, timer, source grouping, or flush-order cause.

2. Repair `boop_gag.lua` pending summary behavior.
   - Keep pending attack state local to known summary windows and flush on balance or prompt as expected.
   - Preserve proc source labels, grouped damage totals, crit tier ordering, and total damage.
   - Keep companion maul flavor suppressed only when it is represented by a summary.
   - Flush kill summaries after attack summaries, and combine kill plus experience when both lines arrive.

3. Keep stats hooks and visible output semantics aligned.
   - Ensure fixes do not break `boop.stats` attack and kill hooks.
   - Unknown or safety-relevant lines should remain visible or traceable rather than silently deleted.

4. Synchronize version fields for the commit.
   - Increment the current patch version once and write it to `mfile.version`, `mfile.title`, `src/scripts/boop/boop_init.lua`, and the `CODEX.md` checkpoint.

## Verification

- `luac -p src/scripts/boop/boop_gag.lua tests/boop_gag_spec.lua`
- `python3 tools/check_release_gates.py`
- `muddle`
- Full Mudlet/Busted with `/tmp/Mudlet.AppImage`; all `boop_gag_spec.lua` failures listed in `G-01-8D` must be gone and no new gag failures may appear.

## Done

Gap `G-01-8D` is closed when compact gag summaries preserve the tested combat signal and the full suite reports no gag-summary failures.
