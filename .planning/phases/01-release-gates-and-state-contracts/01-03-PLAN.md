---
phase: 01-release-gates-and-state-contracts
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - tests/boop_state_contract_spec.lua
  - tests/README.md
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - REL-04
must_haves:
  truths:
    - "D-02: Focused state-contract regression coverage exists alongside the static state-drift gate."
    - "D-03: REL-04 tests pass against current owned-domain contracts and do not require roadmap Phase 2 behavior repairs."
    - "Maintainer can run `python3 tools/check_release_gates.py --check state-drift` and the existing Mudlet Busted path to catch high-risk state-contract drift before behavior changes land."
  artifacts:
    - path: "tests/boop_state_contract_spec.lua"
      provides: "Focused Busted coverage for owned runtime domain defaults and runtime context mapping"
    - path: "tests/README.md"
      provides: "Test inventory entry for the new state-contract spec"
  key_links:
    - from: "tests/boop_state_contract_spec.lua"
      to: "tests/support/boop_test_helper.lua"
      via: "loads helper through TESTS_DIRECTORY and resets real package state before each assertion"
      pattern: "support/boop_test_helper.lua"
    - from: "tests/boop_state_contract_spec.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "asserts `boop.runtime.state()` domains and `boop.runtime.context()` mapping"
      pattern: "boop.runtime.context"
    - from: "tools/check_release_gates.py"
      to: "tests/boop_state_contract_spec.lua"
      via: "static state-drift gate complements Busted owned-domain tests"
      pattern: "--check state-drift"
---

<objective>
Add focused REL-04 state-contract regression tests that pass on the current codebase while preventing new bypasses of owned runtime state contracts.

Purpose: Phase 1 needs high-risk state-contract coverage before behavior changes land, but D-03 requires known behavior repairs to remain assigned to their roadmap phases rather than making CI red here.
Output: A Busted state-contract spec, test inventory documentation, and synchronized version metadata for this plan commit.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-gates-and-state-contracts/01-CONTEXT.md
@.planning/phases/01-release-gates-and-state-contracts/01-RESEARCH.md
@.planning/phases/01-release-gates-and-state-contracts/01-PATTERNS.md
@.planning/phases/01-release-gates-and-state-contracts/01-VALIDATION.md
@.planning/phases/01-release-gates-and-state-contracts/01-01-SUMMARY.md
@AGENTS.md
@CODEX.md
@tests/README.md
@tests/boop_runtime_spec.lua
@tests/boop_event_transitions_spec.lua
@tests/support/boop_test_helper.lua
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_state.lua
@tools/check_release_gates.py
</context>

## Artifacts This Phase Produces

- File: `tools/check_release_gates.py`
- CLI flags: `--check versions`, `--check manifests`, `--check state-drift`
- Python symbols: `main`, `check_versions`, `check_manifests`, `check_state_drift`, `KNOWN_FLAT_STATE_ACCESS`
- CI step name: `Release gates`
- Test file: `tests/boop_state_contract_spec.lua`
- Local commands: `python3 tools/check_release_gates.py`, `python3 tools/check_release_gates.py --check versions`, `python3 tools/check_release_gates.py --check manifests`, `python3 tools/check_release_gates.py --check state-drift`

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Maintainers can trust CI and focused tests to catch release metadata, package membership, and high-risk state-contract drift | 01, 02, 03 | COVERED | Static gates in Plan 01, CI in Plan 02, Busted state contracts in Plan 03.
REQ | REL-01 | CI fails when version sources disagree | 01, 02 | COVERED | Version gate is created and wired before Muddler.
REQ | REL-02 | CI validates JSON and manifest/file parity | 01, 02 | COVERED | Parser/parity gate is created and wired before Muddler.
REQ | REL-04 | High-risk runtime paths have focused regression tests before behavior changes land | 01, 03 | COVERED | Plan 03 creates Busted owned-domain contract tests; Plan 01 freezes static drift.
RESEARCH | - | Use Busted inside Mudlet and helper reset patterns | 03 | COVERED | Task 1 follows existing `tests/boop_runtime_spec.lua` and helper patterns.
RESEARCH | - | Do not make Phase 2 flat-state migration a Phase 1 blocker | 03 | COVERED | Tests assert current stable runtime contracts and pair with static reviewed-drift gate.
CONTEXT | D-01 | Deterministic gates block CI | 02 | COVERED | Plan 02 wires the checker as blocking CI.
CONTEXT | D-02 | State-contract regression checks are deterministic gates | 01, 03 | COVERED | Static drift plus Busted state-contract coverage.
CONTEXT | D-03 | Current codebase remains green | 03 | COVERED | Tests cover existing owned-domain behavior rather than requiring behavior migration.
CONTEXT | D-04 | Checks reusable locally | 02, 03 | COVERED | Local static command and Mudlet Busted command are documented.
CONTEXT | D-05 | Minimal dependency cleanup | 01, 02, 03 | COVERED | No new dependencies are introduced.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused owned-state contract spec</name>
  <files>tests/boop_state_contract_spec.lua</files>
  <read_first>
    - tests/boop_state_contract_spec.lua
    - tests/boop_runtime_spec.lua
    - tests/boop_event_transitions_spec.lua
    - tests/support/boop_test_helper.lua
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_state.lua
    - .planning/phases/01-release-gates-and-state-contracts/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: After `helper.reset()`, `boop.runtime.state()` returns `boop.state` with owned domains `combat`, `targeting`, `gold`, `queue`, `walk`, `diag`, `trace`, `ui`, `rage`, `inventory`, `ih`, and `gag` as tables.
    - Test 2: Domain defaults match the current contract: `combat.pullState == false`, `targeting.currentTargetId == ""`, `targeting.lastRoomDir == ""`, `gold.getPending == false`, `gold.putPending == false`, `queue.prequeuedStandard == false`, `walk.active == false`, `diag.hold == false`, `inventory.wieldedLeft == false`, and `gag.pendingAttack == nil`.
    - Test 3: `boop.runtime.context()` reads seeded values from owned domains for target id/name/shield, queue alias/prequeue, gold pending/pack target, diag hold/label, inventory wielded sides, and rage ready table.
  </behavior>
  <action>Create `tests/boop_state_contract_spec.lua` using the existing Busted pattern: load `tests/support/boop_test_helper.lua` through `TESTS_DIRECTORY`, call `helper.reset()` in `before_each`, and assert production state plus `boop.runtime.context()` behavior without mocking the domain under test. Keep the tests focused on owned-domain defaults and context mapping that are already true in `src/scripts/boop/boop_runtime.lua`; do not assert room, pull, walk, gold, or flee behavior migrations assigned to roadmap Phase 2. This implements REL-04 under D-02 and D-03.</action>
  <verify>
    <automated>python3 tools/check_release_gates.py --check state-drift</automated>
    <automated>test -f tests/boop_state_contract_spec.lua</automated>
    <automated>grep -F "support/boop_test_helper.lua" tests/boop_state_contract_spec.lua</automated>
    <automated>grep -F "boop.runtime.context()" tests/boop_state_contract_spec.lua</automated>
    <human-check>If `/tmp/Mudlet.AppImage` is missing locally, record that local full Mudlet execution is unavailable and use the GitHub Actions Mudlet Busted run as the full-suite evidence.</human-check>
  </verify>
  <acceptance_criteria>
    - `tests/boop_state_contract_spec.lua` exists and loads the shared helper through `TESTS_DIRECTORY`.
    - The spec asserts all named owned runtime domains and defaults from `DOMAIN_DEFAULTS`.
    - The spec asserts `boop.runtime.context()` reads seeded owned-domain values rather than flat state keys.
    - The static state-drift check remains green after adding the spec.
    - Full Mudlet Busted verification is run locally when `/tmp/Mudlet.AppImage` exists, or the missing local runner is explicitly recorded for CI verification.
  </acceptance_criteria>
  <done>REL-04 has focused Busted coverage for current owned-state contracts without forcing known behavior repairs into Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Update test inventory for state contracts</name>
  <files>tests/README.md</files>
  <read_first>
    - tests/README.md
    - tests/boop_state_contract_spec.lua
    - tests/boop_runtime_spec.lua
    - .planning/phases/01-release-gates-and-state-contracts/01-VALIDATION.md
  </read_first>
  <action>Add a `tests/README.md` inventory bullet for `boop_state_contract_spec.lua` near `boop_runtime_spec.lua`. The description must state that it covers owned runtime domain defaults and runtime context mapping for high-risk state-contract drift. Keep the existing `boop_runtime_spec.lua` description intact unless wording must avoid duplicate meaning.</action>
  <verify>
    <automated>grep -F "boop_state_contract_spec.lua" tests/README.md</automated>
    <automated>grep -F "owned runtime domain" tests/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/README.md` names `boop_state_contract_spec.lua`.
    - The inventory description mentions owned runtime domains and runtime context mapping.
    - No unrelated test inventory bullets are removed.
  </acceptance_criteria>
  <done>The test inventory explains the REL-04 state-contract coverage added in this plan.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize plan commit version fields</name>
  <files>mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - AGENTS.md
    - CODEX.md
    - mfile
    - src/scripts/boop/boop_init.lua
    - tools/check_release_gates.py
  </read_first>
  <action>Before committing Plan 03 implementation changes, read the current `mfile.version`, increment its patch component once for this plan, and write the same resulting value to `mfile.version`, `mfile.title` as `boop Hunter &lt;version&gt;`, `src/scripts/boop/boop_init.lua` `boop.version`, and the `CODEX.md` `Current synchronized package version:` checkpoint. Preserve the CODEX local gate documentation added by Plan 02.</action>
  <verify>
    <automated>python3 tools/check_release_gates.py --check versions</automated>
    <automated>python3 tools/check_release_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - The four version sources report exactly the same version value.
    - `mfile.title` is exactly `boop Hunter &lt;that same version&gt;`.
    - The full local release gate exits 0 after the bump.
  </acceptance_criteria>
  <done>The plan's implementation commit can satisfy the repository versioning rule and full Phase 1 local gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Busted test harness -> runtime state | Tests seed and inspect package state through the same helper and runtime APIs used by existing Mudlet specs. |
| static state-drift gate -> source review | The checker prevents new unreviewed flat-state access while Busted verifies stable owned-domain contracts. |
| local Mudlet runner -> CI evidence | Full Busted execution may depend on a local AppImage; CI remains the full-run verification boundary when local runner is absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-state-test-gap | Tampering | `tests/boop_state_contract_spec.lua` | high | mitigate | Assert owned domain defaults and runtime context mappings before later behavior changes can drift. |
| T-01-known-drift-red | Denial of Service | REL-04 Busted tests | medium | mitigate | Keep tests scoped to current stable contracts and pair with static known-drift baseline per D-03. |
| T-01-local-runner | Repudiation | Mudlet Busted evidence | medium | mitigate | Require local full-run command when `/tmp/Mudlet.AppImage` exists and explicit CI evidence when it does not. |
| T-01-SC | Tampering | package-manager installs | low | accept | Plan 03 adds no npm, pip, cargo, LuaRocks, or GitHub Action dependency. |
</threat_model>

<verification>
Run these checks after all Plan 03 tasks:
- `python3 tools/check_release_gates.py --check state-drift`
- `python3 tools/check_release_gates.py`
- `muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror` when `/tmp/Mudlet.AppImage` exists locally
- If `/tmp/Mudlet.AppImage` is unavailable locally, record that limitation and verify the Busted suite through GitHub Actions before phase verification sign-off.
</verification>

<success_criteria>
Plan 03 is complete when focused owned-state Busted coverage exists, the state-drift gate remains green, test inventory documentation names the new spec, and full Phase 1 local gates pass.
</success_criteria>

<output>
Create `.planning/phases/01-release-gates-and-state-contracts/01-03-SUMMARY.md` when done.
</output>
