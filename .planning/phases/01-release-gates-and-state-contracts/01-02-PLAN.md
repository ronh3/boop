---
phase: 01-release-gates-and-state-contracts
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/main.yml
  - CODEX.md
  - mfile
  - src/scripts/boop/boop_init.lua
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "D-01: CI blocks before Muddler when the local release gate exits non-zero."
    - "D-04: CODEX documents the exact local commands CI uses for maintainers."
    - "D-05: CI dependency-risk cleanup is limited to adding the local gate step; no broad action or package pinning is introduced."
  artifacts:
    - path: ".github/workflows/main.yml"
      provides: "Blocking `Release gates` step before package build"
    - path: "CODEX.md"
      provides: "Local release-gate command reference"
  key_links:
    - from: ".github/workflows/main.yml"
      to: "tools/check_release_gates.py"
      via: "GitHub Actions run step invokes local checker before `Read package metadata` and `Muddle`"
      pattern: "python3 tools/check_release_gates.py"
    - from: "CODEX.md"
      to: "tools/check_release_gates.py"
      via: "Maintainer workflow docs list the same local command surface"
      pattern: "--check versions|--check manifests|--check state-drift"
---

<objective>
Wire the local deterministic release gate into GitHub Actions and document the local command surface maintainers should run before pushing.

Purpose: D-01 requires Phase 1 deterministic gates to block CI, and D-04 requires the same checks to be reusable locally rather than CI-only.
Output: A blocking workflow step and CODEX documentation for local release-gate commands.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-gates-and-state-contracts/01-CONTEXT.md
@.planning/phases/01-release-gates-and-state-contracts/01-RESEARCH.md
@.planning/phases/01-release-gates-and-state-contracts/01-PATTERNS.md
@.planning/phases/01-release-gates-and-state-contracts/01-VALIDATION.md
@.planning/phases/01-release-gates-and-state-contracts/01-01-SUMMARY.md
@AGENTS.md
@CODEX.md
@.github/workflows/main.yml
@tools/check_release_gates.py
</context>

## Artifacts This Phase Produces

- File: `tools/check_release_gates.py`
- CLI flags: `--check versions`, `--check manifests`, `--check state-drift`
- Python symbols: `main`, `check_versions`, `check_manifests`, `check_state_drift`, `KNOWN_FLAT_STATE_ACCESS`
- CI step name: `Release gates`
- Test file: `tests/boop_state_contract_spec.lua`
- Local commands: `python3 tools/check_release_gates.py`, `python3 tools/check_release_gates.py --check versions`, `python3 tools/check_release_gates.py --check manifests`, `python3 tools/check_release_gates.py --check state-drift`

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Maintainers can trust CI and focused tests to catch release metadata, package membership, and high-risk state-contract drift | 01, 02, 03 | COVERED | Static gates in Plan 01, CI in Plan 02, Busted state contracts in Plan 03.
REQ | REL-01 | CI fails when version sources disagree | 01, 02 | COVERED | Plan 02 makes the local version gate block CI.
REQ | REL-02 | CI validates JSON and manifest/file parity | 01, 02 | COVERED | Plan 02 runs the local manifest gate before Muddler.
REQ | REL-04 | High-risk runtime paths have focused regression tests | 01, 03 | COVERED | Plan 03 handles Busted tests; Plan 01 provides static state-drift gate.
RESEARCH | - | Release gate step should run immediately after checkout and before Muddler | 02 | COVERED | Task 1 inserts the step at that point.
RESEARCH | - | Keep dependency cleanup minimal in Phase 1 | 02 | COVERED | Task 1 adds no package/action pinning beyond the new local command call.
CONTEXT | D-01 | Deterministic gates block CI | 02 | COVERED | Workflow step exits non-zero on checker failure.
CONTEXT | D-02 | Gate categories include version, JSON, manifests, state contracts | 01, 03 | COVERED | Plan 02 invokes the full checker created by Plan 01.
CONTEXT | D-03 | Passing current baseline | 01, 03 | COVERED | Plan 02 depends on Plan 01 baseline repair.
CONTEXT | D-04 | Checks reusable locally | 02 | COVERED | CODEX documents the local commands.
CONTEXT | D-05 | Minimal dependency cleanup | 02 | COVERED | No broad pinning or dependency churn.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add blocking CI release gate step</name>
  <files>.github/workflows/main.yml</files>
  <read_first>
    - .github/workflows/main.yml
    - tools/check_release_gates.py
    - .planning/phases/01-release-gates-and-state-contracts/01-RESEARCH.md
    - .planning/phases/01-release-gates-and-state-contracts/01-PATTERNS.md
  </read_first>
  <action>Insert a GitHub Actions step named `Release gates` immediately after `actions/checkout@v4` and before `Read package metadata`. The step must run `python3 tools/check_release_gates.py`, must not be configured as advisory, and must leave the existing Muddler, LuaRocks, Mudlet AppImage, Busted, artifact upload, and PR comment steps otherwise unchanged. This implements D-01 and D-04 while honoring D-05.</action>
  <verify>
    <automated>python3 tools/check_release_gates.py</automated>
    <automated>python3 -c "from pathlib import Path; s=Path('.github/workflows/main.yml').read_text(); names=['name: Release gates','name: Read package metadata','name: Muddle']; positions=[s.index(name) for name in names]; assert positions == sorted(positions); block=s[positions[0]:positions[1]]; assert 'python3 tools/check_release_gates.py' in block; assert 'continue-on-error' not in block"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/main.yml` has a `Release gates` step before `Read package metadata`.
    - The `Release gates` step runs exactly the local checker command `python3 tools/check_release_gates.py`.
    - A non-zero checker exit fails the workflow because the step is not advisory.
    - Existing build, Mudlet test, upload, and PR comment behavior remains present.
  </acceptance_criteria>
  <done>GitHub Actions blocks on the same local deterministic gate maintainers can run before pushing.</done>
</task>

<task type="auto">
  <name>Task 2: Document local release-gate commands</name>
  <files>CODEX.md</files>
  <read_first>
    - CODEX.md
    - AGENTS.md
    - README.md
    - .planning/phases/01-release-gates-and-state-contracts/01-VALIDATION.md
    - tools/check_release_gates.py
  </read_first>
  <action>Update `CODEX.md` under the existing CI/versioning or workflow guidance to name the local release-gate commands maintainers should run: `python3 tools/check_release_gates.py`, `python3 tools/check_release_gates.py --check versions`, `python3 tools/check_release_gates.py --check manifests`, and `python3 tools/check_release_gates.py --check state-drift`. Also document the existing full Mudlet Busted path as `muddle` followed by `AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY=$PWD/tests QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`, with an explicit note that local full execution requires `/tmp/Mudlet.AppImage` and otherwise should be verified through the GitHub Actions Mudlet run. Do not change README command-surface docs because this plan does not add operator commands.</action>
  <verify>
    <automated>grep -F "python3 tools/check_release_gates.py --check versions" CODEX.md</automated>
    <automated>grep -F "python3 tools/check_release_gates.py --check manifests" CODEX.md</automated>
    <automated>grep -F "python3 tools/check_release_gates.py --check state-drift" CODEX.md</automated>
    <automated>grep -F "/tmp/Mudlet.AppImage" CODEX.md</automated>
  </verify>
  <acceptance_criteria>
    - CODEX documents the full local checker command and each focused `--check` command.
    - CODEX documents the existing package build plus Mudlet Busted path.
    - CODEX states that local full Mudlet execution depends on `/tmp/Mudlet.AppImage`; CI remains the authoritative full-run fallback when that file is unavailable locally.
  </acceptance_criteria>
  <done>Maintainers can reproduce CI release-gate failures locally and know how full Mudlet Busted verification is run.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize plan commit version fields</name>
  <files>mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - AGENTS.md
    - CODEX.md
    - mfile
    - src/scripts/boop/boop_init.lua
    - tools/check_release_gates.py
  </read_first>
  <action>Before committing Plan 02 implementation changes, read the current `mfile.version`, increment its patch component once for this plan, and write the same resulting value to `mfile.version`, `mfile.title` as `boop Hunter &lt;version&gt;`, `src/scripts/boop/boop_init.lua` `boop.version`, and the `CODEX.md` `Current synchronized package version:` checkpoint. Keep the newly added local-command documentation intact while updating the checkpoint.</action>
  <verify>
    <automated>python3 tools/check_release_gates.py --check versions</automated>
  </verify>
  <acceptance_criteria>
    - The four version sources report exactly the same version value.
    - `mfile.title` is exactly `boop Hunter &lt;that same version&gt;`.
    - `python3 tools/check_release_gates.py --check versions` exits 0 after the bump.
  </acceptance_criteria>
  <done>The plan's implementation commit can satisfy the repository versioning rule and REL-01 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner -> local release checker | CI executes the repository-owned checker as a blocking gate. |
| maintainer docs -> local commands | CODEX guidance influences what maintainers run before push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-ci-bypass | Tampering | `.github/workflows/main.yml` | high | mitigate | Put `Release gates` before Muddler and keep it blocking so metadata/manifest/state gate failures stop CI. |
| T-01-local-drift | Repudiation | `CODEX.md` local command docs | medium | mitigate | Document the exact local commands CI uses so maintainers can reproduce failures and cannot rely on undocumented CI-only behavior. |
| T-01-dependency-churn | Tampering | CI dependencies | medium | accept | D-05 scopes broad pinning out of Phase 1; this plan adds only a Python stdlib command invocation and leaves existing dependencies unchanged. |
| T-01-SC | Tampering | package-manager installs | low | accept | Plan 02 adds no npm, pip, cargo, LuaRocks, or GitHub Action dependency. |
</threat_model>

<verification>
Run these checks after all Plan 02 tasks:
- `python3 tools/check_release_gates.py`
- `python3 -c "from pathlib import Path; s=Path('.github/workflows/main.yml').read_text(); names=['name: Release gates','name: Read package metadata','name: Muddle']; positions=[s.index(name) for name in names]; assert positions == sorted(positions)"`
- `grep -F "python3 tools/check_release_gates.py --check state-drift" CODEX.md`
</verification>

<success_criteria>
Plan 02 is complete when CI calls the local release gate before any package build work, the step blocks on failure, local usage is documented in CODEX, and version metadata remains synchronized.
</success_criteria>

<output>
Create `.planning/phases/01-release-gates-and-state-contracts/01-02-SUMMARY.md` when done.
</output>
