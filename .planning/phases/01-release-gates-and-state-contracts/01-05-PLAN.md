---
phase: 01-release-gates-and-state-contracts
plan: "05"
type: gap_closure
status: planned
gap_closure: true
gap_ids:
  - G-01-8B
  - G-01-8C
files_modified:
  - src/scripts/boop/boop_events.lua
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_init.lua
  - tests/boop_event_transitions_spec.lua
  - tests/boop_pull_spec.lua
  - mfile
  - CODEX.md
requirements:
  - REL-04
---

# Phase 01 Plan 05: Event And Pull State Gap Closure

<objective>
Move room transition, GMCP support recovery, and pull lifecycle behavior onto canonical owned state so the full Mudlet/Busted event and pull specs pass.
</objective>

<context>
@.planning/phases/01-release-gates-and-state-contracts/01-UAT.md
@.planning/debug/phase-01-full-mudlet-busted.md
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_ui.lua
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_init.lua
@tests/boop_event_transitions_spec.lua
@tests/boop_pull_spec.lua
@tests/support/boop_test_helper.lua
@tools/check_release_gates.py
</context>

## Tasks

1. Rewrite `boop.onRoomInfo()` around owned state domains.
   - Use `boop.state.targeting.room`, `lastRoom`, `lastRoomDir`, `movedRooms`, and `targetShield`.
   - Use `boop.state.combat.fleeing` and `boop.state.combat.pullState`.
   - Keep gold queue intent clearing, target-call clearing, shield timer cleanup, walk notifications, stats room-change hooks, and trace output intact.

2. Repair pull lifecycle behavior.
   - Ensure `boop.ui.pullCommand()` records timeout ids and sends the configured separator command.
   - Ensure room transitions move pull phase from `outbound` to `away`, then clear and resume boop only after returning to the origin room.
   - Ensure timeout cleanup resumes boop only when still at origin, and leaves boop paused when away.
   - Keep unsafe mob names and bare separator help behavior covered.

3. Make missing-IRE GMCP support recovery reliable.
   - When `gmcp.IRE`, `gmcp.IRE.Target`, or `gmcp.IRE.Display` is missing during `boop.onCharStatus()`, call `boop.requestCoreSupports()` in a way that is not suppressed by a recent non-recovery announce.
   - Preserve normal throttling for routine connection-ready announces.

4. Synchronize version fields for the commit.
   - Increment the current patch version once and write it to `mfile.version`, `mfile.title`, `src/scripts/boop/boop_init.lua`, and the `CODEX.md` checkpoint.

## Verification

- `luac -p src/scripts/boop/boop_events.lua src/scripts/boop/boop_ui.lua src/scripts/boop/boop_init.lua tests/boop_event_transitions_spec.lua tests/boop_pull_spec.lua`
- `python3 tools/check_release_gates.py`
- `muddle`
- Full Mudlet/Busted with `/tmp/Mudlet.AppImage`; the event transition and pull failures listed in `G-01-8B` and `G-01-8C` must be gone.

## Done

Gaps `G-01-8B` and `G-01-8C` are closed when room transitions, support retry, and pull lifecycle behavior all pass through owned state and the full suite no longer reports those failures.
