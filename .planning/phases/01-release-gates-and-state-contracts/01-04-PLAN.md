---
phase: 01-release-gates-and-state-contracts
plan: "04"
type: gap_closure
status: planned
gap_closure: true
gap_ids:
  - G-01-8A
files_modified:
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_attacks.lua
  - tests/boop_attacks_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
requirements:
  - REL-04
---

# Phase 01 Plan 04: Attack Context Gap Closure

<objective>
Make attack selection treat target HP as known only when it belongs to the current target, and close the shieldbreak-disabled rage fallback failure intentionally.
</objective>

<context>
@.planning/phases/01-release-gates-and-state-contracts/01-UAT.md
@.planning/debug/phase-01-full-mudlet-busted.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_attacks.lua
@src/scripts/boop/attacks/occultist.lua
@tests/boop_attacks_spec.lua
@tests/support/boop_test_helper.lua
@tools/check_release_gates.py
</context>

## Tasks

1. Fix current-target HP validation.
   - Preserve the GMCP target-info id in `boop.runtime.context()` or blank `target.hpperc` when `gmcp.IRE.Target.Info.id` does not match `boop.state.targeting.currentTargetId`.
   - Keep `boop.attacks.getTargetHpPercKnown()` conservative: mismatched or missing target-info id must not enable full-HP openers.
   - Verify `boop_attacks_spec.lua` no longer selects `attend` when target HP belongs to another id.

2. Close the shieldbreak-disabled rage fallback deliberately.
   - Reproduce `boop_attacks_spec.lua @ 48`.
   - If the intended production rule is "only known/ready rage abilities can be selected", seed Harry readiness in the test fixture and leave production strictness intact.
   - If the intended production rule is "configured damage rage may be selected when the IRE button surface is unavailable", adjust `boop.attacks.rageReady()` narrowly and add regression coverage for that behavior.

3. Synchronize version fields for the commit.
   - Increment the current patch version once and write it to `mfile.version`, `mfile.title`, `src/scripts/boop/boop_init.lua`, and the `CODEX.md` checkpoint.

## Verification

- `luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_attacks.lua tests/boop_attacks_spec.lua`
- `python3 tools/check_release_gates.py`
- `muddle`
- Full Mudlet/Busted with `/tmp/Mudlet.AppImage`; the two `boop_attacks_spec.lua` failures must be gone and no new attack-selection failures may appear.

## Done

Gap `G-01-8A` is closed when attack selection no longer trusts stale target HP and the shieldbreak-disabled fallback behavior is explicit, tested, and green in the full suite.
