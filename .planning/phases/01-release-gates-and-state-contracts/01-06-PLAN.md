---
phase: 01-release-gates-and-state-contracts
plan: "06"
type: gap_closure
status: planned
gap_closure: true
gap_ids:
  - G-01-8E
  - G-01-8F
files_modified:
  - src/scripts/boop/boop_ih.lua
  - src/scripts/boop/boop_targets.lua
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_ui_registry.lua
  - tests/support/boop_test_helper.lua
  - tests/boop_ih_spec.lua
  - tests/boop_stats_spec.lua
  - tests/boop_menu_wiring_spec.lua
  - tests/boop_ui_registry_spec.lua
  - tests/boop_ui_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
requirements:
  - REL-04
---

# Phase 01 Plan 06: Output And UI Registry Gap Closure

<objective>
Restore IH/plain whitelist output signal and make UI registries, menu wiring, and config callbacks stable after reset or reload.
</objective>

<context>
@.planning/phases/01-release-gates-and-state-contracts/01-UAT.md
@.planning/debug/phase-01-full-mudlet-busted.md
@src/scripts/boop/boop_ih.lua
@src/scripts/boop/boop_targets.lua
@src/scripts/boop/boop_ui.lua
@src/scripts/boop/boop_ui_registry.lua
@tests/support/boop_test_helper.lua
@tests/boop_ih_spec.lua
@tests/boop_stats_spec.lua
@tests/boop_menu_wiring_spec.lua
@tests/boop_ui_registry_spec.lua
@tests/boop_ui_spec.lua
@tools/check_release_gates.py
</context>

## Tasks

1. Repair IH and plain whitelist output.
   - Decide the intended policy for valid IH object rows: if they should expose whitelist/blacklist actions even before denizen recognition, update `boop.ih.handleLine()` and keep global blacklist suppression.
   - Reproduce the plain whitelist XP summary failure and ensure recorded mob XP appears in non-rich `boop.targets.displayWhitelist()` output.

2. Stabilize shared UI/config registries.
   - Ensure `boop.registry.attachUiConfigRegistries()` repairs stale `boop.config` and `boop.ui` tables after package reload.
   - Ensure `helper.reset()` leaves UI registry tables attached or production config paths reattach/fallback before dispatch.
   - Make `boop.ui.config()` numeric dispatch work for current sections and explicit section syntax.

3. Reconcile menu wiring expectations with the current UI.
   - Update `tests/boop_menu_wiring_spec.lua` counts and callback indices only where current controls were intentionally added or removed.
   - Keep callback behavior checks meaningful: verify actual handlers and command-line seeds, not just raw counts.
   - Preserve return-to-section behavior after rage mode and gold pack changes.

4. Synchronize version fields for the commit.
   - Increment the current patch version once and write it to `mfile.version`, `mfile.title`, `src/scripts/boop/boop_init.lua`, and the `CODEX.md` checkpoint.

## Verification

- `luac -p src/scripts/boop/boop_ih.lua src/scripts/boop/boop_targets.lua src/scripts/boop/boop_ui.lua src/scripts/boop/boop_ui_registry.lua tests/support/boop_test_helper.lua`
- `python3 tools/check_release_gates.py`
- `muddle`
- Full Mudlet/Busted with `/tmp/Mudlet.AppImage`; the IH, stats whitelist, menu wiring, UI registry, and UI config failures listed in `G-01-8E` and `G-01-8F` must be gone.

## Done

Gaps `G-01-8E` and `G-01-8F` are closed when output signal and UI/config registry behavior are stable in the full suite.
