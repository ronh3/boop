---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 15
type: execute
wave: 15
depends_on: [03-14]
files_modified:
  - tests/boop_lifecycle_spec.lua
  - tests/boop_diag_timeout_spec.lua
  - tests/boop_pull_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
  - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md
autonomous: true
requirements:
  - SAFE-02
gap_closure: true
gap_ids:
  - DIAG-TIMEOUT-CONTRACT
  - PULL-TIMER-IDENTITY
  - REAL-MUDLET-SUITE-ISOLATION
must_haves:
  truths:
    - "The lifecycle regression suite restores every replaced boop package table after each case, so the next shared real-Mudlet spec reaches helper.reset() with boop.state.init intact instead of cascading from tests/support/boop_test_helper.lua:164."
    - "Diagnose timeout/tombstone cases that expect runtime prompt consumption establish enabled automation, while the separate disabled lifecycle contract remains automation-inert and fail closed per D-01, D-02, D-05, D-06, D-07, and D-08."
    - "Pull assertions locate operation-owned timeout callbacks through state.combat.pullState.timeoutTimer, not by the shared 8.0-second delay; room-response warning timers remain present and cannot affect pull completion per D-05, D-06, D-08, and D-15."
    - "Focused host checks run in bounded isolated processes, except for one deliberate lifecycle-to-helper-reset canary; no broad shared-global host aggregate is presented as full-suite authority."
    - "The authoritative real-Mudlet suite reaches all specs with zero failures and zero errors, including the Psion and Dragon pull checks that the Occultist-only host bootstrap cannot judge."
    - "Live UAT remains blocked until the parent pushes immutable final HEAD and tools/wait_for_exact_ci.sh reports a successful main.yml run whose headSha exactly matches that HEAD."
  artifacts:
    - path: "tests/boop_lifecycle_spec.lua"
      provides: "Deterministic restoration of package-global state after disabled lifecycle tests."
    - path: "tests/boop_diag_timeout_spec.lua"
      provides: "Executable timeout/tombstone contracts with explicit enabled runtime preconditions."
    - path: "tests/boop_pull_spec.lua"
      provides: "Operation-owned pull timer identity assertions that coexist with equal-delay room-response timers."
    - path: ".planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md"
      provides: "RED/GREEN, package, focused-host, real-Mudlet availability, and parent exact-SHA handoff evidence."
  key_links:
    - from: "tests/boop_lifecycle_spec.lua"
      to: "tests/support/boop_test_helper.lua"
      via: "after_each restores the original boop.state table before a following spec calls helper.reset() and boop.state.init()"
      pattern: "(savedPackageFields|restorePackageFields|boop\\.state)"
    - from: "tests/boop_diag_timeout_spec.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "enabled runtime precondition allows boop.onPrompt() to consume diagnose evidence without weakening the disabled early-return contract"
      pattern: "config\\.enabled"
    - from: "tests/boop_pull_spec.lua"
      to: "src/scripts/boop/boop_ui.lua"
      via: "the fixture captures the exact current pullState.timeoutTimer ID and invokes that callback while unrelated response timers coexist"
      pattern: "pullState\\.timeoutTimer"
    - from: ".github/workflows/main.yml"
      to: "tests"
      via: "the real Mudlet profile imports the built package and executes the complete Busted directory"
      pattern: "AUTORUN_BUSTED_TESTS"
    - from: "tools/wait_for_exact_ci.sh"
      to: ".github/workflows/main.yml"
      via: "parent-owned exact-headSha successful workflow gate after every repository mutation"
      pattern: "(headSha|conclusion)"
---

<objective>
Close both failed verification contracts and the post-verification exact-CI suite-isolation failure without changing shipped hunting behavior.

Purpose: Plan 03-14 intentionally separated disabled lifecycle evidence from automation and calibrated the room-response warning to 8.0 seconds. Its affected test contracts must now reflect that boundary, identify asynchronous work by stable operation-owned IDs, and restore shared package globals so the authoritative real-Mudlet runner can execute every spec. This plan records the assumption-delta result as `decision: no-change`: canonical exact owner/timer identity remains primary, optional walker semantics remain unchanged, and the vocabulary match on “another safety hold” does not introduce a singular-to-plural architecture change.

Output: Restored suite isolation, GREEN diagnose timeout/tombstone contracts, GREEN operation-owned pull timer contracts, synchronized package version 0.1.429 after two package-affecting commits, complete focused/build evidence, and a parent-owned exact-final-SHA CI handoff.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-14-PLAN.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-14-SUMMARY.md

@tests/boop_lifecycle_spec.lua
@tests/boop_diag_timeout_spec.lua
@tests/boop_pull_spec.lua
@tests/support/boop_test_helper.lua
@tests/support/boop_host_busted_helper.lua
@.github/workflows/main.yml
@tools/wait_for_exact_ci.sh

<interfaces>
The executor must preserve these existing contracts:

- `tests/support/boop_test_helper.lua:M.reset()` clears non-function state data and then calls `boop.state.init()` at the current line 164. It is a downstream canary, not the source of the leaked package-table replacement.
- `tests/boop_lifecycle_spec.lua:before_each()` currently assigns fresh tables to `boop.state`, `boop.config`, `boop.handlers`, and `boop.gmcp`; its current `after_each()` restores replaced functions and globals but not those four package fields.
- `tests/boop_diag_timeout_spec.lua` uses `boop.onPrompt()` as the runtime evidence-consumption boundary. Plan 03-14 intentionally returns before runtime processing when `boop.config.enabled == false`.
- `tests/boop_pull_spec.lua:pullTimer()` already resolves the current operation timer through `boop.state.combat.pullState.timeoutTimer`; the separate `pullTimerCount()` is the stale delay-based heuristic.
- `src/scripts/boop/boop_events.lua` intentionally uses an 8.0-second room-response warning. The warning timer is unrelated to pull ownership and remains in the fixture.
- `tools/wait_for_exact_ci.sh` requires clean matching local HEAD, the exact SHA on origin, an exact-`headSha` `main.yml` run, successful conclusion, and unchanged local state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore lifecycle suite isolation and executable diagnose contracts</name>
  <files>tests/boop_lifecycle_spec.lua, tests/boop_diag_timeout_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>CODEX.md</file>
    <file>mfile</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-14-SUMMARY.md</file>
    <file>tests/boop_lifecycle_spec.lua</file>
    <file>tests/boop_diag_timeout_spec.lua</file>
    <file>tests/support/boop_test_helper.lua</file>
    <file>tests/support/boop_host_busted_helper.lua</file>
    <file>src/scripts/boop/boop_state.lua</file>
    <file>src/scripts/boop/boop_events.lua</file>
    <file>src/scripts/boop/boop_init.lua</file>
  </read_first>
  <behavior>
    - "RED isolation baseline: exact-SHA run 30246963329 reports 134 successes, 2 direct diagnose failures, and 552 errors after helper.reset() reaches line 164; the bounded host lifecycle->menu canary reproduces 4 successes followed by 26 reset/teardown errors."
    - "GREEN isolation: every lifecycle case restores the exact pre-test boop.state, boop.config, boop.handlers, and boop.gmcp references even after an assertion failure, and the following menu-wiring suite can call helper.reset() without a missing init method."
    - "GREEN diagnose runtime: the two cases that expect a prompt to absorb or drain diagnose evidence set boop.config.enabled=true before starting the operation and preserve all generation, owner, side-effect, and tombstone assertions."
    - "Disabled safety remains separate: the lifecycle zero-side-effect case continues to prove that disabled prompt evidence cannot run runtime step/apply, tick, gag, target, gold, queue, or walker automation."
  </behavior>
  <action>
Treat GitHub Actions run 30246963329 at exact head `efaeabad823c84da3dabeea69d1e35d6f7997dc8`, the current isolated diagnose result of 1 success / 2 failures, and the measured lifecycle-plus-menu result of 4 successes / 26 errors as the RED baseline. Do not change production lifecycle behavior to make stale tests pass.

In `tests/boop_lifecycle_spec.lua`, add a test-local `savedPackageFields` record and a `restorePackageFields()` helper. At the beginning of every `before_each`, before assigning any fresh table, capture the exact references for `boop.state`, `boop.config`, `boop.handlers`, and `boop.gmcp`. In `after_each`, restore replaced functions in reverse order, restore every replaced `_G` entry, then restore all four package fields to their captured references. Make restoration nil-safe when setup or a case fails partway through, and clear the saved record afterward so one test cannot reuse another test's snapshot.

The CI report describes the cascade in DB terms, but the current run log lands at `tests/support/boop_test_helper.lua:164`, whose current statement is `boop.state.init()`. The missing method is caused by lifecycle cases replacing `boop.state` and never restoring the module table. Fix that leak at the lifecycle test boundary. Do not add fallback production initialization, reload package modules from the helper, swallow reset errors, or weaken `M.reset()`; those approaches would conceal shared-state contamination instead of proving isolation.

In `tests/boop_diag_timeout_spec.lua`, set `boop.config.enabled = true` in the two cases named `absorbs N's late zero-argument result and prompt without mutating diagnose N+1` and `drains a timed-out head once and makes every later terminal call a no-op` before `boop.ui.diag()` creates the runtime operation. Keep the timeout-only owner-release case unchanged unless its behavior requires runtime prompt consumption. Retain all exact FIFO/tombstone, owner, timer, send, tick, warning, output, and trace assertions. The existing disabled lifecycle zero-effect test remains the required disabled-path contract per D-01, D-02, D-05, D-06, D-07, and D-08.

This task changes tests plus version metadata and is package-affecting. Bump the confirmed 0.1.427 baseline monotonically to 0.1.428 in `mfile.version`, `mfile.title` as `boop Hunter 0.1.428`, `src/scripts/boop/boop_init.lua` `boop.version`, and the `CODEX.md` synchronized package checkpoint. Change no other production line. Inspect staged paths and run `python3 tools/check_release_gates.py` before committing; never stage build output.
  </action>
  <acceptance_criteria>
    <criterion>`tests/boop_lifecycle_spec.lua` captures and restores the exact original references for `boop.state`, `boop.config`, `boop.handlers`, and `boop.gmcp` around every case, including failure cleanup.</criterion>
    <criterion>The bounded lifecycle-then-menu host canary completes with zero failures and zero errors, proving the following shared-helper suite can call `boop.state.init()`.</criterion>
    <criterion>All three diagnose-timeout cases pass in their own host process; the two prompt-consuming cases explicitly enable runtime processing, while the disabled lifecycle zero-effect test remains green.</criterion>
    <criterion>No production lifecycle, blocker, interrupt, room-warning, DB, attack, gold, or walker behavior changes; `boop_init.lua` changes only its synchronized version value.</criterion>
    <criterion>All four synchronized version checkpoints equal 0.1.428, staged paths are inspected, generated artifacts are absent from the staged set, and `python3 tools/check_release_gates.py` passes before the package-affecting commit.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_lifecycle_spec.lua tests/boop_diag_timeout_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_lifecycle_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_lifecycle_spec.lua tests/boop_menu_wiring_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_diag_timeout_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <diagnostic_scope>The two-spec lifecycle-to-menu invocation is a deliberate inter-spec restoration canary. It is not a broad host aggregate and does not substitute for the complete real-Mudlet suite.</diagnostic_scope>
  </verify>
  <done>The lifecycle suite no longer poisons shared package globals, the exact diagnose timeout/tombstone contracts are executable under their real enabled precondition, disabled automation remains inert, and the package is synchronized at 0.1.428.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace pull delay heuristics with operation-owned timer identity</name>
  <files>tests/boop_pull_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>CODEX.md</file>
    <file>mfile</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md</file>
    <file>tests/boop_pull_spec.lua</file>
    <file>tests/support/boop_test_helper.lua</file>
    <file>tests/support/boop_host_busted_helper.lua</file>
    <file>src/scripts/boop/boop_ui.lua</file>
    <file>src/scripts/boop/boop_events.lua</file>
    <file>src/scripts/boop/boop_init.lua</file>
  </read_first>
  <behavior>
    - "RED timer baseline: the full isolated host pull spec reports three actionable timer-count failures with observed 3/2/7 versus expected 1/1/3 because equal-delay room-response timers are misclassified."
    - "GREEN timer identity: every pull operation records only its exact state.combat.pullState.timeoutTimer ID, and counts/assertions derive from that ID set rather than delay."
    - "Coexistence: room-response warning timers remain captured at 8.0 seconds, have IDs outside the owned pull timer set, and do not alter return-wins, timeout-away, stale-callback, unrelated-owner, or operator-disabled outcomes."
    - "Host profile exclusions: the Psion and Dragon command cases are not changed to accommodate the Occultist-only host helper and are judged only by the authoritative real-Mudlet package run."
  </behavior>
  <action>
Use the current isolated host result of 8 successes / 5 failures as RED evidence. Classify exactly three failures as actionable timer-identity defects and the Psion/Dragon command failures as host-bootstrap exclusions. Do not change production attack profiles, pull behavior, the 8.0-second warning, or `tests/support/boop_host_busted_helper.lua` to make those two excluded host cases pass.

In `tests/boop_pull_spec.lua`, replace the delay-based `pullTimerCount()` heuristic with test-local operation identity. Add an `ownedPullTimerIds` set reset in `before_each`. When `pullTimer()` resolves `boop.state.combat.pullState.timeoutTimer`, validate the scheduled callback and expected configured delay, then record that exact timer ID in the set. Add or rename a helper such as `ownedPullTimerCount()` to count only unique IDs in that set. Never infer pull ownership from `timer.delay`.

Update the three affected assertions to use the operation-owned count. In return-wins and timeout-away cases, also prove that one or more scheduled timer IDs are outside `ownedPullTimerIds` after room transitions, that equal delay does not grant pull identity, and that invoking the captured pull timeout follows the existing generation/terminal rules. In the operator-disabled matrix, prove that exactly one owned timeout ID is captured per pull generation while additional room-response timers remain unrelated. Keep response-fence callbacks in the fixture rather than deleting or suppressing them.

Preserve every existing send, owner, generation, phase, terminal, warning, trace, tick, config, saveConfig, and stale-callback assertion per D-05, D-06, D-07, and D-08. Preserve the 8.0-second room warning per D-15. This implements the no-change assumption-delta decision: stable owner/timer identity remains primary, and optional walker semantics are untouched.

This task is a second package-affecting commit. Bump all four synchronized version checkpoints from 0.1.428 to 0.1.429. `src/scripts/boop/boop_init.lua` changes only its version value. Inspect staged paths and run `python3 tools/check_release_gates.py` before committing; never stage generated package artifacts.

Run the three exact actionable pull cases in three separate host Busted processes. Also run the complete pull spec in one isolated diagnostic process and confirm that any remaining host failures are limited to the two named Psion/Dragon profile checks with zero errors; do not call that diagnostic run authoritative or GREEN for those profiles.
  </action>
  <acceptance_criteria>
    <criterion>No test helper classifies a pull timer from its delay; the exact current `pullState.timeoutTimer` ID is the only source for the operation-owned timer set.</criterion>
    <criterion>The three exact pull lifecycle cases pass independently while captured equal-delay response-fence timers remain present and outside the owned pull timer set.</criterion>
    <criterion>Return-first, timeout-away, and stale callback paths preserve exact generation owners, unrelated blockers, first-terminal-wins behavior, config intent, and zero unintended sends/ticks.</criterion>
    <criterion>The Psion and Dragon expectations and production profiles are unchanged; host limitations are recorded as exclusions pending the real-Mudlet suite.</criterion>
    <criterion>All four synchronized version checkpoints equal 0.1.429, staged paths are inspected, generated artifacts are absent from the staged set, `python3 tools/check_release_gates.py` passes, and `muddle` succeeds before the package-affecting commit.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_pull_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; bash -o pipefail -c 'set -eu; for case_name in "boop pull command lets return win once and makes the captured timeout a zero-effect no-op" "boop pull command keeps the same owner after timeout away and releases once on matching return" "boop pull command keeps an operator-disabled session disabled after every terminal path"; do env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --name="$case_name" tests/boop_pull_spec.lua; done' &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
    <diagnostic_scope>The full host pull file remains diagnostic because its helper loads only Occultist. Only the three exact timer cases are host GREEN gates; Psion and Dragon require the complete real-Mudlet package run.</diagnostic_scope>
  </verify>
  <done>Pull timer assertions use operation-owned IDs, equal-delay room timers coexist without false identity, all three actionable host cases pass, production behavior remains unchanged, and the package is synchronized at 0.1.429.</done>
</task>

<task type="auto">
  <name>Task 3: Prove affected-suite reachability and hand off exact-final-SHA authority</name>
  <files>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>CODEX.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-PLAN.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md</file>
    <file>tests/boop_lifecycle_spec.lua</file>
    <file>tests/boop_diag_spec.lua</file>
    <file>tests/boop_diag_timeout_spec.lua</file>
    <file>tests/boop_interrupt_spec.lua</file>
    <file>tests/boop_pull_spec.lua</file>
    <file>tests/boop_event_transitions_spec.lua</file>
    <file>tests/boop_prequeue_spec.lua</file>
    <file>tests/boop_tick_spec.lua</file>
    <file>tests/support/boop_host_busted_helper.lua</file>
    <file>.github/workflows/main.yml</file>
    <file>tools/wait_for_exact_ci.sh</file>
    <file>mfile</file>
  </read_first>
  <action>
Rerun every affected host suite as its own Busted process: lifecycle, normal diagnose, diagnose timeout, interrupt, event transitions, prequeue, and tick. Run the lifecycle-plus-menu restoration canary as its own bounded two-spec process. Run each of the three actionable pull cases in a separate process. Do not run or cite a broad all-spec host aggregation, because the host helper does not reproduce the packaged profile load surface and shared package globals make such an aggregate an invalid authority.

Run the complete `boop_pull_spec.lua` once in its own diagnostic process. If it exits nonzero, inspect structured output and require zero errors plus no failures other than the exact Psion and Dragon command checks already identified as Occultist-only host-bootstrap limitations. Any timer, owner, generation, teardown, or unexpected profile failure blocks closeout. Record the diagnostic distinction in the summary.

Run `luac -p` over the three changed specs and synchronized init file, `python3 tools/check_release_gates.py`, `git diff --check`, and `muddle`. Confirm the final synchronized package version is 0.1.429 and no generated build artifact is tracked or staged. If executable `/tmp/Mudlet.AppImage` exists, run the exact full real-Mudlet command from CODEX.md against the complete `tests` directory and require zero failures/errors. If unavailable, record that local real-Mudlet execution is unavailable and do not substitute host Busted.

Create `03-15-SUMMARY.md` through the normal execution workflow. Record the authoritative RED evidence from run 30246963329, the exact focused GREEN commands/results, the lifecycle-to-helper-reset canary, the pull host exclusions, syntax/release/Muddler results, final synchronized version, changed paths, and that shipped behavior did not change. Do not edit `03-VERIFICATION.md`, `03-UAT.md`, existing PLAN/SUMMARY files, ROADMAP, REQUIREMENTS, or STATE from this task.

The executor does not claim terminal CI or live UAT. After this summary and every normal GSD tracking mutation are committed, the parent must inspect staged paths for any final commit, run `python3 tools/check_release_gates.py`, capture immutable `FINAL_SHA`, push it, and run `tools/wait_for_exact_ci.sh "$FINAL_SHA"`. Only a successful `main.yml` run with exact matching `headSha`, zero real-Mudlet failures, and zero errors closes automated Phase 03 authority. The Psion/Dragon checks must pass there. CI evidence is reported and not committed; any later repository mutation invalidates it. Do not proceed to live UAT until this parent gate is green.
  </action>
  <acceptance_criteria>
    <criterion>Lifecycle, diagnose, diagnose-timeout, interrupt, event-transition, prequeue, and tick specs pass in separate host processes, and the lifecycle-plus-menu canary passes with zero reset/teardown errors.</criterion>
    <criterion>The three actionable pull cases pass in separate host processes; the complete pull diagnostic has zero errors and no unexpected failure beyond the two documented host profile omissions.</criterion>
    <criterion>Lua syntax, release gates, diff hygiene, and Muddler pass at synchronized package version 0.1.429 with no tracked or staged generated build output.</criterion>
    <criterion>When local real Mudlet is available, its complete suite passes with zero failures/errors; otherwise the summary marks it unavailable and leaves authority to exact-SHA CI.</criterion>
    <criterion>`03-15-SUMMARY.md` distinguishes test-contract repair from production behavior, records no-change assumption delta, and reserves terminal exact-SHA CI and all live UAT for the parent.</criterion>
    <criterion>After all repository mutations, the parent reports a successful exact-final-SHA `main.yml` run; no live UAT begins before that report.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_lifecycle_spec.lua tests/boop_diag_timeout_spec.lua tests/boop_pull_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; bash -o pipefail -c 'set -eu; for spec in tests/boop_lifecycle_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua tests/boop_interrupt_spec.lua tests/boop_event_transitions_spec.lua tests/boop_prequeue_spec.lua tests/boop_tick_spec.lua; do env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua "$spec"; done; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_lifecycle_spec.lua tests/boop_menu_wiring_spec.lua; for case_name in "boop pull command lets return win once and makes the captured timeout a zero-effect no-op" "boop pull command keeps the same owner after timeout away and releases once on matching return" "boop pull command keeps an operator-disabled session disabled after every terminal path"; do env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --name="$case_name" tests/boop_pull_spec.lua; done' &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; git diff --check &amp;&amp; muddle</automated>
    <diagnostic_scope>Host commands are focused regression evidence. The complete real-Mudlet suite in local AppImage or exact-SHA GitHub Actions is the behavioral authority; the parent exact-SHA gate runs only after the executor and GSD finish all repository mutations.</diagnostic_scope>
  </verify>
  <done>All affected focused contracts and the shared-state canary are green, package/release gates pass at 0.1.429, the summary records honest authority boundaries, and the parent has an exact command for terminal CI before live UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle spec -> shared boop package namespace | A test replaces package-global tables that later specs trust to retain module methods. |
| timer callback queue -> pull assertions | Pull and room-response callbacks share delay values but have different operation ownership. |
| focused host bootstrap -> packaged real-Mudlet runtime | The host helper loads only Occultist while the built package loads the complete profile surface. |
| executor evidence -> terminal repository authority | Local focused/build results precede parent push and exact-final-SHA GitHub Actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15-01 | Tampering | `tests/boop_lifecycle_spec.lua` package-table replacement | high | mitigate | Snapshot and restore exact package-field references in failure-safe teardown; prove restoration through the bounded lifecycle-to-menu helper-reset canary and the real-Mudlet suite. |
| T-03-15-02 | Denial of Service | shared real-Mudlet Busted process | high | mitigate | Repair the first leaking suite rather than swallowing helper reset errors; require zero suite errors in authoritative CI before UAT. |
| T-03-15-03 | Spoofing | delay-based timer classification | high | mitigate | Resolve identity exclusively from the current pull operation's `timeoutTimer` ID and keep unrelated equal-delay callbacks visible in assertions. |
| T-03-15-04 | Elevation of Privilege | disabled prompt fixture | high | mitigate | Enable only runtime diagnose cases that expect prompt processing; retain the separate disabled zero-automation lifecycle contract and make no production early-return change. |
| T-03-15-05 | Repudiation | host profile omissions | medium | mitigate | Record Psion/Dragon as explicit host-bootstrap exclusions, preserve their contracts unchanged, and require the packaged real-Mudlet suite to pass them. |
| T-03-15-06 | Repudiation | pre-final CI evidence | high | mitigate | Parent captures immutable final HEAD after all mutations and uses the tracked exact-SHA script; CI evidence remains uncommitted and is invalidated by later mutation. |
| T-03-15-07 | Tampering | package version metadata | medium | mitigate | Bump all four checkpoints on each package-affecting commit and run release gates after staged-path inspection before commit/push. |
</threat_model>

<verification>
1. RED evidence is fixed and attributable: run 30246963329 at `efaeabad823c84da3dabeea69d1e35d6f7997dc8` failed with 134 successes / 2 failures / 552 errors; focused host runs reproduce both diagnose failures, all three actionable pull timer failures, and the lifecycle-to-helper-reset cascade.
2. GREEN test contracts are bounded: affected host specs run in separate processes, with only the deliberate lifecycle-plus-menu restoration canary sharing a process.
3. Pull identity is structural: the exact current operation timer ID is tested while equal-delay room-response timers remain present.
4. Package gates are blocking: every package-affecting commit synchronizes all four version checkpoints, staged paths are inspected, release gates pass, and Muddler builds from source.
5. Real-Mudlet authority is complete-suite only: local AppImage when available, otherwise the final exact-SHA GitHub Actions run. Psion/Dragon are judged there, not from the Occultist-only host helper.
6. Live UAT is gated behind successful parent exact-final-SHA CI and cannot start from host-only evidence.
</verification>

<success_criteria>
- Both failed `03-VERIFICATION.md` frontmatter truths are GREEN without production behavior changes.
- The 552-error reset cascade is eliminated at its leaking lifecycle teardown root, and authoritative execution reaches meaningful pull/profile results.
- Diagnose runtime prompt tests and disabled evidence-only behavior each exercise their correct explicit precondition.
- Pull tests distinguish operation ownership from equal timing and preserve the 8.0-second fail-closed room warning.
- Release metadata is synchronized through 0.1.429, release gates and Muddler pass, and generated artifacts remain untracked.
- Parent exact-final-SHA CI succeeds with zero real-Mudlet failures/errors before any live UAT.
</success_criteria>

## Artifacts this phase produces

No new production source file, command, API, blocker model, walker behavior, DB schema, or user-facing symbol is intended.

| Artifact or symbol | Status | Purpose |
|--------------------|--------|---------|
| `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-PLAN.md` | New planning file | Executable gap-closure contract for the two verification gaps and exact-CI isolation failure. |
| `savedPackageFields` in `tests/boop_lifecycle_spec.lua` | New test-local symbol | Captures exact pre-case package table references. |
| `restorePackageFields()` in `tests/boop_lifecycle_spec.lua` | New test-local symbol | Restores package tables after every lifecycle case, including failed cases. |
| `tests/boop_diag_timeout_spec.lua` | Modified test contract | Makes enabled runtime prompt preconditions explicit while retaining disabled coverage elsewhere. |
| `ownedPullTimerIds` in `tests/boop_pull_spec.lua` | New test-local symbol | Records exact operation-owned timer IDs. |
| `ownedPullTimerCount()` in `tests/boop_pull_spec.lua` | Renamed/reworked test-local symbol | Counts stable pull timer identity without delay heuristics. |
| `mfile`, `src/scripts/boop/boop_init.lua`, `CODEX.md` | Modified version contracts | Synchronized 0.1.428 then 0.1.429 metadata for the two package-affecting commits. |
| `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md` | New execution file | Records RED/GREEN evidence, final version, authority limits, and parent exact-SHA handoff. |

`tests/support/boop_test_helper.lua` and `tests/support/boop_host_busted_helper.lua` remain read-only contracts. The lifecycle spec must stop corrupting their required package surface; the helpers must not conceal the leak.

## Assumption delta

`decision: no-change`

The detector matched existing vocabulary about “another safety hold” and the optional walker, but this gap introduces no singular-to-plural identity change. Exact owner keys and exact operation timer IDs remain canonical. `demonnicAutoWalker` remains optional with the same explicit install/status and no-silent-update semantics.

## Parent exact-SHA CI gate

After `$gsd-execute-phase 03 --gaps-only` finishes this plan, writes its SUMMARY, and completes every normal GSD tracking mutation:

1. Inspect staged paths for any final commit and run `python3 tools/check_release_gates.py`.
2. Commit all authorized final mutations without changing the synchronized package version for planning-only paths.
3. Capture `FINAL_SHA="$(git rev-parse HEAD)"`, verify the worktree is clean, and push that immutable SHA.
4. Run `tools/wait_for_exact_ci.sh "$FINAL_SHA"`.
5. Require a successful `main.yml` run whose `headSha` exactly equals `FINAL_SHA`; the real-Mudlet summary must have zero failures and zero errors.
6. Report the run URL and exact SHA without committing the evidence.
7. If any later repository mutation occurs, repeat the gate for the new final SHA.
8. Do not begin live UAT until this gate succeeds.

## Multi-source coverage audit

### GOAL coverage

| Source item | Plan coverage | Status | Notes |
|-------------|---------------|--------|-------|
| Timing-sensitive command paths cannot attack, loot, or walk while another safety hold or room-state blocker owns the next action. | 03-01 through 03-15 | COVERED | Plans 03-01 through 03-14 implement behavior; 03-15 restores authoritative regression execution and exact identity assertions. |

### REQ coverage

| Requirement | Plan coverage | Status | Notes |
|-------------|---------------|--------|-------|
| SAFE-02 | 03-01, 03-02, 03-03, 03-08, 03-09, 03-14, 03-15 | COVERED | 03-15 closes diagnose/pull test authority and suite reachability without changing behavior. |
| SAFE-04 | 03-01, 03-04, 03-05, 03-08 through 03-11, 03-13, 03-14 | COVERED | Already satisfied; 03-15 does not claim or alter gold behavior. |
| WALK-01 | 03-01, 03-06, 03-07, 03-09, 03-11, 03-12, 03-14 | COVERED | Already satisfied; room-response timers remain present in pull fixtures. |
| WALK-02 | 03-01, 03-06 through 03-14 | COVERED | Already satisfied; no movement authorization changes. |
| WALK-03 | 03-06 through 03-09, 03-11, 03-12, 03-14 | COVERED | Already satisfied; host profile scope and optional walker behavior remain unchanged. |

### RESEARCH coverage

| Research feature or constraint | Plan coverage | Status | Notes |
|--------------------------------|---------------|--------|-------|
| Exact owner/generation checks, not broad booleans or timing coincidence, authorize asynchronous completion. | Task 2 | COVERED | Tests locate pull callbacks from the current operation-owned timer ID; production generation ownership remains unchanged. |
| Timer cancellation/ID is cleanup in production, while callback generation and terminal checks enforce correctness. | Task 2 | COVERED | Test identity finds the callback; existing generation/terminal assertions remain the behavioral authority. |
| Shared tests must reset real package state and restore every global/stub. | Task 1 | COVERED | Lifecycle teardown restores package table identity before downstream helper reset. |
| Existing Busted/luassert and captured callbacks remain the test stack. | Tasks 1-3 | COVERED | No new framework or dependency. |
| Real Mudlet CI is authoritative when local AppImage is unavailable. | Task 3 and parent gate | COVERED | Host checks are explicitly diagnostic/focused; final exact-SHA CI gates UAT. |
| Package/install legitimacy audit. | None | NOT APPLICABLE | No npm, pip, cargo, LuaRocks, or external package install task is added. |

### CONTEXT coverage

| Decision set | Plan coverage | Status | Notes |
|--------------|---------------|--------|-------|
| D-01 through D-04 — aggregate exact-owner all-clear and deterministic blocker authority | 03-01, 03-08, 03-15 tests | COVERED | 03-15 preserves owners and restores test authority; it adds no blocker model. |
| D-05 through D-08 — operation-specific evidence, owned timeout scope, idempotency, first-terminal-wins | Tasks 1-2 | COVERED | Diagnose preconditions and pull timer identity retain the exact generation/owner terminal contracts. |
| D-09 through D-12 — room-owned gold and inventory-owned packing | 03-04, 03-05, 03-10, 03-11, 03-13, 03-14 | COVERED | Unchanged and not claimed by 03-15. |
| D-13 through D-16 — walker all-clear, complete room evidence, capped warning, one move | 03-06, 03-07, 03-10 through 03-14 | COVERED | D-15's 8.0-second timer remains visible but unrelated to pull identity. |
| D-17 through D-20 — safe manual movement | 03-06 through 03-09 | COVERED | Unchanged and not claimed by 03-15. |
| D-21 through D-24 — owned stop/detach and fresh generation | 03-07, 03-09, 03-11, 03-12, 03-14 | COVERED | Unchanged and not claimed by 03-15. |
| Agent discretion — deterministic test organization with explicit timing and stale-callback assertions | Tasks 1-3 | COVERED | Focused processes, exact IDs, and a bounded restoration canary are selected. |
| Assumption delta false-positive vocabulary match | Objective and assumption-delta section | COVERED | Recorded as `decision: no-change`; owner/timer identity and optional walker semantics remain canonical. |
| Deferred hyena summary and temporary prefix/custom attack work | None | EXCLUDED | Remains assigned to later phases; no task or artifact implements it. |

### Authoritative evidence coverage

| Evidence | Plan coverage | Status |
|----------|---------------|--------|
| Run 30246963329 at exact SHA `efaeabad823c84da3dabeea69d1e35d6f7997dc8` failed with 134 successes / 2 failures / 552 errors. | Tasks 1 and 3 | COVERED |
| Two direct diagnose failures at cases beginning on lines 116 and 186. | Task 1 | COVERED |
| Shared-state cascade from helper line 164 prevents meaningful downstream execution. | Task 1 | COVERED |
| Three actionable pull timer assertions observe 3/2/7 instead of 1/1/3. | Task 2 | COVERED |
| Psion/Dragon host failures are Occultist-only bootstrap limitations. | Tasks 2-3 | COVERED |
| Terminal exact-final-SHA CI is the only final automated authority. | Task 3 and parent gate | COVERED |

Audit result: all GOAL, Phase 03 requirements, applicable RESEARCH constraints, locked CONTEXT decisions, and authoritative new evidence are covered across Plans 03-01 through 03-15. Deferred ideas remain excluded. No source item is unplanned and no phase split is required.

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md` when done. The summary must record run 30246963329 as RED evidence, focused GREEN results, lifecycle shared-state restoration, exact pull timer identity, final synchronized package version 0.1.429, release/Muddler evidence, real-Mudlet availability, unchanged production behavior, and the parent exact-final-SHA CI gate before live UAT.
</output>
