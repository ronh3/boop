---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "05"
type: execute
wave: 5
depends_on:
  - "03-04"
files_modified:
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - tests/boop_event_transitions_spec.lua
  - tests/boop_tick_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "Per D-09 and D-12, Room.Info/List/Add/text interleavings cannot authorize old-room get or retry work."
    - "Per D-10, room movement after confirmed get does not discard inventory-owned packing."
    - "Per D-11, target/room event duplication cannot create a second gold lifecycle."
    - "Per Phase 02 D-19 and D-20, real auto-flee cancels active gold intent, invalidates its timers/retries, schedules zero flush_gold resumption, emits zero get/put, and leaves hunting disabled until the existing explicit operator re-enable path."
    - "After a current GMCP/room, pull, or interrupt owner reaches its real release path and every other relevant owner is clear, one normal tick resumes exactly one unchanged current gold stage through one flush_gold effect."
  artifacts:
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Existing tickStep/applyEffects wiring for exact one-stage gold resumption"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Existing room/list/add/remove and gold terminal handlers wired to the Plan 03-04 operation"
    - path: "tests/boop_event_transitions_spec.lua"
      provides: "Room/target/gold interleaving, target-removal queue-drift, and terminal resumption coverage"
    - path: "tests/boop_tick_spec.lua"
      provides: "Zero attack while gold or another combat/queue owner remains"
  key_links:
    - from: "src/scripts/boop/boop_runtime.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "tickStep emits existing flush_gold; applyEffects calls existing boop.flushPendingGold(reason) once"
      pattern: "(flush_gold|flushPendingGold)"
---

<objective>
Wire the Plan 03-04 gold operation through room events and runtime tick effects, then prove cross-event SAFE-04 behavior.

Purpose: Focused gold correctness must survive actual Room.Info/List/Add/remove, target cleanup, destructive auto-flee cleanup, non-flee owner release, and attack reevaluation order.
Output: Gold room/event/tick wiring, destructive flee cancellation, exact non-flee resumption behavior, and integration regressions without expanding the Plan 03-04 core.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-03-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-04-SUMMARY.md

<interfaces>
Created production symbols and values — exhaustive:
- None. This plan creates no production constant, local function, public API, owner form, state/record/snapshot field, phase, timer/retry field, blocker code, or terminal reason.

Existing symbols modified:
- `boop.onRoomInfo`, `boop.onRoomItemsList`, `boop.onRoomItemsAdd`, `boop.onRoomItemsRemove`, local `tickStep`, `boop.runtime.applyEffects`, `boop.flushPendingGold`, and `boop.maybeFlushPendingGold`.
- Plan 03-04 symbols `GOLD_PHASE`, `state.gold.operation`, `currentGoldOperation`, `goldDispatchAuthorized`, `completeGoldOperation`, and owner `gold:<generation>` are consumed unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin flee cancellation and non-flee gold interleavings</name>
  <files>tests/boop_event_transitions_spec.lua, tests/boop_tick_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_event_transitions_spec.lua` — modified room/target/gold ordering and real-owner terminal coverage.
    - `tests/boop_tick_spec.lua` — modified aggregate hold and one-stage resumption coverage.
    - `src/scripts/boop/boop_runtime.lua` — `tickStep`, `applyEffects`, aggregate holds, and `flush_gold` effect under test.
    - `src/scripts/boop/boop_events.lua` — Room.Info/List/Add/remove and gold terminal/retry entry points under test.
    - `tests/boop_gold_spec.lua` — Plan 03-04 staged gold contract.
    - `tests/boop_gold_retry_spec.lua` — Plan 03-04 retry/timeout contract.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, and D-09 through D-12.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — gold, gold-owner authorization, and target-removal ordering rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Room transition ordering pattern`, `tests/boop_event_transitions_spec.lua`, `tests/boop_tick_spec.lua`, `Preserve target-removal queue-drift regression`, and `Gold ownership transfer`.
  </read_first>
  <behavior>
    - "Old room callbacks cannot send or mutate current gold."
    - "Target removal cleans target state but sends no attack during gold ownership."
    - "Pack terminal reevaluates once; stale terminals reevaluate zero times."
    - "Real auto-flee cancels the active gold operation, invalidates its timers/retries, schedules zero flush_gold resumption, emits zero get/put, and keeps hunting disabled until explicit operator re-enable."
    - "Current GMCP/room, pull, and interrupt owners block, then their real release paths resume exactly one unchanged current stage only after every other relevant owner is clear."
  </behavior>
  <acceptance_criteria>
    - Stale Room.Info/List/Add/remove/text callbacks produce zero gold sends and do not change the current gold generation, phase, retry counters, or timers.
    - Target removal clears target intent while standard/rage sends and prequeue execution remain zero during an active gold owner; the existing no-queue-clear assertion remains intact.
    - Winning pack terminal schedules exactly one reevaluation; a stale terminal schedules zero ticks, zero `flush_gold` effects, and zero sends.
    - For initial get, put, get retry, and put retry, the real auto-flee path cancels the active gold operation, invalidates every operation timer/retry callback, schedules zero ticks and zero `flush_gold`, emits zero get/put commands, and leaves hunting disabled until the existing explicit operator re-enable path.
    - For initial get, put, get retry, and put retry, each current GMCP/room, pull, or interrupt owner blocks with zero send/retry consumption; its real release path produces one tick, one `flush_gold`, one `boop.flushPendingGold(reason)`, and one unchanged-stage send only after every other relevant owner is clear.
    - Walk reevaluation remains zero until gold and every unrelated walk-affecting owner are terminal.
    - One patch increment synchronizes all four version checkpoints, and focused syntax plus release gates succeed.
  </acceptance_criteria>
  <action>
Interleave current and stale Room.Info/List/Add/remove/text callbacks with get success, room movement, pack success/failure, target removal, and ticks. Preserve the existing target-removal queue-drift meaning and exact no-queue-clear assertion. For every active initial get, put, get retry, and put retry stage, invoke the real auto-flee path and assert that it cancels the gold operation, invalidates captured timers/retries, schedules zero ticks and zero `flush_gold`, emits zero get/put, and leaves hunting disabled until the existing explicit operator re-enable path.

Separately table-drive current GMCP/room, pull, and interrupt owners: assert zero send before release, invoke each owner's real release path, keep any remaining relevant owner active to prove no early resume, then clear the final owner and assert one tick, one `flush_gold`, one `boop.flushPendingGold(reason)`, and one unchanged-stage send with no premature retry/generation mutation. Assert exact standard/rage send and walk reevaluation counts. Synchronize the next patch version before commit; do not edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_event_transitions_spec.lua tests/boop_tick_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run the focused event-transition/tick specs in the real-Mudlet suite or exact corresponding CI. Evidence must separately show destructive real auto-flee cancellation with zero gold resumption and unchanged-stage resumption only for current GMCP/room, pull, and interrupt owners after their real release paths and all other relevant owners clear.</authoritative>
  </verify>
  <done>SAFE-04 room/target/owner interleavings explicitly prove destructive real auto-flee cancellation and unchanged-stage resumption only for released non-flee owners, with exact sends, ticks, flushes, and stale no-ops.</done>
</task>

<task type="auto">
  <name>Task 2: Wire gold event invalidation and one-stage tick resumption</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, tests/boop_event_transitions_spec.lua, tests/boop_tick_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_runtime.lua` — modified `tickStep`, `applyEffects`, and aggregate hold ordering.
    - `src/scripts/boop/boop_events.lua` — modified room invalidation, evidence coalescing, and gold terminal wiring.
    - `tests/boop_event_transitions_spec.lua` — modified cross-event implementation assertions.
    - `tests/boop_tick_spec.lua` — modified one-stage effect and aggregate combat-hold assertions.
    - `tests/boop_gold_spec.lua` — staged operation APIs from Plan 03-04.
    - `tests/boop_gold_retry_spec.lua` — generation-safe retry/timeout APIs from Plan 03-04.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-09 through D-12 plus D-01/D-02 all-clear semantics.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — gold owner-authorization and cross-lifecycle rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Coordinator hold-before-effect pattern`, `Shared room-list observation pattern`, `Room transition ordering pattern`, `Gold ownership transfer`, and `Prove cleanup/authorization precedes the first side effect`.
  </read_first>
  <behavior>
    - "Room transitions invalidate only room-owned gold phases."
    - "Current complete list re-evaluates deferred gold once."
    - "Winning owner terminal emits one flush_gold effect."
    - "Real auto-flee cancels gold and never emits a flush_gold effect."
    - "Combat remains held until all combat/queue owners clear."
  </behavior>
  <acceptance_criteria>
    - Current Room.Info invalidates only current-generation `DEFERRED_ROOM`/`PICKUP_PENDING`; it does not clear `PACK_PENDING`.
    - A complete current room list reevaluates one deferred operation once; duplicate Add/remove/text signals coalesce without creating another generation or send.
    - `tickStep` emits one existing `flush_gold` effect only when exact self-owner exclusion leaves no unrelated owner affecting the active stage.
    - `applyEffects` invokes `boop.flushPendingGold(reason)` exactly once and consumes no retry/generation state when authorization prevents a send.
    - The existing real auto-flee cleanup remains destructive per Phase 02 D-19/D-20: active gold is canceled, its timers/retries are invalidated, no `flush_gold` or get/put is emitted, and hunting remains disabled until explicit operator re-enable.
    - Gold terminal reevaluation emits zero attack and zero movement effects while any combat/queue/walk owner remains.
    - Focused event/tick specs, Lua syntax, and the release gate succeed after one synchronized patch increment across all version checkpoints.
  </acceptance_criteria>
  <action>
Modify only the existing symbols listed in `<interfaces>`. Make Room.Info invalidate `DEFERRED_ROOM`/`PICKUP_PENDING` by current generation while preserving `PACK_PENDING`; complete room Lists reevaluate the current deferred operation once; Add/remove/text evidence coalesces through `startGoldOperation`. In `tickStep`, evaluate an active gold stage before the broad combat hold return using only its exact owner exclusion; when unrelated non-flee gold/queue owners clear, emit one existing `flush_gold` effect. Preserve Phase 02 D-19/D-20 by treating real auto-flee as destructive gold cleanup, never as a releasable owner that resumes an unchanged stage. In `applyEffects`, call `boop.flushPendingGold(reason)` once and do not consume retries or generation unless a send actually occurs. Ensure gold terminal reevaluation does not attack or move while another owner remains. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua tests/boop_event_transitions_spec.lua tests/boop_tick_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run event-transition/tick specs and then the full real-Mudlet suite; use exact corresponding CI when local Mudlet is unavailable.</authoritative>
  </verify>
  <done>Gold core is reachable from actual room events and resumes exactly one stage without attack/movement bypass.</done>
</task>

</tasks>

## Artifacts this plan creates

- No created production symbols or values. Integration tests are added to existing spec files only.
- Existing files only; no new command, dependency, schema, manifest, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Room/target events → gold operation | Event order can invalidate ownership or duplicate work. |
| Owner terminal → gold/combat effects | One release can incorrectly resume stale loot or attack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-GOLD-EVENT | Spoofing / Tampering | Room.Info/List/Add/remove | high | mitigate | Current-generation phase checks and exact zero-effect stale tests. |
| T-03-GOLD-RESUME | Tampering | tick/applyEffects | high | mitigate | Destructive flee cancellation is asserted separately from exact-owner exclusion and one-stage resumption for released non-flee owners. |
</threat_model>

<verification>
- No final plan in this split exceeds nine files.
- Syntax/release gates accompany each task; real Mudlet/CI proves behavior.
- Plan 03-04 owns new symbols; this plan only wires and tests them.
- Phase 02 D-19/D-20 destructive flee cleanup is preserved, while Phase 03 D-09 through D-12 unchanged-stage resumption is limited to released non-flee owners.
</verification>

<success_criteria>
- SAFE-04 is covered across room, target, gold, attack, retry, timeout, and owner-terminal interleavings.
- Real auto-flee cancels active gold with zero `flush_gold`/get/put side effects and no automatic hunting re-enable; current GMCP/room, pull, and interrupt releases resume one unchanged stage only after all relevant owners clear.
- Target-removal queue-drift coverage remains intact.
- Version fields stay synchronized per commit and built artifacts remain untouched.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md` when done.
</output>
