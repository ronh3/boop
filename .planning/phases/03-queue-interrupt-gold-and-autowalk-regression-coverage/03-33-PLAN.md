---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 33
type: execute
wave: 33
depends_on:
  - 03-32
files_modified:
  - src/scripts/boop/boop_rage.lua
  - src/triggers/boop/Rage/triggers.json
  - tests/boop_rage_ingestion_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
gap_closure: true
gap_ids:
  - G-03-25
requirements:
  - SAFE-02
  - SAFE-04
assumption_delta_decision:
  primary_noun: owner-keyed operation/blocker generation
  decision: no-change
  rationale: this gap closure repairs established multi-owner lifecycle behavior and does not generalize identity or walker optionality
must_haves:
  truths:
    - The exact no-abilities Battlerage recovery line reopens only the global cooldown gate.
    - No ability is marked ready, and unrelated Rage state is unchanged by that line.
    - Mudlet routes the exact anchored line through the established Rage outcome adapter, while near matches remain unrecognized.
  artifacts:
    - path: src/scripts/boop/boop_rage.lua
      provides: Exact no-abilities recovery parser branch and named line constant.
    - path: src/triggers/boop/Rage/triggers.json
      provides: Anchored exact trigger alternative routed through the established Rage outcome adapter.
    - path: tests/boop_rage_ingestion_spec.lua
      provides: Direct-parser and manifest-to-adapter integration regressions for the exact and near-match lines.
    - path: mfile
      provides: Package version 0.1.476 and synchronized title.
    - path: src/scripts/boop/boop_init.lua
      provides: Runtime package version 0.1.476.
    - path: CODEX.md
      provides: Synchronized package-version checkpoint 0.1.476.
  key_links:
    - from: src/triggers/boop/Rage/triggers.json
      to: src/triggers/boop/Rage/Rage_Command_Outcome.lua
      via: One anchored exact no-abilities recovery alternative.
    - from: src/triggers/boop/Rage/Rage_Command_Outcome.lua
      to: boop.rage.onCommandOutcome
      via: Existing thin adapter call with the matched live line.
    - from: boop.rage.onCommandOutcome
      to: boop.state.rage.globalCooldownOpen
      via: Exact equality branch that mutates only the global cooldown gate.
    - from: tests/boop_rage_ingestion_spec.lua
      to: src/triggers/boop/Rage/triggers.json
      via: Actual manifest loading, adapter dispatch, and before/after state snapshots.
---

<objective>
Atomically teach and package the exact alternate Battlerage recovery sentence from trigger match through the established adapter and parser state transition.

Purpose: Close the implementation side of G-03-25 in one package-affecting commit while preserving the distinction between the global cooldown gate and per-ability readiness.
Output: Exact parser semantics, anchored trigger wiring, integrated manifest/adapter regressions, and synchronized package version 0.1.476.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-28-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-32-SUMMARY.md
@src/scripts/boop/boop_rage.lua
@src/triggers/boop/Rage/triggers.json
@src/triggers/boop/Rage/Rage_Command_Outcome.lua
@tests/boop_rage_ingestion_spec.lua
</context>

## Artifacts this phase produces

- `boop.rage.NO_ABILITIES_RECOVERY`: the canonical exact sentence recognized by the Rage parser.
- `src/scripts/boop/boop_rage.lua`: exact global-cooldown-only state transition.
- `src/triggers/boop/Rage/triggers.json`: anchored exact recovery alternative routed to the existing `Rage_Command_Outcome.lua` adapter.
- No new adapter symbol: `Rage_Command_Outcome.lua` remains the thin call site for existing `boop.rage.onCommandOutcome`.
- `tests/boop_rage_ingestion_spec.lua`: direct parser plus real-manifest/adapter coverage for exact recognition, no fabricated ability readiness, preserved unrelated state, and rejected near matches.
- Synchronized version `0.1.476` in `mfile.version`, `mfile.title`, `boop.version`, and the `CODEX.md` checkpoint.
- `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-33-SUMMARY.md` at execution completion.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse and package the exact no-abilities recovery atomically</name>
  <files>src/scripts/boop/boop_rage.lua, src/triggers/boop/Rage/triggers.json, tests/boop_rage_ingestion_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - AGENTS.md for package-source, version synchronization, release-gate, and immutable-final-HEAD rules.
    - CODEX.md for the current 0.1.475 checkpoint produced by Plan 03-32 and the repository test/build workflow.
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md, G-03-25 only, for the exact missing wire sentence and required semantics.
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md, Test 11 only, for the observed live line.
    - src/scripts/boop/boop_rage.lua in full once, especially `boop.rage.onCommandOutcome`, the Available-list branch, state initialization, pending-operation handling, Triumph state, and return convention.
    - src/triggers/boop/Rage/triggers.json in full once for the current Available-list, cooldown, expiry, denial alternatives, and JSON escaping conventions.
    - src/triggers/boop/Rage/Rage_Command_Outcome.lua in full once to confirm its existing thin call to `boop.rage.onCommandOutcome` and that no adapter mutation is needed.
    - tests/boop_rage_ingestion_spec.lua in full once for host setup, state fixtures, actual manifest loading, regex matching, adapter dispatch, and direct-ingestion assertions.
    - mfile, src/scripts/boop/boop_init.lua, and the version checkpoint in CODEX.md before changing any package file.
  </read_first>
  <behavior>
    - Exact input `You can use another Battlerage ability again, but none of your abilities are currently available.` returns recognized and changes `globalCooldownOpen` from false to true.
    - The exact line matches one anchored packaged trigger and reaches `boop.rage.onCommandOutcome` through the existing adapter exactly once.
    - The exact input leaves every existing ability-ready key and value byte-for-byte unchanged; in particular it never changes a false ability to true and never creates a synthetic ability key.
    - The exact input leaves pending Rage ownership/terminal state, Triumph grant state, expiry state, and local pacing state unchanged.
    - Punctuation changes, prefixes, suffixes, and merely similar prose fail at the trigger/parser boundary and leave the complete Rage state unchanged.
  </behavior>
  <action>
Write direct and integrated tests first and prove the exact sentence currently fails both parser recognition and packaged trigger routing. Add `boop.rage.NO_ABILITIES_RECOVERY` beside the existing exact Rage constants. In `boop.rage.onCommandOutcome`, recognize only equality with that constant and set only `state.globalCooldownOpen = true`; do not route this sentence through the nonempty Available-list parser, mutate any `state.ready` entry, or terminalize/synthesize a Rage operation. Add the exact anchored, escaped trigger alternative `^You can use another Battlerage ability again, but none of your abilities are currently available\.$` to the existing Rage outcome trigger in `triggers.json`. Keep `Rage_Command_Outcome.lua` unchanged as the thin adapter to `boop.rage.onCommandOutcome`; do not duplicate semantics in JSON or the adapter. The integrated regression must load the actual manifest, invoke the actual adapter once, snapshot state before/after, reject near matches, and retain all neighboring Available-list, cooldown, expiry, insufficient-rage, and command-outcome cases. Preserve D-01 through D-08. The assumption-delta decision remains no-change: the primary noun is owner-keyed operation/blocker generation, and this repair does not generalize identity or optional DemonWalker wording. After the combined behavior passes, bump all four package-version authorities once from 0.1.475 to 0.1.476: `mfile.version`, `mfile.title` as `boop Hunter 0.1.476`, `boop.version`, and the `CODEX.md` checkpoint. Do not edit generated artifacts or docs/help because no command surface changes. Create one atomic package-affecting execution commit containing parser, trigger manifest, integrated tests, and synchronized version; do not push.
  </action>
  <acceptance_criteria>
    - The exact sentence is recognized by direct parser ingestion and by one anchored manifest match routed through `Rage_Command_Outcome.lua` to `boop.rage.onCommandOutcome`.
    - A deep snapshot of per-ability readiness and unrelated Rage state is identical before and after ingestion.
    - At least three near matches fail trigger/parser recognition with no state mutation, and existing Rage alternatives remain green.
    - All four version authorities report 0.1.476 before the execution commit.
    - Only the six declared paths are modified; the adapter, docs/help, and built artifacts remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_rage_ingestion_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_rage.lua src/triggers/boop/Rage/Rage_Command_Outcome.lua tests/boop_rage_ingestion_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 -m json.tool src/triggers/boop/Rage/triggers.json &gt;/dev/null &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>The exact line traverses the packaged trigger and existing adapter into `boop.rage.onCommandOutcome`, only the global gate changes, release gates and Muddler pass at synchronized version 0.1.476, and the one atomic package-affecting commit remains unpushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Achaea text to Rage state | Untrusted server text may open a combat timing gate. |
| Trigger manifest to Lua adapter | Packaging or regex drift could prevent authoritative evidence from reaching the parser. |
| Parser transition to queued offense | Incorrectly fabricated readiness could authorize an attack that the server cannot execute. |
| Source tree to Mudlet package | Runtime source and package metadata must remain synchronized through the build boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-33-01 | Spoofing | No-abilities text recognition | high | mitigate | Require exact sentence equality and regress near matches. |
| T-03-33-02 | Tampering | Per-ability readiness table | high | mitigate | Snapshot the table and assert zero key/value mutation while only the global gate opens. |
| T-03-33-03 | Repudiation | Trigger-to-parser routing | medium | mitigate | Load the actual manifest and assert one existing-adapter call to `boop.rage.onCommandOutcome`. |
| T-03-33-04 | Denial of Service | Global cooldown gate | medium | mitigate | Reopen the gate on authoritative recovery while retaining all ability-specific availability checks. |
| T-03-33-SC | Tampering | Package/build inputs | high | mitigate | Install no packages; require release gates, Lua syntax checks, synchronized versions, and Muddler. |
</threat_model>

<verification>
- Direct and real-manifest Rage ingestion tests pass before the full build gate.
- The parser and trigger each use the exact canonical sentence, with the existing adapter targeting `boop.rage.onCommandOutcome`.
- `python3 tools/check_release_gates.py` confirms synchronized 0.1.476 metadata.
- JSON/Lua checks and `muddle` succeed without editing built output.
- G-03-25 remains open for live authority until Plan 03-36 records Test 11.
</verification>

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|--------|------|-------------|
| GOAL | A timing-sensitive command cannot run from fabricated readiness. | Covered by the global-only transition and unchanged per-ability table. |
| REQ | SAFE-02, SAFE-04 | Covered by exact evidence ingestion and preservation of the closed ability gates. |
| RESEARCH | Rage cooldown/readiness are separate authorities and trigger adapters stay thin. | Covered by the isolated state mutation, real manifest, and unchanged adapter. |
| CONTEXT | D-01 through D-08 owner-specific lifecycle rules. | Preserved explicitly; this line does not terminalize an operation or release an unrelated owner. |
| CONTEXT | Deferred temp attack, AOE, and `ch` ideas. | Excluded as out of scope. |

<success_criteria>
- Exact parser, trigger, and integrated adapter behavior pass atomically at version 0.1.476.
- No ability is falsely marked ready and no unrelated Rage state changes.
- Live authority remains for Plan 03-36; this plan does not claim G-03-25 or Phase 3 passed.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-33-SUMMARY.md` when done.
</output>
