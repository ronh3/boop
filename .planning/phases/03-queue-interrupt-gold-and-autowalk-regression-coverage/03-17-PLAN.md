---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 17
type: execute
wave: 17
depends_on: [03-16]
files_modified:
  - tests/boop_walk_spec.lua
  - tests/boop_ui_spec.lua
  - src/scripts/boop/boop_walk.lua
  - src/scripts/boop/boop_ui.lua
  - README.md
  - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements: [WALK-01, WALK-02, WALK-03]
gap_closure: true
gap_ids: [G-03-4]
must_haves:
  truths:
    - "Manual targeting remains a real automatic-walk safety hold and active walk status identifies `manual_targeting`, not `room_clear`."
    - "The manual-targeting blocker tells the operator to run `boop targeting auto`; no movement reservation or command occurs while manual targeting is active."
    - "A non-silent inactive `boop walk stop` emits explicit compact feedback, while silent stop and active owned/attached stop behavior remain unchanged."
    - "The Phase 3 live UAT expects movement only after automatic targeting has been selected."
  artifacts:
    - "tests/boop_walk_spec.lua covers inactive stop output and manual-targeting movement gating."
    - "tests/boop_ui_spec.lua covers command routing and canonical blocker presentation."
    - "src/scripts/boop/boop_walk.lua owns stop feedback and the shared blocker evaluator."
    - "src/scripts/boop/boop_ui.lua projects the shared evaluator into status/dashboard output."
    - "README.md and 03-UAT.md describe the corrected operator sequence."
  key_links:
    - "Active walk status -> shared `evaluateAllClear` result -> `manual_targeting` code/label/action."
    - "`boop walk stop` alias/UI route -> `boop.walk.stop(false)` -> explicit inactive feedback."
    - "UAT manual start -> no movement -> `boop targeting auto` -> one eligible movement."
---

<objective>
Close G-03-4 while preserving manual targeting as a deliberate safety hold.

Purpose: Make the real walk blocker and inactive stop state legible to operators without weakening D-13 through D-24 movement and stop ownership rules.
Output: Command/UI regressions, shared blocker projection, explicit inactive-stop feedback, and corrected live UAT instructions.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@UIDESIGN.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-live-walk-no-start-feedback.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md
@src/scripts/boop/boop_walk.lua
@src/scripts/boop/boop_ui.lua
@tests/boop_walk_spec.lua
@tests/boop_ui_spec.lua
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify manual-targeting status and inactive-stop command behavior</name>
  <files>tests/boop_walk_spec.lua, tests/boop_ui_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - tests/boop_walk_spec.lua
    - tests/boop_ui_spec.lua
    - src/scripts/boop/boop_walk.lua
    - src/scripts/boop/boop_ui.lua
    - src/aliases/boop/Core/Boop_Walk.lua
    - .planning/debug/phase-03-live-walk-no-start-feedback.md
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Direct `boop.walk.stop(false)` while inactive returns false and emits exactly one INFO line: `walk stop: no active boop walk`."
    - "Direct `boop.walk.stop(true)` while inactive returns false and emits nothing."
    - "The `boop walk stop` UI command uses the non-silent path; active owned and attached runs retain their existing distinct stop/detach lines."
    - "An active, otherwise-settled walk under manual targeting reports code `manual_targeting`, label `manual targeting is active`, and next action `boop targeting auto` in command and canonical status surfaces."
    - "Manual targeting creates no move reservation and sends no move; changing only the targeting mode to automatic permits exactly one eligible move."
  </behavior>
  <action>
Add failing focused examples for the walk module and the real UI command route. Capture feedback at the utility boundary and assert exact line counts and text. Build an active settled walk fixture with no higher-priority runtime owner so the UI fallback must consult the shared evaluator; assert that `room_clear` is not reported for this state and that the operator action is `boop targeting auto`.

Exercise D-01/D-02 aggregate all-clear, D-03 primary diagnostic detail, D-04 priority, D-13/D-14 movement eligibility, D-16 one move per room, D-17 through D-20 safe manual movement, and D-21 through D-24 stop ownership/restart behavior. Preserve existing active-owned and attached-external assertions rather than replacing them.

Before committing, bump all synchronized package fields from 0.1.431 to 0.1.432. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `test(03-17): reproduce walk feedback and blocker gap`. Do not push.
  </action>
  <acceptance_criteria>
    - "The new tests fail on the misleading room-clear fallback and missing inactive-stop feedback."
    - "The tests prove manual targeting blocks both reservation and movement until automatic targeting is selected."
    - "Existing active stop/detach contracts remain asserted."
    - "All synchronized version fields read 0.1.432 and the staged release gate passes."
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'output=$(env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_walk_spec.lua tests/boop_ui_spec.lua 2&gt;&amp;1); rc=$?; printf "%s\n" "$output"; test "$rc" -ne 0; printf "%s\n" "$output" | rg -q "manual_targeting|no active boop walk"; python3 tools/check_release_gates.py'</automated>
  </verify>
  <done>The red command/UI suite exposes both G-03-4 symptoms with all safety invariants represented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project the shared manual hold and emit inactive-stop feedback</name>
  <files>src/scripts/boop/boop_walk.lua, src/scripts/boop/boop_ui.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - src/scripts/boop/boop_walk.lua
    - src/scripts/boop/boop_ui.lua
    - tests/boop_walk_spec.lua
    - tests/boop_ui_spec.lua
    - src/scripts/boop/boop_runtime.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "The shared evaluator remains the authority for manual-targeting safety."
    - "UI canonical runtime owners keep their current priority; only the active-walk fallback consults the evaluator before considering room-clear."
    - "The inactive non-silent stop path emits one line and returns false; the silent path returns false without output."
    - "Owned stop invalidates first and stops boop-owned demonwalker; attached stop detaches without stopping an external run."
  </behavior>
  <action>
In `boop_walk.lua`, keep `evaluateAllClear()` returning `manual_targeting` for manual mode and expose enough structured blocker detail for UI projection, including the exact next action `boop targeting auto`. Update `stop(silent, external)` so an inactive non-silent request emits exactly `walk stop: no active boop walk`; do not change active-owned, attached-external, invalidation-first, fresh-restart, or return-value semantics per D-21 through D-24.

In `boop_ui.lua`, retain canonical runtime-owner, disabled, leader, and target priority. For an active walk with no higher-priority owner, ask the shared evaluator for its current blocker and surface `manual_targeting` with its label/action before the existing room-clear fallback. Do not duplicate the safety predicate in UI and do not weaken D-01 through D-04 owner aggregation or D-13 movement gating.

Run focused tests to green, parse both Lua files, and run `muddle`. Before committing, bump all synchronized package fields from 0.1.432 to 0.1.433. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `fix(03-17): expose manual walk hold and inactive stop`. Do not push.
  </action>
  <acceptance_criteria>
    - "All active status surfaces agree on `manual_targeting` and the automatic-targeting action."
    - "No movement occurs in manual mode, and one eligible move occurs after automatic mode is selected."
    - "Inactive, owned, and attached stop feedback remain distinct and exact."
    - "All synchronized version fields read 0.1.433; focused tests, syntax checks, release gates, and muddle pass."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_walk_spec.lua tests/boop_ui_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_walk.lua &amp;&amp; luac -p src/scripts/boop/boop_ui.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>The implementation preserves the manual safety hold while accurately explaining it and acknowledging inactive stop requests.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the operator documentation and Phase 3 live walk procedure</name>
  <files>README.md, .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - README.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/debug/phase-03-live-walk-no-start-feedback.md
    - tests/boop_walk_spec.lua
    - tests/boop_ui_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <action>
Update the walk command/operator text to mention explicit inactive-stop feedback and identify manual targeting as an intentional automatic-walk hold. Correct Phase 3 UAT Test 2 so its preconditions are ordered and observable: select manual targeting, start the route, verify `manual_targeting` and zero movement, run `boop targeting auto`, then and only then expect movement once all other blockers are clear. Include the exact inactive `boop walk stop` line in the live checklist and retain the distinct owned-stop versus attached-detach checks.

Do not alter completed historical UAT results beyond the gap's retest procedure/evidence fields. Do not add the deferred hyena summary or temporary/custom attack ideas.

Before committing, bump all synchronized package fields from 0.1.433 to 0.1.434. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `docs(03-17): correct phase 3 walk UAT order`. Do not push.
  </action>
  <acceptance_criteria>
    - "The live UAT never expects automatic walk movement while targeting remains manual."
    - "The checklist verifies the manual blocker, the transition to automatic targeting, and inactive/owned/attached stop feedback."
    - "README command guidance agrees with the tested behavior."
    - "All synchronized version fields read 0.1.434 and release gates pass."
  </acceptance_criteria>
  <verify>
    <automated>rg -n "manual_targeting|boop targeting auto|walk stop: no active boop walk" README.md .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>Documentation and live UAT now express the real safety precondition and exact stop feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator command → walk lifecycle | Manual commands can stop, detach, or restart an owned/external walker. |
| Runtime blocker state → UI projection | Diagnostic fallback must not conceal a real safety hold or outrank canonical owners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-17-01 | Elevation of Privilege | walk movement gate | high | mitigate | Keep manual targeting in the shared all-clear evaluator and assert zero reservations/moves. |
| T-03-17-02 | Spoofing | UI blocker label | medium | mitigate | Project the shared evaluator result after canonical-owner priority rather than synthesizing room-clear. |
| T-03-17-03 | Tampering | stop ownership | high | mitigate | Preserve invalidate-first owned stop and non-destructive external detach paths. |
| T-03-17-04 | Repudiation | inactive stop command | low | mitigate | Emit one exact INFO acknowledgment for a non-silent inactive request. |
| T-03-17-SC | Tampering | package installs | low | accept | No npm, pip, or cargo package install is introduced by this plan. |
</threat_model>

<verification>
Run focused walk/UI host Busted tests, `luac -p` for modified Lua, documentation text checks, `python3 tools/check_release_gates.py`, and `muddle`.
</verification>

<success_criteria>
- Manual targeting remains a real walk hold and is never displayed as room-clear.
- Inactive stop is explicitly acknowledged without changing active ownership behavior.
- Command and canonical UI surfaces agree.
- Live UAT switches to automatic targeting before expecting movement.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-17-SUMMARY.md` when done.
</output>
