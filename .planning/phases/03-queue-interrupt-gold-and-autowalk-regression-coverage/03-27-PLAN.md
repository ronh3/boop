---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 27
type: execute
wave: 27
depends_on:
  - 03-26
files_modified:
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_ui.lua
  - src/triggers/boop/Diag/triggers.json
  - src/triggers/boop/Diag/Leap_Command_Denied.lua
  - tests/boop_interrupt_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
gap_closure: true
gap_ids:
  - G-03-22
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-02
must_haves:
  truths:
    - "A definitive denial for the exact active leap generation invokes completeInterrupt immediately with command_failed and releases only that generation."
    - "Leap emission reuses Plan 03-25's outbound ledger; generic denial is accepted only in the matching execution window with no intervening unowned outbound request."
    - "An ambiguous/manual leap denial cannot mutate boop and falls back to the existing bounded interrupt timeout."
    - "Matching denial cancels the owned timeout, makes later timeout and room callbacks no-ops, and immediately permits an eligible diag or attack."
    - "A denial with no leap, another interrupt active, duplicate text, or stale generation is diagnostic-only."
    - "Existing leap clearqueue all plus queue addclearfull behavior remains unchanged; no queue-ahead fence or ALIAS CLEAR dependency is added."
    - "The one green package commit synchronizes all four version checkpoints at exactly 0.1.471."
  artifacts:
    - path: src/scripts/boop/boop_runtime.lua
      provides: "Generation-guarded causal leap denial and exact completeInterrupt first-terminal routing."
    - path: src/scripts/boop/boop_ui.lua
      provides: "Expected outbound registration for the existing leap clear/ADDCLEARFULL send sequence."
    - path: src/triggers/boop/Diag/Leap_Command_Denied.lua
      provides: "Thin exact observed leg-denial adapter."
    - path: tests/boop_interrupt_spec.lua
      provides: "Immediate denial, manual ambiguity, first-terminal races, stale callbacks, and follow-up regressions."
  key_links:
    - from: src/scripts/boop/boop_ui.lua
      to: "Plan 03-25 outbound ledger"
      via: "register exact leap owner/generation commands before existing clearqueue all and ADDCLEARFULL sends"
    - from: src/triggers/boop/Diag/Leap_Command_Denied.lua
      to: src/scripts/boop/boop_runtime.lua
      via: "forward exact text to the active causal leap guard"
    - from: "causally matching leap denial"
      to: "completeInterrupt"
      via: "captured generation first-terminalizes with command_failed"
    - from: "completeInterrupt release"
      to: "ordinary follow-up"
      via: "schedule one normal tick while old timeout/room callbacks retain stale generation identity"
---

<objective>
Close G-03-22 with one narrow leap-denial path that reuses the Plan 03-25 outbound ledger and existing completeInterrupt authority.

Purpose: Definitive active-leap failure should release combat/queue ownership immediately, while manual or stale identical text remains harmless and bounded timeout remains the ambiguity fallback.

Output: Exact Diag trigger/adapter, leap outbound registration, generation-guarded immediate completion, focused interrupt regressions, one green package commit, and version 0.1.471.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-26-SUMMARY.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_ui.lua
@src/triggers/boop/Diag/triggers.json
@tests/boop_interrupt_spec.lua

The existing leap path sends clearqueue all and then queue addclearfull freestand leap &lt;direction&gt;. Preserve that native policy. Plan 03-25 already owns sysDataSendRequest observation; this plan registers leap expectations with it rather than adding another observer.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Artifact | Result |
|---|---|
| Leap causal record | Active owner/generation, command, room, outbound window, and timeout identity. |
| Exact denial adapter | Observed leg-denial text reaches runtime only through the Diag manifest. |
| Immediate terminal | One completeInterrupt(command_failed) and one exact aggregate release. |
| Stale safety | Old timeout/room/duplicate callbacks cannot affect current work. |
| Regression evidence | Causal/manual denial, immediate follow-up, manifest, Lua, release, and build coverage. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement, verify, and package immediate causal leap denial</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_ui.lua, src/triggers/boop/Diag/triggers.json, src/triggers/boop/Diag/Leap_Command_Denied.lua, tests/boop_interrupt_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-26-SUMMARY.md
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_ui.lua
    - src/triggers/boop/Diag/triggers.json
    - tests/boop_interrupt_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Leap captures exact interrupt owner/generation, direction command, room authority, outbound baseline/window, timeout token, and first-terminal state."
    - "Existing clearqueue all and ADDCLEARFULL remove prior native work; there is no pre-existing-work fence."
    - "The exact observed leg denial is attributable only to the active leap execution window with no intervening unowned outbound sequence."
    - "Matching denial completes immediately once with command_failed and releases only the captured owner."
    - "Intervening manual send plus identical denial is ambiguous and nonmutating; ordinary timeout remains bounded fallback."
    - "Immediate eligible diag/attack follows matching denial; old timeout and room movement callbacks produce zero later state/trace terminals."
    - "No active leap, another interrupt, duplicate denial, or generation N denial after N+1 is a no-op."
  </behavior>
  <action>
Write focused interrupt and Diag trigger-manifest regressions first. Run the exact focused Busted command once and require named behavioral assertion failure only; setup, helper, syntax, or command failure is not RED. Do not negate the command or commit RED. Continue until the full focused command is green.

At leap dispatch, retain the existing clearqueue all and `queue addclearfull freestand leap &lt;direction&gt;` sends. Register their exact expected owner/generation commands with Plan 03-25's outbound ledger before send. Capture the leap queue-send sequence as the causal baseline, room authority, active interrupt generation, and timeout token. Open its causal response window at the observed owned leap queue request and close it on first terminal/result-prompt authority. Do not add another sysDataSendRequest handler or queue-ahead state.

Package the exact observed leg-denial sentence in Diag/triggers.json with one thin Leap_Command_Denied.lua adapter. The adapter passes candidate text/captures to runtime; it does not own state. Runtime accepts the line only when the active nonterminal operation is leap, owner/generation and room authority match, its command window is open, and no unowned outbound request occurred after the owned leap send. If causality is ambiguous, trace once and leave state untouched for existing timeout.

On a match, call completeInterrupt with captured generation and command_failed. Let its existing first-terminal path cancel only the owned timer and release only that owner's combat/queue/walk effects. Schedule one ordinary follow-up tick so an eligible diag or attack can proceed immediately. Never broad-clear again from the denial adapter.

Regress immediate matching denial, manual intervening send with identical text, denial before a leap window, no active leap, another interrupt active, duplicate line, denial→timeout, denial→room change, timeout→late denial, generation N callbacks after N+1, unrelated blocker preservation, and immediate diag/attack. Assert exactly one operation terminal and trigger manifest stem pairing.

After focused verification is green, bump exactly once from 0.1.470 to 0.1.471 and synchronize mfile.version `0.1.471`, mfile.title `boop Hunter 0.1.471`, boop.version `0.1.471`, and CODEX.md `0.1.471`. Stage one coherent package-affecting commit, inspect staged paths, rerun focused/Lua/JSON/release/Muddler gates, commit once, and do not push. No committed RED state, second implementation commit, or second bump is permitted.
  </action>
  <acceptance_criteria>
    - "Exact active leap denial terminalizes immediately once through completeInterrupt(command_failed)."
    - "Manual/intervening identical denial is nonmutating and timeout remains bounded fallback."
    - "Immediate eligible diag/attack proceeds after matching denial."
    - "Old timeout, room, duplicate, no-owner, other-interrupt, and stale-generation callbacks are no-ops."
    - "Existing leap clearqueue all/ADDCLEARFULL behavior and shared outbound observer remain unchanged."
    - "Focused interrupt behavior, Diag manifest, Lua/JSON, release gates, and Muddler pass."
    - "Exactly one green package commit synchronizes all four version checkpoints at 0.1.471."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_interrupt_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_ui.lua src/triggers/boop/Diag/Leap_Command_Denied.lua tests/boop_interrupt_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 -m json.tool src/triggers/boop/Diag/triggers.json &gt;/dev/null &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-22 is packaged at 0.1.471 with immediate exact leap denial, manual-causality isolation, stale callback safety, and immediate follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Leap send to outbound ledger | Manual and boop commands share sysDataSendRequest. |
| Generic denial to active interrupt | Identical leg denial may belong to manual work. |
| completeInterrupt to aggregate owners | Only captured leap generation may release. |
| Denial to timer/room callbacks | Later evidence from old generation must be inert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-27-01 | Spoofing | leap denial attribution | high | mitigate | Require exact active generation/window and no intervening unowned sequence. |
| T-03-27-02 | Tampering | stale callbacks | high | mitigate | Capture timer/room generation and enforce first-terminal no-op. |
| T-03-27-03 | Denial of Service | active leap owner | high | mitigate | Matching definitive denial completes immediately; ambiguity retains bounded timeout. |
| T-03-27-04 | Elevation of Privilege | aggregate release | high | mitigate | Route only through completeInterrupt for the exact owner. |
| T-03-27-05 | Repudiation | outcome chronology | medium | mitigate | Trace owner/generation/outbound/window/terminal facts. |
| T-03-27-SC | Tampering | dependency surface | low | accept | No package-manager install or new dependency is authorized. |
</threat_model>

<verification>
1. Use the established host Busted form and a real named RED assertion, never command negation.
2. Prove immediate matching denial, manual ambiguity, stale timeout/room no-ops, and immediate follow-up.
3. Parse changed Lua/Diag JSON, run release gates and Muddler.
4. Plan 03-29 reruns the interrupt suite unchanged; Plan 03-30 Test 8 supplies live authority.
5. Parent performs push/exact-SHA CI only after Plan 03-30 and every final mutation.
</verification>

<multi_source_coverage_audit>
## Source coverage

| Source | Item | Coverage |
|---|---|---|
| UAT | G-03-22 | Exact denial, immediate release/follow-up, stale timeout/room no-op. |
| REQ | SAFE-02, SAFE-04 | Exact owner/generation/outbound causality and first-terminal authority. |
| REQ | WALK-02 | Exact leap owner releases without clearing unrelated blockers. |
| Plan 03-25 | Outbound ledger | Reused directly; no second observer or queue fence. |

No standard, gold, rage, trace, docs/help, Phase 4/5, or built-artifact change is included.
</multi_source_coverage_audit>

<success_criteria>
- G-03-22 focused and manifest regressions pass.
- Matching denial releases immediately; ambiguous/manual text cannot mutate boop.
- One green package commit produces synchronized version 0.1.471 without pushing.
</success_criteria>

<output>
Create .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-27-SUMMARY.md after the single green implementation commit. Do not push.
</output>
