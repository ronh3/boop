---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 14
type: execute
wave: 14
depends_on:
  - 03-13
files_modified:
  - tests/boop_lifecycle_spec.lua
  - src/scripts/boop/boop_init.lua
  - src/scripts/boop/boop_events.lua
  - src/triggers/triggers.json
  - src/triggers/boop/Core/triggers.json
  - src/triggers/boop/Core/Prompt.lua
  - src/triggers/boop_lifecycle/triggers.json
  - src/triggers/boop_lifecycle/Prompt.lua
  - mfile
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-01
  - WALK-02
  - WALK-03
gap_closure: true
gap_ids:
  - G-03-3
must_haves:
  truths:
    - "While hunting is disabled, only lifecycle prompt evidence remains observable; combat, gagging, targeting, gold, and walker automation triggers remain disabled per D-01, D-05, D-13, and D-14."
    - "The canonical gmcp:ire owner releases only after current IRE support and a prompt have both been observed, regardless of evidence order, without releasing an unrelated owner per D-02, D-06, D-07, and D-08."
    - "A disabled reconnect followed by IRE, prompt, and enable leaves no stale gmcp_ire_missing owner, while enabling before the next prompt remains held until that prompt arrives."
    - "The lifecycle-only prompt path records evidence but emits no runtime step, prompt gag, attack, target, gold, queue, walker, command, or event side effect while hunting is disabled."
    - "A room response accepted after 4.5 seconds is not warned as missing, while a response still incomplete at 8.0 seconds warns once and remains fail closed under room:observation per D-14, D-15, and D-16."
  artifacts:
    - path: "tests/boop_lifecycle_spec.lua"
      provides: "Focused host regressions for disabled lifecycle evidence, IRE/prompt ordering, zero disabled automation, and the calibrated room fence."
    - path: "src/triggers/triggers.json"
      provides: "Top-level registration for the active lifecycle folder beside the hunting-owned boop folder."
    - path: "src/triggers/boop/Core/triggers.json"
      provides: "Core hunting manifest with the duplicate Prompt entry removed."
    - path: "src/triggers/boop/Core/Prompt.lua"
      provides: "Removed legacy Core prompt dispatcher after atomic relocation to the lifecycle folder."
    - path: "src/triggers/boop_lifecycle/triggers.json"
      provides: "Always-available lifecycle trigger folder containing only the prompt observer."
    - path: "src/triggers/boop_lifecycle/Prompt.lua"
      provides: "Lifecycle prompt dispatcher to boop.onPrompt()."
    - path: "src/scripts/boop/boop_init.lua"
      provides: "Separate lifecycle and hunting-automation trigger-folder synchronization."
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Canonical IRE reconciliation at lifecycle boundaries, disabled prompt evidence guard, and 8.0-second room warning window."
  key_links:
    - from: "src/triggers/triggers.json"
      to: "src/triggers/boop_lifecycle/triggers.json"
      via: "top-level boop lifecycle folder registration"
      pattern: "boop lifecycle"
    - from: "src/triggers/boop_lifecycle/Prompt.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "boop.onPrompt() evidence dispatch"
      pattern: "boop\\.onPrompt"
    - from: "src/scripts/boop/boop_init.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "enable-boundary call to boop.reconcileIreSupport"
      pattern: "reconcileIreSupport"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "exact-owner noteGmcpObserved and notePromptObserved release checks"
      pattern: "note(Gmcp|Prompt)Observed"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "8.0-second warning callback leaves the room:observation owner installed"
      pattern: "timeoutRoomResponseFence"
---

<objective>
Close G-03-3 by separating lifecycle evidence observation from hunting automation, reconciling the existing `gmcp:ire` owner at connection and enable boundaries, and calibrating the room-response warning to measured live latency.

Purpose: Timing-sensitive command paths must remain blocked until their owner-keyed evidence is complete, including when reconnecting while boop is disabled. This plan implements the no-change assumption-delta decision: `gmcp:ire` remains the sole canonical recovery owner; the lifecycle prompt input neither creates a second blocker model nor gains an automation path. It preserves aggregate ownership and exact release semantics per D-01, D-02, D-05, D-06, D-07, D-08, D-13, D-14, D-15, and D-16.

Output: Four focused host regressions, a prompt-only lifecycle trigger folder, lifecycle-boundary IRE reconciliation, disabled prompt side-effect suppression, an 8.0-second room-response warning window, synchronized package version 0.1.427, and an exact live UAT handoff.
</objective>

<scope_exception>
The 10-path execution surface is a bounded atomic closure: the seven `src/` paths relocate one prompt trigger and wire its lifecycle/evidence boundary, `tests/boop_lifecycle_spec.lua` is the single focused regression, and `mfile` plus `CODEX.md` are mandatory synchronized version metadata (`boop_init.lua` also carries the source version checkpoint). Splitting would separate trigger deletion from registration or split mandatory version-gate ownership without reducing the execution surface; no additional path is authorized.
</scope_exception>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@CODEX.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-13-SUMMARY.md

@src/scripts/boop/boop_init.lua
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_runtime.lua
@src/triggers/triggers.json
@src/triggers/boop/Core/triggers.json
@src/triggers/boop/Core/Prompt.lua
@tests/support/boop_host_busted_helper.lua
@tests/boop_event_transitions_spec.lua
@tests/boop_walk_spec.lua
@tests/boop_gold_spec.lua
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify the lifecycle evidence and room-latency contract</name>
  <files>tests/boop_lifecycle_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>CODEX.md</file>
    <file>mfile</file>
    <file>src/scripts/boop/boop_init.lua</file>
    <file>src/scripts/boop/boop_events.lua</file>
    <file>src/scripts/boop/boop_runtime.lua</file>
    <file>src/triggers/triggers.json</file>
    <file>src/triggers/boop/Core/triggers.json</file>
    <file>tests/support/boop_host_busted_helper.lua</file>
    <file>tests/boop_event_transitions_spec.lua</file>
    <file>tests/boop_walk_spec.lua</file>
    <file>tests/boop_gold_spec.lua</file>
  </read_first>
  <behavior>
    - "Test `gap-03-3 keeps only lifecycle evidence active while disabled`: `boop.triggers.syncEnabled()` enables the exact folder `boop lifecycle` and disables the exact folder `boop` when `boop.config.enabled == false`; enabling reconciles current IRE support and then enables both folders."
    - "Test `gap-03-3 releases gmcp ire in either evidence order`: disabled reconnect -> current IRE -> prompt -> enable clears only owner `gmcp:ire`; current IRE -> enable before prompt keeps `gmcp_ire_missing` with `gmcpSeen=true` and `promptSeen=false` until the next prompt; an unrelated seeded owner survives both sequences."
    - "Test `gap-03-3 prompt evidence has zero disabled automation`: invoking the lifecycle dispatcher while disabled records prompt evidence and may release the exact completed `gmcp:ire` owner, but records zero runtime steps, prompt gags, ticks, sends, raised automation events, target changes, gold actions, or walker moves."
    - "Test `gap-03-3 room warning honors measured latency and remains fail closed`: the room-response timer is exactly 8.0 seconds; a canonical accepted response at simulated 4.5 seconds cancels the warning; an incomplete response at 8.0 seconds warns and traces once while `room:observation` remains installed and no command is sent."
  </behavior>
  <action>
Create `tests/boop_lifecycle_spec.lua` using the host helper's production-module load order and the repository's existing Busted stub/restoration patterns. Keep the four named tests independent: reset boop state, Mudlet globals, GMCP tables, captured trigger calls, timers, sends, raised events, runtime effects, targets, gold state, and walker calls before each case, then restore replaced functions after each case.

The first test must expose the missing lifecycle-folder split through the marker `GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN`. Assert exact trigger folder names and call direction, not merely the number of enable/disable calls. The enabled branch must seed a current `gmcp.IRE` table containing `Target` or `Display` and prove the enable boundary invokes the canonical IRE reconciliation hook.

The ordering test must use the real owner APIs. Seed an unrelated blocker with a distinct owner key, run both required orderings, and assert exact owner state after every transition. The owner may clear only through `boop.runtime.noteGmcpObserved("gmcp:ire", "ire")` plus `boop.runtime.notePromptObserved()` semantics; do not delete, overwrite, or directly clear `gmcp:ire` in the test harness. Mark the pre-fix contract failure with `GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN`.

The zero-side-effect test must invoke `boop.onPrompt()` through the same function used by the lifecycle trigger. Replace `boop.runtime.step`, `boop.runtime.applyEffects`, `boop.tick`, prompt gagging, `send`, `sendGMCP`, `raiseEvent`, target mutation, gold dispatch, and `demonwalker.move` with counters or sentinels. Allow only evidence-state mutation; every automation counter must remain zero and all operator-facing target/gold/walker sentinels must remain unchanged. Mark the pre-fix contract failure with `GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN`.

The room-fence test must capture the delay and callback supplied to `tempTimer`, exercise the existing canonical inventory-then-room response path rather than treating persistent `gmcp.Char.Items.List` as a new event, and invoke the captured callback after an accepted response to prove stale callbacks are harmless. In the missing-evidence branch, assert one warning, one timeout trace, retained owner `room:observation` with code `room_partial`, and zero commands. Mark the old 0.35-second behavior with `GAP_03_3_ROOM_WARNING_WINDOW_BROKEN`.

Before committing this RED task, bump the package monotonically from 0.1.425 to 0.1.426 in all synchronized fields: `mfile.version`, `mfile.title` as `boop Hunter 0.1.426`, `boop.version` in `src/scripts/boop/boop_init.lua`, and the current synchronized package-version checkpoint in `CODEX.md`. Do not change production behavior or trigger manifests in RED. Inspect staged paths and run `python3 tools/check_release_gates.py`.

Define the suite name as `boop lifecycle recovery`, making the exact full RED case names `boop lifecycle recovery gap-03-3 keeps only lifecycle evidence active while disabled`, `boop lifecycle recovery gap-03-3 releases gmcp ire in either evidence order`, `boop lifecycle recovery gap-03-3 prompt evidence has zero disabled automation`, and `boop lifecycle recovery gap-03-3 room warning honors measured latency and remains fail closed`. Run each exact full name in a separate Busted 2.3.0 invocation against only `tests/boop_lifecycle_spec.lua`, using `busted --helper=tests/support/boop_host_busted_helper.lua --output=json --name="$case_name"` and shell redirection to a fresh JSON/stderr pair. Require `command -v busted` first. Each run must exit nonzero, emit empty stderr, parse as one JSON object with zero successes, exactly one failure, zero errors, zero pendings, and exactly one total record. The sole failure name must equal its allowlisted full case name and its message must contain only that case's allowed intentional marker: the first three use `GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN`, while the room case uses `GAP_03_3_ROOM_WARNING_WINDOW_BROKEN`. Empty selection, stale output, extra records, an earlier unmarked assertion, or any stderr invalidates RED.
  </action>
  <acceptance_criteria>
    <criterion>`tests/boop_lifecycle_spec.lua` contains exactly four focused tests whose names include `gap-03-3` and whose assertions cover the lifecycle folder split, both IRE/prompt orderings, disabled zero automation, and 4.5/8.0-second room timing.</criterion>
    <criterion>The owner-ordering test proves `gmcp:ire` remains held until both required evidence bits are true, releases exactly once when complete, and never removes the unrelated seeded owner per D-02, D-06, D-07, and D-08.</criterion>
    <criterion>The disabled-prompt test fails if any runtime step, `boop.runtime.applyEffects`, prompt gag, tick, send, raised automation event, target mutation, gold action, or walker move occurs.</criterion>
    <criterion>The room test distinguishes an accepted response at 4.5 seconds from missing evidence at 8.0 seconds and asserts that timeout warning does not release `room:observation` per D-14 and D-15.</criterion>
    <criterion>The RED classifier runs four separate exact-name Busted invocations. Each has a nonzero status, empty stderr, valid single-object JSON, zero successes/errors/pendings, exactly one failure/total record, the exact allowlisted full name, and that case's single allowed marker.</criterion>
    <criterion>All four synchronized version fields equal 0.1.426, staged paths are inspected, and `python3 tools/check_release_gates.py` passes before the RED commit.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_lifecycle_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; command -v busted &gt;/dev/null &amp;&amp; bash -o pipefail -c 'set -eu; red_dir=$(mktemp -d); trap "rm -f \"$red_dir\"/*.json \"$red_dir\"/*.err; rmdir \"$red_dir\" 2&gt;/dev/null || true" EXIT; red_py="import json,sys; d=json.load(open(sys.argv[1], encoding=\"utf-8\")); expected=sys.argv[2]; marker=sys.argv[3]; keys=(\"successes\",\"failures\",\"errors\",\"pendings\"); markers=(\"GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN\",\"GAP_03_3_ROOM_WARNING_WINDOW_BROKEN\"); assert isinstance(d,dict); assert all(isinstance(d.get(k),list) for k in keys); assert len(d[\"successes\"]) == 0; assert len(d[\"failures\"]) == 1; assert len(d[\"errors\"]) == 0; assert len(d[\"pendings\"]) == 0; assert sum(len(d[k]) for k in keys) == 1; failure=d[\"failures\"][0]; assert failure.get(\"name\") == expected, failure.get(\"name\"); message=str(failure.get(\"message\",\"\")); assert message.count(marker) == 1, message; assert all(other == marker or other not in message for other in markers), message"; index=0; for entry in "boop lifecycle recovery gap-03-3 keeps only lifecycle evidence active while disabled|GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN" "boop lifecycle recovery gap-03-3 releases gmcp ire in either evidence order|GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN" "boop lifecycle recovery gap-03-3 prompt evidence has zero disabled automation|GAP_03_3_LIFECYCLE_BOUNDARY_BROKEN" "boop lifecycle recovery gap-03-3 room warning honors measured latency and remains fail closed|GAP_03_3_ROOM_WARNING_WINDOW_BROKEN"; do index=$((index + 1)); case_name=${entry%%|*}; marker=${entry#*|}; red_json="$red_dir/$index.json"; red_err="$red_dir/$index.err"; set +e; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --output=json --name="$case_name" tests/boop_lifecycle_spec.lua &gt;"$red_json" 2&gt;"$red_err"; red_status=$?; set -e; if test -s "$red_err"; then cat "$red_err" &gt;&amp;2; exit 1; fi; test "$red_status" -ne 0; python3 -c "$red_py" "$red_json" "$case_name" "$marker"; done'</automated>
    <diagnostic_scope>Expected nonzero Busted results are RED evidence only. Host Busted is deterministic diagnostic feedback, not real-Mudlet or final-CI authority.</diagnostic_scope>
  </verify>
  <done>The four G-03-3 regressions fail for the intended missing behavior only, the repository is synchronized at 0.1.426, and no production behavior has changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — split lifecycle observation from automation and calibrate the room fence</name>
  <files>src/scripts/boop/boop_init.lua, src/scripts/boop/boop_events.lua, src/triggers/triggers.json, src/triggers/boop/Core/triggers.json, src/triggers/boop/Core/Prompt.lua, src/triggers/boop_lifecycle/triggers.json, src/triggers/boop_lifecycle/Prompt.lua, mfile, CODEX.md</files>
  <read_first>
    <file>tests/boop_lifecycle_spec.lua</file>
    <file>src/scripts/boop/boop_init.lua</file>
    <file>src/scripts/boop/boop_events.lua</file>
    <file>src/scripts/boop/boop_runtime.lua</file>
    <file>src/triggers/triggers.json</file>
    <file>src/triggers/boop/triggers.json</file>
    <file>src/triggers/boop/Core/triggers.json</file>
    <file>src/triggers/boop/Core/Prompt.lua</file>
    <file>src/triggers/boop/Gag/triggers.json</file>
    <file>src/triggers/boop/Gold/triggers.json</file>
    <file>src/triggers/boop/Shield/triggers.json</file>
    <file>mfile</file>
    <file>CODEX.md</file>
  </read_first>
  <behavior>
    - "The sibling top-level folder `boop lifecycle` is always enabled and contains only the prompt observer; the top-level `boop` folder continues to own every combat, gagging, targeting, gold, and walker-related trigger and follows `boop.config.enabled`."
    - "Connection, Char.Status, and enable boundaries call one public `boop.reconcileIreSupport(source, options)` implementation that observes current IRE support into owner `gmcp:ire` without inventing event identity or bypassing its prompt requirement."
    - "`boop.onPrompt()` always records prompt evidence; when disabled it returns before runtime prompt stepping, applying, gagging, ticking, sending, or raising automation events."
    - "Room-response warning uses `ROOM_RESPONSE_FENCE_WARNING_SECONDS = 8.0`; accepted canonical response evidence cancels the timer, while missing evidence warns once and leaves the owner installed."
  </behavior>
  <action>
In `src/triggers/triggers.json`, add a top-level active folder named exactly `boop lifecycle` beside the existing top-level `boop` folder. Create `src/triggers/boop_lifecycle/triggers.json` with exactly one active non-folder child named `Prompt`, matching the established Mudlet prompt pattern `return isPrompt()` and pointing to `Prompt.lua`. Create `src/triggers/boop_lifecycle/Prompt.lua` as the thin dispatcher to `boop.onPrompt()`. Remove the `Prompt` child from `src/triggers/boop/Core/triggers.json` and delete `src/triggers/boop/Core/Prompt.lua` so each prompt produces one observation. Do not place combat, gagging, targeting, gold, queue, or walker automation in the lifecycle folder per D-01, D-05, D-13, and D-14.

In `src/scripts/boop/boop_init.lua`, add the exact lifecycle folder constant `BOOP_LIFECYCLE_TRIGGER_FOLDER = "boop lifecycle"` beside `BOOP_TRIGGER_FOLDER = "boop"`. Refactor `boop.triggers.setEnabled(enabled)` so every synchronization first enables the lifecycle folder and independently enables or disables the automation folder according to the normalized boolean. Preserve the existing protected Mudlet API calls and truthful combined success return. When enabling, invoke `boop.reconcileIreSupport("enable")` if the function is available before hunting automation can consume the next action. Do not alter command syntax, configuration persistence, or the aggregate blocker model.

In `src/scripts/boop/boop_events.lua`, promote the existing local IRE helper to public `boop.reconcileIreSupport(source, options)`. Preserve `ireReady()` as the current-support predicate and preserve exact owner `gmcp:ire`, code `gmcp_ire_missing`, required systems, and required evidence. If current `gmcp.IRE` contains `Target` or `Display`, record only the IRE evidence with `boop.runtime.noteGmcpObserved("gmcp:ire", "ire")`; otherwise ensure the canonical owner exists. Accept an option indicating that the connection boundary already sent the support request so connection handling does not duplicate it. Route `boop.onConnectionEvent()` and `boop.onCharStatus()` through this one function. Reading current IRE at these explicit lifecycle boundaries is reconciliation, not proof that a new room or item event occurred.

Keep `boop.onPrompt()` as the only evidence consumer: always call `boop.runtime.notePromptObserved()`, then immediately return `false` when `boop.config.enabled` is false. The disabled return must occur before `boop.runtime.step({ type = "prompt", context = context })`, `boop.runtime.applyEffects(...)`, prompt gagging, `boop.tick()`, sends, or raised automation events. Do not directly clear `gmcp:ire`; its existing evidence predicate remains authoritative per D-02, D-06, D-07, and D-08.

Replace the 0.35-second room-response warning delay with a named local constant `ROOM_RESPONSE_FENCE_WARNING_SECONDS = 8.0` and use that constant in `boop.requestRoomItemsOnce()`. Retain the one-request cap, accepted-response timer cancellation, stale-callback safety, warning-once behavior, trace, and fail-closed `room:observation` owner exactly as designed per D-14, D-15, and D-16. Do not poll persistent merged GMCP tables, synthesize a room response, auto-release the owner on timeout, or send another request.

After GREEN, bump all synchronized package fields from 0.1.426 to 0.1.427: `mfile.version`, `mfile.title` as `boop Hunter 0.1.427`, `boop.version` in `src/scripts/boop/boop_init.lua`, and the current synchronized package-version checkpoint in `CODEX.md`. This change adds no command surface, so leave README command help unchanged. Never edit generated build artifacts.

Run the new focused spec to GREEN, then run the persistence, safety, event-transition, walk, and gold host regressions together. Run Lua syntax checks, package-manifest/release gates, and `muddle`. Inspect staged paths before committing. Do not push; the parent session owns immutable final-HEAD push and exact-SHA CI.
  </action>
  <acceptance_criteria>
    <criterion>`src/triggers/triggers.json` registers active folder `boop lifecycle`, its child manifest contains only `Prompt`, and no prompt remains under `src/triggers/boop/Core/`.</criterion>
    <criterion>With hunting disabled, folder synchronization enables `boop lifecycle` and disables `boop`; with hunting enabled, it reconciles current IRE support and enables both folders.</criterion>
    <criterion>`boop.reconcileIreSupport` is the shared connection, Char.Status, and enable-boundary implementation and records evidence through owner `gmcp:ire` rather than deleting or bypassing the owner.</criterion>
    <criterion>Both required evidence orderings pass, `gmcp:ire` releases exactly when complete, and unrelated blockers remain installed.</criterion>
    <criterion>`boop.onPrompt()` records lifecycle evidence while disabled and exits before every combat, gagging, targeting, gold, queue, walker, send, or event side effect.</criterion>
    <criterion>The room warning timer is exactly 8.0 seconds; accepted evidence at 4.5 seconds produces no warning, and missing evidence at 8.0 seconds warns once while retaining `room:observation` and sending no command.</criterion>
    <criterion>All four new regressions and the existing persistence, safety, event-transition, walk, and gold regressions pass.</criterion>
    <criterion>All synchronized version fields equal 0.1.427, generated artifacts remain untouched, `python3 tools/check_release_gates.py` passes, and `muddle` succeeds.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/support/boop_host_busted_helper.lua &amp;&amp; command -v busted &gt;/dev/null &amp;&amp; luac -p tests/support/boop_host_busted_helper.lua src/scripts/boop/boop_init.lua src/scripts/boop/boop_events.lua src/triggers/boop_lifecycle/Prompt.lua tests/boop_lifecycle_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --filter='gap%-03%-3' tests/boop_lifecycle_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_persistence_spec.lua tests/boop_safety_spec.lua tests/boop_event_transitions_spec.lua tests/boop_walk_spec.lua tests/boop_gold_spec.lua tests/boop_lifecycle_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
    <diagnostic_scope>Host Busted is deterministic focused evidence only. Muddler proves package construction; neither substitutes for real-Mudlet execution or final exact-SHA CI.</diagnostic_scope>
  </verify>
  <done>The lifecycle observer remains available while hunting automation is disabled, canonical `gmcp:ire` recovery is order-independent and exact-owner safe, measured room latency no longer causes premature warnings, all focused regressions pass, and the package is synchronized at 0.1.427.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the complete closure and write the exact live UAT handoff</name>
  <files>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-14-SUMMARY.md</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md</file>
    <file>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md</file>
    <file>tests/boop_lifecycle_spec.lua</file>
    <file>src/scripts/boop/boop_init.lua</file>
    <file>src/scripts/boop/boop_events.lua</file>
    <file>src/triggers/triggers.json</file>
    <file>src/triggers/boop_lifecycle/triggers.json</file>
    <file>mfile</file>
    <file>CODEX.md</file>
  </read_first>
  <action>
Run the complete focused host command from Task 2 again against the committed GREEN state and label it diagnostic-only. Run `python3 tools/check_release_gates.py`, `git diff --check`, and `muddle`. If executable `/tmp/Mudlet.AppImage` exists, run the exact repository real-Mudlet command from CODEX.md and `03-VALIDATION.md`: `muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`. If it is unavailable, record local real-Mudlet execution as unavailable and state that host Busted is not a substitute; GitHub Actions remains the authoritative full-suite fallback, and the parent exact-final-SHA gate remains terminal authority. Confirm committed paths and prove all synchronized fields are 0.1.427.

Create `03-14-SUMMARY.md` through the execution workflow. Its verification section must reproduce the `Live UAT handoff` below exactly, including commands, expected owner snapshots, the reversible room-response suppression, and cleanup. Record that the executor did not push and that the parent must push immutable final HEAD and run `tools/wait_for_exact_ci.sh`; do not claim final CI before that parent gate completes.
  </action>
  <acceptance_criteria>
    <criterion>The focused G-03-3 suite and the persistence, safety, event-transition, walk, and gold regressions all pass on committed GREEN state.</criterion>
    <criterion>Lua syntax, release gates, and `muddle` pass. When `/tmp/Mudlet.AppImage` exists, the exact CODEX.md/03-VALIDATION.md command passes; when absent, the summary states local real-Mudlet execution was unavailable and does not present host Busted as a substitute.</criterion>
    <criterion>`git diff --check` passes, committed package paths exclude built artifacts, and all synchronized package fields are exactly 0.1.427.</criterion>
    <criterion>`03-14-SUMMARY.md` reproduces every step and expected observation from `Live UAT handoff` rather than referring the operator back to this plan.</criterion>
    <criterion>The summary identifies G-03-3 as the only closed gap, preserves the no-change owner-keyed assumption, and states that final push/exact-SHA CI remains a parent-session responsibility.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/support/boop_host_busted_helper.lua &amp;&amp; command -v busted &gt;/dev/null &amp;&amp; luac -p tests/support/boop_host_busted_helper.lua src/scripts/boop/boop_init.lua src/scripts/boop/boop_events.lua src/triggers/boop_lifecycle/Prompt.lua tests/boop_lifecycle_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_persistence_spec.lua tests/boop_safety_spec.lua tests/boop_event_transitions_spec.lua tests/boop_walk_spec.lua tests/boop_gold_spec.lua tests/boop_lifecycle_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; git diff --check &amp;&amp; if test -x /tmp/Mudlet.AppImage; then muddle &amp;&amp; AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror; else muddle &amp;&amp; printf '%s\n' 'Local real-Mudlet execution unavailable: /tmp/Mudlet.AppImage not found; host Busted is diagnostic only and is not a substitute.'; fi</automated>
    <diagnostic_scope>Host Busted is diagnostic-only. The `/tmp/Mudlet.AppImage` command is local real-Mudlet evidence when available; otherwise only the later GitHub Actions run can supply full real-Mudlet evidence, and parent exact-final-SHA CI remains terminal authority.</diagnostic_scope>
  </verify>
  <done>All automated closure evidence passes, the exact live checklist is carried into the summary, and the repository is ready for operator UAT followed by the parent final-HEAD push and exact-SHA CI gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mudlet prompt trigger -> lifecycle evidence | Untrusted live prompt timing crosses into the owner-keyed blocker state while hunting may be disabled. |
| connection/enable boundary -> persisted GMCP snapshot | A lifecycle transition reads current `gmcp.IRE` support state without treating a persistent merged table as a fresh room/item event. |
| lifecycle trigger folder -> automation trigger folder | The always-active prompt observer must remain isolated from combat, gagging, targeting, gold, queue, and walker triggers. |
| room-response timer -> safety owner | A local warning timer classifies incomplete response evidence but must not release or bypass `room:observation`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14-01 | Elevation of Privilege | `boop.onPrompt()` while disabled | high | mitigate | Record prompt evidence first, then return before runtime step/apply, gagging, ticking, sends, and raised automation events; host regressions count every side-effect channel. |
| T-03-14-02 | Tampering | `gmcp:ire` reconciliation | high | mitigate | Reuse exact owner `gmcp:ire` and existing two-evidence release predicate; test both evidence orders and retain an unrelated seeded owner. |
| T-03-14-03 | Spoofing | current merged `gmcp.IRE` table | medium | mitigate | Consult current IRE only at explicit connection, Char.Status, and enable boundaries and record only IRE support evidence; never use persistent room/item tables as event identity. |
| T-03-14-04 | Elevation of Privilege | `boop lifecycle` trigger manifest | high | mitigate | Keep exactly one prompt child in the always-active folder, keep all automation in folder `boop`, remove the duplicate Core prompt, and assert exact folder calls and manifest membership. |
| T-03-14-05 | Denial of Service | room-response warning window | medium | mitigate | Use a measured 8.0-second bounded warning, cancel it on canonical accepted evidence, warn once when incomplete, and retain the owner so commands remain fail closed. |
| T-03-14-06 | Repudiation | warning and owner transitions | low | mitigate | Preserve source-tagged owner traces, emit one timeout trace/warning, and require live trace paste-back for reconnect and forced-missing-response scenarios. |
</threat_model>

<verification>
1. RED is valid only when the four new cases fail with the two named markers and no errors.
2. GREEN requires all four new cases plus persistence, safety, event-transition, walk, and gold host suites to pass.
3. Lua syntax, package release gates, synchronized version 0.1.427, clean diffs, and `muddle` are blocking.
4. The always-active manifest is inspected to contain only `Prompt`; all automation remains under `boop`.
5. The summary carries the exact live UAT handoff and accurately reserves push/exact-SHA CI for the parent session.
</verification>

<success_criteria>
- Disabled reconnect can collect IRE and prompt evidence without activating hunting automation or retaining a stale `gmcp_ire_missing` owner.
- Enable-before-prompt remains held until the next prompt, then releases only `gmcp:ire`; unrelated owners survive.
- Ordinary accepted room evidence at 4.5 seconds is not mislabeled missing; genuinely incomplete evidence warns at 8.0 seconds and remains fail closed.
- No direct blocker bypass, no persistent merged room/item table as event identity, and no lifecycle-only automation command is introduced.
- G-03-3 has focused automated regressions and a reproducible live UAT handoff.
</success_criteria>

## Artifacts this phase produces

| Artifact | Status | Purpose |
|----------|--------|---------|
| `tests/boop_lifecycle_spec.lua` | New file | Four host regressions for G-03-3 lifecycle, ordering, disabled-side-effect, and room-latency behavior. |
| `src/triggers/triggers.json` | Modified file | Registers active sibling folder `boop lifecycle` while preserving hunting automation under `boop`. |
| `src/triggers/boop/Core/triggers.json` | Modified file | Removes the Core `Prompt` leaf so only one prompt observer remains. |
| `src/triggers/boop/Core/Prompt.lua` | Removed file | Deletes the former hunting-owned prompt dispatcher as part of the atomic relocation. |
| `src/triggers/boop_lifecycle/triggers.json` | New file | Prompt-only always-active lifecycle trigger manifest. |
| `src/triggers/boop_lifecycle/Prompt.lua` | New file | Thin prompt lifecycle dispatcher. |
| `boop.reconcileIreSupport(source, options)` in `src/scripts/boop/boop_events.lua` | New public symbol promoted from the existing local helper | Shared current-IRE reconciliation at connection, Char.Status, and enable boundaries. |
| `BOOP_LIFECYCLE_TRIGGER_FOLDER` in `src/scripts/boop/boop_init.lua` | New local symbol | Exact lifecycle folder identifier used independently of hunting automation. |
| `ROOM_RESPONSE_FENCE_WARNING_SECONDS` in `src/scripts/boop/boop_events.lua` | New local symbol | Named 8.0-second warning boundary for incomplete room response evidence. |
| `03-14-SUMMARY.md` | Execution output | Automated results, exact live UAT handoff, version evidence, and parent CI responsibility. |

## Live UAT handoff

Use package 0.1.427 built from the final execution commit. Keep `boop trace on` during all steps and paste back `boop status`, the owner snapshot line, and `boop trace show 100` for each scenario.

1. Disabled reconnect, IRE then prompt then enable:
   - Run `boop off`, `boop trace clear`, and `boop trace on`, then reconnect.
   - After `gmcp.IRE` is present and one prompt has arrived, run `lua local b=boop.runtime.state().combat.blockersByOwner["gmcp:ire"]; echo(string.format("enabled=%s ire=%s owner=%s gmcpSeen=%s promptSeen=%s\n", tostring(boop.config.enabled), tostring(type(gmcp.IRE)=="table"), tostring(b and b.code or "none"), tostring(b and b.gmcpSeen or false), tostring(b and b.promptSeen or false)))`.
   - Expect `enabled=false`, `ire=true`, and `owner=none`. Run `boop trace show 100`; between reconnect and enable there must be no boop-generated attack, target, `get`, `put`, queue execution, prompt gag, or `demonwalker.move`.
   - Run `boop on`, then repeat the snapshot. Expect `enabled=true`, `ire=true`, and `owner=none`; `boop status` must not list `gmcp_ire_missing`.

2. Enable before the next prompt:
   - Run `boop off`, reconnect, and run `boop on` after IRE support has arrived but before the next prompt.
   - Immediately run the same owner snapshot. Expect `enabled=true`, `ire=true`, `owner=gmcp_ire_missing`, `gmcpSeen=true`, and `promptSeen=false`.
   - Cause one prompt with operator command `ql`, then repeat the snapshot. Expect `owner=none`. A second prompt must remain idempotent and must not release any unrelated owner shown by `boop status`.

3. Ordinary room latency:
   - Run `boop trace clear`, `boop trace on`, and a normal `boop walk start`, then traverse at least five room transitions.
   - For every `Char.Items.Room` response arriving within 8.0 seconds, `boop trace show 100` must contain no `room response fence timeout`; each settled room may produce at most one `demonwalker.move`.

4. Forced incomplete room evidence:
   - Run `boop walk stop`, then `lua _boopUatRoomItems=boop.onRoomItemsList; boop.onRoomItemsList=function() return false end`, followed by `boop trace clear`, `boop trace on`, and `boop walk start`.
   - Wait 9 seconds without restoring the handler. Expect exactly one `[WARN] room_partial -- room response fence incomplete`, one `room response fence timeout` trace, an active `room_partial`/`room:observation` blocker in `boop status`, and no `demonwalker.move`, attack, `get`, or `put` during the suppression window.
   - Restore immediately with `lua boop.onRoomItemsList=_boopUatRoomItems; _boopUatRoomItems=nil`, then run `boop walk stop` and `boop walk start`. Confirm fresh complete evidence settles normally; do not leave the UAT override installed.

5. Original blocked UAT rerun:
   - During a live room transition, run `diag`, then run `lua local s=boop.runtime.state(); local op=s.diag.operation; echo(string.format("interruptOwner=%s roomOwner=%s\n", tostring(type(op)=="table" and op.blockerOwner or "none"), tostring(s.combat.blockersByOwner["room:observation"] and "room:observation" or "none")))`. Expect `interruptOwner=interrupt:<generation>` and `roomOwner=room:observation`. Confirm no attack, gold transfer, queue action, or walker move occurs until that exact generation-owned interrupt owner and `room:observation` are both clear, then exactly one next eligible action occurs.
   - With `boop on`, automatic gold on, and a configured pack, kill one mobile and send a same-room `ql`. Confirm exactly one eligible `queue add freestand get sovereigns` and one matching `queue add freestand put sovereigns in <pack>`, with no stale `gmcp_ire_missing` owner.

6. Final handoff:
   - Run `boop status`, the owner snapshot, and `boop trace show 100`; paste back all three outputs.
   - The executor does not push. After all repository mutations are complete, the parent session pushes immutable final HEAD and runs `tools/wait_for_exact_ci.sh`; success is valid only for a `main.yml` run whose `headSha` exactly matches that final HEAD.

## Multi-source coverage audit

### GOAL coverage

| Source item | Coverage | Plan/task evidence |
|-------------|----------|--------------------|
| Timing-sensitive command paths cannot attack, loot, or walk while another safety hold or room-state blocker owns the next action. | COVERED | Tasks 1-2 assert zero disabled automation, exact aggregate-owner retention, no room-timeout release, and no command before complete evidence; Task 3 reruns the live cross-owner scenario. |

### REQ coverage

| Requirement | Coverage | Plan/task evidence |
|-------------|----------|--------------------|
| SAFE-02 | COVERED | Tasks 1-2 preserve prompt-backed blocker ownership and exact release across reconnect/enable orderings. |
| SAFE-04 | COVERED | Tasks 1-2 count zero target, queue, gold, or send effects while disabled or room evidence is incomplete; Task 3 reruns gold after recovery. |
| WALK-01 | COVERED | Tasks 1-2 keep `room:observation` installed through incomplete evidence and prevent movement. |
| WALK-02 | COVERED | Tasks 1-2 preserve one capped request, canonical accepted-response settlement, one warning, and at-most-one movement; Task 3 validates ordinary and missing live response paths. |
| WALK-03 | COVERED | No external installer/update path is introduced; existing anti-update coverage runs in the safety regression set. |

### RESEARCH coverage

| Research feature or constraint | Coverage | Plan/task evidence |
|--------------------------------|----------|--------------------|
| Owner-keyed aggregate blocker state remains canonical. | COVERED | Tasks 1-2 use only owner `gmcp:ire` and preserve unrelated owners. |
| Evidence correlation must use operation-specific events rather than persistent merged GMCP state. | COVERED | Current IRE is read only at explicit lifecycle boundaries; room settlement remains on the canonical accepted response queue. |
| Prompt and GMCP evidence may arrive in either order. | COVERED | Task 1 specifies both orders and Task 2 makes the shared reconciliation path pass. |
| Timeout is bounded but must remain fail closed. | COVERED | The warning moves to measured 8.0 seconds, fires once, and retains `room:observation`. |
| Package/install legitimacy gate. | NOT APPLICABLE | The plan adds no npm, pip, cargo, luarocks, or other package install. |

### CONTEXT coverage

| Decision set | Coverage | Plan/task evidence |
|--------------|----------|--------------------|
| D-01, D-03, D-04 — aggregate all-clear, primary display, fixed priority | COVERED | Existing aggregate state is unchanged; tests seed multiple owners and assert exact retention. |
| D-02, D-05, D-06, D-07, D-08 — exact owner/evidence release, timeout scope, idempotency, first terminal result | COVERED | Both evidence orderings use canonical note APIs, repeated prompts are idempotent, and room timeout never clears the owner. |
| D-09, D-10, D-11, D-12 — gold room/inventory staging | COVERED | No gold trigger or state path moves into lifecycle scope; gold regressions and live corpse transfer rerun after recovery. |
| D-13, D-14, D-15, D-16 — all-clear walking, complete evidence, capped refresh, one move | COVERED | The lifecycle folder cannot walk; room warning remains capped/fail closed; host and live checks enforce one move after complete evidence. |
| D-17, D-18, D-19, D-20 — manual movement semantics | COVERED | No manual movement state is changed; existing walk regressions run in GREEN. |
| D-21, D-22, D-23, D-24 — walker stop/restart semantics | COVERED | No walker lifecycle state is changed; existing walk regressions and UAT cleanup use the established stop/restart path. |
| Assumption delta: lifecycle evidence adds no second blocker or automation model. | COVERED | `gmcp:ire` remains canonical and the lifecycle prompt is an evidence-only input while disabled. |
| Deferred hyena output and prefix/custom attack features | EXCLUDED | Explicitly deferred; no task, artifact, test, command, or documentation change implements them. |

Audit result: all GOAL, phase REQ, applicable RESEARCH, and locked CONTEXT items are covered; deferred ideas are excluded; no source item requires a phase split.

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-14-SUMMARY.md` when done. The summary must include the exact `Live UAT handoff`, synchronized version and gate results, G-03-3 closure statement, no-change owner assumption, committed paths, and the parent final-HEAD push/exact-SHA CI responsibility.
</output>
