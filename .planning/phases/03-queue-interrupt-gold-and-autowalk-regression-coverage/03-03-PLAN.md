---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - tests/boop_runtime_spec.lua
  - tests/boop_pull_spec.lua
  - tests/boop_event_transitions_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - "Per D-05, pull holds attack/target/gold/walk work from dispatch through trustworthy current-generation return or timeout recovery."
    - "Per D-06, pull never changes saved enabled configuration and releases only `pull:<generation>`."
    - "Per D-07, repeating pull while active sends no second chain and does not restart its timer."
    - "Per D-08, return/timeout races produce one winning transition and stale callbacks cannot release a newer pull."
  artifacts:
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Canonical pullGeneration and inactive pullState defaults"
    - path: "src/scripts/boop/boop_ui.lua"
      provides: "Generation-owned pull creation, timeout, repeat, and terminal boundary"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Generation-checked away/return room transitions"
    - path: "tests/boop_pull_spec.lua"
      provides: "Repeat, callback order, stale generation, unrelated owner, and config immutability coverage"
  key_links:
    - from: "src/scripts/boop/boop_ui.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "state.combat.pullState generation, phase, and pull:<generation> owner"
      pattern: "(pullGeneration|pullState|pull:)"
---

<objective>
Replace pull's persisted enable toggles and broad timeout state with a D-05–D-08 generation-owned runtime lifecycle.

Purpose: Pull must pause relevant automation without changing operator intent, and room/timer callbacks must not release a newer pull or unrelated hold.
Output: Canonical pull generation/record, exact terminal/recovery behavior, and explicit return/timeout ordering tests.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-02-SUMMARY.md

<interfaces>
Created symbols and values — exhaustive:
- State field `state.combat.pullGeneration`, initialized to `0`.
- Existing `state.combat.pullState` gains an explicit inactive default `false`; active records have exact fields `active`, `generation`, `blockerOwner`, `phase`, `terminal`, `originRoom`, `direction`, `returnDirection`, `command`, and `timeoutTimer`.
- Stable owner form `pull:<generation>`.
- Exact phase values `outbound`, `away`, `timed_out_away`, and `terminal`.
- Exact blocker codes `pull_active` and `pull_timeout_away`.
- Public API `boop.ui.completePull(generation, terminalReason, opts)`.
- Stable terminal reason values `returned_origin`, `timeout_at_origin`, and `returned_after_timeout`; `timeout_away` is a recovery transition reason, not a release.

Existing symbols modified, not created:
- `DOMAIN_DEFAULTS.combat.pullState`, `boop.ui.pullCommand`, `boop.ui.clearPullState`, local `armPullTimeout`, local `stopPullTimeout`, and `boop.onRoomInfo`.
- Existing active-record fields `active`, `phase`, `originRoom`, `direction`, `returnDirection`, and `timeoutTimer`.
- Existing field `restoreEnabled` is removed; it is not part of the created record.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin pull ownership, config immutability, and callback ordering</name>
  <files>tests/boop_runtime_spec.lua, tests/boop_pull_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_runtime_spec.lua` — modified pull default/reset/deep-copy assertions.
    - `tests/boop_pull_spec.lua` — modified generation, timeout/return, repeat, owner, and config-immutability coverage.
    - `tests/boop_event_transitions_spec.lua` — modified Room.Info return and target-removal interleavings.
    - `src/scripts/boop/boop_runtime.lua` — `DOMAIN_DEFAULTS.combat`, blocker APIs, and pull state under test.
    - `src/scripts/boop/boop_ui.lua` — `pullCommand`, timeout, and terminal lifecycle source.
    - `src/scripts/boop/boop_events.lua` — current Room.Info away/return transition source.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-05 through D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — pull ordering row, exact callback counts, and target-removal preservation.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — section `Pattern 2: Generation-Owned Operation with First-Terminal-Wins`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Pull ownership seam`, `tests/boop_pull_spec.lua`, and `Prove cleanup/authorization precedes the first side effect`.
  </read_first>
  <behavior>
    - "Return then timeout releases once and late timeout is a no-op."
    - "Timeout away retains the same owner until a trustworthy return."
    - "Old timeout after a new pull cannot mutate the new record."
    - "Repeat sends once and retains the original timer."
    - "Enabled config and saveConfig call count never change."
  </behavior>
  <acceptance_criteria>
    - Reset/deep-copy tests assert `pullGeneration == 0`, `pullState == false`, and independent copies of every active-record field listed in `<interfaces>`.
    - Return→timeout terminates once with zero late-timeout effects; timeout-away→return retains one owner through recovery and releases it exactly once on trustworthy return.
    - Repeating pull while active sends zero additional chained commands and creates zero replacement timers or generations.
    - An old timeout invoked after a newer pull starts leaves the newer generation, phase, owner, timer, and command unchanged.
    - `boop.ui.setEnabled` and `boop.db.saveConfig("enabled", ...)` each have call count zero, and an operator-disabled session remains disabled after every pull terminal.
    - One patch increment synchronizes all four version checkpoints, and `luac -p` plus the release gate succeed.
  </acceptance_criteria>
  <action>
Add reset/deep-copy assertions for the exact pull defaults and active-record fields in `<interfaces>`. Capture ordered timers and outbound commands, then test return→timeout, timeout-away→return, old-timeout-after-new-pull, and repeat-while-active with exact send, warning, trace, tick, timer, owner, phase, and generation counts. Seed an unrelated owner and prove every pull terminal preserves it. Assert `boop.ui.setEnabled` and `boop.db.saveConfig("enabled", ...)` are never called and operator-disabled state remains disabled after pull. Preserve target-removal queue-drift semantics. Synchronize the next patch version before commit and do not edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_runtime_spec.lua tests/boop_pull_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>Pull's exact record, repeat behavior, both callback orders, stale-generation no-op, and saved-config invariants are RED contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement generation-owned pull and exact room release</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_ui.lua, src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_runtime.lua` — modified `DOMAIN_DEFAULTS.combat.pullGeneration/pullState` and exact-owner runtime APIs.
    - `src/scripts/boop/boop_ui.lua` — modified pull creation, timeout, repeat, and `completePull` boundary.
    - `src/scripts/boop/boop_events.lua` — modified generation/phase-checked Room.Info transitions.
    - `tests/boop_pull_spec.lua` — Task 1 RED lifecycle and config-immutability contracts.
    - `tests/boop_event_transitions_spec.lua` — Task 1 Room.Info ordering and stale-event contracts.
    - `tests/boop_runtime_spec.lua` — Task 1 default/reset/deep-copy contracts.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, and D-05 through D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — pull callback-order and unrelated-owner invariants.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — section `First-Terminal-Wins Operation`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Namespace and state-default pattern`, `Pull ownership seam`, and `Room transition ordering pattern`.
  </read_first>
  <behavior>
    - "Pull allocates one generation/owner without persisted toggles."
    - "Every room/timeout transition compares generation and phase."
    - "Only current-generation terminal clears pull:<generation>."
    - "Timeout away stays fail closed until current-generation return."
  </behavior>
  <acceptance_criteria>
    - `DOMAIN_DEFAULTS.combat` materializes `pullGeneration = 0` and `pullState = false`, and no active pull record contains `restoreEnabled`.
    - Fresh pull allocates one `pull:<generation>` owner affecting combat, queue, target, gold, and walk, captures one timeout, and sends the unchanged validated command chain once.
    - `completePull` returns false with zero effects for wrong generation or terminal records and clears only the exact current pull owner when it returns true.
    - Timeout at origin terminates; timeout away changes the same owner to `pull_timeout_away`, sets phase `timed_out_away`, warns once, and remains held until matching return.
    - Pull start, timeout, return, and completion never call enabled-setting persistence APIs.
    - Focused runtime/pull/event specs, Lua syntax, and the release gate succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Implement the exact created inventory from `<interfaces>`. Add `pullGeneration` and inactive `pullState` to `DOMAIN_DEFAULTS.combat`; remove `restoreEnabled` and all temporary enable persistence. `pullCommand` allocates one owner, sets `pull_active` across combat, queue, target, gold, and walk, captures its generation in the timeout, and rejects repeats without replacing state. `completePull` checks generation/terminal, marks terminal before effects, kills only the matching timer, clears only the exact owner, traces the stable reason, and returns whether it won. A timeout at origin terminates; a timeout away changes the same owner to `pull_timeout_away`, phase to `timed_out_away`, warns once, and waits for matching return before release. Update Room.Info handling to compare generation and phase. Keep command validation/construction unchanged for Phase 04. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_ui.lua src/scripts/boop/boop_events.lua tests/boop_runtime_spec.lua tests/boop_pull_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run focused pull/event specs and then the full Mudlet suite locally, or require the matching GitHub Actions Mudlet/Busted result.</authoritative>
  </verify>
  <done>Pull is an exact-owner runtime operation with config-safe repeat, return, timeout, and stale-callback behavior.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol list; no additional pull constant, function, API, owner, phase, field, or reason value may be introduced without adding it there.
- Existing files only; no new command, persistence key, dependency, schema, manifest, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull alias → chained Achaea movement | Duplicate dispatch can move or attack unexpectedly. |
| Room.Info/timer → pull record | Late evidence can release a newer pull or resume away from origin. |
| Runtime hold → saved config | Temporary lifecycle state must not alter durable operator intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-PULL-STALE | Spoofing / Tampering | timeout and Room.Info callbacks | high | mitigate | Generation/phase compare and both-order stale tests. |
| T-03-PULL-DISPATCH | Tampering | repeat `pullCommand` | high | mitigate | One active lane with exact send/timer assertions. |
| T-03-PULL-CONFIG | Tampering | `config.enabled` | medium | mitigate | Runtime owner replaces persistence; save count remains zero. |
</threat_model>

<verification>
- Syntax/release gates after both tasks.
- Focused pull and event specs in real Mudlet, then full suite or exact CI fallback.
- Live pull return/timeout-away checks remain for `$gsd-verify-work 03`.
</verification>

<success_criteria>
- SAFE-02 pull behavior satisfies D-05 through D-08.
- Saved enabled configuration is unchanged before, during, and after pull.
- Every created identifier/value is explicit and version metadata is synchronized per commit.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-03-SUMMARY.md` when done.
</output>
