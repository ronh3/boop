---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "06"
type: execute
wave: 6
depends_on:
  - "03-05"
files_modified:
  - tests/support/boop_test_helper.lua
  - tests/boop_runtime_spec.lua
  - tests/boop_walk_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_walk.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "Per D-13–D-16, automatic movement emits only for the exact current run, room generation, and reservation ID after current settlement and complete all-clear, at most once per room generation."
    - "Per D-14 and D-15, prompt/timer evidence cannot settle a room; missing evidence requests one refresh then warns/traces while held."
    - "Per D-17, D-18, D-19, and D-20, manual move uses the same gate, is idempotent while queued, reports denial without mutation, and leaves walk mode active after success."
    - "The real walker lifecycle owns `walk:<generation>` and transitions it through `walk_room_unsettled`, `walk_move_pending`, `walker_unavailable`, and exact-owner clear without self-blocking its reserved emitter."
    - "Per WALK-03, status/start/move/package-loss never install or update the optional walker; only explicit install can do so."
  artifacts:
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Canonical walk generation, room generation, monotonic reservation ID, reservation flags, and timer defaults"
    - path: "src/scripts/boop/boop_walk.lua"
      provides: "Normal all-clear evaluator, exact-reservation evaluator, one emitter path, real owner transitions, package boundary, and generation invalidation API"
    - path: "tests/support/boop_test_helper.lua"
      provides: "Deterministic timer queue and walker/install stubs"
    - path: "tests/boop_walk_spec.lua"
      provides: "Automatic/manual gate, one-per-room, package-loss, explicit-install, and stale-emitter contracts"
  key_links:
    - from: "src/scripts/boop/boop_walk.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "immutable room/blocker snapshots plus exact `walk:<generation>` exclusion consumed by normal and reserved evaluators"
      pattern: "(evaluateAllClear|evaluateReservedEmission|roomObservationSnapshot|shouldHold)"
    - from: "src/scripts/boop/boop_walk.lua"
      to: "demonnicAutoWalker"
      via: "one generation-checked demonwalker.move emitter"
      pattern: "demonwalker.move"
---

<objective>
Build the D-13–D-20 walker core: canonical generations/reservations, one automatic/manual evaluator, one guarded emitter, and an explicit optional-package boundary.

Purpose: Movement must never outrun current room evidence, duplicate a room cycle, or emit from a stale callback.
Output: Exact walker state/reason/API inventory, focused lifecycle tests, and generation-safe move emission.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@UIDESIGN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md

<interfaces>
Created symbols and values — exhaustive:
- Local constant `WALK_REASON_LABELS` with exact keys/labels: `walker_unavailable="demonnicAutoWalker unavailable"`, `walk_inactive="walk mode is inactive"`, `hunting_disabled="hunting is disabled"`, `manual_targeting="manual targeting is active"`, `room_unsettled="current room evidence is incomplete"`, `move_pending="move already queued"`, `target_active="current target still set"`, `room_denizen_active="valid room target remains"`, `leader_call_pending="waiting for leader target call"`, `gold_pending="loot handling is pending"`, `interrupt_pending="interrupt is pending"`, `pull_pending="pull is pending"`, `flee_active="flee is active"`, `walker_lost_before_emit="demonnicAutoWalker became unavailable"`, and `room_refresh_exhausted="room item refresh completed without settlement"`.
- State fields `state.walk.generation` initialized to `0`, `roomGeneration` initialized to `0`, `moveIssuedForRoomGeneration` initialized to `false`, `reservationId` initialized to `0`, `refreshTimer` initialized to `nil`, `emitterTimer` initialized to `nil`, and `refreshWarned` initialized to `false`.
- Existing state field `state.walk.moveQueued` remains the reservation boolean.
- Stable owner form `walk:<generation>` and exact blocker codes `walk_room_unsettled`, `walk_move_pending`, and `walker_unavailable`.
- Public APIs `boop.walk.evaluateAllClear(runGeneration, roomGeneration)` and `boop.walk.invalidateCurrentGeneration(reason)`.
- New local functions `walkSnapshot()`, `evaluateReservedEmission(runGeneration, roomGeneration, reservationId)`, and `emitReservedMove(runGeneration, roomGeneration, reservationId)`.
- New test helpers `M.newTimerQueue()` and `M.setWalker(opts)`.

Existing symbols modified, not created:
- `DOMAIN_DEFAULTS.walk`; it contains and reset/deep-copies every exact walk field: `active`, `owned`, `roomSettled`, `moveQueued`, `arrivalRoom`, `generation`, `roomGeneration`, `moveIssuedForRoomGeneration`, `reservationId`, `refreshTimer`, `emitterTimer`, and `refreshWarned`.
- Existing constant `WALKER_PACKAGE_URL`.
- Existing locals `stateDomains`, `walkState`, `runtimeWalkBlocker`, `blockedReason`, `available`, `attached`, `armArrivalFallback`, and `resetRuntimeFlags`.
- Existing public APIs `boop.walk.blockedReason`, `isAvailable`, `install`, `isActive`, `status`, `start`, `stop`, `onFinished`, `onRoomSettled`, `onRoomChange`, `maybeAdvance`, and `move`.
- Existing fields `active`, `owned`, `roomSettled`, `moveQueued`, `arrivalRoom`, and `arrivalTimer`; `roomSettled` becomes derived from shared room observation and `arrivalTimer` is replaced by the exact refresh/emitter timers above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build walker core, package-boundary, and movement-gate contracts</name>
  <files>tests/support/boop_test_helper.lua, tests/boop_runtime_spec.lua, tests/boop_walk_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/support/boop_test_helper.lua` — modified timer queue and walker/install boundary helpers.
    - `tests/boop_runtime_spec.lua` — modified `DOMAIN_DEFAULTS.walk` reset/deep-copy assertions.
    - `tests/boop_walk_spec.lua` — modified automatic/manual gate, exact reservation, owner lifecycle, package loss, and install coverage.
    - `src/scripts/boop/boop_runtime.lua` — existing `DOMAIN_DEFAULTS.walk`, room snapshots, exact-owner blocker APIs, and reset behavior under test.
    - `src/scripts/boop/boop_walk.lua` — existing package, blocker, start/stop/room, movement, and emitter seams under test.
    - `src/scripts/boop/boop_targets.lua` — target and valid-room-denizen predicates consumed by the all-clear evaluator.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-13 through D-20 plus package-loss discretion.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — rows 03-W0-04/05 and walk settlement, duplicate, external-loss, reservation, and owner-transition invariants.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — section `Pattern 5: One Walk Evaluator and One Emitter`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `src/scripts/boop/boop_walk.lua`, `tests/boop_walk_spec.lua`, `Walker boundary`, `Capture callbacks as an ordered queue`, and `Preserve event-capture shape`.
  </read_first>
  <behavior>
    - "Runtime reset materializes every exact walk field independently."
    - "Automatic/manual rows return the same reason code and emit zero while blocked."
    - "One current room cycle increments and captures one reservation ID, emits once, and a new settled generation permits one new event."
    - "Normal evaluation reports move_pending, while exact reserved evaluation bypasses only its matching walk owner and reservation."
    - "Real start/room/settlement/reservation/package-loss/stop transitions set, update, and clear walk:<generation>."
    - "Package loss before emit produces zero movement/install/update calls."
    - "Explicit install distinguishes throw, false/nil-plus-error, and success."
  </behavior>
  <acceptance_criteria>
    - Reset/deep-copy tests independently assert exact values for `active`, `owned`, `roomSettled`, `moveQueued`, `arrivalRoom`, `generation`, `roomGeneration`, `moveIssuedForRoomGeneration`, `reservationId == 0`, `refreshTimer`, `emitterTimer`, and `refreshWarned`.
    - Active start/restart and current Room.Info without complete current items call `setBlocker` for exact owner `walk:<generation>`, code `walk_room_unsettled`, systems `{walk=true}`, and current-room/items waits.
    - Complete current evidence with no reserved move clears exactly `walk:<generation>`; reservation creation increments `reservationId`, captures it in one emitter callback, and updates the same owner to `walk_move_pending`.
    - `evaluateAllClear(runGeneration, roomGeneration)` returns `move_pending` whenever `moveQueued == true`.
    - `evaluateReservedEmission(runGeneration, roomGeneration, reservationId)` authorizes only when all three captured values match; it excludes only `walk:<generation>` and still rejects external unavailability, incomplete room evidence, target/denizen/leader/gold/interrupt/pull/flee state, and every unrelated walk owner.
    - Wrong run, room generation, or reservation ID callbacks emit zero events and leave the current snapshot unchanged; one matching callback raises exactly one `demonwalker.move`, and invoking it again raises zero.
    - Mid-run package loss invalidates the queued callback, updates the same owner to `walker_unavailable`, emits one warning/trace transition, and calls install/update zero times.
    - Stop, detach, and finish fixtures invalidate callbacks before clearing the exact walker owner.
    - One patch increment synchronizes all four version checkpoints, and focused Lua syntax plus release gates succeed.
  </acceptance_criteria>
  <action>
Add only `M.newTimerQueue()` and `M.setWalker(opts)` with stable IDs, callback lookup, cancel tracking, package availability/owned/attached modes, install throw/return/success modes, and raised-event capture. Add reset/deep-copy assertions for every exact `DOMAIN_DEFAULTS.walk` field, including `reservationId == 0`, and snapshot assertions that include the current reservation ID. Table-drive automatic and manual movement across every `WALK_REASON_LABELS` condition plus a canonical runtime primary blocker; assert identical reason code/label, exact event/timer/install/update counts, and unchanged generation/reservation on denial. Directly invoke real `start`, `onRoomChange`, `onRoomSettled`, reservation, package-loss, stop/detach, and finish entry points to assert `setBlocker`/update/`clearBlocker` transitions for `walk:<generation>`. Add normal-evaluator `move_pending`, exact-reserved-evaluator self-exclusion, unrelated-owner blocking, one-refresh, monotonic reservation capture, one-event-per-room, duplicate callback, new-room rearm, wrong reservation, stale emitter, explicit install, and mid-run package-loss rows. Synchronize the next patch version before commit; do not edit built output.
  </action>
  <verify>
    <automated>luac -p tests/support/boop_test_helper.lua tests/boop_runtime_spec.lua tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>D-13–D-20 and WALK-03 core behavior is RED with exact state, reason, event, timer, install, and stale-callback assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement one all-clear evaluator and guarded move emitter</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_walk.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_runtime.lua` — modified `DOMAIN_DEFAULTS.walk`, room/blocker snapshots, and exact-owner exclusion support.
    - `src/scripts/boop/boop_walk.lua` — modified normal/reserved evaluators, lifecycle owner transitions, package boundary, reservation scheduling, and emitter.
    - `tests/boop_runtime_spec.lua` — Task 1 exact walk default/reset/deep-copy contracts.
    - `tests/boop_walk_spec.lua` — Task 1 evaluator, reservation, lifecycle owner, package-loss, and stale-callback contracts.
    - `tests/support/boop_test_helper.lua` — deterministic timer/walker fixtures from Task 1.
    - `src/scripts/boop/boop_targets.lua` — target and room-denizen checks that both evaluators must preserve.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-13 through D-20 and package-loss discretion.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — walker reservation, owner-transition, settlement, duplicate, and external-loss requirements.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — sections `Pattern 5: One Walk Evaluator and One Emitter` and `Generation-Guarded Walker Emission`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Namespace and state-default pattern`, `Existing all-clear inventory`, `Current move seam to harden`, and `Walker boundary`.
  </read_first>
  <behavior>
    - "Only current run + current room + current reservation can emit."
    - "Normal evaluation treats moveQueued as blocked; reserved evaluation bypasses only the exact current reservation's move-pending owner."
    - "Emitter repeats package, room, target, leader, gold, interrupt, pull, flee, and unrelated-owner checks immediately before the event."
    - "Walker lifecycle updates and clears the exact walk owner at real start, room, settlement, reservation, loss, stop, detach, and finish transitions."
    - "Missing settlement refreshes once and remains held."
    - "Successful manual move keeps active mode and waits for the next room cycle."
  </behavior>
  <acceptance_criteria>
    - Existing `DOMAIN_DEFAULTS.walk` lists every exact walk field, including `reservationId = 0`; `ensureState()` deep-copies defaults and `resetRuntimeFlags` explicitly resets every field, as proven by focused runtime/walk assertions.
    - `evaluateAllClear` never bypasses `moveQueued`; it returns `(false, "move_pending", WALK_REASON_LABELS.move_pending)` whenever a reservation is pending.
    - Local `evaluateReservedEmission(runGeneration, roomGeneration, reservationId)` bypasses only the matching `moveQueued`/`walk_move_pending` condition and exact owner `walk:<generation>`; every unrelated owner affecting walk remains blocking.
    - `maybeAdvance` and `move` increment `reservationId` before scheduling, capture run/room/reservation in the sole emitter, set `moveQueued`/`moveIssuedForRoomGeneration`, and update the same owner to `walk_move_pending`.
    - A matching emitter marks its callback consumed before raising exactly one `demonwalker.move`, retains `walk_move_pending` until new Room.Info, and makes duplicate invocation a zero-effect no-op.
    - New Room.Info updates the current owner to `walk_room_unsettled`; complete current items with no reservation clears it; package absence/loss updates it to `walker_unavailable` and invalidates the captured reservation without implicit install/update.
    - Stop, attached detach, and external finish invalidate timer/reservation callbacks before clearing exactly the captured `walk:<generation>` owner.
    - One capped room refresh may be scheduled, but no prompt or timer stamps settlement; exhausted recovery warns/traces and emits zero movement.
    - Focused runtime/walk specs, Lua syntax, and the release gate succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Implement the exact created inventory in `<interfaces>`. Add every listed existing/new walk field to `DOMAIN_DEFAULTS.walk`, including `reservationId = 0`, preserve deep-copy materialization, and make `resetRuntimeFlags` explicitly restore each exact field. Derive settlement only from `boop.runtime.roomObservationSnapshot()`. `walkSnapshot()` captures active/owned/config/target/denizen/leader/gold/interrupt/pull/flee/blocker/room/reservation/package state, including `reservationId`. `evaluateAllClear` returns `(ok, reasonCode, label)` for normal/automatic/manual requests and must return `move_pending` whenever `moveQueued` is true. Add local `evaluateReservedEmission(runGeneration, roomGeneration, reservationId)` for the deferred emitter: require exact current run, room generation, reservation ID, active mode, and a live unconsumed emitter; bypass only the matching `moveQueued` condition and exact `walk:<generation>` owner via `boop.runtime.shouldHold("walk", owner)`, while rechecking external availability, settlement, target, valid denizen, leader wait, gold, interrupt, pull, flee, and every unrelated walk blocker.

Wire the real owner lifecycle through `boop.runtime.setBlocker`/`clearBlocker`. Active start/restart and current Room.Info without complete current items set/update `walk:<generation>` to `walk_room_unsettled` for `{walk=true}` with current room/items waits. Complete current evidence clears that exact owner only when no move is reserved. Reservation increments and captures `reservationId` before scheduling, sets `moveIssuedForRoomGeneration`/`moveQueued`, and updates the same owner to `walk_move_pending`; retain it through the one successful emission until confirmed new Room.Info transitions it back to `walk_room_unsettled`. Package absence/loss while active cancels/invalidate the queued reservation, updates the same owner to `walker_unavailable`, warns/traces once, and never installs/updates. Stop, detach, and external finish invalidate callbacks first, then clear the captured owner.

`maybeAdvance` and `move` share the normal evaluator and sole `emitReservedMove(runGeneration, roomGeneration, reservationId)` path. The emitter delegates to the reserved evaluator, marks the matching callback consumed before raising exactly `demonwalker.move`, and leaves wrong/stale/duplicate callbacks without state mutation or events. Replace timer-assumed settlement with one shared room refresh plus one bounded warning timer that never stamps evidence. Harden explicit install result handling only. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_walk.lua tests/boop_runtime_spec.lua tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run the focused walk spec and full Mudlet suite locally, or require the matching GitHub Actions Mudlet/Busted result.</authoritative>
  </verify>
  <done>D-13–D-20 use one immutable all-clear decision and one current-generation emitter with no timer/manual settlement shortcut.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol list, including `state.walk.reservationId`, local `evaluateReservedEmission(runGeneration, roomGeneration, reservationId)`, and the three-argument `emitReservedMove`; no additional walker constant, function, API, owner, state/timer field, blocker code, or reason code may be introduced without adding it there.
- Existing files only; no new package module, manifest entry, dependency, command, alias, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GMCP/runtime snapshot → move event | Incomplete or stale evidence can cause unsafe movement. |
| Deferred emitter → current run | Stop/restart/package loss can invalidate captured work. |
| boop → optional external package | Availability and install outcomes are outside boop control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-WALK-MOVE | Tampering | evaluator/emitter | high | mitigate | Current generation/reservation and final recheck. |
| T-03-WALK-STALE | Spoofing / Tampering | deferred emitter | high | mitigate | Captured run/room comparison and zero-event stale tests. |
| T-03-WALK-EXT | Tampering / Denial of Service | optional package | high | mitigate | Fail closed, explicit install only, and exact zero implicit calls. |
</threat_model>

<verification>
- Focused syntax/release gates after each task.
- Real Mudlet walk/full suite or exact CI fallback is authoritative.
- Stop/arrival/event integration is assigned to Plan 03-07.
</verification>

<success_criteria>
- WALK-01/02 core movement behavior satisfies D-13 through D-20.
- WALK-03 remains explicit and optional with no silent installation/update.
- Every commit synchronizes version fields and avoids built artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-06-SUMMARY.md` when done.
</output>
