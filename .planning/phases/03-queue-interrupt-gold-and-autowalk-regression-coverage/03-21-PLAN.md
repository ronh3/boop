---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 21
type: execute
wave: 21
depends_on: [03-20]
files_modified:
  - tests/boop_event_transitions_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - src/scripts/boop/boop_targets.lua
  - src/scripts/boop/boop_attacks.lua
  - src/scripts/boop/boop_util.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements: [SAFE-02, SAFE-04, WALK-02]
gap_closure: true
gap_ids: [G-03-7]
must_haves:
  truths:
    - "Automatic target selection, combat effects, delayed leader-target wake-up, delayed prequeue, prequeue refresh, and target-loss retarget work carry the immutable source authority that produced them."
    - "Every local mutation and external target/combat effect revalidates exact room ID and observation generation immediately before it occurs."
    - "The 4255-to-4249 chronology emits zero generation-45 settarget, BOOP_ATTACK, direct standard, rage, prequeue, refresh, retarget, gold, or walk effects after the moved-room boundary."
    - "Stale callbacks validate their captured generation-45/application authority and cannot obtain generation 46 authority from the destination's current context."
    - "Rejecting stale target/combat work clears only its local intent and never sends a global queue clear or removes unrelated shared queue work."
    - "Current-generation work still emits exactly once after its own accepted room application."
  artifacts:
    - "tests/boop_event_transitions_spec.lua contains the exact 4255-to-4249 stale-dispatch regression and current-generation control case."
    - "src/scripts/boop/boop_runtime.lua carries source authority from contexts into target/combat effects and validates the captured value at effect application."
    - "src/scripts/boop/boop_events.lua captures authority for automatic tick, delayed prequeue, refresh, and target-loss retarget call sites/callbacks."
    - "src/scripts/boop/boop_targets.lua validates room authority before target state/stat/call/send effects."
    - "src/scripts/boop/boop_attacks.lua treats target/combat dispatch as successful only when authority remains current."
    - "src/scripts/boop/boop_util.lua validates authority before each standard/rage external send boundary."
  key_links:
    - "Plan 03-20 room application -> runtime target/combat effects -> immutable room authority."
    - "Captured room authority -> runtime applyEffects / events callbacks -> targets.setTarget / attacks.execute -> executeAction / executeRageAction."
    - "Balance/prequeue, shield refresh, and target-loss retarget callbacks -> captured source authority -> final validator."
    - "Moved Room.Info -> captured authority mismatch -> zero local/external stale effects and no queue-wide cleanup."
---

<objective>
Complete the stale-dispatch half of G-03-7 by carrying Plan 03-20's room authority through target and combat execution to every external send.

Purpose: Ensure a moved Room.Info can invalidate pending prior-room decisions before they become irreversible server commands, while preserving exact-owner and unrelated-queue safety.
Output: The exact 4255→4249 regression plus generation-aware runtime effects, target, standard, alias, queue, rage, delayed prequeue, refresh, and retarget dispatch.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-live-room-evidence-wakeup-regression.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-20-SUMMARY.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_targets.lua
@src/scripts/boop/boop_attacks.lua
@src/scripts/boop/boop_util.lua
@tests/boop_event_transitions_spec.lua

<interfaces>
- Plan 03-20 provides immutable room authority on generation-owned application/effect records and a runtime predicate that validates current room ID, observation generation, settlement, and `gmcp.Room.Info`.
- `boop.runtime.tickStep()` and `boop.runtime.applyEffects()` create/apply automatic target and combat effects; they must retain the originating context's source authority.
- `boop.onBalanceUsed()` → `boop.schedulePrequeue()` → its timer → `boop.prequeueStandard()` is a delayed automatic dispatch chain currently carrying no room authority.
- The incoming leader-target path in `boop_targets.lua` schedules a zero-delay `boop.tick()` and currently lets the callback use mutable current room state.
- `boop.targets.onShielded()` → `boop.refreshPrequeuedStandard()` rebuilds queued combat from mutable current state.
- `boop.onRoomItemsRemove()` selects a replacement target synchronously and schedules a zero-delay retarget tick without preserving the removed item's room authority.
- `boop.targets.setTarget(id)` currently mutates target state and immediately sends `settarget`.
- `boop.attacks.execute(plan, context)` currently calls `boop.executeAction` and `boop.executeRageAction` without room authority.
- `boop.executeAction(action, forceQueue)` currently emits `setalias BOOP_ATTACK`, `queue addclearfull freestand BOOP_ATTACK`, or direct standard commands.
- `boop.executeRageAction(action)` currently emits direct rage commands.
</interfaces>
</context>

<scope_exception>
This is one tightly coupled test-first task because runtime effects, event callbacks, target, attack, and utility modules form one irreversible external-dispatch chain. The RED state remains uncommitted, followed by one synchronized version bump and one package-affecting commit.
</scope_exception>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate exact room authority through target and combat dispatch, then bump the synchronized package version</name>
  <files>tests/boop_event_transitions_spec.lua, src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, src/scripts/boop/boop_targets.lua, src/scripts/boop/boop_attacks.lua, src/scripts/boop/boop_util.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/debug/phase-03-live-room-evidence-wakeup-regression.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-20-SUMMARY.md
    - tests/boop_event_transitions_spec.lua
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_events.lua
    - src/scripts/boop/boop_targets.lua
    - src/scripts/boop/boop_attacks.lua
    - src/scripts/boop/boop_util.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Generation-45 room 4255 accepts denizen 44026 and creates pending work; moved Room.Info for 4249 arrives before dispatch; invoking all old callbacks emits zero stale target, standard, alias, queue, rage, prequeue, refresh, retarget, gold, or walker effects."
    - "The captured target/combat effect, delayed leader-target wake-up, delayed prequeue callback, prequeue refresh, and target-loss retarget callback each still present `{applicationId=<generation-45 id>, roomId='4255', observationGeneration=45}` to validation after room 4249/generation 46 becomes current."
    - "No stale callback calls a current-authority accessor or constructs authority from room 4249/generation 46; replacing the old token with destination authority would make the regression fail."
    - "Stale rejection leaves unrelated runtime blockers, queue alias state, prequeue ownership, and an unrelated captured native queue command unchanged; it emits no queue clear."
    - "A current generation-46 application may select, apply, and send one target and one eligible combat plan, delayed prequeue, refresh, and retarget after its own evidence settles."
    - "Target state, target stats/calls, opener use, shieldbreak-attempt state, rage readiness/stats, gag intent, queue alias cache, and combat limiter state do not advance when authority is stale."
    - "Queued standards validate before `setalias` and again before the BOOP_ATTACK queue command; direct standards and rage validate before each individual send."
    - "Automatic prequeue scheduling captures current settled authority once, stores it with prequeue state, and passes the same value through the timer, prequeue dispatch, shield refresh, and final sends."
    - "Target-loss removal captures current settled authority once and passes it through the deferred retarget selection, target mutation/send, and follow-up tick."
    - "Flee and other intentional non-room command families remain callable without room source authority and retain their existing behavior."
  </behavior>
  <action>
Start by adding a named `G-03-7 4255-to-4249 external dispatch fence` context to `tests/boop_event_transitions_spec.lua`. Drive the real Plan 03-20 accepted-application seam and capture five distinct generation-45 work families before moving: runtime target/combat effects, the incoming leader-target zero-delay wake-up, the `schedulePrequeue` timer callback, a `refreshPrequeuedStandard` rebuild using the prequeue's stored authority, and the zero-delay target-loss retarget callback created by `onRoomItemsRemove`. Move with Room.Info to 4249/generation 46 before applying/invoking each captured item. Instrument the authority validator so every stale check must receive the original application id, room 4255, and observation generation 45; assert no callback asks for or receives destination generation 46 authority. Record sent commands by category and snapshot target, queue, combat, rage, blocker, gold, and walker state. Assert exact zero stale `settarget`, target-call, `setalias`, BOOP_ATTACK queue, direct standard, rage, prequeue/refresh trace/state, and retarget/tick effects. Include corresponding current-generation-46 controls that each emit exactly once after destination evidence settles, so the test distinguishes safe suppression from globally disabling combat. Require RED failures at the missing authority propagation/final checks, then leave that RED state uncommitted.

In `boop_runtime.lua`, preserve `context.sourceAuthority` when `tickStep` creates target and `combat_plan` effects, attach a copied authority to each effect, and make `applyEffects` validate and forward that exact captured value. If the originating room-owned context lacks authority or the captured value is stale, reject the automatic effect; never call a current-authority accessor as fallback. Keep non-room effects such as flee explicitly outside this requirement.

In `boop_events.lua`, update every automatic room-derived call site and callback. Allow `boop.tick(sourceAuthority)` to accept a captured token; synchronous entry points may capture the currently accepted authority once at tick entry, but a scheduled callback must close over and reuse its original token with no destination fallback. Thread authority through `onBalanceUsed`, `schedulePrequeue`, its timer, `prequeueStandard`, and `refreshPrequeuedStandard`; store the source authority beside prequeue state so shield-triggered refresh uses the queued work's origin. On moved Room.Info, invalidate that authority and let local intent cleanup remove the timer/prequeue authority. Refactor the `onRoomItemsRemove` target-loss path so its zero-delay callback owns replacement selection, `setTarget`, and follow-up tick under the source authority captured when the removal was accepted; movement before callback execution makes the entire retarget a no-op. Do not let delayed prequeue, refresh, or retarget code reacquire authority from current room 4249.

In `boop_targets.lua`, extend target application/dispatch options so automatic target state mutation, stats, party target call state, `settarget`, and target-call sends first prove the passed source authority. Capture accepted source authority when an incoming leader target schedules its zero-delay tick and pass that exact token into the callback; if it is stale, do not substitute current authority. Pass the stored prequeue authority from `onShielded` into `refreshPrequeuedStandard`. A stale operation returns false and leaves those state surfaces unchanged.

In `boop_attacks.lua`, require the active automatic plan context's room authority to remain current before standard execution, before rage execution, and before marking opener/shield/rage/stat side effects. Count an action only when its downstream emitter reports success. Preserve intentional non-room command families such as interrupt, pull, party-share, and explicit external-walker events; this task guards automatic target/combat effects derived from accepted room evidence.

In `boop_util.lua`, add an options/source-authority argument without changing existing `forceQueue` meaning. Automatic callers must identify themselves as room-owned and provide authority; revalidate immediately before every irreversible `send`: `setalias BOOP_ATTACK`, `queue addclearfull freestand BOOP_ATTACK`, each direct standard segment, and each rage segment. Update queue alias cache, gag intent, maul readiness, and trace only for commands that passed validation. When authority is stale, return false and trace a compact application/generation/room rejection; never attempt queue-wide cleanup or mutable-index removal. Preserve calls that are intentionally non-room-owned, especially `boop.safety.flee()` and existing interrupt/pull/manual families: absent authority remains valid only for those explicit callers, while automatic target/combat/prequeue/refresh/retarget paths always declare room ownership and fail closed without a token.

Preserve D-01, D-02, D-03, D-04, D-13, D-14, D-15, and D-16 for aggregate room/movement authorization. Preserve D-17, D-18, D-19, D-20, D-21, D-22, D-23, and D-24 by leaving manual movement, reservation, stop/detach, and restart ownership on the existing walker path. Do not rely on elapsed delay, increase a timeout, or clear shared native queues to repair stale work.

After GREEN, read the current synchronized package version, increment its patch component once, and update `mfile.version`, `mfile.title` as `boop Hunter <version>`, `src/scripts/boop/boop_init.lua` `boop.version`, and the CODEX current synchronized package-version checkpoint. Stage only the nine listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before the single commit, and commit as `fix(03-21): guard room-owned combat dispatch`. Do not push.
  </action>
  <acceptance_criteria>
    - "The exact 4255→4249 old-generation path has zero stale local and external effects."
    - "Current generation 46 still targets, attacks, prequeues, refreshes, and retargets exactly once after its own accepted evidence."
    - "Runtime effects and every automatic target/standard/rage/prequeue/refresh/retarget call site and callback retain and finally validate captured source authority."
    - "The regression proves stale generation-45 work cannot be validated with destination generation-46 authority."
    - "Intentional non-room callers such as flee remain functional."
    - "No stale rejection sends queue clear or mutates unrelated blocker/queue ownership."
    - "All four version checkpoints are equal and monotonically greater than the task-start value; release gates pass immediately before the single commit."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_event_transitions_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua &amp;&amp; luac -p src/scripts/boop/boop_events.lua &amp;&amp; luac -p src/scripts/boop/boop_targets.lua &amp;&amp; luac -p src/scripts/boop/boop_attacks.lua &amp;&amp; luac -p src/scripts/boop/boop_util.lua &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-7 prior-room target and combat decisions cannot escape after their exact room generation becomes stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime room decision → target/combat modules | A copied but stale plan crosses from reversible client state toward command intent. |
| Target/combat modules → Achaea `send()` | Server commands are irreversible and cannot be safely removed by broad shared-queue cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-21-01 | Spoofing | stale room authority | high | mitigate | Carry immutable room ID/generation and validate against current settled observation at every automatic boundary. |
| T-03-21-02 | Tampering | BOOP_ATTACK alias/queue | high | mitigate | Revalidate before alias mutation and before queue insertion; update cache only after authorized sends. |
| T-03-21-03 | Elevation of Privilege | stale target/rage/prequeue/refresh/retarget side effects | high | mitigate | Suppress state/stat/readiness mutations unless the exact captured source authority and downstream dispatch succeed. |
| T-03-21-04 | Denial of Service | unrelated shared queue work | medium | mitigate | Reject locally and prohibit queue-wide or index-based cleanup. |
| T-03-21-05 | Spoofing | destination authority substitution | high | mitigate | Tests inspect validator arguments and forbid stale callbacks from reacquiring the destination's current authority. |
| T-03-21-SC | Tampering | package installs | low | accept | No package install or dependency change is introduced. |
</threat_model>

<live_uat_evidence>
At final immutable HEAD, capture `boop trace live` through a real 4255→4249 transition. Evidence must show the old room application/dispatch authority rejected before any `settarget`, `BOOP_ATTACK`, standard, rage, delayed prequeue, shield refresh, or target-loss retarget command; no queue-clear command may appear. Record each callback's validator input and prove it remains the generation-45 application/room 4255 authority rather than destination generation 46. The destination room must settle and perform only its own eligible target/combat/prequeue/refresh/retarget action.
</live_uat_evidence>

<verification>
Run the focused event-transition host spec, syntax checks for all three dispatch modules, diff hygiene, release gates, and Muddler. Final authority remains the packaged real-Mudlet suite plus live trace at the exact pushed SHA.
</verification>

<success_criteria>
- Target and combat effects are exact-room/exact-generation owned through the last `send()`.
- Generation 45 target, combat, prequeue, refresh, and retarget work cannot dispatch after the 4255→4249 boundary.
- Generation 46 remains live and can dispatch each current-generation path once from its own evidence.
- Delayed work validates the authority captured at creation and cannot adopt destination authority.
- Explicit non-room dispatch such as flee remains valid.
- Unrelated shared queue work survives stale-work rejection.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-21-SUMMARY.md` when done.
</output>
