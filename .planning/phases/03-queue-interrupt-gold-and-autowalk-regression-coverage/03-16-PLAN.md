---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 16
type: execute
wave: 16
depends_on: [03-15]
files_modified:
  - tests/boop_gold_spec.lua
  - tests/boop_event_transitions_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements: [SAFE-04, WALK-02]
gap_closure: true
gap_ids: [G-03-5]
must_haves:
  truths:
    - "A settled non-gold room List followed by a same-room gold Add requests exactly one response-fenced room revalidation for that exact gold operation."
    - "The room-ID-less Add remains non-canonical: no get is sent until a current fenced room List contains the exact gold item."
    - "Wrong-room, stale-generation, stale-fence, duplicate-signal, and movement-before-response paths cannot produce get, put, attack, or walk movement."
    - "Accepted evidence preserves the exact gold owner and generation, aggregate blocker semantics, and exactly one get-confirm-put lifecycle per item."
  artifacts:
    - "tests/boop_gold_spec.lua contains the G-03-5 focused gold lifecycle regression."
    - "tests/boop_event_transitions_spec.lua contains the cross-subsystem blocker and movement race regression."
    - "src/scripts/boop/boop_runtime.lua models a bounded room-only response fence without weakening ordinary observation fences."
    - "src/scripts/boop/boop_events.lua starts, times out, and consumes one settled-Add revalidation for the current gold operation."
  key_links:
    - "Char.Items.Add -> exact DEFERRED_ROOM gold operation -> room-only response fence -> Char.Items.Room request."
    - "Accepted current room response -> copied acceptedItems -> canonicalGoldEvidence -> same operation generation -> one get."
    - "Room movement/invalidation -> stale fence drain -> no gold promotion and no downstream release side effects."
---

<objective>
Close G-03-5 by making a same-room gold Add revalidate settled room evidence through one bounded response fence before gold pickup can begin.

Purpose: Preserve D-09 through D-12 exact-room gold authority and D-14 through D-16 bounded observation behavior while eliminating the live DEFERRED_ROOM stall.
Output: Focused regressions plus runtime/event wiring for a single operation-scoped room-only revalidation.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-gold-deferred-hunting-stall.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-14-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-15-SUMMARY.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_events.lua
@tests/boop_gold_spec.lua
@tests/boop_event_transitions_spec.lua
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce the settled-List then gold-Add stall and its race boundaries</name>
  <files>tests/boop_gold_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - tests/boop_gold_spec.lua
    - tests/boop_event_transitions_spec.lua
    - tests/support/boop_host_busted_helper.lua
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_events.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Gold lifecycle: accept a current settled non-gold List, receive a same-room gold Add, retain the same DEFERRED_ROOM operation generation, send exactly one Char.Items.Room and no Char.Items.Inv, and send no get before the fenced room response."
    - "Duplicate Add events, duplicate text signals, and timer callbacks do not create another request, another operation generation, or another get."
    - "An inventory response cannot authorize the room-only fence; a current room response containing the exact gold item promotes the same operation and yields one get, one confirmed put, and no duplicates."
    - "A later distinct gold operation in the same room receives its own one-request allowance."
    - "Cross-subsystem race: target removal may retarget, but the exact gold owner remains an aggregate queue blocker; an unrelated owner still blocks after evidence acceptance until its real release."
    - "Movement before the response invalidates the old fence; a delayed old-room response drains without get, put, attack, or walk movement."
  </behavior>
  <action>
Add focused failing examples under clearly named `G-03-5 settled-add-revalidation` describe/context labels. Drive the real event entry points and inspect sent commands, runtime owner snapshots, gold generation, fence phase, timer count, target transitions, and movement reservations. Use the existing host helper and exact-room GMCP fixtures rather than constructing an alternate test harness.

Keep D-01/D-02 aggregate ownership, D-03 diagnostic detail, D-04 priority, D-09 exact-room evidence, D-10 confirmed pickup, D-11 coalescing, D-12 settled observation, and D-13 movement gating explicit in assertions. The Add itself must never be inserted into canonical `acceptedItems`; the failing tests must prove authority comes from the subsequent fenced room List.

Before committing, bump the synchronized package version from 0.1.429 to 0.1.430 in all four fields: `mfile.version`, `mfile.title`, `boop.version` in `src/scripts/boop/boop_init.lua`, and the CODEX current checkpoint. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `test(03-16): reproduce settled-add gold revalidation gap`. Do not push.
  </action>
  <acceptance_criteria>
    - "The new regressions fail against the pre-fix implementation for the missing room-only revalidation, not because of fixture or syntax errors."
    - "The assertions cover exact owner/generation, one request, one get-confirm-put, stale response rejection, and aggregate blocking."
    - "All synchronized version fields read 0.1.430 and the release gate passes for the staged test commit."
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'output=$(env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_gold_spec.lua tests/boop_event_transitions_spec.lua 2&gt;&amp;1); rc=$?; printf "%s\n" "$output"; test "$rc" -ne 0; printf "%s\n" "$output" | rg -q "G-03-5 settled-add-revalidation"; python3 tools/check_release_gates.py'</automated>
  </verify>
  <done>The red suite deterministically exposes G-03-5 and the version-synchronized test commit exists without a push.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence and consume one operation-scoped room revalidation</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_events.lua
    - tests/boop_gold_spec.lua
    - tests/boop_event_transitions_spec.lua
    - .planning/debug/phase-03-gold-deferred-hunting-stall.md
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Ordinary observation fences retain the Inv then Room response sequence and the D-15 capped-refresh rule."
    - "A `roomOnly`/room-revalidation fence may start after a settled List only for a current exact-room, exact-generation DEFERRED_ROOM gold operation with no active fence."
    - "The room-only fence begins in the room-waiting phase, preserves FIFO drain behavior, and accepts only the current room/generation response."
    - "Each exact gold operation records whether its Add-triggered revalidation was attempted and the fence it owns; repeated signals cannot spend the allowance twice."
    - "Acceptance replaces canonical room items through the existing copied-list transition and promotes only the same operation; timeout, movement, stale room, or missing exact item fail closed."
  </behavior>
  <action>
Extend `boop.runtime.beginRoomResponseFence(reason, opts)` with an explicit room-only mode. In that mode, require the current stable room identity and generation, reject another active fence, append a typed fence that starts at `await_room`, and preserve the existing queue order, invalidation, stale-drain, and copied-list acceptance path. Do not loosen the ordinary fence guard or treat room-ID-less Add payloads as canonical evidence.

Add operation-scoped fields such as `revalidationAttempted` and `revalidationFenceId` to the exact gold operation. In `boop_events.lua`, create a focused request helper invoked only when a room Add leaves the current operation in DEFERRED_ROOM after an already settled List. It must spend that operation's allowance before sending one `Char.Items.Room`, use the existing bounded response timer/warning discipline, and never send `Char.Items.Inv` for this path. Keep D-05 through D-08 terminal ownership semantics, D-09 through D-12 gold authority, D-14/D-15 response fencing, and D-16 one-move discipline intact. On accepted evidence, use the existing canonical transition so owner release, target retargeting, and walk movement remain aggregate-gated.

Run both focused specs to green, then host Lua syntax checks and `muddle`. Before committing, bump all synchronized package fields from 0.1.430 to 0.1.431. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `fix(03-16): fence settled-add gold revalidation`. Do not push.
  </action>
  <acceptance_criteria>
    - "The settled Add path sends one room-only request and cannot authorize pickup until the exact current fenced room response."
    - "Ordinary Inv then Room observation, stale fence rejection, aggregate blockers, and the exact get-confirm-put lifecycle remain green."
    - "All synchronized version fields read 0.1.431; focused host tests, syntax checks, release gates, and muddle pass."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_gold_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua &amp;&amp; luac -p src/scripts/boop/boop_events.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-5 is closed with one bounded operation-scoped revalidation and all preserved safety invariants remain executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GMCP `Char.Items.Add/List` → room observation | Server events are asynchronous, room-sensitive, and may arrive stale or out of order. |
| Room observation → gold command emission | Only canonical exact-room evidence may authorize `get`, and aggregate owners must continue blocking unrelated actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16-01 | Tampering | `observeRoomItemsList` fence matching | high | mitigate | Bind room-only fences to immutable room/generation snapshots and preserve FIFO stale-drain rejection. |
| T-03-16-02 | Spoofing | room-ID-less `Char.Items.Add` | high | mitigate | Keep Add non-canonical and require a fenced current room List containing the exact item ID. |
| T-03-16-03 | Denial of Service | duplicate Add/text/timer signals | medium | mitigate | Spend one allowance per exact gold operation before sending and coalesce repeated signals. |
| T-03-16-04 | Elevation of Privilege | owner release and movement | high | mitigate | Preserve exact owner tokens and aggregate all-clear checks before queue, attack, or movement release. |
| T-03-16-SC | Tampering | package installs | low | accept | No npm, pip, or cargo package install is introduced by this plan. |
</threat_model>

<verification>
Run the two focused host Busted files, parse both modified Lua files with `luac -p`, run `python3 tools/check_release_gates.py`, and run `muddle`. The parent phase executor should also run the full host Busted suite after all gap plans are complete.
</verification>

<success_criteria>
- A settled non-gold List followed by gold Add can no longer strand the operation in DEFERRED_ROOM.
- The Add never becomes canonical evidence on its own.
- Wrong-room/stale-fence responses, duplicate signals, and movement races remain fail-closed.
- The exact owner/generation and one get-confirm-put lifecycle remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-16-SUMMARY.md` when done.
</output>
