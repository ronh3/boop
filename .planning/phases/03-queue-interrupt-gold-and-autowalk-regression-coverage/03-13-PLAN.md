---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "13"
type: execute
wave: 13
depends_on:
  - "03-12"
files_modified:
  - tests/boop_gold_spec.lua
  - tests/boop_event_transitions_spec.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-04
  - WALK-02
gap_closure: true
gap_ids: [G-03-2]
must_haves:
  truths:
    - "Per D-09, a DEFERRED_ROOM or PICKUP_PENDING operation survives same-room Room.Info with the same generation, room identity, item identity, timers, retries, and exact owner; only an actual room-ID change cancels it."
    - "List→one get→same-room Info→item removal/get success in either relative order produces exactly one inventory-owned put; removal alone is not pickup confirmation, and duplicate success/removal callbacks cannot produce a second put."
    - "Per D-10, confirmed pickup clears room identity before PACK_PENDING, which survives actual movement and completes/retries through inventory ownership."
    - "Per D-11 and D-12, pre-barrier or wrong-room stale gold Lists send nothing; after an actual Room.Info/fresh-start boundary, the first complete room response following the requested inventory barrier sends one get, while duplicate/delayed evidence sends no extra get, retry, or put."
    - "Gold authorization reads only the canonical copied room observation accepted through Plan 03-11's current Inv→Room response fence and the exact operation room/generation; it does not use persistent merged gmcp.Room.Info or gmcp.Char.Items.List as event identity."
    - "Gold remains separate from combat commands and blocks walker reservation until terminal; the existing guarded reevaluation releases at most one next action."
  artifacts:
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Canonical-observation gold authorization, same-room acquisition preservation, actual-change cancellation, and inventory-owned packing transfer"
    - path: "tests/boop_gold_spec.lua"
      provides: "Pre-barrier zero-send, current-fence one-get, and full get/same-room Info/removal/success/put contracts"
    - path: "tests/boop_event_transitions_spec.lua"
      provides: "Wrong-room stale evidence, actual-change one-shot cancellation, duplicates/delays, walker release, and attack separation integration"
  key_links:
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "goldDispatchAuthorized and startGoldOperation consume only response-fenced roomObservationSnapshot roomId/generation/copied accepted items"
      pattern: "(goldDispatchAuthorized|startGoldOperation|roomObservationSnapshot)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "state.gold.operation"
      via: "Actual movement cancels DEFERRED_ROOM/PICKUP_PENDING; onGoldGetSuccess transfers PICKUP_PENDING to roomless PACK_PENDING"
      pattern: "(room_changed|transferGoldToPacking|PACK_PENDING)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_walk.lua"
      via: "Gold terminal schedules the existing normal tick; goldPending blocks the evaluator until operation terminal"
      pattern: "(completeGoldOperation|goldPending|walk_advance)"
    - from: "src/scripts/boop/boop_util.lua"
      to: "Achaea combat queue"
      via: "executeAction continues dispatching only its supplied combat action; gold uses independent freestand get/put commands"
      pattern: "(executeAction|queue add freestand)"
---

<objective>
Close G-03-2 with the complete same-room get-confirm-put lifecycle, per D-09 through D-12 and aggregate D-01/D-02/D-13 safety.

Purpose: The response-fenced shared epoch from Plans 03-11/03-12 prevents false room transitions; gold must now consume only accepted canonical evidence and prove that same-room refresh, removal/success ordering, wrong-room data, movement, packing, and walker resumption remain exact.
Output: RED gold pipeline contracts, response-fenced copied-evidence authorization, same-room pickup preservation, actual-change-only cancellation, one inventory-owned put, and one guarded post-terminal reevaluation.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-room-evidence-ordering.md
@.planning/debug/phase-03-gold-same-room-cancellation.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-10-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-11-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-12-SUMMARY.md

<interfaces>
Canonical room contract consumed from Plans 03-11/03-12:
- `boop.runtime.roomObservationSnapshot()` returns the bound roomId, generation, Info/items completeness, fence state, and a deep-copied accepted `items` list.
- `boop.requestRoomItemsOnce(reason)` records and sends Char.Items.Inv then Char.Items.Room; pre-barrier room Lists and inventory responses cannot invoke gold.
- Only the first complete room response accepted from the current queue-head fence invokes gold detection/advancement.
- Same-room Info preserves the epoch; actual room change advances it and runs room-owned invalidation once.

Existing gold contract retained:
- `currentGoldOperation(generation, expectedPhase)` validates current nonterminal identity.
- `goldDispatchAuthorized(operation)` is the final initial/retry send gate.
- `startGoldOperation(source, observation, packTarget)` creates/coalesces DEFERRED_ROOM/PICKUP_PENDING.
- `transferGoldToPacking(generation)` is the only pickup→PACK_PENDING ownership transfer.
- `completeGoldOperation(generation, reason)` is the one terminal boundary.
- `boop.onGoldGetSuccess()` is pickup confirmation. `boop.onRoomItemsRemove()` may observe removal but does not transfer ownership.
- Gold sends remain `queue add freestand get sovereigns` and `queue add freestand put sovereigns in <pack>`, separate from `boop.executeAction`.
</interfaces>
</context>

<assumption_delta>
`changed`: Plan 03-05 intentionally canceled room-owned gold on every Room.Info. The UAT trace proves this destroys a valid queued get on moved=no. The corrected interpretation follows D-09 literally: cancellation belongs to an actual room-ID change. Item removal remains non-authoritative because the established product contract requires confirmed pickup before inventory ownership.
</assumption_delta>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED same-room get-confirm-put and wrong-room regressions</name>
  <files>tests/boop_gold_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_gold_spec.lua` — inspect evidence seeding, duplicate coalescing, get→pack transfer, movement cancellation, owner exclusion, and combat separation.
    - `tests/boop_event_transitions_spec.lua` — inspect Plan 03-11 same-room/actual-change tests, stale evidence, gold/walker ordering, and target-removal queue drift.
    - `tests/boop_gold_retry_spec.lua` — read-only wrong-room/stale timeout/retry and PACK_PENDING movement contracts that must continue passing.
    - `src/scripts/boop/boop_events.lua` — implementation under test: `goldDispatchAuthorized`, `startGoldOperation`, `transferGoldToPacking`, success/removal handlers, `onRoomItemsList`, and `onRoomInfo`.
    - `src/scripts/boop/boop_runtime.lua` — read-only canonical room snapshot and aggregate tick/effect path.
    - `src/scripts/boop/boop_walk.lua` — read-only goldPending evaluator and guarded reservation/emitter.
    - `src/scripts/boop/boop_util.lua` — read-only combat-only executeAction contract.
    - `mfile`, `src/scripts/boop/boop_init.lua`, `CODEX.md` — synchronized baseline `0.1.420`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, D-09, D-10, D-11, D-12, and D-13.
    - `.planning/debug/phase-03-gold-same-room-cancellation.md` — exact live sequence and explicit removal/success authority distinction.
  </read_first>
  <behavior>
    - "An actual Room.Info/fresh-start boundary records Inv then Room; pre-barrier stale room and inventory responses produce no pickup, while the first accepted complete room response produces one generation and one get."
    - "After get queues, same-room Info preserves the operation and timers; Remove→success and success→Remove each produce one roomless PACK_PENDING operation and one put."
    - "Removal without success produces no put; duplicate success/removal and delayed callbacks produce no second put."
    - "Actual movement before success cancels acquisition once and makes success/removal/retry callbacks no-ops; PACK_PENDING survives movement."
    - "Wrong-room stale gold cannot replace denizens, dispatch get/retry, release walk, or cause an attack/loot chain."
  </behavior>
  <action>
In `tests/boop_gold_spec.lua`, drive room evidence only through the public production adapters from Plans 03-11/03-12 rather than directly forcing operation or observation fields. Open an actual Room.Info or fresh-start boundary and assert the outbound GMCP commands are exactly `Char.Items.Inv` then `Char.Items.Room`. Deliver a stale gold-bearing room List before the inventory barrier and prove no generation/get; deliver the inventory response and prove inventory/wield-only effects; then deliver the first complete gold-bearing room response and require one pickup generation and exactly one get despite duplicate text/Add/List signals.

Add the full live regression sequence: accepted List binds gold → one get queues → same-room Room.Info arrives → operation generation/phase/room/item/timer/retry/owner snapshot remains identical → room Remove and `boop.onGoldGetSuccess()` occur in both relative orders → exactly one roomless PACK_PENDING operation and one put. Assert removal alone never transfers ownership, duplicate success/removal never queues a second put, actual movement after PACK_PENDING preserves it, and put success completes once.

In `tests/boop_event_transitions_spec.lua`, extend the Plan 03-11 actual-change/fence-drain case with wrong-room gold, captured retry/timeout callbacks, and exact terminal counts. A pre-barrier or invalidated-fence prior-room List containing gold must produce zero get/retry/put, zero denizen replacement, zero owner release, and zero move. A true room change before get success must cancel once; delayed old inv/room responses and late callbacks are no-ops. Retain the existing attack-separation assertion and prove no standard/rage/alias command contains either loot verb.

Create exact RED cases `boop staged gold handling same-room-gold-pipeline queues one inventory-owned put in either removal order` and `boop event-driven state transitions same-room-gold-pipeline rejects wrong-room gold and cancels actual movement once`. Put `GOLD_SAME_ROOM_PIPELINE_BROKEN` only on the assertions exposing pre-fix cancellation/persistent-GMCP authorization. The RED classifier must select two failures, no Busted errors, and both markers.

This RED commit is package-affecting. Synchronize all four version checkpoints from `0.1.420` to `0.1.421`, inspect staged paths, and run `python3 tools/check_release_gates.py` before committing `test(03-13): pin same-room gold pipeline regressions`. Do not modify production behavior, generated output, operator docs, or planning artifacts in this task.
  </action>
  <verify>
    <automated>luac -p tests/boop_gold_spec.lua tests/boop_event_transitions_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; bash -o pipefail -c 'set -eu; red_json=$(mktemp); red_err=$(mktemp); trap "rm -f \"$red_json\" \"$red_err\"" EXIT; set +e; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --output=json --filter="same%-room%-gold%-pipeline" tests/boop_gold_spec.lua tests/boop_event_transitions_spec.lua &gt;"$red_json" 2&gt;"$red_err"; red_status=$?; set -e; test "$red_status" -ne 0; test ! -s "$red_err"; python3 -c "import json,sys; d=json.load(open(sys.argv[1], encoding=\"utf-8\")); f=d.get(\"failures\",[]); assert not d.get(\"errors\",[]); assert len(f) == 2; assert all(\"GOLD_SAME_ROOM_PIPELINE_BROKEN\" in str(x.get(\"message\",\"\")) for x in f)" "$red_json"'</automated>
    <diagnostic_scope>Expected nonzero Busted status is intentional RED evidence; it is not a Mudlet runtime claim.</diagnostic_scope>
  </verify>
  <done>The live same-room cancellation, pre-barrier/invalidated-fence wrong-room authorization, removal/success ordering, and one-put expectations are represented by exactly two intentional RED failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authorize gold from canonical copied room evidence</name>
  <files>src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_events.lua` — modify gold evidence/authorization and actual-change checks only; preserve established phases, owners, timers, retry limits, commands, and terminal paths.
    - `src/scripts/boop/boop_runtime.lua` — read-only canonical copied observation and aggregate tick/effect contract.
    - `src/scripts/boop/boop_walk.lua` — read-only gold hold and one-shot evaluator/emitter.
    - `tests/boop_gold_spec.lua`, `tests/boop_event_transitions_spec.lua` — Task 1 RED contracts.
    - `tests/boop_gold_retry_spec.lua`, `tests/boop_tick_spec.lua`, `tests/boop_walk_spec.lua` — read-only regression suites for retries, aggregate owners, and eventual movement.
    - `mfile`, `src/scripts/boop/boop_init.lua`, `CODEX.md` — Task 1 synchronized value `0.1.421`.
  </read_first>
  <behavior>
    - "Acquisition authorization matches operation roomId/roomGeneration/item identity against the response-fenced canonical observation and copied accepted items."
    - "Same-room Info does not call the gold terminal path; actual changed room does so once."
    - "Get success alone transfers to inventory ownership; room removal is observational."
    - "Packing, retries, aggregate holds, attack separation, and guarded post-terminal movement retain existing ownership."
  </behavior>
  <action>
Refactor `goldDispatchAuthorized` and `startGoldOperation` to use `boop.runtime.roomObservationSnapshot()` plus canonical `state.targeting.room`. For room-owned phases, require exact operation roomId and roomGeneration, complete Info/items flags, no pending current fence, and the expected gold item in the snapshot's deep-copied accepted `items`. Remove authorization reads of persistent `gmcp.Room.Info` and `gmcp.Char.Items.List`; those tables may carry merged state but cannot identify the event or prove that the response fence completed.

Preserve duplicate coalescing by exact operation generation/room/item. DEFERRED_ROOM may advance to PICKUP_PENDING only from accepted current-fence evidence; retries repeat the same canonical room/generation/item checks. Pre-barrier, inventory-only, invalidated, wrong-room, rejected, duplicate, or stale evidence returns without sends or retry mutation.

Keep the Plan 03-11 movement-first `onRoomInfo` behavior: same-room Info leaves DEFERRED_ROOM/PICKUP_PENDING unchanged, while an actual changed normalized room ID invokes `completeGoldOperation(..., "room_changed")` once. Do not cancel PACK_PENDING. Keep `onRoomItemsRemove` observational and `onGoldGetSuccess` as the sole transfer into PACK_PENDING; the transfer clears room identity before queuing one put.

Do not alter command strings, retry limits, timeout-token semantics from Plan 03-10, aggregate exact-owner exclusion, combat executeAction, walker evaluator/emitter, or optional package behavior. After a winning terminal, retain the existing generation-guarded normal tick so all-clear—not a direct release hook—owns the one next action.

Run named GREEN cases, focused gold/event/retry/tick/walk specs, syntax, release gates, and Muddler. Synchronize all four version checkpoints from `0.1.421` to `0.1.422`; inspect staged paths and run the release gate before committing `fix(03-13): preserve same-room gold ownership`. Do not edit generated output, push, or claim terminal CI.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_events.lua src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_walk.lua tests/boop_gold_spec.lua tests/boop_event_transitions_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --filter="same%-room%-gold%-pipeline" tests/boop_gold_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_gold_spec.lua tests/boop_gold_retry_spec.lua tests/boop_event_transitions_spec.lua tests/boop_tick_spec.lua tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
    <diagnostic_scope>Focused host tests and Muddler do not replace real-Mudlet execution or exact-HEAD GitHub Actions.</diagnostic_scope>
  </verify>
  <done>Same-room pickup reaches exactly one inventory-owned put, actual movement alone cancels room ownership, wrong-room evidence sends nothing, and walker/combat release remains aggregate and one-shot.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Timing-sensitive paths cannot attack, loot, or walk under stale room/owner state | 03-11–03-13 | COVERED | This plan closes the end-to-end loot transfer and resumption path. |
| REQ | SAFE-02 | Interrupt/pull/manual holds prevent attacks | 03-01–03-10 | COVERED | Existing aggregate owner behavior remains unchanged and regression-run. |
| REQ | SAFE-04 | Pickup/pack/retry/warning/stale behavior cannot send in the wrong room | 03-11, 03-13 | COVERED | Response-fenced authorization and end-to-end gold behavior are direct task scope. |
| REQ | WALK-01 | Walker lifecycle and event coverage | 03-11, 03-12 | COVERED | Gold plan consumes, but does not change, walker contracts. |
| REQ | WALK-02 | Gold and room state block movement until safe | 03-11–03-13 | COVERED | Active operation blocks; terminal reaches one existing guarded path. |
| REQ | WALK-03 | Walker remains optional and never silently updates | 03-06–03-12 | COVERED | Unchanged by gold repair. |
| RESEARCH | — | Owner-keyed all-clear and generation-owned callbacks | 03-01–03-10, 03-13 | COVERED | Exact gold owner and terminal/timer semantics remain. |
| RESEARCH | — | Shared canonical room observation | 03-11, 03-13 | COVERED | Gold consumes only copied evidence accepted through the current response fence. |
| RESEARCH | — | Two-stage room-owned get and inventory-owned put | 03-13 | COVERED | Direct task scope. |
| RESEARCH | — | One guarded walker evaluator/emitter and capped refresh | 03-11, 03-12, 03-13 | COVERED | Post-terminal path is existing aggregate reevaluation. |
| RESEARCH | — | Existing stack/no package installs and final CI authority | 03-11–03-13 | COVERED | No dependency/install task; parent retains exact-HEAD authority. |
| CONTEXT | D-01, D-02 | Aggregate per-system holds and subsystem release | 03-01–03-10, 03-13 | COVERED | Gold terminal clears only its owner and normal tick rechecks all others. |
| CONTEXT | D-03, D-04 | Primary/count rendering and priority | 03-01, 03-08, 03-09 | COVERED | Unchanged. |
| CONTEXT | D-05, D-06, D-07, D-08 | Interrupt lifecycle | 03-02, 03-03, 03-09 | COVERED | Unchanged. |
| CONTEXT | D-09, D-10, D-11, D-12 | Exact-room acquisition, inventory packing, duplicate coalescing, settled evidence | 03-11, 03-13 | COVERED | The response fence supplies current-room evidence; staged ownership remains direct task scope. |
| CONTEXT | D-13, D-14, D-15, D-16 | All-clear move, complete evidence, capped refresh, one move | 03-11–03-13 | COVERED | Gold cannot bypass or double-release walker. |
| CONTEXT | D-17, D-18, D-19, D-20 | Safe/idempotent manual move | 03-06–03-09, 03-12 | COVERED | Unchanged. |
| CONTEXT | D-21, D-22, D-23, D-24 | Stop/detach, stale invalidation, fresh restart, output | 03-07–03-12 | COVERED | Unchanged. |
| CONTEXT | Deferred | Hyena-maul summary and temporary/custom attacks | — | EXCLUDED | Outside Phase 03. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Response-fenced canonical room evidence → Achaea gold queue | Pre-barrier, invalidated-pair, wrong-room, stale, or mutable evidence must not authorize get/retry. |
| Room.Info → room-owned gold terminal | Same-room refresh must preserve acquisition; actual movement must cancel once. |
| Pickup success/removal → inventory ownership | Only confirmed success may authorize one put; removal/duplicates must not spoof ownership. |
| Gold terminal → combat/walker reevaluation | Clearing gold must not bypass another owner or emit more than one next action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13-WRONG | Spoofing / Tampering | `goldDispatchAuthorized` | high | mitigate | Match response-fenced copied items plus exact room/generation/item; test pre-barrier and invalidated-fence wrong-room zero sends. |
| T-03-13-SAME | Tampering / Denial of Service | `onRoomInfo` gold branch | high | mitigate | Same-room snapshot preservation and actual-change one-shot terminal tests. |
| T-03-13-TRANSFER | Elevation of Privilege | success/removal handlers | high | mitigate | Success is sole transfer; both callback orders and duplicates assert one put. |
| T-03-13-RELEASE | Tampering | gold terminal → tick/walk | medium | mitigate | Clear exact owner, retain aggregate reevaluation, and assert one reservation/event only after terminal. |
| T-03-13-COMMAND | Tampering | combat/gold command separation | medium | mitigate | Preserve independent freestand loot commands and assert combat aliases contain no loot fragments. |
</threat_model>

<verification>
- Task 1 produces exactly two intentional `GOLD_SAME_ROOM_PIPELINE_BROKEN` RED failures and no Busted errors.
- Task 2 passes named and complete focused gold, retry, event-transition, tick, and walk specs.
- Lua syntax, release gates, and Muddler pass with all four checkpoints synchronized at `0.1.422`.
- If `/tmp/Mudlet.AppImage` exists, run the canonical full suite. If absent, record real-Mudlet as unavailable and retain host Busted as diagnostic only.
- Each package-affecting task is committed only after staged-path classification, version synchronization, and `python3 tools/check_release_gates.py`. The executor does not push.
- After all gap PLAN/SUMMARY/STATE/ROADMAP/verification mutations finish, the parent captures immutable final HEAD, pushes it, and runs `tools/wait_for_exact_ci.sh "$FINAL_SHA"`; any later mutation invalidates that evidence.
</verification>

<success_criteria>
- G-03-2's live accepted fenced List→get→same-room Info→removal/success sequence queues one inventory-owned put.
- A pre-barrier room List and inventory barrier produce no get; the first accepted current-fence room response produces one get, while duplicate/delayed/wrong-room evidence produces no extra command.
- Same-room Info preserves room ownership; actual movement cancels acquisition once; packing survives movement.
- Gold remains separate from attacks and releases walker/combat only through aggregate one-shot reevaluation.
- No source outside the focused event seam, no command/help surface, no dependency, no generated artifact, and no deferred feature changes.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-13-SUMMARY.md` during execution closeout. Record RED/GREEN, focused host, syntax, release-gate, Muddler, and available real-Mudlet evidence. Do not push; terminal exact-HEAD CI belongs to the parent after all repository mutations.
</output>
