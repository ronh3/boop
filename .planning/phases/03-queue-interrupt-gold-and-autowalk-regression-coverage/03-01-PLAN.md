---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/support/boop_test_helper.lua
  - tests/boop_runtime_spec.lua
  - tests/boop_trace_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - tests/boop_event_transitions_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-01
  - WALK-02
must_haves:
  truths:
    - "Per D-01 and D-02, clearing one owner never authorizes an attack, gold command, or movement event while another owner affecting that subsystem remains."
    - "Per D-01 and D-02, exact-owner exclusion permits a lifecycle's own command only when every unrelated owner affecting that subsystem is clear."
    - "Per D-04, normal observation of the same active owners selects the same primary blocker regardless of insertion or clear order."
    - "Per D-12 and D-14, room settlement cannot authorize loot or movement until current Room.Info and a complete current-cycle room item list are both present."
    - "Per D-15, incomplete room evidence can request one refresh, but prompt/timer callbacks never authorize loot or movement."
  artifacts:
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Owner-keyed blockers, exact-owner aggregate holds, deterministic snapshots, and canonical room-observation state/APIs"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Stable owners for existing GMCP/room/target blockers plus current-cycle room evidence and one capped refresh boundary"
    - path: "tests/support/boop_test_helper.lua"
      provides: "Owner-aware blocker seeding and deterministic room-observation fixtures"
    - path: "tests/boop_runtime_spec.lua"
      provides: "Both-clear-order, exact-owner exclusion, immutable snapshot, and observable side-effect hold contracts"
    - path: "tests/boop_event_transitions_spec.lua"
      provides: "Current-generation Room.Info/List and stale-callback contracts"
  key_links:
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "Room.Info starts a generation; a later complete room List stamps it; consumers read an immutable snapshot"
      pattern: "(startRoomObservation|stampRoomItemsObservation|roomObservationSnapshot)"
    - from: "src/scripts/boop/boop_runtime.lua"
      to: "Phase 03 interrupt, pull, gold, walk, UI, attack, and prequeue plans"
      via: "setBlocker/clearBlocker exact ownership and shouldHold optional exact-owner exclusion"
      pattern: "(blockersByOwner|shouldHold)"
---

<objective>
Establish the exact-owner and current-room evidence foundation required by D-01, D-02, D-04, D-12, D-14, and D-15.

Purpose: Later lifecycle commands may release or self-authorize only against immutable aggregate state, never a singular overwritten blocker or timer-assumed room.
Output: Canonical owner registry, deterministic primary/all snapshots, shared room observation, capped GMCP refresh, and RED/GREEN regression coverage.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-07-SUMMARY.md

<interfaces>
Created symbols and values — exhaustive:
- Local constant `BLOCKER_PRIORITY` with exact code priorities: `gmcp_ire_missing=10`, `missing_room=20`, `room_partial=21`, `target_lost=22`, `flee_active=30`, `pull_timeout_away=31`, `pull_away=32`, `pull_active=40`, `interrupt_pending=50`, `gold_deferred_room=60`, `gold_pickup_pending=61`, `gold_pack_pending=62`, `walk_room_unsettled=70`, `walk_move_pending=71`, `walker_unavailable=72`; unmapped codes sort at `100`.
- Local functions `blockerPriority(blocker)` and `sortedBlockerRecords()`.
- State field `state.combat.blockersByOwner`; map keys are exact owners and records carry `owner`, `code`, `label`, `systems`, `waitsFor`, `observed`, `source`, `since`, `promptSeen`, `gmcpSeen`, `lastWarningAt`, `lastWarningCode`, `lastRetryAt`, and `warningThrottleSeconds`.
- Public API `boop.runtime.blockersSnapshot()` returns immutable records in priority/code/owner order.
- Modified primary API `boop.runtime.blockerSnapshot()` adds exact fields `owner` and `additionalCount` to the existing snapshot fields.
- Modified signatures `boop.runtime.setBlocker(owner, code, label, systems, waitsFor, opts)`, `boop.runtime.clearBlocker(owner, observed)`, and `boop.runtime.shouldHold(system, exceptOwner)`.
- Stable migrated owner values `gmcp:ire`, `room:observation`, and `target:loss`; lifecycle owners `interrupt:<generation>`, `pull:<generation>`, `gold:<generation>`, and `walk:<generation>` are reserved for later plans.
- State field `state.targeting.roomObservation` with exact fields `generation`, `roomId`, `infoSeen`, `itemsSeen`, `refreshAttempted`, `refreshReason`, and `warned`.
- Public APIs `boop.runtime.startRoomObservation(roomId)`, `boop.runtime.stampRoomItemsObservation()`, `boop.runtime.roomObservationSnapshot()`, and `boop.requestRoomItemsOnce(reason)`.
- New test helper `M.seedRoomObservation(roomId, opts)`.

Existing symbols modified, not created:
- `DOMAIN_DEFAULTS`, `currentBlocker`, `blockerChanged`, `setBlockerFields`, `boop.runtime.blockerSnapshot`, `boop.runtime.setBlocker`, `boop.runtime.clearBlocker`, `boop.runtime.shouldHold`, `boop.runtime.notePromptObserved`, `boop.runtime.noteGmcpObserved`, `boop.onRoomInfo`, `boop.onRoomItemsList`, and `M.setRuntimeBlocker`.
- Event-adapter symbols `setBlocker`, `enterGmcpIreBlocker`, `reconcileIreSupport`, `enterRoomBlocker`, `noteRoomGmcpObserved`, `noteTargetGmcpObserved`, `boop.onConnectionEvent`, `boop.onRoomItemsAdd`, `boop.onRoomItemsRemove`, `boop.onTargetSet`, and `boop.onTargetInfo`; each singleton GMCP/room/target path passes its stable owner to the owner-aware runtime API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin exact-owner and observable all-clear contracts</name>
  <files>tests/support/boop_test_helper.lua, tests/boop_runtime_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/support/boop_test_helper.lua` — modified helper; inspect `M.reset`, `M.setRuntimeBlocker`, and owned-domain fixture setup.
    - `tests/boop_runtime_spec.lua` — modified spec; inspect existing `blockerSnapshot` and `shouldHold` assertions.
    - `src/scripts/boop/boop_runtime.lua` — implementation under test, especially `DOMAIN_DEFAULTS`, blocker normalization, snapshots, and coordinator hold checks.
    - `src/scripts/boop/boop_events.lua` — room-observation consumer and GMCP side-effect boundary used by the new helper fixtures.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint and release-gate guidance.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, D-04, D-12, D-14, and D-15.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — rows 03-W0-01 and the walk-settlement ordering matrix.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `src/scripts/boop/boop_runtime.lua`, `tests/support/boop_test_helper.lua`, `tests/boop_runtime_spec.lua`, `Shared Test Patterns`, and `Shared room observation`.
  </read_first>
  <behavior>
    - "Clearing either of two combat owners leaves attack/prequeue held until both clear."
    - "Clearing either of two gold/walk owners leaves get/put/move effects at zero until every affecting owner clears."
    - "Excluding owner A authorizes A only when no unrelated owner affects the requested system."
    - "Primary selection and all-owner ordering are insertion-order independent."
    - "Returned snapshots are immutable copies."
  </behavior>
  <acceptance_criteria>
    - `M.setRuntimeBlocker` seeds blockers through `boop.runtime.setBlocker(owner, ...)`, and `M.seedRoomObservation(roomId, opts)` is the only new helper added by this task.
    - Attack, gold, and movement cases each exercise both owner-clear orders and assert zero corresponding sends/events while one affecting owner remains, followed by exactly one normal reevaluation only after the final owner clears.
    - Exact-owner exclusion succeeds only for the matching owner key; excluding a blocker code, code family, or subsystem leaves the unrelated hold active.
    - `blockersSnapshot()` is asserted in priority/code/owner order and mutation of a returned record does not change runtime state.
    - One patch increment leaves `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint identical, and the version release gate `python3 tools/check_release_gates.py --check versions` succeeds.
  </acceptance_criteria>
  <action>
Modify existing `M.setRuntimeBlocker` to seed the exact public owner-aware API and add only the new helper `M.seedRoomObservation(roomId, opts)`. Add both-clear-order tests for attack, gold, and movement authorization, including exact send/event/timer counts and retained owner records after each clear, per D-01 and D-02. Add exact-owner exclusion rows proving a lifecycle can bypass only its own map key, never a code, code family, or subsystem name. Add D-04 deterministic-primary and immutable-copy assertions using the exact priority inventory in `<interfaces>`. Before this task's commit, increment one patch version and synchronize `mfile.version`, `mfile.title` as `boop Hunter <version>`, `boop.version`, and the CODEX checkpoint. Do not edit generated package artifacts.
  </action>
  <verify>
    <automated>luac -p tests/support/boop_test_helper.lua tests/boop_runtime_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run the focused runtime spec inside Mudlet when `/tmp/Mudlet.AppImage` exists; otherwise require the matching GitHub Actions Mudlet/Busted result. Host Busted is diagnostic only.</authoritative>
  </verify>
  <done>RED contracts prove observable attack/loot/movement holds, exact-owner self-authorization, deterministic primary selection, and immutable all-owner snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical blocker ownership and snapshots</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, tests/boop_trace_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_runtime.lua` — modified owner registry, normalization, snapshot, hold, observation, and trace implementation.
    - `src/scripts/boop/boop_events.lua` — modified local blocker adapter plus every existing GMCP IRE, room-observation, and target-loss blocker set/evidence/clear caller.
    - `tests/boop_trace_spec.lua` — modified owner-specific enter/exit trace coverage.
    - `tests/boop_runtime_spec.lua` — Task 1 RED contracts that this task must make green.
    - `tests/boop_event_transitions_spec.lua` — modified focused assertions for exact stable owner arguments at the real GMCP, room, and target event entry points.
    - `tests/support/boop_test_helper.lua` — owner-aware fixture API consumed by the focused specs.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01 through D-04.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — row 03-W0-01 and owner-state assertions.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `src/scripts/boop/boop_runtime.lua`, `Deterministic normalization pattern`, `Current replacement seam`, `tests/boop_trace_spec.lua`, and `Ownership identity`.
  </read_first>
  <behavior>
    - "Setting one owner preserves every other owner."
    - "Clearing requires the exact owner key."
    - "Primary and all-owner snapshots follow priority/code/owner ordering."
    - "Owner-specific enter/exit trace retains unrelated owners."
    - "GMCP IRE, room observation, and target-loss events set and satisfy only gmcp:ire, room:observation, and target:loss respectively."
  </behavior>
  <acceptance_criteria>
    - `state.combat.blockersByOwner` is authoritative, while `state.combat.blocker` is a derived primary snapshot containing exact `owner` and `additionalCount` fields.
    - `setBlocker(owner, ...)` preserves other owners, `clearBlocker(owner, observed)` removes only that exact key, and `shouldHold(system, exceptOwner)` excludes only the exact supplied owner.
    - `blockersSnapshot()` sorts mapped priorities before unmapped priority `100`, then code, then owner, and returns deep copies.
    - Owner enter/exit trace assertions observe exactly one transition for the changed owner and retain every unrelated owner in the canonical snapshot.
    - The local `boop_events.lua` blocker adapter accepts `owner` before `code` and forwards that owner unchanged; missing/ready IRE paths use only `gmcp:ire`, missing/partial/current room paths use only `room:observation`, and target-loss plus Target.Set/Target.Info evidence paths use only `target:loss`.
    - Focused `tests/boop_event_transitions_spec.lua` assertions observe the exact owner on event-originated set and evidence/clear calls, prove one singleton event path cannot replace or clear either of the other two owners, and retain the existing target-removal queue-drift regression.
    - The Task 1 runtime contracts, `luac -p`, and the release gate `python3 tools/check_release_gates.py` succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Implement the exact created symbol inventory from `<interfaces>`. Make `blockersByOwner` the only authoritative active collection; keep `state.combat.blocker` only as the derived primary compatibility snapshot. Normalize and deep-copy every record, sort through `blockerPriority(blocker)` then code then owner, and set `additionalCount` to total active owners minus one. Auto-clear only owners whose declared evidence is complete.

In `boop_events.lua`, change the local blocker adapter to `setBlocker(owner, code, label, systems, waitsFor, opts)` and forward that owner unchanged to `boop.runtime.setBlocker`. Migrate every existing singleton event caller: `enterGmcpIreBlocker`, `reconcileIreSupport`, and `boop.onConnectionEvent` set or satisfy only `gmcp:ire`; `enterRoomBlocker` and `noteRoomGmcpObserved`, including their Room.Info and room List/Add/Remove entry points, set or satisfy only `room:observation`; the target-loss branch of `boop.onRoomItemsRemove` sets only `target:loss`, while `noteTargetGmcpObserved` from `boop.onTargetSet` and `boop.onTargetInfo` satisfies only `target:loss`. Preserve each existing blocker code and evidence requirement. Do not choose a record to mutate or clear by comparing the derived primary snapshot's code: owner identity selects the record, so unrelated owners survive every event.

Add focused event-boundary assertions in `tests/boop_event_transitions_spec.lua` that spy on the owner-aware runtime calls for missing/ready IRE, missing/partial/complete room evidence, and target loss followed by Target.Set/Target.Info evidence. Assert the exact stable owner argument, retained unrelated owner records, and unchanged target-removal queue behavior. Update trace coverage for exact owner enter/exit and deterministic all-owner order. Synchronize the next patch version before commit and never edit built output.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua tests/boop_runtime_spec.lua tests/boop_trace_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run focused Mudlet/Busted behavior for `tests/boop_runtime_spec.lua`, `tests/boop_trace_spec.lua`, and `tests/boop_event_transitions_spec.lua`; require exact stable owner arguments and unrelated-owner retention at GMCP, room, and target event boundaries.</authoritative>
  </verify>
  <done>The runtime passes the Task 1 contracts with one canonical owner map and deterministic primary/all snapshots, and every existing GMCP IRE, room-observation, and target-loss event caller uses its exact stable owner.</done>
</task>

<task type="auto">
  <name>Task 3: Establish current-generation room evidence and capped refresh</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_runtime.lua` — modified `roomObservation` defaults and snapshot APIs.
    - `src/scripts/boop/boop_events.lua` — modified Room.Info/List sequencing and GMCP refresh boundary.
    - `tests/boop_event_transitions_spec.lua` — modified room-cycle, stale callback, and target-removal queue-drift coverage.
    - `tests/support/boop_test_helper.lua` — `M.seedRoomObservation` and GMCP fixtures created in Task 1.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-12, D-14, and D-15.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — walk-settlement, gold-evidence, and target-removal ordering rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Shared room-list observation pattern`, `Room transition ordering pattern`, `tests/boop_event_transitions_spec.lua`, `Shared room observation`, and `Preserve target-removal queue-drift regression`.
  </read_first>
  <behavior>
    - "Room.Info starts a new generation and invalidates prior item evidence."
    - "Only a complete room List observed after current Room.Info stamps itemsSeen."
    - "Missing evidence requests Char.Items.Room once per generation and remains held."
    - "Old-generation, prompt-only, and timer-only callbacks cannot settle or authorize gold/movement."
  </behavior>
  <acceptance_criteria>
    - Every current Room.Info starts or invalidates one `state.targeting.roomObservation.generation` and resets `itemsSeen`, `refreshAttempted`, `refreshReason`, and `warned`.
    - Only a later `gmcp.Char.Items.List` with `location == "room"` and table-valued `items` stamps the current observation; prompt-only, timer-only, and prior-generation lists leave settlement false.
    - Incomplete evidence sends exactly one `Char.Items.Room` GMCP request per generation, a duplicate request sends zero, and neither path sends loot or raises movement.
    - The existing target-removal test still asserts zero queue-clear commands and preserves its deferred retarget callback behavior.
    - `luac -p` and the full release gate succeed after one patch increment with all four version checkpoints synchronized.
  </acceptance_criteria>
  <action>
Add the exact `roomObservation` shape and four APIs listed in `<interfaces>`. Call `startRoomObservation(roomId)` before Room.Info consumers evaluate a transition. Call `stampRoomItemsObservation()` only for `location == "room"` with table-valued complete items after the current Room.Info cycle began. `requestRoomItemsOnce(reason)` records `refreshAttempted` and `refreshReason`, sends exactly one `Char.Items.Room` request, and never stamps settlement. Add old-list/new-room, duplicate-list, prompt-only, timer-only, one-refresh, and no-second-refresh tests with exact `sendGMCP`, loot-send, and movement-event counts. Preserve the target-removal queue-drift case. Synchronize the next patch version before commit; do not edit generated artifacts.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>Current Room.Info plus a later complete room List is the only settlement path, and incomplete evidence stays fail closed after one refresh.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol list; no additional constant, local function, public API, owner form, state field, snapshot field, table field, or reason code may be introduced without adding it there first.
- Existing files only; no new package module, manifest entry, dependency, persistence schema, command, alias, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mudlet GMCP/prompt/timer events → runtime state | Evidence can be partial, duplicated, or stale. |
| Runtime registry → attack/loot/movement effects | A wrong clear or broad exclusion can release unsafe work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-OWNER | Tampering | `blockersByOwner` / exact clear | high | mitigate | Exact owner identity, immutable snapshots, and both-clear-order side-effect tests. |
| T-03-ROOM | Spoofing / Tampering | room observation | high | mitigate | Generation reset, ordered complete-list stamp, one refresh, and stale-callback zero-effect tests. |
| T-03-TRACE | Repudiation | owner transitions | medium | mitigate | Deterministic owner-specific enter/exit trace assertions. |
</threat_model>

<verification>
- Every task runs Lua syntax and the release gate; focused behavior runs in real Mudlet or exact corresponding CI.
- D-01/D-02 are proven at attack, gold, and movement effect boundaries, not only internal storage.
- D-12/D-14/D-15 are proven with exact refresh/send/event counts.
- Event-boundary tests prove `gmcp:ire`, `room:observation`, and `target:loss` are passed explicitly and cannot replace or clear one another.
</verification>

<success_criteria>
- SAFE-02, SAFE-04, WALK-01, and WALK-02 have a reachable exact-owner/current-room foundation.
- Every created symbol/value is enumerated separately from existing modified symbols.
- Version fields are synchronized in every commit-bearing task and built artifacts remain untouched.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md` when done.
</output>
