---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 28
type: execute
wave: 28
depends_on:
  - 03-27
files_modified:
  - src/scripts/boop/boop_events.lua
  - src/scripts/boop/boop_rage.lua
  - src/scripts/boop/boop_attacks.lua
  - src/triggers/boop/Rage/triggers.json
  - src/triggers/boop/Rage/Rage_Command_Outcome.lua
  - tests/boop_rage_ingestion_spec.lua
  - tests/boop_rage_contract_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
gap_closure: true
gap_ids:
  - G-03-25
requirements:
  - SAFE-02
  - SAFE-04
must_haves:
  truths:
    - "Achaea's global battlerage cooldown is tracked independently from per-ability readiness and blocks every boop rage send until exact recovery."
    - "boop_attacks begins exact rage owner/generation/ability/target/room identity, then passes outcome metadata into Plan 03-25's executeAction API; it never pre-registers the unsplit logical rage string."
    - "executeAction registers the final assist/prefix/slash-expanded rage wire commands in exact expected/observed order immediately before send, and the final observed owned wire sequence anchors rage causality."
    - "Rage-kind dispatch emits only rage direct traces and cannot call noteStandardIntent or markUnnamableMaulUsed; standard-kind behavior remains the Plan 03-25 compatibility baseline."
    - "Cooldown and insufficient-rage outcomes mutate boop only inside the exact pending rage dispatch window with no later outbound from outside that exact dispatch."
    - "The same manual denial while boop rage is pending is ambiguous and cannot close the global gate, consume Triumph, or terminalize boop work."
    - "The exact Available abilities recovery line opens the global gate and marks only listed abilities ready while preserving unlisted state."
    - "Triumph free rage is one generation-owned credit, first-terminal on use, exact expiry, causally matching insufficient-rage denial, reconnect, or bounded timeout."
    - "Stale callbacks and prior connection generations cannot mutate current rage/free-rage state, and ordinary rage remains usable after Triumph expires."
    - "Focused behavior uses the current ingestion and contract rage suites exclusively."
    - "The one green package commit synchronizes all four version checkpoints at exactly 0.1.472."
  artifacts:
    - path: src/scripts/boop/boop_rage.lua
      provides: "Exact pending rage dispatch, global cooldown, Available-list recovery, and bounded Triumph lifecycle."
    - path: src/scripts/boop/boop_events.lua
      provides: "Shared outbound-ledger integration, global rage gate, result windows, and reconnect reset."
    - path: src/scripts/boop/boop_attacks.lua
      provides: "Atomic rage identity begin plus outcome metadata handoff into executeAction before canonical wire expansion/send."
    - path: src/triggers/boop/Rage/Rage_Command_Outcome.lua
      provides: "Thin exact cooldown/recovery/insufficient/expiry adapter."
    - path: tests/boop_rage_ingestion_spec.lua
      provides: "Exact trigger text, listed recovery, manual causality, and Triumph ingestion regressions."
    - path: tests/boop_rage_contract_spec.lua
      provides: "Assist/Psion ordered wire registration, causal denial, global gate, bounded credit, reconnect, and stale-generation regressions."
  key_links:
    - from: src/scripts/boop/boop_attacks.lua
      to: "Plan 03-25 executeAction outcome API"
      via: "begin rage identity, then pass metadata with the logical action so executeAction registers only final transformed wire parts immediately before send"
    - from: "ordered observed rage wires"
      to: "generic rage outcome"
      via: "the last owned wire sequence opens the causal window; any later command outside that exact dispatch contaminates it"
    - from: src/triggers/boop/Rage/Rage_Command_Outcome.lua
      to: src/scripts/boop/boop_rage.lua
      via: "forward exact line/captures for runtime causal classification"
    - from: "Available abilities recovery"
      to: "global gate and per-ability readiness"
      via: "open global gate and set exactly parsed listed keys true"
    - from: "Triumph grant"
      to: "bounded free-rage generation"
      via: "one owned timer and first-terminal use/expiry/denial/reconnect transitions"
---

<objective>
Close G-03-25 with one narrow global battlerage cooldown and bounded Triumph lifecycle reusing Plan 03-25's outbound ledger.

Purpose: Stop repeated rage sends during the server's shared cooldown without allowing identical manual output to mutate boop, and prevent stale free-rage authority from leaking across rooms or connections.

Output: Canonical ordered rage wire registration through Plan 03-25's executeAction API, global gate/recovery, causally guarded outcomes, bounded Triumph terminals, Rage manifest/behavior regressions, one green package commit, and version 0.1.472. README, DESIGN, and installed-help alignment is deferred to Plan 03-29.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-27-SUMMARY.md
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_rage.lua
@src/scripts/boop/boop_attacks.lua
@src/triggers/boop/Rage/triggers.json
@src/triggers/boop/Rage/Triumph_Free_Rage.lua
@src/triggers/boop/Rage/Rage_Ready_List.lua
@tests/boop_rage_ingestion_spec.lua
@tests/boop_rage_contract_spec.lua

The existing short local rage limiter is pacing only and cannot represent Achaea's shared cooldown. Reuse the Plan 03-25 outbound sequence and direct-send result window rather than adding another send observer.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Artifact | Result |
|---|---|
| Global rage gate | Shared cooldown independent of per-ability ready flags and local pacing. |
| Canonical wire ownership | One logical rage dispatch owns the exact ordered transformed wires and final observed sequence. |
| Causal outcome adapter | Matching pending final-wire window required; manual identical output ignored. |
| Exact recovery | Available list opens the gate and updates only listed abilities. |
| Triumph lifecycle | One bounded generation with use/expiry/denial/reconnect/timeout terminals. |
| Rage package | Exact dispatch wiring and trigger manifest plus ingestion and contract regressions. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire, implement, verify, and package rage cooldown and Triumph</name>
  <files>src/scripts/boop/boop_events.lua, src/scripts/boop/boop_rage.lua, src/scripts/boop/boop_attacks.lua, src/triggers/boop/Rage/triggers.json, src/triggers/boop/Rage/Rage_Command_Outcome.lua, tests/boop_rage_ingestion_spec.lua, tests/boop_rage_contract_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-27-SUMMARY.md
    - src/scripts/boop/boop_events.lua
    - src/scripts/boop/boop_rage.lua
    - src/scripts/boop/boop_attacks.lua
    - src/scripts/boop/boop_util.lua
    - src/triggers/boop/Rage/triggers.json
    - src/triggers/boop/Rage/Triumph_Free_Rage.lua
    - src/triggers/boop/Rage/Rage_Ready_List.lua
    - src/triggers/boop/Rage/Rage_Ready_Single.lua
    - tests/boop_rage_ingestion_spec.lua
    - tests/boop_rage_contract_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "After eligibility and standard-pending checks, boop_attacks begins one rage owner/generation with ability, target, room/connection authority, logical action, and first-terminal state."
    - "boop_attacks passes that identity as outcomeRegistration plus direct dispatchMode into executeAction; it does not register or compare the unsplit logical rage string."
    - "Plan 03-25's unchanged boop_util implementation performs assist prefixing and slash splitting, registers each final trimmed wire part immediately before send, and stores exact ordered expected/observed wires."
    - "Plan 03-25's kind-aware direct branch labels every rage wire as rage direct and skips standard intent and standard maul bookkeeping for the complete logical dispatch."
    - "Assist-enabled rage owns assist then rage wire commands in order; Psion assist plus transcend-prefix plus rage expansion owns every resulting wire in exact send order."
    - "The last exact observed owned wire is the rage causal baseline; any later command outside that dispatch contaminates a generic denial."
    - "Plan 03-25's standard-pending barrier refuses rage emission while a queued standard generation remains nonterminal."
    - "Exact global cooldown denial closes the shared gate only when causally attributable to that pending direct send."
    - "Any later outbound outside the exact rage owner, including unowned/manual or a different boop owner, makes identical cooldown or insufficient-rage text ambiguous and leaves boop unchanged."
    - "While global cooldown is closed, canUseRage returns false before arming the short pacing limiter; repeated ticks emit no rage command."
    - "Exact Available abilities recovery opens the gate and marks exactly parsed listed abilities ready; unlisted values remain unchanged."
    - "Triumph clears once on matching use, exact rage-fades expiry, matching insufficient-rage denial, reconnect, or bounded generation timer."
    - "Movement neither renews nor immediately clears Triumph; timeout prevents leakage and ordinary rage works after expiry when otherwise eligible."
  </behavior>
  <action>
Write focused ingestion, contract, canonical-wire, and Rage manifest regressions first in the two listed rage specs. Run the exact focused Busted command once and require named missing-behavior failures only; setup, helper, syntax, or command failure is not RED. Do not negate the command or commit RED. Continue until every focused assertion is green.

Normalize one pending rage dispatch with monotonic owner/generation, ability, logical action, target, room/connection authority, ordered expected/observed wire commands, final owned wire sequence, direct-send execution window, terminal status/reason, and response timer. In boop_attacks.lua, after every eligibility and standard-pending check, begin the rage owner/generation/ability/target/room record. Pass `{ owner, generation, dispatchId, kind = "rage" }` as `outcomeRegistration` and `dispatchMode = "direct"` into Plan 03-25's `executeAction`; do not pre-register the logical action and do not compare output against it.

Let executeAction perform its existing assist prefixing and slash splitting, including any Psion `psi transcend shatter/` prefix already built into the logical rage action, then register each final trimmed wire part immediately before actual send. Require the rage record to receive the exact ordered expected and observed wire lists and to set `finalOwnedWireSequence` only from the last successfully observed owned wire. The supplied `kind = "rage"` must produce `rage direct` traces for every emitted part and must never invoke `boop.gag.noteStandardIntent` or `markUnnamableMaulUsed`; do not recreate those hooks in boop_attacks or boop_rage. If identity begin, authority validation, registration, any part send, or observation fails, cancel only that just-created rage generation and emit no orphan pending owner; a partial ordered list remains diagnostic and cannot authorize a generic outcome. Do not modify boop_util.lua or add another sysDataSendRequest observer in this plan. Have canUseRage consult the global gate before mutating the existing short pacing limiter.

Track globalCooldown independently from rage.ready. Accept the exact cooldown denial only from the matching pending owner/generation after its complete ordered wire list and final observed owned sequence, before its result prompt, with no later outbound sequence from outside that exact dispatch. The same line after a manual or differently owned boop send is ambiguous: trace it and leave pending/global/Triumph state unchanged. A matching denial first-terminalizes pending work and closes the gate. No local pacing timer can reopen it.

Treat the exact line beginning `You can use another Battlerage ability again. Available abilities:` as authoritative global recovery even if no pending dispatch exists. Open the global gate, parse the nonempty comma-separated list, normalize exact ability keys, and set only listed keys true. Preserve unlisted readiness and legacy Now Available ingestion.

Replace freeNext's unbounded authority with one Triumph generation and owned timer while retaining compatibility reads. Grant increments generation and replaces only its old timer. Matching boop use consumes once. Exact `Your rage fades away.` clears current credit. Exact insufficient-rage output clears it only when causally attributable to matching pending rage; manual ambiguity is a no-op. Reconnect clears prior-connection pending/global/free state and timers. Use the existing named fallback duration, defaulting to 26 seconds only if no narrower source contract exists. Every stale timer/outcome/reconnect callback is generation-guarded.

Keep Rage_Command_Outcome.lua thin and package exact anchored trigger alternatives for cooldown, nonempty Available list, expiry, and insufficient rage. Preserve Triumph grant and legacy readiness triggers. Add exact ordered-wire regressions for: ordinary single-wire rage; assist-enabled `assist &lt;leader&gt;/&lt;rage&gt;` expansion; Psion assist plus `psi transcend shatter/&lt;rage&gt;` multi-command expansion; no unsplit logical-string registration; only `rage direct` traces for all three forms; zero calls to `noteStandardIntent` and `markUnnamableMaulUsed`; matching generic denial after each uncontaminated final wire; and the same denial after a later manual or differently owned boop wire, which must remain ambiguous. Also regress part-send failure cleanup, standard-pending refusal, repeated ticks, listed/unlisted recovery, direct matching use, expiry, timeout, movement, reconnect, re-grant, stale callbacks, and ordinary post-expiry rage. Use only tests/boop_rage_ingestion_spec.lua and tests/boop_rage_contract_spec.lua for this behavior.

Defer README, DESIGN, and installed-help alignment to Plan 03-29. Add no command and do not modify boop_util.lua, UI/help files, or unrelated tests here.

After focused verification is green, bump exactly once from 0.1.471 to 0.1.472 and synchronize mfile.version `0.1.472`, mfile.title `boop Hunter 0.1.472`, boop.version `0.1.472`, and CODEX.md `0.1.472`. Stage only the ten listed implementation, focused-test, trigger, and metadata files as one package-affecting commit; boop_util.lua remains unchanged. Inspect staged paths, rerun Lua/JSON/diff/release/Muddler gates, commit once, and do not push. No committed RED state, second implementation commit, or second bump is allowed.
  </action>
  <acceptance_criteria>
    - "Global cooldown is independent of per-ability readiness and blocks all rage dispatch before local pacing."
    - "boop_attacks begins exact rage identity, then passes outcomeRegistration/direct dispatchMode into executeAction without pre-registering the unsplit logical action."
    - "Assist-enabled and Psion prefix-plus-rage expansions register exact expected/observed wire commands in send order and set causality from the final owned wire sequence."
    - "Ordinary, assist-expanded, and Psion-expanded rage produce only rage direct traces and never create standard intent or invoke standard maul bookkeeping."
    - "Generic denial after each clean final-wire chronology terminalizes once; any later outside-dispatch wire makes the same denial nonmutating."
    - "A failed or partial emission cleans up without an orphan owner and cannot authorize a generic outcome."
    - "The Plan 03-25 standard-pending barrier blocks rage sends, and boop_util.lua is consumed unchanged."
    - "Generic rage outcomes require exact owner/generation direct-send causality; manual adversarial cases are no-ops."
    - "Available recovery opens the global gate and changes only listed abilities."
    - "Triumph is generation-owned, bounded, first-terminal, reconnect-safe, and does not suppress later ordinary rage."
    - "Only the current rage ingestion/contract suites are referenced and green."
    - "Exact Rage triggers, Lua/JSON, release gates, and Muddler pass; docs/help remain assigned to Plan 03-29."
    - "Exactly one green package commit synchronizes all four version checkpoints at 0.1.472."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_rage_ingestion_spec.lua tests/boop_rage_contract_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_events.lua src/scripts/boop/boop_rage.lua src/scripts/boop/boop_attacks.lua src/triggers/boop/Rage/Rage_Command_Outcome.lua tests/boop_rage_ingestion_spec.lua tests/boop_rage_contract_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 -m json.tool src/triggers/boop/Rage/triggers.json &gt;/dev/null &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-25 is packaged at 0.1.472 with exact global cooldown/recovery, manual-causal isolation, bounded Triumph, and current rage suite coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Logical rage to canonical wires | Assist and Psion prefixes can expand one decision into multiple actual sends. |
| Final owned wire to outbound observer | Manual and boop commands share sysDataSendRequest after the rage dispatch. |
| Generic failure to pending rage | Identical output can belong to a manual command. |
| Available list to readiness | Global recovery must not mark unlisted abilities ready. |
| Timer/reconnect/room to Triumph | Prior-generation callbacks must not clear newer credit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-28-01 | Spoofing | rage denial attribution | high | mitigate | Require a complete ordered wire list, matching final owned sequence, and no later outbound outside the exact dispatch. |
| T-03-28-02 | Tampering | Available parser | high | mitigate | Parse exact prefix/list and mutate only listed keys. |
| T-03-28-03 | Denial of Service | global cooldown | high | mitigate | Gate before local pacing and reopen only on exact recovery/reset policy. |
| T-03-28-04 | Elevation of Privilege | Triumph credit | high | mitigate | One generation, at-most-once consumption, bounded timer, and first-terminal clears. |
| T-03-28-05 | Repudiation | stale chronology | medium | mitigate | Trace owner/generation/outbound/window/terminal facts. |
| T-03-28-06 | Tampering | unsplit logical registration | high | mitigate | Pass metadata into executeAction and register only each transformed part immediately before send. |
| T-03-28-SC | Tampering | dependency surface | low | accept | No package-manager install or new dependency is authorized. |
</threat_model>

<verification>
1. Use the established env/helper Busted form and named RED assertions only.
2. Prove ordinary, assist-enabled, and Psion-prefix rage ordered wire registration; clean and contaminated denial causality; global recovery; repeated-send suppression; and every Triumph terminal.
3. Parse changed Lua/Rage JSON and run release gates and Muddler; Plan 03-29 owns docs/help alignment.
4. Plan 03-29 reruns current rage suites unchanged; Plan 03-30 Test 11 supplies live authority.
5. Parent performs push/exact-SHA CI only after Plan 03-30 and every final mutation.
</verification>

<multi_source_coverage_audit>
## Source coverage

| Source | Item | Coverage |
|---|---|---|
| UAT | G-03-25 | Global denial/recovery, manual ambiguity, and Triumph use/expiry/denial/reconnect/timeout. |
| REQ | SAFE-02, SAFE-04 | Exact causal identity, listed mutation, and stale-generation guards. |
| Plan 03-25 | executeAction wire ledger | Reused for transformed multi-wire rage causality; no duplicate observer or unsplit pre-registration. |
| Current tests | Rage surfaces | ingestion_spec and contract_spec replace stale rage_spec reference. |

No standard, target, gold, leap, trace implementation, Phase 4/5, broad queue mutation, or built-artifact edit is included.
</multi_source_coverage_audit>

<success_criteria>
- G-03-25 focused and manifest regressions pass, including assist/Psion ordered wire registration and final-sequence causality.
- Manual identical outcomes cannot mutate pending boop rage.
- One green package commit produces synchronized version 0.1.472 without pushing.
</success_criteria>

<output>
Create .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-28-SUMMARY.md after the single green implementation commit. Do not push.
</output>
