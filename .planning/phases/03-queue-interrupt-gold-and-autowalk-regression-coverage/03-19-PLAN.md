---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 19
type: execute
wave: 19
depends_on: [03-18]
files_modified:
  - src/aliases/boop/Diagnostics/aliases.json
  - src/aliases/boop/Diagnostics/Boop_Trace_Live_On_Off.lua
  - src/scripts/boop/boop_ui_registry.lua
  - README.md
  - DESIGN.md
  - UIDESIGN.md
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements: [SAFE-02, SAFE-04, WALK-01]
gap_closure: true
gap_ids: [G-03-6]
must_haves:
  truths:
    - "Operators can invoke `boop trace live on` and `boop trace live off` through the packaged alias surface."
    - "Help and operator documentation clearly separate persisted collection from session-only live streaming."
    - "Documentation states that live mode starts off, resets off, does not enable collection, and preserves show/clear behavior."
    - "The final package builds from src, passes host tests and release gates, and is handed to the parent session for one immutable push and exact-SHA CI wait."
  artifacts:
    - "src/aliases/boop/Diagnostics/aliases.json registers the exact live on/off pattern."
    - "src/aliases/boop/Diagnostics/Boop_Trace_Live_On_Off.lua forwards the captured value to `boop.ui.traceCommand`."
    - "src/scripts/boop/boop_ui_registry.lua exposes the command in diagnostics help."
    - "README.md, DESIGN.md, and UIDESIGN.md document the collection/live distinction and output contract."
  key_links:
    - "Packaged alias regex -> captured on/off -> `boop.ui.traceCommand('live', value)`."
    - "Help registry and docs -> exact tested command/status semantics from Plan 03-18."
    - "Final immutable HEAD -> push -> `tools/wait_for_exact_ci.sh` exact `headSha` success."
---

<objective>
Complete G-03-6 by wiring the live trace command into the package and aligning all operator-facing contracts.

Purpose: Make the tested session-only stream discoverable and shippable without introducing another configuration setting.
Output: Packaged alias, help registration, synchronized documentation, complete source audit, and final CI handoff.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@UIDESIGN.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-18-SUMMARY.md
@src/aliases/boop/Diagnostics/aliases.json
@src/aliases/boop/Diagnostics/Boop_Trace_On_Off.lua
@src/scripts/boop/boop_ui_registry.lua
@tests/boop_trace_spec.lua
</context>

<scope_exception>
This plan follows 03-18 because alias, help, and documentation consume the command contract created there. The tasks stay within the five-file package-commit limit imposed by synchronized version metadata.
</scope_exception>

<tasks>

<task type="auto">
  <name>Task 1: Package the exact live trace alias</name>
  <files>src/aliases/boop/Diagnostics/aliases.json, src/aliases/boop/Diagnostics/Boop_Trace_Live_On_Off.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - src/aliases/boop/Diagnostics/aliases.json
    - src/aliases/boop/Diagnostics/Boop_Trace_On_Off.lua
    - src/aliases/boop/Diagnostics/Boop_Trace_Show_Number.lua
    - src/scripts/boop/boop_ui.lua
    - tests/boop_trace_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <action>
Register a dedicated diagnostics alias named consistently with the existing trace aliases. Match only `boop trace live on` or `boop trace live off`, case-insensitively and with the repository's normal whitespace convention. Create `Boop_Trace_Live_On_Off.lua` to forward the captured mode as `boop.ui.traceCommand("live", value)`. Keep it separate from the persisted collection alias so capture indexes and existing command behavior cannot regress.

Add focused route verification for both commands. Parse the diagnostics manifest and require exactly one enabled `Boop Trace Live On Off` entry with the complete anchored regex `^(?i)boop\s+trace\s+live\s+(on|off)$`, the normal empty script field, and a leaf alias. Then execute the actual alias Lua body twice with Mudlet-style `matches` tables for `boop trace live on` and `boop trace live off`, stub `boop.ui.traceCommand`, and assert exactly two two-argument calls in order: `("live", "on")`, then `("live", "off")`. This behavioral seam must prove capture index 2 reaches the exact UI command API; a broad text search is not sufficient.

Work only in `src/`; do not edit generated package artifacts. Validate the JSON manifest, Lua syntax, and package build. Before committing, bump all synchronized package fields from 0.1.437 to 0.1.438. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `feat(03-19): expose live trace alias`. Do not push.
  </action>
  <acceptance_criteria>
    - "The parsed manifest contains exactly one enabled leaf alias with the complete anchored live on/off regex and remains valid JSON."
    - "Executing the packaged alias body for both commands produces exactly `boop.ui.traceCommand('live', 'on')` and `boop.ui.traceCommand('live', 'off')`, in order and with no extra arguments or calls."
    - "Existing trace aliases remain unchanged and `muddle` packages the new source."
    - "All synchronized version fields read 0.1.438 and release gates pass."
  </acceptance_criteria>
  <verify>
    <automated>python3 -c 'import json, pathlib; data = json.loads(pathlib.Path("src/aliases/boop/Diagnostics/aliases.json").read_text()); entries = [entry for entry in data if entry.get("name") == "Boop Trace Live On Off"]; assert len(entries) == 1; assert entries[0] == {"name": "Boop Trace Live On Off", "isActive": "yes", "regex": r"^(?i)boop\s+trace\s+live\s+(on|off)$", "script": "", "isFolder": "no"}' &amp;&amp; lua -e 'local calls = {}; local function capture(...) local call = { n = select("#", ...) }; for i = 1, call.n do call[i] = select(i, ...) end; calls[#calls + 1] = call end; boop = { ui = { traceCommand = capture } }; for _, value in ipairs({ "on", "off" }) do matches = { "boop trace live " .. value, value }; dofile("src/aliases/boop/Diagnostics/Boop_Trace_Live_On_Off.lua") end; assert(#calls == 2); assert(calls[1].n == 2 and calls[1][1] == "live" and calls[1][2] == "on"); assert(calls[2].n == 2 and calls[2][1] == "live" and calls[2][2] == "off")' &amp;&amp; luac -p src/aliases/boop/Diagnostics/Boop_Trace_Live_On_Off.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>The packaged on/off command routes are behaviorally proven to reach the exact session-only UI API without changing collection aliases.</done>
</task>

<task type="auto">
  <name>Task 2: Add live trace to diagnostics help and command documentation</name>
  <files>src/scripts/boop/boop_ui_registry.lua, README.md, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - src/scripts/boop/boop_ui_registry.lua
    - README.md
    - src/scripts/boop/boop_ui.lua
    - src/aliases/boop/Diagnostics/aliases.json
    - tests/boop_ui_registry_spec.lua
    - tests/boop_trace_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <action>
Update the diagnostics registry's quick-help and detailed command line to include `boop trace live on|off`. Describe collection `on|off` and live streaming as separate controls. In README, document that live starts off, is session-only, resets on package/session initialization, does not persist, and does not activate collection. Show that useful live output requires collection and live mode to both be on; keep existing show/clear and bounded-buffer documentation accurate.

Run registry/trace host tests and the package build. Before committing, bump all synchronized package fields from 0.1.438 to 0.1.439. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `docs(03-19): document live trace command and help`. Do not push.
  </action>
  <acceptance_criteria>
    - "Diagnostics help exposes the exact packaged command."
    - "README clearly distinguishes persisted collection from session-only live streaming."
    - "The docs preserve existing buffer/show/clear semantics."
    - "All synchronized version fields read 0.1.439; host tests, release gates, and muddle pass."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_trace_spec.lua tests/boop_ui_registry_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_ui_registry.lua &amp;&amp; rg -n "boop trace live on\\|off|session" src/scripts/boop/boop_ui_registry.lua README.md &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>Help and README make the live command and its independent session semantics discoverable.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize design contracts and prepare the exact-SHA parent handoff</name>
  <files>DESIGN.md, UIDESIGN.md, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - DESIGN.md
    - UIDESIGN.md
    - README.md
    - src/scripts/boop/boop_ui.lua
    - src/scripts/boop/boop_util.lua
    - tests/boop_trace_spec.lua
    - AGENTS.md
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <action>
Record the trace live contract in DESIGN: runtime-only state, initialization reset, collection gate before append/output, exact-once post-append emission, non-recursive output, and unchanged bounded buffer/show/clear behavior. Record in UIDESIGN the operator-facing status language, the `trace live: {timestamped entry}` stream prefix, and the explicit note shown when live is enabled while collection remains off.

Run the full host suite, syntax-check all production and alias Lua source under `src/`, run release gates, and run `muddle`. Before committing, bump all synchronized package fields from 0.1.439 to 0.1.440. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `docs(03-19): record live trace UX semantics`. Do not push.

After this task and after the executor has written all required 03-16 through 03-19 SUMMARY/STATE/ROADMAP/UAT artifacts, hand control to the parent Codex session. The parent must inspect staged paths before every final planning commit, apply the same version rule if any path outside `.planning/` appears, run `python3 tools/check_release_gates.py`, require a clean worktree, capture immutable final HEAD, push that exact HEAD, and run `tools/wait_for_exact_ci.sh "$FINAL_SHA"`. CI evidence must show a successful `main.yml` run with `headSha` exactly equal to final HEAD and real Mudlet execution with zero failures/errors. The evidence is reported, not committed; any later repository mutation invalidates it and requires a new exact-SHA wait.
  </action>
  <acceptance_criteria>
    - "DESIGN and UIDESIGN match the tested runtime, command, and output behavior."
    - "All synchronized version fields read 0.1.440."
    - "The full host suite, all src Lua syntax checks, release gates, and muddle pass."
    - "No task pushes; the parent handoff explicitly owns the one final immutable push and exact-SHA CI gate."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests &amp;&amp; find src -type f -name '*.lua' -print0 | xargs -0 -n1 luac -p &amp;&amp; rg -n "session|collection|trace live:|show|clear" DESIGN.md UIDESIGN.md &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>The design contracts are synchronized, the complete local gate is green, and the parent has an exact immutable CI handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mudlet alias input → UI command API | Operator text and captured mode enter the tested session toggle. |
| Source package → generated Mudlet artifact | Only src changes should feed the build; generated artifacts must not be edited. |
| Local final HEAD → GitHub Actions | CI evidence is valid only for the immutable pushed SHA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-19-01 | Spoofing | trace alias capture | medium | mitigate | Use an exact anchored on/off pattern and forward only the captured enum. |
| T-03-19-02 | Information Disclosure | discoverable live stream | medium | mitigate | Document explicit session opt-in, default/reset off, and collection independence. |
| T-03-19-03 | Tampering | generated package | medium | mitigate | Modify only source files and prove packaging through `muddle`. |
| T-03-19-04 | Repudiation | CI evidence | high | mitigate | Push immutable final HEAD and require successful exact-headSha `main.yml` evidence. |
| T-03-19-SC | Tampering | package installs | low | accept | No npm, pip, or cargo package install is introduced by this plan. |
</threat_model>

<multi_source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | Queue/interrupt, gold, and autowalk regression coverage remains fail-safe under live races | Covered by prior Phase 3 plans plus G-03-4 in 03-17, G-03-5 in 03-16, and G-03-6 in 03-18/03-19. |
| REQ | SAFE-02 safe defaults and non-persistent session state | Covered by 03-18 reset/default tests and 03-19 operator contract. |
| REQ | SAFE-04 no unsafe cross-subsystem release | Covered by 03-16 exact gold owner/aggregate blocker tests and 03-18 collection isolation. |
| REQ | WALK-01 explicit, observable walk control | Covered by 03-17 blocker/stop feedback and 03-19 trace diagnostics. |
| REQ | WALK-02 current-room evidence before movement | Covered by 03-16 fenced room revalidation and 03-17 shared all-clear projection. |
| REQ | WALK-03 owned stop/detach/restart lifecycle | Covered by 03-17 preserved active and inactive command tests. |
| RESEARCH | Per-system owners, generations, and one shared all-clear evaluator | Implemented per D-01/D-02/D-04 in 03-16 and 03-17 without adding a parallel authority path. |
| RESEARCH | Response-fenced room observation and two-stage gold pickup | Implemented per D-09/D-10/D-11/D-12/D-14/D-15 in 03-16. |
| RESEARCH | Canonical diagnostics and one non-recursive emitter | Implemented per D-03 in 03-17 through 03-19. |
| RESEARCH | Existing Lua/Busted/muddle stack; no new dependencies | Every plan uses the established stack; no package install is planned. |
| CONTEXT | D-01 through D-04 owner aggregation, diagnostics, priority | Preserved and directly asserted in 03-16/03-17. |
| CONTEXT | D-05 through D-08 exact interrupt evidence and terminal ownership | Preserved in 03-16's revalidation timeout/invalidation path. |
| CONTEXT | D-09 through D-12 exact-room gold, confirmed pickup, coalescing, settled observation | Implemented directly in 03-16. |
| CONTEXT | D-13 through D-16 all-clear movement, complete room list, capped refresh, one move | Preserved by 03-16 response fencing and 03-17 manual hold tests. |
| CONTEXT | D-17 through D-20 safe manual movement | Preserved in 03-17; no new manual movement path is introduced. |
| CONTEXT | D-21 through D-24 stop ownership, invalidation, restart, distinct feedback | Implemented and regression-tested in 03-17. |
| CONTEXT | Agent discretion: exact compact labels and trace prefix | Chosen as `walk stop: no active boop walk`, `manual_targeting`, and `trace live: {entry}` with exact tests. |
| CONTEXT | Deferred hyena summary and temporary/custom attacks | Excluded from all gap plans. |
</multi_source_coverage_audit>

<verification>
Run the full host Busted suite, syntax-check every Lua source under `src/`, validate the diagnostics JSON, run `python3 tools/check_release_gates.py`, and run `muddle`. After all repository mutations, the parent performs the immutable push and `tools/wait_for_exact_ci.sh "$FINAL_SHA"` exact-SHA gate.
</verification>

<success_criteria>
- The packaged `boop trace live on|off` route reaches the session-only command API.
- Help, README, DESIGN, and UIDESIGN agree with tested collection/live semantics.
- All G-03-4, G-03-5, and G-03-6 coverage is accounted for with no deferred idea included.
- The final package receives successful exact-SHA `main.yml` evidence after all mutations cease.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-19-SUMMARY.md` when done.
</output>
