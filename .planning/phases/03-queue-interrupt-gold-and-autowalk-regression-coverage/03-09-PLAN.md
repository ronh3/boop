---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "09"
type: execute
wave: 9
depends_on:
  - "03-08"
files_modified:
  - tests/boop_event_transitions_spec.lua
  - tests/README.md
  - README.md
  - DESIGN.md
  - UIDESIGN.md
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "Every 03-VALIDATION ordering row has both callback directions, exact command/event/timer counts, and a retained owner/generation stale no-op assertion."
    - "tests/README independently describes owner, interrupt/pull, gold, room observation, walker, SAFE-02 manual-hold, and authority coverage without stale wording."
    - "README, DESIGN, and UIDESIGN independently describe shipped ownership/timing behavior and reject chained loot, timer/prompt settlement, and unconditional walker-stop semantics."
    - "Task 3 passes release gates, the exact 21-file Lua syntax union, Muddler, and the canonical local real-Mudlet suite when available; these checks are pre-terminal evidence only."
    - "Plan task commits touching tests, docs, source, or any other path outside `.planning/` are package-affecting and monotonically synchronize all four version checkpoints under AGENTS.md."
    - "Standard GSD SUMMARY/STATE/ROADMAP/REQUIREMENTS/phase-completion commits are planning-only and version-exempt when every staged path is under `.planning/`."
    - "After upstream GSD execution and every repository mutation complete, the parent pushes immutable final HEAD and runs `tools/wait_for_exact_ci.sh \"$FINAL_SHA\"` as the mandatory AGENTS.md/CODEX.md repository extension."
    - "CI evidence remains uncommitted; any later repository mutation invalidates it and requires the terminal exact-HEAD gate to rerun."
    - "Unavailable push/authentication/exact-headSha run/success blocks repository completion and readiness for UAT; live Achaea checks remain assigned to `$gsd-verify-work 03`."
  artifacts:
    - path: "tests/boop_event_transitions_spec.lua"
      provides: "Final cross-lifecycle ordering matrix coverage"
    - path: "tests/README.md"
      provides: "Topic-specific Phase 03 test inventory and authority boundary"
    - path: "README.md"
      provides: "Operator contract for blockers, interrupts/pull, staged gold, room settlement, and walker ownership"
    - path: "DESIGN.md"
      provides: "Owner-keyed/shared-room/generation architecture contract"
    - path: "UIDESIGN.md"
      provides: "Primary-plus-count and all-owner diagnostic rendering contract"
    - path: ".planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-09-SUMMARY.md"
      provides: "Ordinary GSD execution summary; planning-only and version-exempt when committed only with `.planning/` paths"
    - path: "tools/wait_for_exact_ci.sh"
      provides: "Repository-owned exact-final-HEAD CI authority invoked by the parent after upstream GSD and all mutations finish"
  key_links:
    - from: "tests/boop_event_transitions_spec.lua"
      to: ".planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md"
      via: "every required ordering row maps to a named executable case"
      pattern: "stale"
    - from: "AGENTS.md"
      to: "tools/wait_for_exact_ci.sh"
      via: "mandatory parent-session terminal extension after upstream GSD execution and every repository mutation"
      pattern: "wait_for_exact_ci"
    - from: "tools/wait_for_exact_ci.sh"
      to: ".github/workflows/main.yml"
      via: "exact headSha discovery, blocking watch, success check, and immutable-HEAD/worktree recheck"
      pattern: "(headSha|conclusion|git rev-parse HEAD)"
---

<objective>
Close Phase 03 with the complete cross-lifecycle matrix, independently verified docs/test inventory, explicit commit classification, and the repository-owned exact-final-HEAD terminal extension.

Purpose: The phase is complete only when executable ordering coverage, operator/design wording, package build, and real Mudlet evidence all agree.
Output: Final integration cases, synchronized README/DESIGN/UIDESIGN/tests README, all-touched-Lua syntax evidence, Muddler/local real-Mudlet evidence, ordinary planning-only GSD closeout artifacts, and repository-level parent instructions for immutable exact-final-SHA CI before live-UAT handoff.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@UIDESIGN.md
@tests/README.md
@.github/workflows/main.yml
@tools/check_release_gates.py
@tools/wait_for_exact_ci.sh
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-02-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-03-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-04-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-06-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-07-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-08-SUMMARY.md

<interfaces>
Created production symbols and values — exhaustive:
- None. This plan creates no production constant, local function, public API, owner form, state/record/snapshot field, timer/retry field, blocker code, phase, or terminal reason.

Existing symbols modified:
- No production Lua behavior symbol is modified.
- Existing integration spec and documentation sections are modified in place.
- Existing version metadata fields are modified only by package-affecting Tasks 1 and 2; standard GSD closeout/tracking commits whose staged paths are all under `.planning/` preserve the synchronized package version.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the ordering matrix and independently document test coverage</name>
  <files>tests/boop_event_transitions_spec.lua, tests/README.md, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_event_transitions_spec.lua` — modified final cross-lifecycle ordering matrix and target-removal queue-drift coverage.
    - `tests/README.md` — modified Phase 03 test inventory, commit-classification statement, and repository terminal-CI statement.
    - `src/scripts/boop/boop_runtime.lua` — final owner, room observation, gold effect, and reservation contracts exercised by matrix cases.
    - `src/scripts/boop/boop_events.lua` — final interrupt/pull/gold/room/walker event entry points exercised by matrix cases.
    - `src/scripts/boop/boop_walk.lua` — final exact reservation and walker owner transitions exercised by matrix cases.
    - `tests/support/boop_test_helper.lua` — callback queue, room, owner, and walker fixtures.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint and authoritative test guidance.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01 through D-24 and deferred scope.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — every required ordering row and exact-count/sign-off rule.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `tests/boop_event_transitions_spec.lua`, `tests/README.md`, `Shared Test Patterns`, `Preserve target-removal queue-drift regression`, and `Integration Order the Planner Should Preserve`.
  </read_first>
  <behavior>
    - "Every required matrix row has both callback orders and exact side-effect counts."
    - "Target-removal queue-drift semantics remain intact."
    - "tests README names every Phase 03 ownership/timing topic and authority rule."
  </behavior>
  <acceptance_criteria>
    - Every row in `03-VALIDATION.md` maps to a named executable case covering both listed callback orders, exact send/event/timer counts, retained unrelated owner state, and wrong-generation/reservation no-op behavior.
    - Cross-module cases include interrupt/pull during room transition, gold during target removal, walk settlement during gold/target changes, and old gold/walker callbacks after new room/run generations.
    - The target-removal regression continues to assert zero queue-clear commands before executing its captured retarget callback.
    - `tests/README.md` contains all eight exact topic/policy sentences listed in `<action>` and none of the four stale statements rejected by `<verify>`.
    - One patch increment synchronizes `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint.
    - `luac -p tests/boop_event_transitions_spec.lua`, the release gate, and every README positive/negative assertion succeed.
  </acceptance_criteria>
  <action>
Audit all implemented specs against every row in 03-VALIDATION and add only missing cross-module cases to `tests/boop_event_transitions_spec.lua`: interrupt/pull during room transition, gold during target removal, walk settlement during gold/target changes, and old gold/walker callbacks after new room/run generations. Preserve target-removal no-queue-clear meaning. Update `tests/README.md` with these exact topic/policy sentences: `Owner-keyed blocker tests cover both clear orders and exact-owner exclusion.`, `Interrupt and pull tests cover idempotent repeats, first-terminal callbacks, and stale generations.`, `Gold tests cover room-owned pickup, inventory-owned packing, get-confirm-put order, and wrong-room retries.`, `Room observation tests require current Room.Info plus a complete current room item list; prompts and timers never settle the room.`, `Walker tests cover shared automatic/manual gating, one move per room generation, owned stop, attached detach, explicit install, and package loss.`, `SAFE-02 tests cover disabled and manual-targeting holds plus existing release actions.`, `Standard GSD SUMMARY/STATE/ROADMAP/REQUIREMENTS/phase-completion commits are planning-only and version-exempt when every staged path is under .planning; plan task commits touching tests, docs, or source are package-affecting and synchronize all four version checkpoints.`, and `Repository terminal CI authority is the mandatory AGENTS.md/CODEX.md extension: after upstream GSD execution and every repository mutation finish, the parent pushes immutable FINAL_SHA and runs tools/wait_for_exact_ci.sh "$FINAL_SHA"; CI evidence remains uncommitted, and any later mutation requires a rerun.` Because this task's commit touches tests and documentation outside `.planning/`, classify it as package-affecting, increment one patch version, and synchronize `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint before commit. Do not edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; rg -F "Owner-keyed blocker tests cover both clear orders and exact-owner exclusion." tests/README.md &amp;&amp; rg -F "Interrupt and pull tests cover idempotent repeats, first-terminal callbacks, and stale generations." tests/README.md &amp;&amp; rg -F "Gold tests cover room-owned pickup, inventory-owned packing, get-confirm-put order, and wrong-room retries." tests/README.md &amp;&amp; rg -F "Room observation tests require current Room.Info plus a complete current room item list; prompts and timers never settle the room." tests/README.md &amp;&amp; rg -F "Walker tests cover shared automatic/manual gating, one move per room generation, owned stop, attached detach, explicit install, and package loss." tests/README.md &amp;&amp; rg -F "SAFE-02 tests cover disabled and manual-targeting holds plus existing release actions." tests/README.md &amp;&amp; rg -F "Standard GSD SUMMARY/STATE/ROADMAP/REQUIREMENTS/phase-completion commits are planning-only and version-exempt when every staged path is under .planning; plan task commits touching tests, docs, or source are package-affecting and synchronize all four version checkpoints." tests/README.md &amp;&amp; rg -F 'Repository terminal CI authority is the mandatory AGENTS.md/CODEX.md extension: after upstream GSD execution and every repository mutation finish, the parent pushes immutable FINAL_SHA and runs tools/wait_for_exact_ci.sh "$FINAL_SHA"; CI evidence remains uncommitted, and any later mutation requires a rerun.' tests/README.md &amp;&amp; ! rg -F "Confirms auto-gold freestand queueing and pending-gold flush behavior." tests/README.md &amp;&amp; ! rg -F "prompt marks the room settled" tests/README.md &amp;&amp; ! rg -F "timer marks the room settled" tests/README.md &amp;&amp; ! rg -F "walk stop always stops the external run" tests/README.md</automated>
  </verify>
  <done>The full matrix is executable and tests/README independently passes positive topic assertions plus stale-semantic rejection.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize operator, architecture, and UI documentation</name>
  <files>README.md, DESIGN.md, UIDESIGN.md, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `README.md` — modified operator blocker, interrupt/pull, gold, room settlement, walker stop/detach, and explicit-install contract.
    - `DESIGN.md` — modified owner-keyed/shared-room/generation/gold/walker architecture contract.
    - `UIDESIGN.md` — modified primary-plus-count and all-owner diagnostic rendering contract.
    - `src/scripts/boop/boop_ui_registry.lua` — shipped in-game help wording that root docs must match.
    - `src/scripts/boop/boop_runtime.lua` — shipped owner-keyed blocker and shared room observation behavior.
    - `src/scripts/boop/boop_walk.lua` — shipped reservation, owner lifecycle, stop/detach, and explicit package behavior.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-03, D-05 through D-24, and phase scope fences.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — documentation assertions and no-live-UAT-claim boundary.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Operator documentation files`, `src/scripts/boop/boop_ui_registry.lua`, and `Status, warnings, and trace`.
  </read_first>
  <acceptance_criteria>
    - `README.md` contains all seven exact operator/maintenance sentences listed in `<action>`.
    - `DESIGN.md` contains exact anchors `owner-keyed blockers`, `shared room observation`, `inventory-owned packing`, `generation-owned operations`, `get-confirm-put`, and `owned stop / attached detach`.
    - `UIDESIGN.md` contains exact anchors `code -- label | +N more` and `all active blocker owners`.
    - Every stale chained-loot, prompt/timer settlement, unconditional stop, and persisted-pull statement rejected by `<verify>` is absent from its scoped document.
    - Command grammar and external route ownership remain unchanged, and no document claims that Phase 03 performed live Achaea UAT.
    - One patch increment synchronizes all four version checkpoints, and the full release gate plus every positive/negative documentation assertion succeeds.
  </acceptance_criteria>
  <action>
Update only existing Phase 03 behavior and maintenance sections. In README add exact sentences `Blockers display as code -- label | +N more; trace/debug list all active blocker owners.`, `Gold uses get-confirm-put and never chains loot with an attack.`, `Room settlement requires current Room.Info plus a complete current room item list; prompts and timers never settle the room.`, `boop walk stop ends a boop-owned run or detaches from an external run.`, `demonnicAutoWalker installation is explicit; runtime paths never install or update it.`, `Pull uses a runtime hold and never changes saved enabled configuration.`, and `Repository terminal CI authority runs only after upstream GSD execution and every repository mutation finish: the parent pushes immutable final HEAD, then runs tools/wait_for_exact_ci.sh "$FINAL_SHA"; CI evidence is not committed, and any later mutation requires a rerun.` In DESIGN include exact anchors `owner-keyed blockers`, `shared room observation`, `inventory-owned packing`, `generation-owned operations`, `get-confirm-put`, and `owned stop / attached detach`. In UIDESIGN include exact anchors `code -- label | +N more` and `all active blocker owners`. Preserve command grammar, external route ownership, deterministic plain text, and no live-UAT claim. Because this task's commit touches documentation outside `.planning/`, classify it as package-affecting, increment one patch version, and synchronize all four checkpoints before commit. Do not edit generated artifacts.
  </action>
  <verify>
    <automated>python3 tools/check_release_gates.py &amp;&amp; rg -F "Blockers display as code -- label | +N more; trace/debug list all active blocker owners." README.md &amp;&amp; rg -F "Gold uses get-confirm-put and never chains loot with an attack." README.md &amp;&amp; rg -F "Room settlement requires current Room.Info plus a complete current room item list; prompts and timers never settle the room." README.md &amp;&amp; rg -F "boop walk stop ends a boop-owned run or detaches from an external run." README.md &amp;&amp; rg -F "demonnicAutoWalker installation is explicit; runtime paths never install or update it." README.md &amp;&amp; rg -F "Pull uses a runtime hold and never changes saved enabled configuration." README.md &amp;&amp; rg -F 'Repository terminal CI authority runs only after upstream GSD execution and every repository mutation finish: the parent pushes immutable final HEAD, then runs tools/wait_for_exact_ci.sh "$FINAL_SHA"; CI evidence is not committed, and any later mutation requires a rerun.' README.md &amp;&amp; rg -F "owner-keyed blockers" DESIGN.md &amp;&amp; rg -F "shared room observation" DESIGN.md &amp;&amp; rg -F "inventory-owned packing" DESIGN.md &amp;&amp; rg -F "generation-owned operations" DESIGN.md &amp;&amp; rg -F "get-confirm-put" DESIGN.md &amp;&amp; rg -F "owned stop / attached detach" DESIGN.md &amp;&amp; rg -F "code -- label | +N more" UIDESIGN.md &amp;&amp; rg -F "all active blocker owners" UIDESIGN.md &amp;&amp; ! rg -F 'auto gold pickup is prepended to the next queued standard attack' README.md &amp;&amp; ! rg -F 'boop walk stop` halts in the current room' README.md &amp;&amp; ! rg -F "then resumes boop when GMCP confirms you returned" README.md &amp;&amp; ! rg -F 'prepends `get sovereigns/` to the next queued standard attack' DESIGN.md &amp;&amp; ! rg -F "prompt marks the room settled" README.md DESIGN.md UIDESIGN.md &amp;&amp; ! rg -F "timer marks the room settled" README.md DESIGN.md UIDESIGN.md &amp;&amp; ! rg -F "walk stop always stops the external run" README.md DESIGN.md UIDESIGN.md</automated>
  </verify>
  <done>README, DESIGN, and UIDESIGN independently pass topic-specific positive assertions and stale chained/settlement/stop wording rejection.</done>
</task>

<task type="auto">
  <name>Task 3: Run release, all-touched-Lua, build, and available local real-Mudlet gates</name>
  <files>None; verification-only task. Generated Muddler output is not edited or committed.</files>
  <read_first>
    - `.github/workflows/main.yml` — Mudlet/Busted workflow name, package build, and test job selected later by the repository terminal extension.
    - `tools/check_release_gates.py` — local versions/manifests/state-drift gate invoked by this task.
    - `tools/wait_for_exact_ci.sh` — existing parent-session terminal extension; read only and do not invoke from this task.
    - `mfile` — package metadata consumed by Muddler and release gates; read only.
    - `src/scripts/boop/boop_init.lua` — runtime version and final touched-Lua union; read only.
    - `CODEX.md` — canonical local Muddler/Mudlet command and CI fallback policy; read only.
    - `tests/README.md` — authoritative test boundary recorded by Task 1.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — Phase 03 scope and live-UAT boundary.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — sampling, commit classification, repository terminal authority, all ordering rows, and manual-only handoff.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — sections `Validation Architecture`, `Test Assertions That Must Be Explicit`, and `Sampling Rate`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — section `Integration Order the Planner Should Preserve`.
  </read_first>
  <acceptance_criteria>
    - `python3 tools/check_release_gates.py` exits zero.
    - The exact all-touched-Lua `luac -p` command in `<verify>` exits zero without removing any listed source or spec file.
    - `muddle` exits zero and generated output remains untracked/uncommitted as source.
    - If `/tmp/Mudlet.AppImage` exists, the exact canonical full-suite command in `<verify>` exits zero; otherwise local real-Mudlet execution is recorded as unavailable for the repository terminal extension.
    - This task performs no push, GitHub exact-SHA selection, git-history mutation, or live Achaea UAT and cannot establish repository terminal authority.
    - Normal GSD closeout creates the ordinary `03-09-SUMMARY.md`; if its commit and any accompanying tracking commit stage only `.planning/` paths, those commits are planning-only and preserve the package version.
    - Repository completion and UAT readiness still require the parent to finish upstream GSD execution and all repository mutations, push immutable final HEAD, and run `tools/wait_for_exact_ci.sh "$FINAL_SHA"` successfully.
  </acceptance_criteria>
  <action>
Run the automated command below against the already committed Task 2 state. The Lua list is the audited union of every final Phase 03 plan's 21 touched `.lua` files and must not be shortened. If `/tmp/Mudlet.AppImage` exists, run the exact canonical full suite from `<verify>` and require success; if it does not exist, record local real-Mudlet execution as unavailable without substituting host Busted. Do not push, select or watch GitHub runs, capture final authority, mutate git history, or perform live Achaea UAT in this task. During normal execute-plan closeout, create the ordinary `03-09-SUMMARY.md` and record Task 3's local evidence plus the remaining repository terminal-extension requirement; do not require or claim a custom upstream gate. Classify the SUMMARY and normal STATE/ROADMAP/REQUIREMENTS/phase-completion commits as planning-only and version-exempt only when every staged path is under `.planning/`.
  </action>
  <verify>
    <automated>python3 tools/check_release_gates.py &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua src/scripts/boop/boop_ui.lua src/scripts/boop/boop_util.lua src/scripts/boop/boop_walk.lua src/scripts/boop/boop_ui_registry.lua src/scripts/boop/boop_init.lua tests/support/boop_test_helper.lua tests/boop_runtime_spec.lua tests/boop_trace_spec.lua tests/boop_event_transitions_spec.lua tests/boop_interrupt_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua tests/boop_prequeue_spec.lua tests/boop_pull_spec.lua tests/boop_gold_spec.lua tests/boop_gold_retry_spec.lua tests/boop_tick_spec.lua tests/boop_walk_spec.lua tests/boop_ui_spec.lua &amp;&amp; muddle</automated>
    <local_real_mudlet>If `/tmp/Mudlet.AppImage` exists, require `muddle &amp;&amp; AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror` to succeed. If it is absent, record the local suite as unavailable; the parent-session repository terminal extension remains mandatory after upstream GSD and all mutations finish.</local_real_mudlet>
  </verify>
  <done>Static release gates, the exact 21-file all-touched-Lua syntax command, Muddler, and the canonical local real-Mudlet suite when available have passed; repository completion and UAT readiness still require the parent-session terminal extension after upstream GSD and all mutations finish. Live interrupt, pull, gold, owned/detached stop, and one-safe-move checks remain assigned to `$gsd-verify-work 03`.</done>
</task>

</tasks>

## Repository Commit Classification and Terminal CI Extension

This section is ordinary plan guidance. Upstream `$gsd-execute-phase 03` executes the normal tasks, SUMMARY, tracking, verification, and phase-completion flow; it does not parse or execute a custom post-summary gate.

- Tasks 1 and 2 are package-affecting because their commits touch tests, docs, source/version metadata, or other paths outside `.planning/`. Before each such commit, monotonically bump and synchronize `mfile.version`, `mfile.title` as `boop Hunter <version>`, `src/scripts/boop/boop_init.lua` `boop.version`, and the CODEX checkpoint.
- Standard GSD SUMMARY, STATE, ROADMAP, REQUIREMENTS, verification, and phase-completion commits are planning-only and version-exempt when every staged path is under `.planning/`. If any staged path falls outside `.planning/`, the commit is package-affecting and must follow the four-checkpoint bump rule.
- Only after upstream GSD execution and every repository mutation have completed does the parent Codex session capture `FINAL_SHA="$(git rev-parse HEAD)"`, push that immutable final HEAD, then run `tools/wait_for_exact_ci.sh "$FINAL_SHA"`.
- The script is the executable repository authority: it requires a full SHA, matching clean local HEAD, authenticated `gh`, the SHA on `origin`, an exact-`headSha` `main.yml` run, successful conclusion, and unchanged HEAD/worktree through completion.
- CI evidence is reported to the user and is not committed. Any later verifier, UAT artifact, state update, version bump, documentation change, or other repository mutation invalidates the evidence and requires the parent to push the new immutable final HEAD and rerun the script.
- Missing push, authentication, exact run discovery, successful conclusion, clean immutable HEAD, or unchanged worktree blocks repository completion and UAT readiness. Live Achaea validation remains assigned to `$gsd-verify-work 03`.

## Artifacts this plan creates

- No production symbol or value; existing integration tests and documentation are modified.
- Generated Muddler output may be produced for verification but is never edited or committed as source.

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Timing-sensitive paths cannot attack, loot, or walk while another safety/room owner blocks the next action | 03-01–03-09 | COVERED | Foundation, lifecycle cores/integration, UI/help, docs, local gates, and the repository terminal exact-final-HEAD extension. |
| REQ | SAFE-02 | Interrupt, pull, disabled, and manual-targeting holds prevent attacks until trustworthy/existing release | 03-01, 03-02, 03-03, 03-08, 03-09 | COVERED | Exact owners, first terminal, config-safe pull, and manual hold matrix. |
| REQ | SAFE-04 | Gold pickup/pack/retry/stale behavior cannot send wrong-room or bypass holds | 03-01, 03-04, 03-05, 03-08, 03-09 | COVERED | Shared evidence, staged ownership, event/tick integration, docs. |
| REQ | WALK-01 | Start/stop/move/status/settlement/blocker/event coverage | 03-01, 03-06, 03-07, 03-09 | COVERED | Core gate plus stop/arrival integration. |
| REQ | WALK-02 | Unsafe target/gold/diag/flee/pull/leader/room states block movement | 03-01, 03-06, 03-07, 03-08, 03-09 | COVERED | One automatic/manual gate plus integration. |
| REQ | WALK-03 | Optional walker with explicit install/status and no silent update | 03-06, 03-07, 03-08, 03-09 | COVERED | Boundary tests, help, docs. |
| RESEARCH | — | Owner-keyed blockers and shared room observation | 03-01 | COVERED | Canonical registry, exact exclusion, start/stamp/snapshot/refresh. |
| RESEARCH | — | Generation-owned interrupt and pull operations | 03-02, 03-03 | COVERED | Separate exact terminal lifecycles. |
| RESEARCH | — | Staged room/inventory gold ownership and no chaining | 03-04, 03-05 | COVERED | Core plus event/tick integration. |
| RESEARCH | — | Generation-guarded walker and ownership-aware stop | 03-06, 03-07 | COVERED | Core plus stop/arrival integration. |
| RESEARCH | — | UI/help/docs, release gates, Muddler, real Mudlet/exact-HEAD CI | 03-08, 03-09 | COVERED | File/topic assertions, Task 3 local gates, commit classification, and the parent-session repository terminal extension. |
| CONTEXT | D-01, D-02 | Per-system all-clear and subsystem-specific release | 03-01, 03-08 | COVERED | Observable effect boundaries and both clear orders. |
| CONTEXT | D-03, D-04 | Primary/count, full diagnostics, fixed priority | 03-01, 03-08, 03-09 | COVERED | Runtime selection, UI rendering, docs. |
| CONTEXT | D-05, D-06, D-07, D-08 | Evidence, timeout isolation, repeat idempotency, first terminal | 03-02, 03-03, 03-09 | COVERED | Interrupt/pull matrices. |
| CONTEXT | D-09, D-10, D-11, D-12 | Room pickup, inventory pack, duplicates, settled evidence | 03-04, 03-05, 03-09 | COVERED | Focused and cross-event gold preserve these decisions while inherited Phase 02 auto-flee cleanup remains destructive. |
| CONTEXT | D-13, D-14, D-15, D-16 | All-clear move, current evidence, capped refresh, one move/room | 03-01, 03-06, 03-07, 03-09 | COVERED | Shared observation and emitter. |
| CONTEXT | D-17, D-18, D-19, D-20 | Safe/idempotent manual move and active-mode continuity | 03-06, 03-07 | COVERED | Same evaluator and exact denial/success tests. |
| CONTEXT | D-21, D-22, D-23, D-24 | Owned stop/detach, invalidation, fresh restart, distinct output | 03-07, 03-08, 03-09 | COVERED | Event behavior plus help/docs. |
| CONTEXT | Deferred | Infernal hyena maul summary and temporary/custom attacks | — | EXCLUDED | Explicitly Phase 05 / after Phase 04. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source/tests → generated package | Build/load behavior differs from host syntax. |
| Local/CI evidence → release claim | Evidence can belong to a different commit or omit real Mudlet execution. |
| Implementation → docs/test inventory | Stale wording can create unsafe operator expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-VALIDATION | Spoofing | release-quality claim | high | mitigate | Parent pushes immutable final HEAD after upstream GSD/all mutations, then the repository script requires exact-headSha CI success and unchanged local state. |
| T-03-BUILD | Tampering | package/load order | medium | mitigate | Release gates, audited Lua union, Muddler build. |
| T-03-DOCS | Repudiation | docs/help/test inventory | medium | mitigate | Per-file/topic positive checks and stale-text negatives. |
</threat_model>

<verification>
- Every validation row has executable exact-count coverage.
- In-game help is independently verified in Plan 03-08; tests README and each root doc are independently verified here.
- Tasks 1 and 2 are package-affecting and synchronize all four version checkpoints; normal `.planning/`-only GSD closeout/tracking commits are version-exempt.
- Final `luac -p` includes the union of all touched Lua, including `boop_ui_registry.lua` and `boop_init.lua`.
- Task 3 runs static release gates, the exact 21-file Lua union, Muddler, and the canonical local real-Mudlet suite when available without claiming final authority.
- After upstream GSD execution and every repository mutation complete, the parent pushes immutable final HEAD and runs `tools/wait_for_exact_ci.sh "$FINAL_SHA"`.
- CI evidence remains uncommitted; missing push/authentication/exact run/success or any later repository mutation blocks completion/readiness until the script succeeds for the new final HEAD.
</verification>

<success_criteria>
- SAFE-02, SAFE-04, WALK-01, WALK-02, WALK-03 and D-01–D-24 are covered.
- README, DESIGN, UIDESIGN, in-game help, and tests README match shipped behavior without stale contradictions.
- Plan-task checks pass; package-affecting task commits synchronize all four checkpoints; standard `.planning/`-only GSD metadata commits preserve the version; and the parent repository terminal extension succeeds for immutable final HEAD.
- No Phase 04 validation, Phase 05 gag work, silent walker update, schema task, or built-artifact edit enters Phase 03.
- Live game checks are routed to `$gsd-verify-work 03`.
</success_criteria>

<output>
The executor creates `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-09-SUMMARY.md` during normal post-task closeout and records Task 3's local evidence without claiming final exact-HEAD authority. Standard SUMMARY/STATE/ROADMAP/REQUIREMENTS/verification/phase-completion commits are planning-only and version-exempt when every staged path is under `.planning/`; Plan 03-09 task commits touching tests/docs/source remain package-affecting and synchronize all four checkpoints. After upstream `$gsd-execute-phase 03` and every repository mutation finish, the parent pushes immutable final HEAD and runs `tools/wait_for_exact_ci.sh "$FINAL_SHA"`. CI evidence is reported to the user, not committed, and must be regenerated after any later mutation.
</output>
