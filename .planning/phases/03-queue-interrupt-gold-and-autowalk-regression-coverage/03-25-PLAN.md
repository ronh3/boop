---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 25
type: execute
wave: 25
depends_on:
  - 03-24
files_modified:
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_util.lua
  - src/scripts/boop/boop_events.lua
  - src/triggers/boop/Core/triggers.json
  - src/triggers/boop/Core/Standard_Command_Outcome.lua
  - tests/boop_prequeue_spec.lua
  - tests/boop_event_transitions_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
gap_closure: true
gap_ids:
  - G-03-21
  - G-03-24
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-01
  - WALK-02
must_haves:
  truths:
    - "Standard dispatch preserves the existing queue addclearfull freestand BOOP_ATTACK semantics; it never changes to QUEUE ADD."
    - "ADDCLEARFULL is the authoritative whole-native-queue replacement, so pre-existing manual queue entries are intentionally gone before boop's standard generation becomes pending."
    - "Every standard dispatch has exact owner/generation and room/target authority, and reaches one first-terminal executed, denied, expired, or cancelled outcome."
    - "While a queued standard is nonterminal, boop holds SETTARGET replacement, SETALIAS rebinding, another standard, and rage/direct target-changing sends behind that exact generation."
    - "executeAction accepts optional outcome-registration metadata and registers each fully transformed wire command immediately before its actual send under one logical owner/generation dispatch."
    - "One logical dispatch owns an ordered expected/observed wire-command list and a final owned sysDataSendRequest sequence; unsplit logical actions are never pre-registered as wire commands."
    - "Exact denial and success candidates reconcile at their immediately following authoritative prompt whether that prompt is ready or not, and mutate only the matching uncontaminated standard generation there."
    - "Authoritative success candidates include matching Balance Used or Equilibrium Used evidence and existing standard attack-success/result adapters."
    - "Any outbound command after the owned ADDCLEARFULL, whether manual/unowned or another boop owner, contaminates generic standard attribution."
    - "When a prompt has no valid candidate, readiness alone controls silence: a non-ready prompt leaves the generation queued with no timer, and the first later ready prompt starts one bounded grace and at most one replacement."
    - "Proven death, departure, Item.Remove, or room movement sends no clearqueue all, enters a short local retarget quarantine, and keeps the fixed alias/target binding unchanged until the old generation terminal."
    - "Operator blacklist, retarget, or eligibility revocation while the old target is present or presence is unknown sends clearqueue all exactly once, traces intentional collateral clearing, and forbids the old attack."
    - "Stale or unrelated candidates cannot terminate another generation, and correctness never depends on unique aliases, immutable queued-command assumptions, ALIAS CLEAR, or undocumented queued-alias timing."
    - "The one green package commit synchronizes all four version checkpoints at exactly 0.1.469."
  artifacts:
    - path: src/scripts/boop/boop_runtime.lua
      provides: "Exact standard lifecycle, first-terminal transitions, pending-standard mutation barrier, departure quarantine, and timers."
    - path: src/scripts/boop/boop_util.lua
      provides: "Preserved fixed BOOP_ATTACK/ADDCLEARFULL emission plus optional executeAction metadata that registers transformed wire parts just before send."
    - path: src/scripts/boop/boop_events.lua
      provides: "sysDataSendRequest ledger, pre-prompt candidate buffering, prompt reconciliation, bounded silence, and target evidence routing."
    - path: src/triggers/boop/Core/Standard_Command_Outcome.lua
      provides: "Thin exact standard denial and attack-result candidate adapter guarded by prompt-time runtime causality."
    - path: tests/boop_prequeue_spec.lua
      provides: "Ordered wire registration, ADDCLEARFULL replacement, not-ready prompt terminals, all-later-outbound ambiguity, bounded recovery, and stale-generation regressions."
    - path: tests/boop_event_transitions_spec.lua
      provides: "Departure retarget quarantine, mutation-barrier chronology, and present-or-unknown forbidden-target reset regressions."
  key_links:
    - from: src/scripts/boop/boop_util.lua
      to: src/scripts/boop/boop_runtime.lua
      via: "executeAction transforms assist/prefix/slash actions first, then registers each exact wire part immediately before send and appends its observed sequence to the logical dispatch"
    - from: "owned ADDCLEARFULL sysDataSendRequest"
      to: "pre-prompt candidate buffer"
      via: "the replacement sequence is the baseline; the following prompt accepts only candidates with no later outbound from outside the exact standard owner"
    - from: "buffered outcome candidate"
      to: "immediately following authoritative prompt"
      via: "causality and first-terminal reconciliation happen before readiness is consulted; readiness matters only when no valid candidate exists"
    - from: "nonterminal queued standard"
      to: "boop target/alias/standard/rage/direct sends"
      via: "one runtime mutation barrier holds every target-changing boop path until the generation terminal"
    - from: "proven departure"
      to: "replacement targeting"
      via: "a local retarget quarantine preserves the old fixed alias binding through old result or ready-grace terminal before SETTARGET/SETALIAS replacement"
    - from: "present-or-unknown forbidden revocation"
      to: "native queue and exact lifecycle"
      via: "send clearqueue all once, then terminalize the exact old generation before normal replacement gates"
---

<objective>
Close G-03-21 and G-03-24 with an exact standard-command lifecycle built around the existing destructive `queue addclearfull freestand BOOP_ATTACK` boundary, prompt-reconciled outcomes, ready-only bounded recovery, serialized fixed-alias retargeting, and explicit present-or-unknown forbidden-target clearing.

Purpose: Remove sticky standard state and wrong-target races without changing native queue or alias semantics. ADDCLEARFULL authoritatively removes pre-existing queue work; every later outbound from outside the exact standard owner makes generic output ambiguous, while a pending-generation mutation barrier prevents boop itself from rebinding the fixed alias before old work resolves.

Output: One small shared outbound ledger with canonical transformed-wire registration, readiness-independent prompt reconciliation, one pending-standard mutation barrier, evidence-specific invalidation, focused regressions, one green package commit, and version 0.1.469. README, DESIGN, and help alignment is deferred to Plan 03-29.

The assumption-delta decision remains no-change: exact boop-owned operation/dispatch owner plus generation is primary identity, with room/target authority where applicable. Tests must prove stale and unrelated outcomes cannot terminate a different generation.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-24-SUMMARY.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_util.lua
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_targets.lua
@src/triggers/boop/Core/triggers.json
@tests/boop_prequeue_spec.lua
@tests/boop_event_transitions_spec.lua

Current source sends `setalias BOOP_ATTACK <action>` followed by `queue addclearfull freestand BOOP_ATTACK`. Phase research records official ADDCLEARFULL behavior as clearing all queues and inserting the new command, but does not establish when queued aliases resolve. This plan therefore preserves the fixed alias, serializes every boop rebind/target-changing send until the queued generation terminates, and does not use unique aliases, immutable queued-command assumptions, QUEUE ADD, queue indices, or ALIAS CLEAR.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Artifact | Result |
|---|---|
| Standard lifecycle | Exact queued/executed/denied/expired/cancelled records with first-terminal authority. |
| Outbound ledger | Ordered expected/observed transformed wire commands and final sequence for each logical dispatch. |
| Destructive replacement boundary | Existing ADDCLEARFULL removes pre-existing native queue work before standard ownership. |
| Prompt reconciliation | Exact denial and named success candidates terminalize at the immediately following prompt even when it is not ready. |
| Ready-only recovery | Only candidate-free prompts consult readiness; non-ready waits and the first later ready prompt starts grace. |
| Fixed-alias serialization | SETTARGET, SETALIAS, new standard, and rage/direct target-changing sends wait for old terminal. |
| Target policy | Proven departure quarantines without explicit clear; present/unknown forbidden revocation clears once. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement, verify, and package the prompt-reconciled ADDCLEARFULL standard lifecycle</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_util.lua, src/scripts/boop/boop_events.lua, src/triggers/boop/Core/triggers.json, src/triggers/boop/Core/Standard_Command_Outcome.lua, tests/boop_prequeue_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-24-SUMMARY.md
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_util.lua
    - src/scripts/boop/boop_events.lua
    - src/scripts/boop/boop_targets.lua
    - src/triggers/boop/Core/triggers.json
    - src/triggers/boop/Core/Balance_Used.lua
    - tests/boop_prequeue_spec.lua
    - tests/boop_event_transitions_spec.lua
    - tests/boop_targets_spec.lua
    - README.md
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "The native standard queue command remains queue addclearfull freestand BOOP_ATTACK; no QUEUE ADD implementation is introduced."
    - "manual queue-add → boop ADDCLEARFULL → manual entry absent → first ready → boop outcome or silence is deterministic and contains no pre-existing-work ambiguity."
    - "executeAction accepts optional outcome metadata without changing callers that omit it; assist prefixing, class/prefix expansion already present in the logical action, and slash splitting complete before wire registration."
    - "executeAction treats outcomeRegistration.kind as semantic dispatch identity: omitted/standard kind preserves the existing std direct trace, standard intent hook, and maul bookkeeping, while rage kind emits rage direct traces and invokes none of those standard-only hooks."
    - "Immediately before every actual send(part), executeAction registers the fully transformed trimmed part under the supplied logical dispatch owner/generation; sysDataSendRequest appends the exact observed sequence to that dispatch's ordered expected/observed wire list."
    - "One logical dispatch may own one or more wire commands, and its final owned wire sequence is the last exact observed part; an unsplit logical string is never treated as a wire command."
    - "Standard direct wire parts and the queued standard's generated control wires use the same optional registration API where applicable, with owned ADDCLEARFULL remaining the queued-result baseline."
    - "Any outbound sequence after the standard's owned ADDCLEARFULL from outside that exact standard owner—including another boop rage/direct owner—contaminates generic success and denial attribution."
    - "Exact denial and success lines are candidates only: their immediately following authoritative prompt reconciles them to the exact nonterminal owner/generation or diagnoses them as ambiguous regardless of prompt readiness."
    - "Matching Balance Used or Equilibrium Used evidence and existing standard attack-success/result adapters are authoritative executed candidates in an uncontaminated window."
    - "A valid success followed by a not-ready prompt terminalizes executed once; a valid denial followed by a not-ready prompt terminalizes denied once; later ready prompts and timers are no-ops for either terminal generation."
    - "Only a prompt with no valid candidate checks readiness: non-ready leaves queued with no timer, while the first later ready prompt starts one generation-owned grace and expiry creates at most one zero-budget ADDCLEARFULL replacement."
    - "Attributed paralysis, stun, prone, web, impale, or unavailable-arms denial terminalizes once, waits for exact recovery plus later readiness, and retries once."
    - "While queued work is nonterminal, every boop SETTARGET replacement, SETALIAS rebind, new standard, rage send, or direct target-changing send is held."
    - "Departure marks a short local retarget quarantine: discovered replacement work stays held through old ready/result/grace terminal, then and only then emits replacement SETTARGET, SETALIAS, and ADDCLEARFULL."
    - "Every stale timer, prompt, denial, target event, and callback is a no-op against another owner/generation."
    - "Death/Item.Remove/departure/room movement sends no clearqueue all; work queued after boop's original ADDCLEARFULL survives, the fixed alias remains bound to the old target until terminal, and an old invocation may fail harmlessly once against that absent target."
    - "Blacklist/retarget/eligibility revocation with old-target presence true or unknown sends one clearqueue all when hazardous boop work remains, traces intentional unrelated clearing, and executes zero forbidden attacks."
  </behavior>
  <action>
Start with focused behavior and Core trigger-manifest regressions. Run the exact focused Busted command once and require named missing-behavior assertions to fail. Setup, helper, syntax, or command failures are not RED; do not negate the command and do not commit RED. Continue in this task until the entire focused command is green.

Implement one small outbound ledger shared by later plans and expose it through an optional outcome-registration descriptor on the existing `boop.executeAction(action, forceQueue, options)` path: `options.outcomeRegistration = { owner, generation, dispatchId, kind }`. Add `options.dispatchMode = "direct"` to select the existing direct-send branch explicitly so Plan 03-28 does not inherit normal standard queue configuration; omitting both options preserves every current caller and queue-selection behavior. Make the direct branch kind-aware: omitted or `standard` kind retains the existing `std direct` trace plus `boop.gag.noteStandardIntent` and `markUnnamableMaulUsed`, while `rage` kind traces `rage direct` and skips both standard-only hooks. Keep executeRageAction as a compatibility surface if existing callers require it, but canonical outcome-aware rage from Plan 03-28 enters executeAction. One logical descriptor owns ordered `expectedWireCommands` and `observedWireCommands` entries plus `finalOwnedWireSequence`.

Keep transformation canonical and registration just-in-time. `executeAction` first applies the existing assist prefix and receives any class/Psion prefix already included in the logical action, then uses the existing separator to split and trim the final direct-send action. Immediately before each actual `send(part, false)`, register that exact fully transformed part as the next expected wire command under the supplied owner/generation. The matching sysDataSendRequest advances the global monotonic sequence and appends `{ command, sequence }` to that logical dispatch's ordered observed list; the last observed owned part becomes `finalOwnedWireSequence`. Never pre-register the unsplit logical action, never guess future parts, and bound each one-shot expectation so a missing observer event cannot claim later traffic. Unmatched sends are unowned/manual.

Use the same API for standard dispatch where it reflects actual wires. Direct standard actions register every transformed/split part. Queued standard keeps fixed SETALIAS and `queue addclearfull freestand BOOP_ATTACK`; register generated control sends under the standard dispatch immediately before their actual sends, and retain the observed ADDCLEARFULL sequence as its generic-result baseline. Do not reinterpret the alias payload as an already executed attack. For a standard candidate, any observed outbound after that final owned baseline whose dispatch identity differs contaminates attribution, including another boop rage/direct dispatch as well as manual traffic. Keep no queue-ahead state and add no coordinator.

Represent each standard dispatch with owner, generation, queued/direct mode, lifecycle status, action, target and copied room authority, fixed-alias binding state, ADDCLEARFULL outbound baseline, candidate/result prompt facts, first-ready sequence, obstacle/recovery evidence, one retry budget, and one owned grace token. Preserve compatibility reads from prequeuedStandard while making the lifecycle record authoritative. Only an exact matching nonterminal generation can transition.

Preserve the existing `setalias BOOP_ATTACK &lt;action&gt;` and `queue addclearfull freestand BOOP_ATTACK` chronology. Treat the observed owned ADDCLEARFULL as replacement of the whole native queue: manual work queued before it is intentionally removed and cannot contaminate boop's later result. Model that chronology inside the two focused specs without changing the host helper: manual queue-add → owned ADDCLEARFULL → manual entry absent → result candidate/ready prompt or bounded silence. Do not add a queue-ahead fence, unique aliases, immutable queued payloads, QUEUE ADD, append queueing, or ALIAS CLEAR behavior.

Install one exact pending-standard mutation barrier at the runtime/util/events boundaries. From owned ADDCLEARFULL observation through first terminal, refuse or defer boop SETTARGET replacement, SETALIAS rebinding, another standard generation, executeRageAction, and any other boop direct target-changing send. The shared outbound ledger remains defensive: if a legacy/racing callback nevertheless produces another boop-owned outbound, it contaminates generic standard attribution exactly like a manual send. Add separate tests that normal rage/direct attempts are held and that an injected later boop-owner sequence makes an identical generic denial nonmutating.

Buffer exact standard outcome candidates rather than mutating on their trigger line. Candidate records include kind, reason, owner/generation snapshot, target/room authority, and current outbound sequence. Denial candidates cover the observed paralysis, stun, prone, web, impale, and unavailable-arms lines. Executed candidates explicitly include matching `Balance Used` or `Equilibrium Used` evidence and existing standard attack-success/result adapter evidence. At each candidate's immediately following authoritative prompt—ready or not—accept at most one candidate only when it follows the owned ADDCLEARFULL, the same generation is nonterminal, room authority still matches, candidate target equals that generation's captured target (including its recorded departure quarantine), and no later outbound exists outside that exact standard owner. Otherwise trace ambiguity and leave lifecycle state untouched.

Prompt authority and readiness are separate. First reconcile the candidate at its immediately following authoritative prompt without consulting ready state: valid success terminalizes executed once, and valid denial terminalizes denied once while retaining the exact recovery obstacle. Add exact regressions `success candidate → immediately following not-ready prompt → one executed terminal → later ready prompt/timer no-op` and `denial candidate → immediately following not-ready prompt → one denied terminal → later prompt/timer no duplicate`. Only when that prompt has no valid candidate—including because a candidate was contaminated—consult readiness. A non-ready prompt leaves the generation queued and arms no grace. The first later authoritative ready prompt with no valid candidate starts one bounded generation-owned grace. Later prompts cannot restart or extend it. Grace expiry terminalizes expired and creates at most one zero-budget replacement through the same ADDCLEARFULL path after every target, room, config, safety, gold, aggregate-operation, and walk gate passes. A denial retry likewise waits for matching recovery and later readiness. Stale candidates, prompts, timers, and recovery callbacks are no-ops.

Keep Core outcome adapters thin: they only submit exact candidate text/reason to runtime/events. Preserve the existing Balance_Used.lua entry point and route its evidence through the same candidate buffer in boop_events.lua. Regress canonical single/multi-wire registration, omitted/standard-kind compatibility including unchanged standard traces/hooks, candidate-before-prompt ordering, no mutation before prompt, success and denial terminals on not-ready prompts, candidate-free non-ready no-timer behavior, first-later-ready grace, manual contamination, another-boop-owner contamination, duplicate candidates/prompts, changed room/target, and generation N callbacks after N+1 exists. Plan 03-28 owns the complementary rage-kind regression proving no standard trace or hook leakage.

Split target invalidation without assuming queued-alias resolution timing. For proven death, Item.Remove departure, or player room movement, mark the old generation target-invalid and enter a short local retarget quarantine without sending clearqueue all or rebinding BOOP_ATTACK. A newly discovered replacement may be remembered, but all SETTARGET, SETALIAS, standard, rage, and direct target-changing sends remain held. Let the old fixed-alias invocation reach its authoritative ready/result prompt; reconcile at most one harmless absent-old-target failure, or let ready-only grace expire. Only after that exact old generation terminal may normal gates emit replacement SETTARGET, SETALIAS, then ADDCLEARFULL. Add the required chronology old invocation pending → departure → replacement discovered → attempted rebind/send held → old result or grace terminal → replacement settarget/setalias/addclearfull, and prove no new-target attack occurs in the race. Benign native work added after the old ADDCLEARFULL survives because this branch sends no clear.

For operator blacklist, explicit/automatic retarget, or eligibility revocation while old-target presence is true or unknown, use the safety-first branch: send documented `clearqueue all` exactly once while matching hazardous work is pending, record intentional unrelated queue clearing, terminalize that exact generation, then permit replacement only through normal gates. Seed unrelated work after standard ADDCLEARFULL and prove it is removed only in this branch. No forbidden attack may execute. Documentation and installed-help changes for these contracts are deliberately deferred to Plan 03-29; add no command and do not expand Phase 4/5 scope.

After focused verification is green, bump exactly once from 0.1.468 to 0.1.469 and synchronize mfile.version `0.1.469`, mfile.title `boop Hunter 0.1.469`, boop.version `0.1.469`, and CODEX.md `0.1.469`. Stage only the ten listed implementation, focused-test, manifest, and metadata files as one package-affecting commit. Inspect staged paths, rerun all task gates including release checks and Muddler, commit once, and do not push. No committed RED state, second implementation commit, second bump, or documentation edit is allowed.
  </action>
  <acceptance_criteria>
    - "Standard queueing still uses queue addclearfull freestand BOOP_ATTACK and intentionally replaces all pre-existing native queue work."
    - "No queue-ahead fence or QUEUE ADD standard path exists."
    - "The manual-before-ADDCLEARFULL chronology proves the manual entry absent before boop readiness/outcome handling."
    - "Optional executeAction outcome metadata owns the exact ordered fully transformed wire commands registered immediately before send, with observed sequences and one final owned wire sequence."
    - "Assist-prefixed and slash-expanded logical actions are never pre-registered unsplit; omitted metadata preserves existing callers."
    - "Dispatch kind is behaviorally isolated: omitted/standard kind preserves existing std traces and hooks, while rage kind uses rage traces and cannot call noteStandardIntent or markUnnamableMaulUsed."
    - "Denial and named success evidence remain candidates until the immediately following authoritative prompt—ready or not—where exact owner/generation and uncontaminated outbound chronology permit one terminal."
    - "Any later outbound outside the exact standard owner—including manual or another boop owner—makes generic candidates nonmutating."
    - "Success or denial followed by a not-ready prompt terminalizes once and makes later ready/timer callbacks no-ops."
    - "A candidate-free non-ready prompt leaves queued with no timer; the first later ready prompt starts one bounded grace and recovery/replacement obeys one retry budget."
    - "A nonterminal queued standard blocks boop target/alias replacement, another standard, rage, and direct target-changing sends."
    - "The exact departure race holds replacement SETTARGET/SETALIAS/ADDCLEARFULL until old result/grace terminal and emits no new-target attack."
    - "Departure sends no clearqueue all and permits at most one old-target failure; present/unknown forbidden revocation clears exactly once and executes no forbidden attack."
    - "No unique alias, immutable queued-command assumption, alias clear, append queue path, or documentation edit is introduced."
    - "Focused behavior, Core manifest, Lua/JSON, release gates, and Muddler pass."
    - "Exactly one green package commit synchronizes all four version checkpoints at 0.1.469."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_prequeue_spec.lua tests/boop_event_transitions_spec.lua tests/boop_targets_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_util.lua src/scripts/boop/boop_events.lua src/scripts/boop/boop_targets.lua src/triggers/boop/Core/Standard_Command_Outcome.lua tests/support/boop_test_helper.lua tests/boop_prequeue_spec.lua tests/boop_event_transitions_spec.lua tests/boop_targets_spec.lua &amp;&amp; python3 -m json.tool src/triggers/boop/Core/triggers.json &gt;/dev/null &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-21 and G-03-24 are packaged at 0.1.469 with preserved fixed BOOP_ATTACK/ADDCLEARFULL semantics, prompt-reconciled outcomes, bounded recovery, serialized departure retargeting, and explicit forbidden-target clearing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ADDCLEARFULL to native queue | Standard dispatch intentionally destroys all pre-existing native queue work. |
| sysDataSendRequest to owner | Manual and boop sends share one outbound event stream. |
| Logical action to wire commands | Assist and slash expansion can turn one owner into several actual sends. |
| Generic candidate to pending standard | A later manual or differently owned boop command can emit identical output after replacement. |
| Candidate line to following prompt | Trigger arrival alone cannot prove which command produced success or denial. |
| Prompt authority to readiness | Every candidate reconciles first even on a not-ready prompt; readiness applies only when no valid candidate exists. |
| Fixed alias to replacement targeting | Rebinding before old terminal can redirect an unresolved queued invocation. |
| Target evidence to queue policy | Proven absence and present/unknown forbidden revocation require different clearing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-25-01 | Tampering | pre-existing queue | high | accept/trace | Preserve documented ADDCLEARFULL replacement and explicitly document intentional collateral. |
| T-03-25-02 | Spoofing | generic outcome | high | mitigate | Buffer to the following prompt and reject any chronology with later outbound traffic outside the exact owner. |
| T-03-25-03 | Denial of Service | silent native loss | high | mitigate | Start one grace at first ready and force bounded first-terminal expiry/replacement. |
| T-03-25-04 | Tampering | stale callback | high | mitigate | Match owner, generation, timer token, room, target, and first-terminal state. |
| T-03-25-05 | Elevation of Privilege | fixed alias rebind race | high | mitigate | Hold SETTARGET, SETALIAS, standard, rage, and direct target-changing sends until old terminal. |
| T-03-25-06 | Elevation of Privilege | forbidden target | high | mitigate | One explicit clear for present/unknown revocation and zero old-target execution. |
| T-03-25-07 | Repudiation | queue collateral | medium | mitigate | Trace normal ADDCLEARFULL and explicit forbidden-target clears distinctly. |
| T-03-25-08 | Repudiation | pre-prompt mutation | high | mitigate | Candidate records do not mutate lifecycle state until exact following-prompt reconciliation. |
| T-03-25-09 | Spoofing | unsplit logical command | high | mitigate | Register each transformed wire part just before send and retain ordered expected/observed sequences. |
| T-03-25-SC | Tampering | dependency surface | low | accept | No package-manager install or new dependency is authorized. |
</threat_model>

<verification>
1. Use only the established env/TESTS_DIRECTORY/helper Busted form; setup or syntax failure never counts as RED.
2. Prove ordered canonical wire registration, destructive ADDCLEARFULL replacement, success/denial on immediately following not-ready prompts, candidate-free non-ready waiting, all-later-outbound ambiguity, first-ready bounded silence, and stale-generation isolation.
3. Prove the pending-standard send barrier, no-clear departure quarantine chronology, and exact forbidden-target clear with unrelated work seeded after standard replacement.
4. Parse changed Lua/Core JSON, run diff hygiene, release gates, and Muddler.
5. Plan 03-29 reruns this suite unchanged; Plan 03-30 Test 7 requires actual live silent loss and Test 10 verifies both target branches.
6. Parent owns push and exact-SHA CI only after Plan 03-30 and every final phase mutation.
</verification>

<multi_source_coverage_audit>
## Source coverage

| Source | Item | Coverage |
|---|---|---|
| UAT | G-03-21 | Canonical wire chronology, readiness-independent prompt terminals, ADDCLEARFULL baseline, all-owner causal isolation, denial/recovery, and actual silent-loss authority. |
| UAT | G-03-24 | Fixed-alias departure quarantine and explicit present/unknown forbidden-target reset. |
| REQ | SAFE-02, SAFE-04 | Exact owner/generation, room/target authority, first-terminal, and stale no-op. |
| REQ | WALK-01, WALK-02 | Bounded queue ownership and target-transition release. |
| Research | Native queueing | Existing ADDCLEARFULL whole-queue replacement remains authoritative. |
| Assumption delta | no-change | Exact operation/dispatch owner plus generation remains primary identity. |

No leap, gold, rage lifecycle, trace implementation, documentation, unique alias, immutable queued payload, queue-ahead fence, QUEUE ADD standard path, Phase 4/5 expansion, built-artifact edit, or broad architecture rewrite is included.
</multi_source_coverage_audit>

<success_criteria>
- G-03-21 and G-03-24 focused suites are green.
- Standard queue semantics remain ADDCLEARFULL and their unrelated-queue collateral is explicit.
- executeAction records ordered transformed wire commands and final observed sequence for optional outcome-owned dispatches.
- executeAction preserves standard direct traces/hooks for omitted or standard kind and provides a rage-kind direct path with no standard intent or standard maul bookkeeping.
- Success/denial candidates reconcile at the following prompt regardless of readiness; all later foreign-owner traffic makes them ambiguous.
- Ready-only silence and denial recovery terminate within exact bounded budgets.
- The pending-standard barrier serializes fixed-alias replacement, and target invalidation follows both evidence-specific policies without a new-target race.
- One green package commit produces synchronized version 0.1.469 without pushing.
</success_criteria>

<output>
Create .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md after the single green implementation commit. Do not push; the parent performs final exact-SHA CI only after Plan 03-30 and all phase mutations.
</output>
