---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 31
type: execute
wave: 31
depends_on:
  - 03-30
files_modified:
  - src/scripts/boop/boop_runtime.lua
  - tests/boop_prequeue_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
gap_closure: true
gap_ids:
  - G-03-21
requirements:
  - SAFE-02
  - SAFE-04
must_haves:
  truths:
    - "Captured normal and assist Magi Staffcast/Erode chronologies reduce to deterministic fixtures before production behavior changes."
    - "A clean owned standard success remains attributable when a newer accepted room application represents the same room and observation generation."
    - "The immediately following prompt creates exactly one executed terminal for that clean generation."
    - "A changed room, changed observation generation, manual command, or differently owned boop command still makes generic evidence non-authoritative."
    - "Every package-affecting execution commit synchronizes all four version fields; this plan produces one green commit at 0.1.474."
  artifacts:
    - path: tests/boop_prequeue_spec.lua
      provides: "Normal and assist Magi live-chronology fixtures plus clean/contaminated authority regressions."
    - path: src/scripts/boop/boop_runtime.lua
      provides: "Reasoned standard-candidate disposition and post-dispatch same-room authority reconciliation."
    - path: mfile
      provides: "Package title/version 0.1.474 synchronized with runtime and CODEX checkpoints."
  key_links:
    - from: "owned ADDCLEARFULL baseline"
      to: "Equilibrium Used candidate"
      via: "exact standard owner/generation, target, and outbound sequence"
    - from: "same-room application A→B"
      to: "candidate reconciliation"
      via: "room id and observation generation remain current after the command has already crossed the dispatch boundary"
    - from: "foreign outbound or changed room generation"
      to: "ambiguous disposition"
      via: "unchanged contamination and stale-authority rejection"
assumption_delta_decision:
  primary_noun: "owner-keyed operation/blocker generation"
  decision: "no-change"
  rationale: "this gap closure repairs established multi-owner lifecycle behavior and does not generalize identity or walker optionality"
---

<objective>
Close G-03-21 by reproducing the actual normal and assist Magi chronology, identifying the exact candidate rejection predicate, and repairing post-dispatch room authority without weakening contamination.

Purpose: A command already owned at its outbound baseline must not lose its terminal merely because current-room evidence is reapplied for the same observation generation, while genuinely stale or differently owned evidence remains harmless per D-01, D-02, and D-08.

Output: Deterministic live-chronology regressions, one narrow runtime repair, focused safety evidence, and synchronized package 0.1.474.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-30-SUMMARY.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_util.lua
@src/scripts/boop/boop_events.lua
@src/scripts/boop/attacks/magi.lua
@tests/boop_prequeue_spec.lua

The outbound baseline, owner/generation, target, and candidate fields are captured from the same active standard. The remaining clean-live rejection seam is strict application identity after an already-authorized send: a newer accepted application for the same room observation must not retroactively revoke that dispatch. Dispatch-time authority remains strict; only result-time same-room continuity changes.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Symbol/file | Result |
|---|---|
| `standardCandidateDisposition` in `boop_runtime.lua` | Private classifier returning accepted or a concrete rejection reason for identity, target, baseline, contamination, room, or generation. |
| `tests/boop_prequeue_spec.lua` | Named normal Staffcast, assist Staffcast/Erode, same-room refresh, manual contamination, different-owner contamination, and changed-room-generation cases. |
| Package metadata | `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint all become 0.1.474. |
| New files/public API | None. The helper is private and existing standard lifecycle entry points remain stable. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce the live Magi chronology and repair exact standard result authority</name>
  <files>src/scripts/boop/boop_runtime.lua, tests/boop_prequeue_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_util.lua
    - src/scripts/boop/boop_events.lua
    - src/scripts/boop/attacks/magi.lua
    - tests/boop_prequeue_spec.lua
    - tests/support/boop_test_helper.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <action>
First add two deterministic RED cases to the existing G-03-21 describe block, without committing RED. Reproduce the captured order rather than inventing a generic shortcut: settle room application A; queue `staffcast horripilation at 42` through the real `executeAction` path; observe owned SETALIAS then owned `queue addclearfull freestand BOOP_ATTACK`; accept application B for the same room id and unchanged observation generation; feed `Equilibrium used: 1.56s`; then run its immediately following prompt. Repeat with assist enabled so the alias payload is `assist Ada/staffcast ...`, and include Erode as the shield-selected standard. Assert the exact expected/observed outbound list, owner, generation, dispatch id, baseline, target, application A/B ids, room id, observation generation, candidate sequence, prompt, and terminal count. The clean fixtures must currently fail as ambiguous/expired for the same reason as live generations 19-25, 155-161, and 198-225.

Replace the boolean-only candidate check with private `standardCandidateDisposition(operation, candidate)`, returning a stable rejection reason. Preserve exact owner/generation/dispatch, target, baseline, and outbound-sequence checks. Preserve D-08 first-terminal behavior. Keep dispatch-time `validateRoomSourceAuthority` strict. At result reconciliation only, allow the captured application id to have been superseded when the canonical room id and observation generation are unchanged and current; reject an actual room change, observation-generation change, absent accepted room evidence, or target change. Trace the concrete disposition reason when ambiguous so future live evidence distinguishes authority from contamination.

Retain manual and different-owner contamination as hard failures. Extend the focused cases so a non-whitespace manual command and a separately owned rage/direct command after the standard baseline each remain ambiguous, and so same-room application refresh cannot erase their contamination. Add changed-room and changed-observation-generation cases that remain nonmutating. Do not broaden identity, remove application checks from dispatch, accept stale target evidence, or alter ADDCLEARFULL/fixed-alias semantics. This implements D-01/D-02 subsystem ownership, D-03 diagnostics, and D-08 first-terminal authority.

Run the focused suite green, then bump once from 0.1.473 to 0.1.474 across all four required fields. Stage exactly the five listed files as one package-affecting green commit, inspect staged paths, run release/Muddler gates, commit once, and do not push. README, DESIGN, installed help, built artifacts, and arbitrary temp attack/AOE/`ch` todos remain untouched because the intended user contract is unchanged.
  </action>
  <acceptance_criteria>
    - "Normal and assist Magi fixtures fail before production repair and pass afterward."
    - "Same-room application supersession reaches one executed terminal at the immediately following prompt."
    - "Manual and differently owned outbound traffic remain ambiguous and nonmutating."
    - "Changed room, observation generation, target, owner, baseline, and stale callbacks remain rejected."
    - "No ADDCLEARFULL, fixed alias, retry budget, or public API behavior changes."
    - "Focused tests, Lua syntax, release gates, and Muddler pass."
    - "One green package commit synchronizes all four version checkpoints at 0.1.474 and is not pushed."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_prequeue_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua tests/boop_prequeue_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-21 has deterministic normal/assist chronology coverage and package 0.1.474 no longer rejects clean same-room success while contamination remains strict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Current-room application → owned standard result | New same-room evidence may supersede an application after the command was authorized. |
| Shared outbound stream → generic success | Manual and differently owned commands can produce identical balance evidence. |
| Candidate → following prompt | Only the exact active generation may terminalize. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-31-01 | Spoofing | standard success attribution | high | mitigate | Require exact owner/generation/dispatch, target, baseline, and clean outbound chronology. |
| T-03-31-02 | Tampering | room authority | high | mitigate | Permit only application supersession inside the same current room and observation generation. |
| T-03-31-03 | Elevation of Privilege | foreign outbound | high | mitigate | Keep manual and different-owner contamination non-authoritative. |
| T-03-31-04 | Repudiation | ambiguous result | medium | mitigate | Trace the exact failed disposition predicate. |
| T-03-31-SC | Tampering | dependency surface | low | accept | No dependency or package-manager installation is authorized. |
</threat_model>

<verification>
Focused host tests must precede live Test 7. Package source remains under `src/`; every package-affecting commit includes the synchronized version fields, release gate, and Muddler build. Parent push and exact-SHA CI remain deferred until every gap plan, UAT record, summary, verification, and phase mutation is complete.
</verification>

<multi_source_coverage_audit>
## Source coverage

| Source | Item | Coverage |
|---|---|---|
| GOAL | Commands do not stall or escape another owner | Exact owned standard reaches one terminal without weakening foreign-owner holds. |
| REQ | SAFE-02, SAFE-04 | Owner/generation, room, target, baseline, and stale-callback safety. |
| RESEARCH | Generation-owned first terminal and shared room observation | Existing architecture is retained. |
| CONTEXT | D-01, D-02, D-03, D-08 | Per-system ownership, scoped release, diagnostics, and first terminal are explicit. |
| VERIFICATION/UAT | G-03-21 | Normal and assist Staffcast/Erode live chronology is the fixture source. |

Deferred hyena work and arbitrary temp attack, AOE, and `ch` ideas are excluded.
</multi_source_coverage_audit>

<success_criteria>
- Both captured clean Magi forms terminalize exactly once in focused tests.
- Adversarial contamination and stale authority remain nonmutating.
- Package 0.1.474 is built and locally gated without a push.
- Phase passage is not claimed before live Plans 03-34 through 03-38 record authority.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-31-SUMMARY.md` after the green execution commit. Do not push.
</output>
