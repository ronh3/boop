---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "10"
type: execute
wave: 10
depends_on:
  - "03-09"
files_modified:
  - tests/support/boop_host_busted_helper.lua
  - tests/boop_gold_retry_spec.lua
  - tests/boop_tick_spec.lua
  - tests/boop_walk_spec.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-04
  - WALK-02
gap_closure: true
must_haves:
  truths:
    - "A matching current gold timeout consumes its own expired timer token before any authorization/hold branch, while stale-generation and duplicate callbacks remain zero-effect."
    - "Per D-01 and D-02, an unrelated owner keeps pickup or packing fail closed with zero gold sends, zero movement events, and unchanged retry counts after the matching timeout fires."
    - "Per D-09 and D-10, pickup remains room/generation-owned and packing remains inventory-owned; timeout-token repair does not change either lifecycle identity or stage boundary."
    - "After the final unrelated owner clears, one normal runtime tick resumes exactly one unchanged pickup or packing send and installs one replacement timeout; the expired callback can never act again."
    - "Per D-13, walk remains blocked while the gold operation is active, then a terminal no-pack pickup path permits exactly one safe reservation and one `demonwalker.move` event through the existing evaluator/emitter."
  artifacts:
    - path: "tests/support/boop_host_busted_helper.lua"
      provides: "Repository-tracked host Busted bootstrap for the focused diagnostic specs"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Matching gold timeout token consumption before held/authorized branching in armGoldPendingTimeout"
    - path: "tests/boop_gold_retry_spec.lua"
      provides: "Pickup/packing, stale-generation, duplicate-callback, zero-send, zero-retry, and exact-once replacement-timer regressions"
    - path: "tests/boop_tick_spec.lua"
      provides: "Real timeout-under-owner followed by final-owner release and one unchanged-stage runtime resumption"
    - path: "tests/boop_walk_spec.lua"
      provides: "End-to-end proof that timeout recovery terminates gold and permits one safe walker reservation/event"
  key_links:
    - from: "tests/support/boop_host_busted_helper.lua"
      to: "tests/boop_gold_retry_spec.lua, tests/boop_tick_spec.lua, tests/boop_walk_spec.lua"
      via: "The tracked helper stubs the required Mudlet globals and loads the focused production modules before Busted evaluates the specs"
      pattern: "(BOOP_REPO_ROOT|boop_events.lua|boop_walk.lua)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "The matching timeout clears operation.timeoutTimer and derived pendingTimer before authorization; the later normal tick sees no timer and emits one existing flush_gold effect"
      pattern: "(timeoutTimer|markGoldQueueIntent|goldDispatchAuthorized)"
    - from: "src/scripts/boop/boop_runtime.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "tickStep emits flush_gold once and applyEffects delegates to boop.flushPendingGold"
      pattern: "(flush_gold|flushPendingGold)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_walk.lua"
      via: "Gold terminal schedules the existing generation-guarded normal tick; walk evaluates only after goldPending becomes false"
      pattern: "(completeGoldOperation|goldPending|walk_advance)"
---

<objective>
Close the verified SAFE-04/WALK-02 liveness gap without changing gold or walker identity.

Purpose: A timer callback that has already fired must relinquish its timer authority even when an unrelated owner still holds gold/walk; otherwise runtime recovery and autowalk remain permanently blocked.
Output: A tracked focused-host bootstrap, deterministic RED regressions, the narrow `armGoldPendingTimeout` ordering correction, exact-once pickup/packing recovery, and proof that normal walker advancement becomes reachable after gold terminates.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-04-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-06-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-09-SUMMARY.md

<interfaces>
Created production symbols and values — exhaustive:
- None. This repair creates no production constant, local/public function, owner form, operation field, phase, blocker code, terminal reason, command, event, dependency, or file.

Existing production symbol modified:
- Local `armGoldPendingTimeout(generation, expectedPhase)` in `src/scripts/boop/boop_events.lua`.

Existing contracts consumed unchanged:
- `state.gold.operation.timeoutTimer` is the current pending-timeout authority token.
- `currentGoldOperation(generation, expectedPhase)` validates exact current generation, phase, and nonterminal state.
- `goldDispatchAuthorized(operation)` excludes only `operation.blockerOwner` and fails closed against every unrelated owner.
- `boop.markGoldQueueIntent(operation.packTarget)` mirrors operation timers/stage into derived compatibility fields, including `state.gold.pendingTimer`.
- `tickStep` emits one `flush_gold` effect only when a current nonterminal gold operation has no timeout token and every unrelated owner is clear.
- `completeGoldOperation` terminates exactly once, clears only `gold:<generation>`, and schedules the existing generation-guarded normal tick.
- `boop.walk.maybeAdvance` and its reserved emitter remain the only path to `demonwalker.move`.
</interfaces>
</context>

<assumption_delta>
`no-change`: The generic detector terms `another` and `optional` do not broaden the identity model. The exact `gold:<generation>` blocker owner plus current operation generation/phase remain the primary lifecycle identity; this plan only consumes a matching expired `timeoutTimer`. Optional `demonnicAutoWalker` remains an independent existing integration and receives no source/API/installation/update change.
</assumption_delta>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED timeout-under-owner and eventual-walk regressions</name>
  <files>tests/support/boop_host_busted_helper.lua, tests/boop_gold_retry_spec.lua, tests/boop_tick_spec.lua, tests/boop_walk_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/support/boop_host_busted_helper.lua` — planned new tracked diagnostic bootstrap; create and inspect this stable path in Task 1, and do not source or copy any temporary path at execution time.
    - `tests/boop_gold_retry_spec.lua` — modified spec; inspect `scheduled`, `startPickup`, `copyOperation`, the unrelated-owner retry cases, old-timeout case, and current-stage timeout case.
    - `tests/boop_tick_spec.lua` — modified spec; inspect `scheduled`, `startGoldStage`, `readyBlockedStage`, `ownerCases`, and the four-stage unchanged-resumption matrix. This is the verifier-identified fixture that currently assigns `operation.timeoutTimer = false` directly.
    - `tests/boop_walk_spec.lua` — modified spec; inspect `resetCase`, `seedReadyWalk`, timer queue use, `countRaised`, gold denial, reservation, and one-event cases.
    - `tests/support/boop_test_helper.lua` — read-only fixture source for `helper.newTimerQueue`, `helper.setRuntimeBlocker`, `helper.seedRoomObservation`, and `helper.setWalker`.
    - `src/scripts/boop/boop_events.lua` — implementation under test; inspect `currentGoldOperation`, `goldDispatchAuthorized`, `armGoldPendingTimeout`, `queueGoldCommands`, `completeGoldOperation`, `flushPendingGold`, and pickup/packing success handlers.
    - `src/scripts/boop/boop_runtime.lua` — read-only normal reevaluation path; inspect `tickStep` active-gold handling and `applyEffects` handling of `flush_gold` and `walk_advance`.
    - `src/scripts/boop/boop_walk.lua` — read-only goldPending check, all-clear evaluator, reservation, and guarded emitter.
    - `mfile` — modified package version/title metadata; current synchronized baseline is `0.1.414`.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`; current synchronized baseline is `0.1.414`.
    - `CODEX.md` — modified synchronized package-version checkpoint and source of local/CI authority rules.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, D-09, D-10, and D-13; preserve all other D-XX decisions unchanged.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md` — authoritative blocker trace and all three `missing` closure items.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md` — established unchanged-stage `flush_gold` resumption and generation-guarded terminal tick.
  </read_first>
  <behavior>
    - "A real captured current pickup timeout fired under an unrelated gold/walk owner consumes its token, sends nothing, raises no movement event, consumes no retry, and leaves generation/phase/owner unchanged."
    - "The same sequence applies to PACK_PENDING without restoring room ownership or consuming putRetries."
    - "Invoking the expired callback a second time is zero-effect; invoking an old-generation callback while a newer operation owns a replacement timer cannot consume or replace the newer token."
    - "After the final unrelated owner clears, one boop.tick call emits one unchanged-stage get or put and records one replacement timeout distinct from the expired token."
    - "After resumed pickup succeeds with no pack, the existing terminal tick reaches one safe walk reservation and its emitter raises demonwalker.move exactly once."
  </behavior>
  <acceptance_criteria>
    - New tracked `tests/support/boop_host_busted_helper.lua` requires `BOOP_REPO_ROOT`, supplies only the host-safe Mudlet globals needed by these specs (`echo`, absent `cecho`, `getEpoch`, `killTimer`, `raiseEvent`, `send`, `sendGMCP`, and `tempTimer`), and loads the focused production scripts in the package order named in `<action>`; it does not load from `/tmp`, install packages, mutate manifests, or claim Mudlet equivalence.
    - `luac -p tests/support/boop_host_busted_helper.lua` passes before Busted runs, and both RED and GREEN commands use `--helper=tests/support/boop_host_busted_helper.lua` with repository-root environment variables.
    - `tests/boop_tick_spec.lua` no longer makes `readyBlockedStage` assign or clear `operation.timeoutTimer`; it resolves the operation's captured timer entry, fires that real callback while the named owner is active, and asserts `operation.timeoutTimer == false` plus `state.gold.pendingTimer == nil`.
    - The existing four gold stages × GMCP/room/pull/interrupt owner matrix asserts zero new gold sends, zero standard/rage sends, zero retry consumption, zero walk advancement, and unchanged generation/phase while at least one owner remains.
    - Final-owner release followed by one explicit normal `boop.tick()` produces exactly one `boop.flushPendingGold` call, one stage-appropriate queue send, unchanged retry counters, and one new timeout token that differs from the expired ID.
    - `tests/boop_gold_retry_spec.lua` covers both `pickup_pending` and `pack_pending`; each case invokes the matching callback under a hold, invokes it again as a duplicate, releases the owner, and proves one replacement send/timer with no retry mutation.
    - The existing old-generation timeout case additionally proves it cannot clear or replace the current generation's timeout token.
    - `tests/boop_walk_spec.lua` executes the real pickup timeout callback under a walk-affecting owner, observes zero `demonwalker.move`, releases the owner, runs the normal tick/resumed get/success/no-pack/terminal-tick path, and observes one reservation plus exactly one move event after running the guarded emitter.
    - No test obtains the desired state by manually assigning `timeoutTimer`, clearing `state.gold.operation`, or calling the walker emitter directly.
    - Every new/renamed RED case includes `timeout-under-owner` in its Busted test name. The focused pickup, packing, and walker cases respectively include the stable name fragments `timeout-under-owner pickup consumes fired token`, `timeout-under-owner packing consumes fired token`, and `timeout-under-owner recovery permits one guarded walker move`. Their exact pre-fix token assertions carry the custom failure marker `TIMEOUT_UNDER_OWNER_TOKEN_NOT_CONSUMED`.
    - The RED classifier runs the three stable cases as three separate Busted 2.3.0 invocations with the official JSON output handler, exact full `--name` selection, and only the containing spec file. For each invocation it requires a nonzero status, empty stderr, valid single-object JSON, exactly one selected record, zero successes, exactly one failure, zero errors, and zero pending records. The sole failure name must equal its allowlisted full case name and its failure message must contain `TIMEOUT_UNDER_OWNER_TOKEN_NOT_CONSUMED`; no other failure can coexist in any run. Syntax and release gates pass independently before RED classification.
    - This package-affecting RED commit advances all four synchronized version checkpoints from `0.1.414` to `0.1.415`; `python3 tools/check_release_gates.py` runs immediately before commit.
  </acceptance_criteria>
  <action>
Create `tests/support/boop_host_busted_helper.lua` as the repository-controlled focused-host bootstrap. Require `BOOP_REPO_ROOT`; define no-op/default host globals for `echo`, `getEpoch`, `killTimer`, `raiseEvent`, `send`, `sendGMCP`, and `tempTimer`, and set `cecho` absent so shipped fallback output paths remain deterministic. Define one local loader rooted at `BOOP_REPO_ROOT`, then load exactly: `boop_init.lua`, `boop_util.lua`, `boop_theme.lua`, `boop_skills.lua`, `boop_db.lua`, `boop_runtime.lua`, `boop_state.lua`, `boop_afflictions.lua`, `boop_rage.lua`, `boop_ih.lua`, `boop_targets.lua`, `boop_gag.lua`, `boop_attacks.lua`, `attacks/attack_profile_bootstrap.lua`, `attacks/occultist.lua`, `boop_safety.lua`, `boop_stats.lua`, `boop_walk.lua`, `boop_ui_registry.lua`, `boop_ui.lua`, and `boop_events.lua` in that order. Keep this helper diagnostic-only: do not load `boop_bootstrap.lua`, register it in a Muddler manifest, add package installation/update behavior, or reference temporary filesystem state.

Replace the manual timer-token clearing in `readyBlockedStage(stage, owner)` with lookup and invocation of the real callback whose ID equals the current operation's `timeoutTimer`. Start each stage through existing public gold entry points, add the unrelated owner, snapshot generation/phase/retries/send/event counts, fire the callback, and retain the existing two-owner release ordering. Per D-01 and D-02, assert that one remaining owner still prevents `flush_gold`; only after the final owner clears may one explicit normal tick resume the unchanged stage.

In `tests/boop_gold_retry_spec.lua`, table-drive pickup and packing applicability of the shared timeout helper. Capture exact timer IDs rather than array positions, prove callback duplication and stale generation are no-ops, and require a distinct replacement token after one resumed send. Preserve D-09 room/generation identity for pickup and D-10 inventory ownership for packing. Name the cases with the stable fragments `timeout-under-owner pickup consumes fired token` and `timeout-under-owner packing consumes fired token`, and attach `TIMEOUT_UNDER_OWNER_TOKEN_NOT_CONSUMED` only to the assertion that the fired current token is absent.

Rename the existing `boop_tick_spec.lua` owner/stage matrix cases with the same `timeout-under-owner` prefix so RED output identifies the exact regression path. In `tests/boop_walk_spec.lua`, add a case containing the stable name fragment `timeout-under-owner recovery permits one guarded walker move`, using the existing timer queue and walker fixture to reproduce the complete verified defect: current matching gold timeout → unrelated walk/gold hold → callback fires → final owner releases → normal tick resumes get → get success without pack terminates gold → existing terminal reevaluation reaches the normal no-target walk path → guarded emitter raises exactly one `demonwalker.move`. Attach the same custom marker only to this case's fired-token assertion. Per D-13, assert zero movement before gold terminal and do not bypass `boop.walk.maybeAdvance`.

Keep the three RED classifier names exact and unique: `boop generation-owned gold retry handling timeout-under-owner pickup consumes fired token`, `boop generation-owned gold retry handling timeout-under-owner packing consumes fired token`, and `boop walk integration timeout-under-owner recovery permits one guarded walker move`. Run each exact full name separately against only its containing spec with Busted's official JSON handler. Treat the structured arrays as the complete result set: each run must contain one record total, classified as one failure with zero successes/errors/pendings, and that sole failure must have the allowlisted exact name plus the intentional token marker in its message. Empty selection, extra records, fixture/bootstrap errors, an earlier unmarked assertion, and any stderr output must all make RED verification fail.

The tracked test bootstrap makes this task package-affecting under repository policy. Before the RED commit, synchronize `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint to `0.1.415`, inspect staged paths, and run the release gate. Commit the helper with the RED specs and synchronized checkpoints. Do not edit production behavior, generated build output, docs, or planning artifacts in this task.
  </action>
  <verify>
    <automated>test -f tests/support/boop_host_busted_helper.lua &amp;&amp; luac -p tests/support/boop_host_busted_helper.lua tests/boop_gold_retry_spec.lua tests/boop_tick_spec.lua tests/boop_walk_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; command -v busted &gt;/dev/null &amp;&amp; bash -o pipefail -c 'set -eu; red_dir=$(mktemp -d); trap "rm -f \"$red_dir\"/*.json \"$red_dir\"/*.err; rmdir \"$red_dir\" 2&gt;/dev/null || true" EXIT; red_py="import json,sys; d=json.load(open(sys.argv[1], encoding=\"utf-8\")); expected=sys.argv[2]; marker=sys.argv[3]; keys=(\"successes\",\"failures\",\"errors\",\"pendings\"); assert isinstance(d,dict); assert all(isinstance(d.get(k),list) for k in keys); assert len(d[\"successes\"]) == 0; assert len(d[\"failures\"]) == 1; assert len(d[\"errors\"]) == 0; assert len(d[\"pendings\"]) == 0; assert sum(len(d[k]) for k in keys) == 1; failure=d[\"failures\"][0]; assert failure.get(\"name\") == expected, failure.get(\"name\"); assert marker in str(failure.get(\"message\",\"\")), failure.get(\"message\")"; marker=TIMEOUT_UNDER_OWNER_TOKEN_NOT_CONSUMED; index=0; for entry in "boop generation-owned gold retry handling timeout-under-owner pickup consumes fired token|tests/boop_gold_retry_spec.lua" "boop generation-owned gold retry handling timeout-under-owner packing consumes fired token|tests/boop_gold_retry_spec.lua" "boop walk integration timeout-under-owner recovery permits one guarded walker move|tests/boop_walk_spec.lua"; do index=$((index + 1)); case_name=${entry%%|*}; spec=${entry#*|}; red_json="$red_dir/$index.json"; red_err="$red_dir/$index.err"; set +e; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --output=json --name="$case_name" "$spec" &gt;"$red_json" 2&gt;"$red_err"; red_status=$?; set -e; cat "$red_json"; if test -s "$red_err"; then cat "$red_err" &gt;&amp;2; exit 1; fi; test "$red_status" -ne 0; python3 -c "$red_py" "$red_json" "$case_name" "$marker"; done'</automated>
    <diagnostic_scope>The expected nonzero focused Busted result is RED evidence only. Host Busted is deterministic diagnostic feedback, not a real-Mudlet or final-CI substitute.</diagnostic_scope>
  </verify>
  <done>The exact timeout-under-owner defect is reproducible without manually clearing the timer token, and RED consists of exactly three independently classified, exact-name, intentional marker failures with zero Busted errors or pending/success records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume matching timeout authority before hold branching</name>
  <files>src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_events.lua` — modified `armGoldPendingTimeout`; also inspect adjacent `stopGoldPendingTimeout`, `boop.markGoldQueueIntent`, `queueGoldCommands`, `completeGoldOperation`, and `flushPendingGold` before editing.
    - `tests/support/boop_host_busted_helper.lua` — Task 1 tracked diagnostic bootstrap; verify its exact global stubs/load order and use it unchanged for GREEN.
    - `tests/boop_gold_retry_spec.lua` — Task 1 RED pickup/packing, duplicate, stale-generation, and replacement-token contracts.
    - `tests/boop_tick_spec.lua` — Task 1 real callback-under-owner four-stage/four-owner matrix.
    - `tests/boop_walk_spec.lua` — Task 1 eventual safe reservation/event regression.
    - `src/scripts/boop/boop_runtime.lua` — read-only `tickStep`/`applyEffects`; do not add a second recovery effect or special-case owner release.
    - `src/scripts/boop/boop_walk.lua` — read-only `goldPending`, evaluator, and emitter; do not weaken gold_pending or optional-package gates.
    - `mfile` — modified package version/title metadata; Task 1 synchronized value is `0.1.415`.
    - `src/scripts/boop/boop_init.lua` — modified runtime version; Task 1 synchronized value is `0.1.415`.
    - `CODEX.md` — modified checkpoint plus canonical build/real-Mudlet/final-CI policy.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01/D-02 aggregate all-clear, D-09/D-10 ownership staging, and D-13 walk all-clear.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md` — exact causal trace: matching callback retained expired authority before authorization.
  </read_first>
  <behavior>
    - "Only a callback matching the current generation, expected phase, and current timeout token can consume that token."
    - "Token consumption and derived-state synchronization happen before goldDispatchAuthorized."
    - "Held matching callbacks leave the operation active and otherwise unchanged; released operations resume through the existing tick/flush path."
    - "Authorized matching callbacks retain the existing pending_timeout terminal warning/trace path exactly once."
  </behavior>
  <acceptance_criteria>
    - Inside `armGoldPendingTimeout`'s callback, the current operation and exact `active.timeoutTimer == timerId` checks remain first; immediately after they pass, `active.timeoutTimer` becomes `false` and `boop.markGoldQueueIntent(active.packTarget)` synchronizes `state.gold.pendingTimer` before `goldDispatchAuthorized(active)` is evaluated.
    - If authorization is held, the callback returns with the same current operation generation, phase, blocker owner, room/inventory ownership, and retry counts; it emits zero sends, warnings, terminal traces, ticks, reservations, or movement events.
    - After all unrelated owners clear, the existing `tickStep` → `flush_gold` → `boop.flushPendingGold` → `queueGoldCommands` path emits the current stage once and arms one replacement timeout.
    - If the matching callback is authorized when it fires, the existing `pending_timeout` terminal path warns, traces, clears only the matching `gold:<generation>` owner, and schedules its existing generation-guarded reevaluation once.
    - A stale generation/phase/token callback and a duplicate invocation cannot consume a current replacement timer or mutate any current operation field.
    - `src/scripts/boop/boop_runtime.lua` and `src/scripts/boop/boop_walk.lua` remain byte-for-byte unchanged; owner/generation identity, get-confirm-put staging, wrong-room guards, and optional walker behavior remain unchanged.
    - The same repository-controlled host helper used for RED reports zero failures and zero errors for the named timeout-under-owner cases and then for all focused gold-retry/tick/walk cases; Lua syntax, release gates, and Muddler build pass.
    - This package-affecting GREEN commit advances all four synchronized version checkpoints from `0.1.415` to `0.1.416`; `python3 tools/check_release_gates.py` runs immediately before commit.
  </acceptance_criteria>
  <action>
Modify only the callback body inside existing `armGoldPendingTimeout(generation, expectedPhase)`. Preserve the current generation/phase lookup and exact timer-ID comparison. Once both match, consume the fired timer's authority by assigning the operation token to `false`, then immediately call `boop.markGoldQueueIntent(active.packTarget)` so the derived pending timer is also absent. Only after those two state updates may the callback call `goldDispatchAuthorized(active)` and choose the held return or existing authorized `pending_timeout` terminal branch.

Do not rearm from inside the held callback and do not add an owner-release hook: normal runtime reevaluation already owns one-stage resumption. Do not change `goldDispatchAuthorized`, `tickStep`, `applyEffects`, `completeGoldOperation`, `goldPending`, walk evaluation/emission, operation fields, phases, owner formats, room checks, retry limits, warning text, or optional package behavior. This ordering implements the recorded `no-change` assumption delta and preserves D-01/D-02, D-09/D-10, and D-13.

Run the focused GREEN suite, Lua syntax, Muddler, and release gates. Before commit, synchronize all four version checkpoints to `0.1.416`, inspect staged paths, and rerun `python3 tools/check_release_gates.py`. Do not edit generated build artifacts, commit CI evidence, push, or claim terminal CI.
  </action>
  <verify>
    <automated>test -f tests/support/boop_host_busted_helper.lua &amp;&amp; command -v busted &gt;/dev/null &amp;&amp; luac -p tests/support/boop_host_busted_helper.lua src/scripts/boop/boop_events.lua src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_walk.lua tests/boop_gold_retry_spec.lua tests/boop_tick_spec.lua tests/boop_walk_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --filter=timeout-under-owner tests/boop_gold_retry_spec.lua tests/boop_tick_spec.lua tests/boop_walk_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_gold_retry_spec.lua tests/boop_tick_spec.lua tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
    <diagnostic_scope>The host Busted run is deterministic focused evidence only. Muddler proves package construction, not real-Mudlet behavior.</diagnostic_scope>
  </verify>
  <done>The matching callback relinquishes expired timer authority before hold evaluation, one normal lifecycle recovery occurs after final-owner release, and the permanent gold_pending walk stall regression is green.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new production symbols, public APIs, operation fields, owner forms, phases, blocker codes, terminal reasons, dependencies, source files, commands, or events.
- New tracked `tests/support/boop_host_busted_helper.lua` provides the focused diagnostic host bootstrap; it stubs only required Mudlet globals, loads the existing focused package modules, and is never treated as real-Mudlet authority.
- Existing `armGoldPendingTimeout(generation, expectedPhase)` changes ordering only: exact matching timer-token consumption and derived-state synchronization precede authorization.
- Existing test files gain named deterministic regressions for pickup, packing, duplicate/stale callbacks, normal tick resumption, terminal recovery, and one safe walker event.
- Existing version checkpoints advance once per package-affecting task; generated Muddler output and CI evidence are not committed.

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Timing-sensitive paths cannot attack, loot, or walk while another safety/room owner controls the next action | 03-01–03-10 | COVERED | 03-10 closes the verified timeout-under-owner liveness hole without weakening fail-closed behavior. |
| REQ | SAFE-02 | Interrupt, pull, disabled, and manual holds prevent attacks until their exact release | 03-01, 03-02, 03-03, 03-08, 03-09 | COVERED | Existing completed plans remain unchanged. |
| REQ | SAFE-04 | Gold pickup/pack/retry/stale behavior cannot bypass holds or retain unrecoverable pending authority | 03-01, 03-04, 03-05, 03-08, 03-09, 03-10 | COVERED | 03-10 directly covers the verified expired timeout token. |
| REQ | WALK-01 | Walker start/stop/move/status/settlement/event coverage | 03-01, 03-06, 03-07, 03-09 | COVERED | Existing completed plans remain unchanged. |
| REQ | WALK-02 | Gold and every other unsafe state block movement without causing a permanent stall | 03-01, 03-05, 03-06, 03-07, 03-08, 03-09, 03-10 | COVERED | 03-10 proves zero movement while held and one safe event after gold terminal. |
| REQ | WALK-03 | Optional walker, explicit install/status, no silent update | 03-06, 03-07, 03-08, 03-09 | COVERED | 03-10 explicitly preserves this independent contract. |
| RESEARCH | — | Owner-keyed per-system all-clear | 03-01, 03-08, 03-10 | COVERED | Exact owner remains identity; held callback makes zero effects. |
| RESEARCH | — | Generation/phase/token ownership for asynchronous callbacks | 03-02–03-07, 03-09, 03-10 | COVERED | 03-10 consumes only a matching current token and tests stale/duplicate no-ops. |
| RESEARCH | — | Staged room-owned pickup and inventory-owned packing | 03-04, 03-05, 03-09, 03-10 | COVERED | Both phases use the corrected shared timeout helper without changing staging. |
| RESEARCH | — | One guarded walker evaluator/emitter and explicit optional boundary | 03-06, 03-07, 03-09, 03-10 | COVERED | Eventual-move test uses existing normal evaluator/emitter; source remains unchanged. |
| RESEARCH | — | Release gates, Muddler, real Mudlet/CI authority distinction | 03-09, 03-10 | COVERED | The tracked focused-host bootstrap is diagnostic; parent exact-final-HEAD CI remains authoritative. |
| CONTEXT | D-01, D-02 | Per-system all-clear and subsystem-specific release | 03-01, 03-08, 03-10 | COVERED | Held callback consumes no retries/effects; final owner release permits one normal recovery. |
| CONTEXT | D-03, D-04 | Primary/count, all-owner diagnostics, fixed priority | 03-01, 03-08, 03-09 | COVERED | Unchanged by gap repair. |
| CONTEXT | D-05–D-08 | Interrupt evidence, timeout isolation, repeat idempotency, first terminal | 03-02, 03-03, 03-09 | COVERED | Gold repair does not generalize or alter interrupt identity. |
| CONTEXT | D-09–D-12 | Room pickup, inventory pack, duplicate coalescing, settled evidence | 03-04, 03-05, 03-09, 03-10 | COVERED | Pickup/packing applicability and wrong-room protections remain explicit. |
| CONTEXT | D-13–D-16 | All-clear movement, current evidence, capped refresh, one move/room | 03-01, 03-06, 03-07, 03-09, 03-10 | COVERED | Walk is zero while gold is active and emits once only after terminal. |
| CONTEXT | D-17–D-20 | Safe/idempotent manual move and active-mode continuity | 03-06, 03-07 | COVERED | Unchanged by gap repair. |
| CONTEXT | D-21–D-24 | Owned stop/detach, invalidation, fresh restart, distinct output | 03-07, 03-08, 03-09 | COVERED | Unchanged by gap repair. |
| CONTEXT | Deferred | Infernal hyena maul summary and temporary/custom attacks | — | EXCLUDED | Explicitly Phase 05 / after Phase 04; absent from this plan. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mudlet timer callback → current gold operation | A fired, duplicated, or stale callback may claim authority over the wrong generation/phase/token. |
| Runtime owner release → gold queue effect | Clearing one owner must not dispatch while another owner remains, and final release must not strand recovery. |
| Gold terminal → walker effect | Movement must remain impossible until gold is terminal and the existing walker all-clear recheck succeeds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10-TOKEN | Tampering | `armGoldPendingTimeout` | high | mitigate | Compare generation/phase/token, consume matching token before authorization, sync derived state, and test pickup/packing exact counts. |
| T-03-10-STALE | Spoofing / Tampering | duplicate and old callbacks | high | mitigate | Duplicate and stale-generation tests prove zero current-token mutation, sends, retries, terminals, ticks, reservations, and events. |
| T-03-10-STALL | Denial of Service | gold tick/walk liveness | high | mitigate | Final-owner release plus normal tick must emit one unchanged stage; terminal tick then reaches one guarded walk event. |
| T-03-10-AUTH | Elevation of Privilege | exact-owner exclusion | medium | mitigate | Preserve `goldDispatchAuthorized` unchanged and prove every unrelated owner keeps both gold and walk fail closed. |
</threat_model>

<verification>
- RED: after the repository-tracked helper and focused specs pass Lua parsing, three separate exact-name Busted JSON invocations run pickup, packing, and walker recovery against only their containing spec. Every invocation must return nonzero with empty stderr and parse to exactly one selected record: zero successes, one failure, zero errors, zero pendings. The sole record must match its allowlisted full case name and carry `TIMEOUT_UNDER_OWNER_TOKEN_NOT_CONSUMED`; therefore no fourth or unrecognized failure can hide beside an intended RED.
- GREEN: the same repository-tracked helper runs the named cases and all three focused specs with zero failures/errors after Task 2. This is deterministic diagnostic evidence only and must not be described as real-Mudlet authority.
- `luac -p` passes for `boop_events.lua`, read-only `boop_runtime.lua`/`boop_walk.lua`, all three focused specs, and `boop_init.lua`.
- `python3 tools/check_release_gates.py` passes before each package-affecting commit and after the plan, with all four checkpoints synchronized first to `0.1.415` and then `0.1.416`.
- `muddle` builds the package successfully; generated output is neither edited nor committed.
- If `/tmp/Mudlet.AppImage` exists, run the canonical full suite: `muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`. If absent, record local real-Mudlet execution as unavailable; do not substitute host Busted.
- The gap executor does not push or claim terminal CI. After `$gsd-execute-phase 03 --gaps-only`, its SUMMARY/tracking/verification mutations, and every other repository mutation finish, the parent captures immutable `FINAL_SHA`, pushes it, and runs `tools/wait_for_exact_ci.sh "$FINAL_SHA"`. Exact-`headSha` successful `main.yml` evidence is reported but not committed; any later mutation invalidates it.
</verification>

<success_criteria>
- Every `missing` item in `03-VERIFICATION.md` maps to a Task 1 regression and Task 2 acceptance criterion.
- A matching fired token is absent before any held/authorized decision, and stale or duplicate callbacks cannot consume current authority.
- Pickup and packing remain fail closed under unrelated owners with zero sends, retries, reservations, and events.
- Final-owner release produces one existing lifecycle recovery effect, one same-stage send, and one replacement timer; no expired token survives.
- Gold terminal makes walk reachable through the existing normal evaluator/emitter exactly once, eliminating the permanent `gold_pending` stall without weakening D-09–D-16.
- No production symbol, identity model, command/help surface, dependency, optional walker contract, wrong-room guard, generated artifact, or deferred feature changes.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-10-SUMMARY.md` during execution closeout. The summary records focused diagnostic, syntax, release-gate, Muddler, and available local real-Mudlet evidence without claiming final CI authority. The parent owns immutable-final-HEAD push and `tools/wait_for_exact_ci.sh "$FINAL_SHA"` only after all GSD and repository mutations finish.
</output>
