---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "12"
type: execute
wave: 12
depends_on:
  - "03-11"
files_modified:
  - tests/boop_walk_spec.lua
  - tests/boop_event_transitions_spec.lua
  - src/scripts/boop/boop_walk.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - WALK-01
  - WALK-02
  - WALK-03
gap_closure: true
gap_ids: [G-03-1]
must_haves:
  truths:
    - "Real demonwalker.arrived is tokenless. Through boop.onWalkArrived(\"demonwalker.arrived\") it cannot validate a run/generation, create correlation evidence, start/reset an epoch, bind a List, mutate reservation state, clear an owner, settle, or emit movement."
    - "For an active incomplete epoch, tokenless arrival may only call the non-authorizing Plan 03-11 capped response-fence opener; inactive, complete, repeated, delayed, stop-era, and restart-era arrivals are otherwise zero-effect, and synthetic numeric arguments have no authority."
    - "Per D-13 through D-16, only a current Plan 03-11 Inv→Room response fence can complete the epoch and produce at most one reservation and one guarded demonwalker.move; pre-barrier room Lists and inventory barriers have zero walk effects."
    - "Per D-22 and D-23, stop/restart invalidates old callbacks and opens a fresh room epoch/fence without reusing accepted items, settlement, blockers, or reservation; old fence responses drain and only the new pair may settle even when the room ID is unchanged."
    - "A complete same-room epoch plus any pending/emitted reservation survives repeated tokenless arrival and same-room Room.Info with no fence refresh, rearm, second reservation, or second movement."
  artifacts:
    - path: "src/scripts/boop/boop_walk.lua"
      provides: "Tokenless non-authorizing arrival handling, fresh-start response-fence lifecycle, canonical settlement checks, and one-shot reservation continuity"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Real demonwalker.arrived adapter that discards event metadata instead of manufacturing optional generation tokens"
    - path: "tests/boop_walk_spec.lua"
      provides: "Real-adapter tokenless initial/repeated/delayed/stop/restart arrival, fresh response fence, reservation preservation, capped refresh, and exact event-count contracts"
    - path: "tests/boop_event_transitions_spec.lua"
      provides: "Tokenless arrival plus pre-barrier/inventory/accepted-room response ordering and stale cross-epoch adapter coverage"
  key_links:
    - from: "src/scripts/boop/boop_walk.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "start opens fresh_start and its response fence; onArrived may only request the current incomplete epoch's existing capped fence and never records evidence"
      pattern: "(onArrived|requestRoomItemsOnce|fresh_start)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_walk.lua"
      via: "onWalkArrived ignores every Mudlet argument and calls tokenless onArrived; actual Room.Info movement and accepted response-fenced room Lists remain the only room-change/settlement callbacks"
      pattern: "(onWalkArrived|onArrived|onRoomChange|onRoomSettled)"
    - from: "src/scripts/boop/boop_walk.lua"
      to: "demonwalker.move"
      via: "The existing reservation/emitter path rechecks run generation, room generation, reservation, canonical settlement, package availability, and aggregate all-clear"
      pattern: "(maybeAdvance|emitReservedMove|demonwalker.move)"
---

<objective>
Make real tokenless `demonwalker.arrived` strictly non-authorizing while preserving one-shot walker ownership, per D-13 through D-16 and D-22 through D-23.

Purpose: G-03-1 is not closed until the shared response-fenced room epoch works through the real optional-walker lifecycle without invented arrival tokens, metadata shortcuts, same-room rearming, stale callback authority, or double movement.
Output: RED real-adapter contracts, tokenless capped-refresh behavior, fresh restart fencing, invalidated-response drain, and exact one-shot release/move behavior.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-room-evidence-ordering.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-07-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-11-SUMMARY.md

<interfaces>
Interfaces produced by Plan 03-11 and consumed here:
- `boop.runtime.startRoomObservation(roomId, { boundary = "fresh_start" | "room_change" })` advances one canonical epoch and clears inherited evidence.
- `boop.requestRoomItemsOnce(reason)` records one serialized fence before sending Char.Items.Inv then Char.Items.Room.
- `boop.runtime.observeRoomInfo(...)`, `observeRoomItemsList(location, items)`, and `roomObservationSnapshot()` own room identity, fence queue/drain, copied accepted evidence, and transition results.
- A room List has authority only as the current valid queue-head response after its inventory barrier and while room ID/epoch remain unchanged.

Existing walker interfaces retained:
- `boop.walk.start(options)`, `stop(silent, external)`, `onRoomChange(runGeneration, roomGeneration)`, `onRoomSettled(reason, runGeneration, roomGeneration)`, `maybeAdvance(reason)`, and `move()`.
- `boop.onWalkArrived(...)` discards all event arguments and invokes zero-argument `boop.walk.onArrived()`. Arrival has no optional generation-token contract.
- `walk.generation`, `walk.roomGeneration`, `walk.reservationId`, `walk.moveQueued`, `walk.moveIssuedForRoomGeneration`, `walk.refreshTimer`, and `walk.emitterTimer` remain the callback/reservation authority.
- `emitReservedMove` remains the sole `demonwalker.move` side-effect path.
</interfaces>
</context>

<assumption_delta>
`changed`: Plan 03-07 treated every arrival/Room.Info as a reason to arm a fresh fallback, and the prior gap-plan draft assumed optional numeric arguments could validate arrival correlation. The real Mudlet event supplies only the event name, which `boop.onWalkArrived("demonwalker.arrived")` normalizes to no tokens. Arrival therefore has no run/room identity at all. It may request the current incomplete epoch's one response fence, but it cannot create authority, reset lifecycle state, or settle; safety comes from the fenced GMCP responses and final walker checks.
</assumption_delta>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tokenless-arrival and fenced-restart regressions</name>
  <files>tests/boop_walk_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_walk_spec.lua` — inspect `resetCase`, `seedReadyWalk`, start/owner tests, one-reservation test, stale emitter matrix, stop/restart, stale arrival callbacks, and capped refresh.
    - `tests/boop_event_transitions_spec.lua` — inspect walker owner routing, adapter metadata normalization, same-room preservation from Plan 03-11, and cross-lifecycle callback tests.
    - `src/scripts/boop/boop_walk.lua` — implementation under test: `currentRoomId`, `armArrivalFallback`, `start`, `onArrived`, `onRoomChange`, `onRoomSettled`, `maybeAdvance`, and reserved emission.
    - `src/scripts/boop/boop_events.lua` — implementation under test: real `onWalkArrived` adapter plus Plan 03-11 movement-first and response-fenced item callbacks.
    - `src/scripts/boop/boop_runtime.lua` — read-only Plan 03-11 epoch/fence queue APIs.
    - `tests/support/boop_test_helper.lua` — canonical epoch, timer queue, and external walker fixtures.
    - `mfile`, `src/scripts/boop/boop_init.lua`, `CODEX.md` — synchronized baseline `0.1.418`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-13, D-14, D-15, D-16, D-22, and D-23.
  </read_first>
  <behavior>
    - "boop.onWalkArrived(\"demonwalker.arrived\") has zero evidence, epoch, owner, settlement, reservation, and movement authority; it can only trigger the current incomplete epoch's one capped request pair."
    - "Public walk start opens a fresh epoch and records exactly Char.Items.Inv then Char.Items.Room; a pre-barrier room List and the inventory barrier cannot settle or reserve."
    - "The first accepted current-fence room response may complete one owner release, reservation, and guarded move; repeated tokenless arrival and duplicate/interleaved Lists cannot repeat them."
    - "Stop/restart in the same room rejects old emitter/refresh responses and delayed tokenless arrivals; old fence pairs drain and only the new pair can settle."
  </behavior>
  <action>
Add `arrival-tokenless-fence` cases to `tests/boop_event_transitions_spec.lua` and `tests/boop_walk_spec.lua`. Every arrival must enter through the real adapter call `boop.onWalkArrived("demonwalker.arrived")`; do not call `boop.walk.onArrived` directly and do not supply test generations as authority. Snapshot canonical observation/fence queue, target/gold/walk state, blockers, timers, sends, GMCP requests, and raised events before each call.

Drive `boop.walk.start()` through the public API with a normalized current Room.Info ID. Assert it opens a D-23 fresh-start epoch, clears accepted items/settlement without reusing persistent `gmcp.Char.Items.List`, and records exactly `{ "Char.Items.Inv", "Char.Items.Room" }`. Before the inventory response, deliver a complete stale room List and tokenless arrival; prove zero denizen/gold/owner/tick/reservation/move effects and no duplicate pair. Deliver the inventory response and prove only inventory/wield effects, then the current complete room response and prove one exact owner release, reservation, and guarded move.

Seed both an unreserved complete room and an already reserved/emitted room. Repeated tokenless same-room/search-target arrival and same-room Room.Info must leave observation generation/items/fence queue, denizens, room-owned work, moveQueued, reservationId, emitterTimer, owner, GMCP request count, and move count unchanged. Also call `boop.onWalkArrived(999, 999)` and prove numeric synthetic arguments are discarded rather than treated as a generation contract.

Extend stop/restart coverage using only real tokenless arrival adapter calls. Stop must invalidate callbacks before owner cleanup. Deliver a delayed `boop.onWalkArrived("demonwalker.arrived")` while stopped and prove zero mutation/request. Restart in the same room, deliver another delayed tokenless arrival plus the old fence's inv/room responses, and prove they cannot bind, release, reserve, or move. Only the new fence's inv then complete room response may produce one move. Invoke old and duplicate timer/emitter callbacks afterward and assert zero additional effects. Add a missing-barrier case where tokenless arrival causes at most the one current capped pair and the Plan 03-11 timeout leaves a visible room/walk blocker plus one warning without settlement.

Create exact RED cases `boop event-driven state transitions arrival-tokenless-fence requests no authorizing evidence`, `boop walk integration arrival-tokenless-fence preserves a complete same-room reservation`, and `boop walk integration arrival-tokenless-fence restart drains old responses before one move`. Put `ARRIVAL_TOKENLESS_FENCE_BROKEN` only on the pre-fix assertions. The RED classifier must select these three records, observe failures rather than Busted errors, and require the marker.

This RED commit is package-affecting. Synchronize all four version checkpoints from `0.1.418` to `0.1.419`, inspect staged paths, and run `python3 tools/check_release_gates.py` before committing `test(03-12): pin tokenless arrival fence regressions`. Do not modify production behavior, generated output, operator docs, or planning artifacts in this task.
  </action>
  <verify>
    <automated>luac -p tests/boop_walk_spec.lua tests/boop_event_transitions_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; bash -o pipefail -c 'set -eu; red_json=$(mktemp); red_err=$(mktemp); trap "rm -f \"$red_json\" \"$red_err\"" EXIT; set +e; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --output=json --filter="arrival%-tokenless%-fence" tests/boop_event_transitions_spec.lua tests/boop_walk_spec.lua &gt;"$red_json" 2&gt;"$red_err"; red_status=$?; set -e; test "$red_status" -ne 0; test ! -s "$red_err"; python3 -c "import json,sys; d=json.load(open(sys.argv[1], encoding=\"utf-8\")); f=d.get(\"failures\",[]); assert not d.get(\"errors\",[]); assert len(f) == 3; assert all(\"ARRIVAL_TOKENLESS_FENCE_BROKEN\" in str(x.get(\"message\",\"\")) for x in f)" "$red_json"'</automated>
    <diagnostic_scope>Expected nonzero Busted status is RED evidence only; host execution is not a substitute for Mudlet.</diagnostic_scope>
  </verify>
  <done>The walker-specific defect surface is pinned by three intentional RED cases covering real tokenless adapter behavior, exact Inv→Room requests, pre-barrier/inventory zero effects, repeated same-room preservation, and stop/restart response draining.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make tokenless arrival non-authorizing without rearming movement</name>
  <files>src/scripts/boop/boop_walk.lua, src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_walk.lua` — modify tokenless arrival/fresh-start/settlement logic only; preserve optional install, stop ownership, evaluator inventory, and reserved emitter.
    - `src/scripts/boop/boop_runtime.lua` — read-only Plan 03-11 response-fence and epoch interfaces.
    - `src/scripts/boop/boop_events.lua` — modify only the `onWalkArrived` argument boundary; preserve movement-first Room.Info and response-fenced List routing.
    - `tests/boop_walk_spec.lua`, `tests/boop_event_transitions_spec.lua` — Task 1 RED contracts.
    - `mfile`, `src/scripts/boop/boop_init.lua`, `CODEX.md` — Task 1 synchronized value `0.1.419`.
  </read_first>
  <behavior>
    - "onWalkArrived discards event-name and numeric arguments; zero-argument onArrived cannot validate or record run/room correlation."
    - "Active incomplete arrival may call only requestRoomItemsOnce; complete/inactive arrival is no-op, and no arrival path starts an epoch, resets reservation state, or stamps settlement."
    - "Fresh start opens an evidence-empty response-fenced epoch; actual room change synchronizes the walk only to the epoch created by the Room.Info adapter."
    - "Only an accepted current-fence room response can reach settlement; reservation and emission remain one-shot."
  </behavior>
  <action>
In `boop_events.lua`, change `boop.onWalkArrived(...)` to discard every argument—including Mudlet's event-name string and any synthetic numeric values—and call zero-argument `boop.walk.onArrived()`. Remove the optional arrival run/room token contract from this adapter; no caller can turn event metadata into production authority.

Refactor `boop.walk.onArrived` and remove the arrival rearming fallback. If walk is inactive, return false with no mutation. If active and the canonical epoch is incomplete, call only `boop.requestRoomItemsOnce("tokenless walker arrival awaiting room fence")`; the Plan 03-11 opener owns capping, request order, blocker, and warning. Return without calling `startRoomObservation`, reading persistent GMCP as new evidence, changing roomGeneration, clearing an emitter, resetting moveQueued/moveIssuedForRoomGeneration, recording correlation evidence, binding a List, reserving, or settling. If the epoch is complete, return true as a no-op with no request or lifecycle mutation.

In `boop.walk.start`, use Plan 03-11's explicit `fresh_start` boundary with the current normalized Room.Info ID, clear inherited accepted items/settlement, synchronize `walk.roomGeneration`, establish the exact walk owner, and immediately call the capped fence opener. Do not treat the currently merged `gmcp.Char.Items.List` as newly observed evidence. Preserve monotonic run/reservation IDs, owned-vs-attached behavior, and optional-package checks. The fresh run remains unsettled until the serialized current fence receives inv then complete room responses; old invalidated records drain with zero walk effects.

In `onRoomChange`, synchronize `arrivalRoom` and `roomGeneration` from the canonical actual-change epoch, invalidate only the current reservation/emitter for that movement, and rely on the already-opened Plan 03-11 fence instead of an arrival fallback. In `onRoomSettled`, snapshots/evaluator observed-room fields, and emitter checks, use canonical accepted observation identity rather than persistent GMCP as proof. Only the accepted-list applicator may call `onRoomSettled`; timers and tokenless arrivals never do. Keep `maybeAdvance` and `emitReservedMove` as the sole reservation/event path.

Run named and full focused walker/event specs, syntax, release gates, and Muddler. Synchronize all four version checkpoints from `0.1.419` to `0.1.420`; inspect staged paths and run the release gate before committing `fix(03-12): make tokenless arrival non-authorizing`. Do not change install/update behavior, route selection, command surfaces, generated output, push state, or CI evidence.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_walk.lua src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua tests/boop_walk_spec.lua tests/boop_event_transitions_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --filter="arrival%-tokenless%-fence" tests/boop_event_transitions_spec.lua tests/boop_walk_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_walk_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
    <diagnostic_scope>Host Busted and Muddler are focused diagnostics/build evidence; real-Mudlet and final CI authority remain separate.</diagnostic_scope>
  </verify>
  <done>Real tokenless arrival has no authority beyond requesting one incomplete-epoch fence, repeated/non-movement arrival preserves reservations, and fresh restart drains old responses before new fenced evidence permits one guarded move.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Timing-sensitive holds and room state prevent unsafe movement | 03-11–03-13 | COVERED | This plan closes the external-arrival and one-shot movement portion. |
| REQ | SAFE-02 | Interrupt/pull/manual holds prevent attacks | 03-01–03-10 | COVERED | Unchanged and regression-run through aggregate walk tests. |
| REQ | SAFE-04 | Gold remains room-safe | 03-11, 03-13 | COVERED | Walker does not reinterpret ambiguous gold evidence. |
| REQ | WALK-01 | Start/stop/move/settlement/event coverage | 03-12 | COVERED | Real tokenless adapter, fresh fence, stop/restart, and one move are direct task scope. |
| REQ | WALK-02 | Every unsafe state blocks movement without permanent stall | 03-11–03-13 | COVERED | Only the current Inv→Room response pair reaches one move; missing responses remain visibly held. |
| REQ | WALK-03 | Optional external walker, explicit install, no silent update | 03-12 | COVERED | Existing install/status behavior is preserved and focused specs run. |
| RESEARCH | — | Owner-keyed all-clear and generation-owned callbacks | 03-01–03-10, 03-12 | COVERED | Current run/room/reservation checks remain authoritative. |
| RESEARCH | — | Shared room observation and capped refresh | 03-11, 03-12 | COVERED | Arrival contributes no correlation evidence; it may only request the existing capped fence. |
| RESEARCH | — | Two-stage gold ownership | 03-11, 03-13 | COVERED | No walker shortcut bypasses gold. |
| RESEARCH | — | One walker evaluator/emitter and explicit optional boundary | 03-12 | COVERED | Direct task scope. |
| RESEARCH | — | Existing stack/no package installs and CI authority distinction | 03-11–03-13 | COVERED | No package-manager task; parent retains exact-HEAD authority. |
| CONTEXT | D-01, D-02 | Aggregate holds and subsystem release | 03-01–03-10 | COVERED | Preserved. |
| CONTEXT | D-03, D-04 | Primary blocker/count and deterministic priority | 03-01, 03-08, 03-09 | COVERED | Preserved. |
| CONTEXT | D-05, D-06, D-07, D-08 | Interrupt lifecycle contracts | 03-02, 03-03, 03-09 | COVERED | Preserved. |
| CONTEXT | D-09, D-10, D-11, D-12 | Gold room/inventory ownership and evidence | 03-11, 03-13 | COVERED | Preserved by walker all-clear. |
| CONTEXT | D-13, D-14, D-15, D-16 | Safe move, two-part settlement, capped refresh, one move | 03-11, 03-12 | COVERED | The response fence supplies two-part evidence; tokenless arrival cannot bypass it. |
| CONTEXT | D-17, D-18, D-19, D-20 | Safe/idempotent manual move | 03-06–03-09, 03-12 | COVERED | Existing evaluator remains shared. |
| CONTEXT | D-21, D-22, D-23, D-24 | Stop/detach, invalidation, fresh restart, output | 03-07–03-09, 03-12 | COVERED | Restart/stale callbacks are direct task scope; ownership output is unchanged. |
| CONTEXT | Deferred | Hyena-maul summary and temporary/custom attacks | — | EXCLUDED | Outside Phase 03. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External tokenless `demonwalker.arrived` → boop refresh request | Event-name, delayed, and synthetic arguments must not become run identity, correlation evidence, room evidence, or settlement. |
| Invalidated old fence responses → fresh restart epoch | Prior inv/room responses must drain without releasing the new walk owner or reserving movement. |
| Canonical room epoch → walker reservation | Only complete current evidence may clear the exact owner and reserve movement. |
| Deferred emitter → external `demonwalker.move` | Old run/room/reservation callbacks must not emit after stop, restart, rearm, or package loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12-ARRIVE | Spoofing | `boop.onWalkArrived`, `boop.walk.onArrived` | high | mitigate | Discard all event arguments, remove synthetic token authority, and permit only one non-authorizing current-epoch fence request. |
| T-03-12-REARM | Tampering / Denial of Service | tokenless arrival path | high | mitigate | Preserve complete epoch/reservation and send no refresh on repeated/non-movement arrival. |
| T-03-12-STALE | Tampering | stop/restart callbacks and old response pairs | high | mitigate | Fresh epoch, drain-only old fences, and run/room/reservation comparisons make delayed arrival/responses/callbacks zero-effect. |
| T-03-12-DOUBLE | Denial of Service | reservation/emitter | medium | mitigate | Both-order and duplicate tests assert one reservation and one event. |
| T-03-12-OPTIONAL | Tampering | optional package boundary | low | accept | Existing explicit install/status and final availability recheck remain unchanged; no new package action is introduced. |
</threat_model>

<verification>
- Task 1 produces exactly three intentional `ARRIVAL_TOKENLESS_FENCE_BROKEN` RED failures and no Busted errors.
- Task 2 passes named and complete focused walker/event specs with exact event/timer/reservation assertions.
- Lua syntax, release gates, and Muddler pass with synchronized version `0.1.420`.
- If the Mudlet AppImage exists, run the canonical full suite; otherwise report it unavailable without elevating host Busted.
- Each package-affecting task commits only after staged-path classification, synchronized version fields, and the release gate. The executor does not push.
</verification>

<success_criteria>
- G-03-1 settles only through a current Inv→Room response fence and fails closed before, outside, or across one.
- Real tokenless `demonwalker.arrived` has no correlation, generation, room-evidence, reset, settlement, or reservation authority.
- Same-room and repeated arrival preserve settled state and reservations.
- Fresh restart requires a new response fence; stale tokenless arrivals, old fence responses, and old callbacks cannot mutate or move.
- Movement releases/reserves/emits exactly once through the existing guarded path.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-12-SUMMARY.md` during execution closeout. Record focused diagnostics, version commits, and available real-Mudlet evidence. Do not push; the parent runs exact-final-HEAD CI only after all gap and tracking mutations finish.
</output>
