---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "08"
type: execute
wave: 8
depends_on:
  - "03-07"
files_modified:
  - tests/boop_trace_spec.lua
  - tests/boop_ui_spec.lua
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_ui_registry.lua
  - tests/boop_prequeue_spec.lua
  - tests/boop_tick_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "Per D-03, normal surfaces show deterministic primary code/label plus count; trace/debug show every sorted owner and affected system."
    - "Walker status/trace coverage obtains `walk_room_unsettled`, `walk_move_pending`, and `walker_unavailable` from real start/room/reservation/package-loss lifecycle entry points."
    - "Per D-01 and D-02, clearing one lifecycle never authorizes attack/prequeue while another combat/queue owner remains."
    - "Per D-06 and D-08, interrupt timeout and final-owner release preserve aggregate prequeue authorization, and a fresh post-timeout interrupt creates a new generation without altering ADDCLEARFULL dispatch."
    - "SAFE-02 disabled and manual-targeting states produce zero attack/prequeue effects until the existing explicit release action."
    - "In-game interrupt, gold, settlement, and walk help independently states the implemented ownership/timing contract and contains no stale contradictory wording."
  artifacts:
    - path: "src/scripts/boop/boop_ui.lua"
      provides: "Primary-plus-count normal rendering and complete all-owner diagnostics"
    - path: "src/scripts/boop/boop_ui_registry.lua"
      provides: "Topic-specific interrupt, gold, room settlement, walker stop/detach, and explicit install help"
    - path: "tests/boop_ui_spec.lua"
      provides: "Cross-surface deterministic rendering coverage"
    - path: "tests/boop_trace_spec.lua"
      provides: "All-owner order/promotion/exit trace coverage"
    - path: "tests/boop_prequeue_spec.lua"
      provides: "Aggregate owner, interrupt-timeout boundary, captured-callback, and SAFE-02 manual-hold prequeue coverage"
    - path: "tests/boop_tick_spec.lua"
      provides: "Aggregate owner and SAFE-02 manual-hold attack coverage"
  key_links:
    - from: "src/scripts/boop/boop_ui.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "normal surfaces consume blockerSnapshot; full diagnostics consume blockersSnapshot"
      pattern: "(blockerSnapshot|blockersSnapshot)"
    - from: "tests/boop_prequeue_spec.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "final side-effect checks aggregate every owner across interrupt timeout, both clear orders, and captured prequeue callbacks"
      pattern: "(shouldHold|completeInterrupt)"
---

<objective>
Implement D-03 operator rendering, synchronize in-game help, and prove aggregate attack/prequeue behavior across every completed lifecycle, including interrupt timeout and fresh post-timeout dispatch.

Purpose: Operators need compact primary state plus complete diagnostics, while final command boundaries must retain every unrelated hold and SAFE-02 manual gate through timeout and owner-clear races.
Output: Canonical UI rendering, independently asserted help topics, paired-owner/interrupt-timeout regressions, and disabled/manual-targeting release coverage.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@UIDESIGN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-02-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-03-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-07-SUMMARY.md

<interfaces>
Created symbols and values — exhaustive:
- New local UI function `allBlockerDetails()`.
- New local UI function `echoAllBlockerDetails(details)`.
- New debug section heading value `ALL ACTIVE BLOCKER OWNERS`.
- Exact normal rendering suffix value ` | +N more`, appended to existing `code -- label` when `additionalCount > 0`.

Existing symbols modified, not created:
- `currentBlockerDetails`, `echoBlockerDetails`, `printBlockerDetailRows`, `boop.ui.statusLine`, `boop.ui.status`, and `boop.ui.debug`.
- Existing help topic records with keys `interrupts`, `loot`, and `party`; no help topic, command, or alias is created.
- Existing tests/spec helpers only; no new runtime state, owner form, blocker code, completion reason, timer/retry field, or public API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin primary-plus-count and complete owner rendering</name>
  <files>tests/boop_trace_spec.lua, tests/boop_ui_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_trace_spec.lua` — modified owner ordering/promotion/transition trace coverage.
    - `tests/boop_ui_spec.lua` — modified cross-surface compact/full rendering and real walker lifecycle coverage.
    - `src/scripts/boop/boop_ui.lua` — compact and diagnostic blocker renderers under test.
    - `src/scripts/boop/boop_runtime.lua` — primary/all snapshots and deterministic owner priority under test.
    - `src/scripts/boop/boop_walk.lua` — real start, room, settlement, reservation, and package-loss entry points used to produce walker owner states.
    - `src/scripts/boop/boop_events.lua` — real Room.Info/List walker lifecycle adapters used by integration assertions.
    - `tests/support/boop_test_helper.lua` — deterministic walker/package/timer and owner fixtures.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01 through D-04 and walker package-loss discretion.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — rows 03-W0-01/04/05 and walker owner-transition rendering checks.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Stable blocker rendering pattern`, `tests/boop_trace_spec.lua`, `tests/boop_ui_spec.lua`, `Walker boundary`, and `Status, warnings, and trace`.
  </read_first>
  <behavior>
    - "Status/home/control/config/party/compact debug show one primary plus exact count."
    - "Full debug/trace list every owner in priority/code/owner order."
    - "Non-primary release decrements count; primary release promotes the deterministic next owner."
    - "Systems/waits and plain text remain stable."
    - "Walker blocker rows and traces are produced by real lifecycle transitions, not only synthetic owner seeding."
  </behavior>
  <acceptance_criteria>
    - Status/home/control/config/party/compact-debug surfaces render exactly one `code -- label | +N more` primary row, with `N == active owner count - 1`.
    - Full debug under heading `ALL ACTIVE BLOCKER OWNERS` and trace list every owner in priority/code/owner order with stable `systems:` and `waits:` fragments.
    - Releasing a non-primary owner decrements the suffix once; releasing the primary promotes the deterministic next owner and emits one owner-specific exit transition.
    - Tests invoke actual walker start/room-change before complete items to produce `walk_room_unsettled`, actual reservation creation to produce `walk_move_pending`, and actual mid-run package loss to produce `walker_unavailable`, then assert each status/trace code and exact `walk:<generation>` owner.
    - Synthetic owner seeding is used only for generic multi-priority combinations and is not the sole proof for any walker code.
    - One patch increment synchronizes all four version checkpoints, and focused Lua syntax plus release gates succeed.
  </acceptance_criteria>
  <action>
Seed at least three owners spanning missing-state, interrupt/pull, and gold/walk priority groups for generic priority combinations. Extend existing cross-surface assertions for exact `code -- label | +N more`, sorted `systems:`/`waits:`, the exact debug heading, owner order, primary promotion, non-primary decrement, and owner-specific transition trace counts. Separately invoke actual walker lifecycle entry points: start/current Room.Info without complete items for `walk_room_unsettled`, current settlement plus movement reservation for `walk_move_pending`, and mid-run package loss for `walker_unavailable`. Assert status and trace consume those real `walk:<generation>` transitions; synthetic seeding must not be the sole walker-integration proof. Distinguish the compact debug row from the complete owner section. Synchronize the next patch version before commit; do not edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_trace_spec.lua tests/boop_ui_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>D-03 rendering is RED across every compact and diagnostic surface with exact stable output.</done>
</task>

<task type="auto">
  <name>Task 2: Render canonical owner lists and synchronize each help topic</name>
  <files>src/scripts/boop/boop_ui.lua, src/scripts/boop/boop_ui_registry.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_ui.lua` — modified primary/all owner renderers and cross-surface status/debug output.
    - `src/scripts/boop/boop_ui_registry.lua` — modified existing `interrupts`, `loot`, and `party` help records.
    - `tests/boop_trace_spec.lua` — Task 1 exact owner/order/transition output contracts.
    - `tests/boop_ui_spec.lua` — Task 1 cross-surface and real walker lifecycle output contracts.
    - `src/scripts/boop/boop_runtime.lua` — canonical `blockerSnapshot()` and `blockersSnapshot()` sources.
    - `src/scripts/boop/boop_walk.lua` — shipped walker ownership, evidence, stop/detach, and explicit-install semantics reflected in help.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-03, interrupt/gold/settlement/walker decisions, and WALK-03 discretion.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — help/UI synchronization and walker owner-transition expectations.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Stable blocker rendering pattern`, `src/scripts/boop/boop_ui_registry.lua`, `Operator documentation files`, and `Status, warnings, and trace`.
  </read_first>
  <behavior>
    - "Normal UI consumes the primary snapshot; full debug consumes all owners."
    - "Interrupt help states operation ownership and repeat idempotency."
    - "Gold help states get-confirm-put and no attack chaining."
    - "Walk help states current room evidence, owned stop/attached detach, and explicit install."
  </behavior>
  <acceptance_criteria>
    - Normal renderers consume only `boop.runtime.blockerSnapshot()` and append exact suffix ` | +N more` only when `additionalCount > 0`.
    - Full debug consumes `boop.runtime.blockersSnapshot()` and prints the exact `ALL ACTIVE BLOCKER OWNERS` heading plus every sorted owner.
    - Existing help topics contain each of the five exact positive sentences listed in `<action>` and retain existing parser grammar/topic keys.
    - The five stale statements rejected by `<verify>` are absent from the registry.
    - No command, alias, help topic, runtime owner, blocker code, or public API is created by this task.
    - Focused UI/trace specs, every positive/negative text assertion, Lua syntax, and the release gate succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Add only the two local UI functions and two exact text values in `<interfaces>`. Modify the listed existing renderers to consume runtime snapshots rather than rebuilding interrupt/pull/gold/room/walk blockers from lifecycle booleans. In existing help topic records, add the exact sentences `Pending interrupts are operation-owned; repeats do not resend or restart the timer.`, `Gold is get-confirm-put: pickup, packing, and attacks are separate queued effects.`, `Movement requires current Room.Info plus a complete current room item list; prompts and timers never settle the room.`, `Walk stop ends a boop-owned run or detaches from an externally owned run.`, and `Walker install is explicit; status, start, move, and package-loss paths never install or update it.` Preserve parser grammar and workflow-first topic shape; create no commands or topics. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_ui.lua src/scripts/boop/boop_ui_registry.lua tests/boop_trace_spec.lua tests/boop_ui_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; rg -F "Pending interrupts are operation-owned; repeats do not resend or restart the timer." src/scripts/boop/boop_ui_registry.lua &amp;&amp; rg -F "Gold is get-confirm-put: pickup, packing, and attacks are separate queued effects." src/scripts/boop/boop_ui_registry.lua &amp;&amp; rg -F "Movement requires current Room.Info plus a complete current room item list; prompts and timers never settle the room." src/scripts/boop/boop_ui_registry.lua &amp;&amp; rg -F "Walk stop ends a boop-owned run or detaches from an externally owned run." src/scripts/boop/boop_ui_registry.lua &amp;&amp; rg -F "Walker install is explicit; status, start, move, and package-loss paths never install or update it." src/scripts/boop/boop_ui_registry.lua &amp;&amp; ! rg -F "If the expected confirmation line is missed, the timeout releases the attack hold." src/scripts/boop/boop_ui_registry.lua &amp;&amp; ! rg -F "With queueing on, pickup is prepended to the next queued standard attack; otherwise boop falls back to the game-side freestand queue." src/scripts/boop/boop_ui_registry.lua &amp;&amp; ! rg -F "prompt marks the room settled" src/scripts/boop/boop_ui_registry.lua &amp;&amp; ! rg -F "timer marks the room settled" src/scripts/boop/boop_ui_registry.lua &amp;&amp; ! rg -F "boop walk stop always stops the external run" src/scripts/boop/boop_ui_registry.lua</automated>
  </verify>
  <done>Canonical UI rendering and each affected in-game help topic independently pass positive and stale-wording assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Prove aggregate attack/prequeue and SAFE-02 manual holds</name>
  <files>tests/boop_prequeue_spec.lua, tests/boop_tick_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_prequeue_spec.lua` — modified paired-owner clear-order, interrupt-timeout, captured callback, disabled, and manual-targeting coverage.
    - `tests/boop_tick_spec.lua` — modified paired-owner combat-effect and explicit release coverage.
    - `src/scripts/boop/boop_runtime.lua` — aggregate owner hold checks and tick/effect ordering under test.
    - `src/scripts/boop/boop_events.lua` — prequeue callback and real lifecycle terminal/release paths under test.
    - `src/scripts/boop/boop_ui.lua` — fresh interrupt dispatch plus existing `setEnabled` and `setTargetingMode` release actions.
    - `tests/boop_interrupt_spec.lua` — read-only command/timer generation and timeout callback contract established by Plan 03-02.
    - `tests/support/boop_test_helper.lua` — exact-owner and automation-intent fixtures.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, D-06, D-08, and SAFE-02 manual-hold interpretation.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — rows 03-W0-02/06, interrupt owner ordering, and manual attack hold ordering.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Interrupt command seam`, `Coordinator hold-before-effect pattern`, `tests/boop_prequeue_spec.lua`, `tests/boop_tick_spec.lua`, `Ownership identity`, and `Prove cleanup/authorization precedes the first side effect`.
  </read_first>
  <behavior>
    - "Interrupt+gold, pull+room, and gold+walk pairs stay at zero after either owner clears."
    - "Interrupt timeout and either owner-clear order keep prequeue at zero until the final affecting owner releases."
    - "Captured callbacks recheck aggregate owners."
    - "Final-owner release permits one reevaluation, and a fresh post-timeout interrupt retains ADDCLEARFULL behavior with a new generation and timer."
    - "Disabled state resumes only through setEnabled(true)."
    - "Manual targeting resumes only through setTargetingMode('auto')."
  </behavior>
  <acceptance_criteria>
    - Interrupt+gold, pull+room, and gold+walk pairs are cleared in both orders; after the first clear, standard/rage sends, alias changes, prequeue timers, and ticks remain exactly zero and the other owner remains present.
    - Interrupt-first and unrelated-owner-first clear orders both keep standard/rage sends, alias changes, prequeue scheduling, and captured callback execution at zero while one combat/queue owner remains.
    - A prequeue callback captured before an owner is added rechecks aggregate ownership at invocation and consumes no queued state while held.
    - Clearing the final affecting owner permits exactly one normal reevaluation, with no duplicate timer or tick.
    - After interrupt timeout and final unrelated-owner release, a fresh interrupt uses the unchanged `queue addclearfull freestand <command>` dispatch and creates exactly one new generation and timer.
    - `setEnabled(false)` produces zero tick/captured-prequeue effects until existing `setEnabled(true)` runs, then permits exactly one fresh evaluation.
    - `setTargetingMode("manual")` produces zero tick/captured-prequeue effects until existing `setTargetingMode("auto")` runs, then permits exactly one fresh evaluation.
    - No hold command, hold state field, alias, help row, or release API is created.
    - Focused prequeue/tick specs, Lua syntax, and the release gate succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Table-drive paired real lifecycle owner records, including interrupt plus an unrelated combat/queue owner, clear in both orders, and assert exact standard/rage sends, alias changes, prequeue timers, ticks, and retained owners. Exercise the real interrupt timeout callback while the unrelated owner remains and prove timeout clears only `interrupt:<generation>` with zero prequeue scheduling/execution. Capture a prequeue callback before adding an owner and prove the callback becomes a no-op at invocation without consuming queued state. After timeout and final unrelated-owner release, assert exactly one normal reevaluation with no duplicate timer/tick, then dispatch a fresh interrupt and assert unchanged `queue addclearfull freestand <command>` text plus exactly one new generation and timer.

Preserve SAFE-02's resolved manual tests: call existing `boop.ui.setEnabled(false)`, assert zero tick/captured-prequeue effects until `setEnabled(true)`, then one fresh evaluation; repeat with existing `setTargetingMode("manual")` and release only through `setTargetingMode("auto")`. Create no hold alias/state/help entry. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p tests/boop_prequeue_spec.lua tests/boop_tick_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run interrupt, diagnose, diagnose-timeout, prequeue, tick, UI, and trace specs plus the full real-Mudlet suite, or require exact corresponding GitHub Actions evidence.</authoritative>
  </verify>
  <done>D-01/D-02/D-06/D-08 and SAFE-02 are proven at final attack/prequeue boundaries, including interrupt timeout, captured callbacks, final-owner release, fresh post-timeout ADDCLEARFULL dispatch, and disabled/manual-targeting release actions.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol/value list; no other UI function, state field, owner, reason code, command, or help topic is created.
- Existing help entries and specs are modified in place; no built artifact is edited.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime state → operator output/help | Omitted or stale wording can misrepresent why work is held. |
| Owner release → attack/prequeue | One lifecycle ending can authorize work blocked by another. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-UI-OMIT | Repudiation | status/debug/help | medium | mitigate | Canonical snapshots and per-topic positive/negative assertions. |
| T-03-CROSS-RELEASE | Tampering | tick/prequeue | high | mitigate | Both-clear-order and final pre-effect checks. |
| T-03-INT-PREQUEUE | Tampering | interrupt timeout → prequeue boundary | high | mitigate | Timeout/final-owner clear-order, captured-callback, exact zero-effect, and fresh-generation ADDCLEARFULL assertions in `tests/boop_prequeue_spec.lua`. |
</threat_model>

<verification>
- Every help topic has its own positive assertion and stale semantic negatives.
- SAFE-02 disabled/manual-targeting tests remain explicit.
- Interrupt timeout, both owner-clear orders, captured callbacks, final-owner release, and fresh post-timeout ADDCLEARFULL dispatch remain explicit in the aggregate prequeue task.
- Real Mudlet/CI remains authoritative for package behavior.
</verification>

<success_criteria>
- D-03 is implemented across all normal/diagnostic surfaces.
- D-01/D-02/D-06/D-08 remain true at final attack/prequeue boundaries.
- In-game help matches interrupt, gold, settlement, and walker contracts without stale contradictions.
- Version metadata is synchronized for every commit and built artifacts remain untouched.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-08-SUMMARY.md` when done.
</output>
