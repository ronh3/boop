---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 18
type: execute
wave: 18
depends_on: [03-17]
files_modified:
  - tests/boop_trace_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_bootstrap.lua
  - src/scripts/boop/boop_util.lua
  - src/scripts/boop/boop_ui.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements: [SAFE-02, SAFE-04, WALK-01]
gap_closure: true
gap_ids: [G-03-6]
must_haves:
  truths:
    - "Live trace streaming is off by default and resets off at every package-load boundary, including same-session reloads whose bootstrap lifecycle returns early, without clearing the existing trace buffer."
    - "Live mode is session-only and never calls the config persistence path."
    - "Live mode does not independently enable collection: with collection off, trace attempts append nothing and print nothing."
    - "With collection and live mode on, each newly appended timestamped entry prints exactly once through a non-recursive output path."
    - "Buffer trimming, `trace show`, and `trace clear` behavior remain unchanged."
  artifacts:
    - "tests/boop_trace_spec.lua specifies state reset, persistence exclusion, exact-once emission, and buffer/show/clear parity."
    - "src/scripts/boop/boop_runtime.lua defines the session live-state default, and src/scripts/boop/boop_bootstrap.lua resets it at the unconditional package-load boundary."
    - "src/scripts/boop/boop_util.lua emits only after an accepted append."
    - "src/scripts/boop/boop_ui.lua exposes the in-memory live-mode command contract."
  key_links:
    - "Top-level `boop_bootstrap.lua` execution -> `state.trace.live = false` before `boop.bootstrap()` can return early, while retaining the existing trace table and buffer."
    - "`boop.util.trace.log` -> collection gate -> append/trim -> direct non-tracing INFO emission."
    - "`boop.ui.traceCommand('live', on|off)` -> runtime state only, never `saveConfigValue`."
---

<objective>
Close the core of G-03-6 by implementing session-scoped, exact-once live trace semantics behind the command API.

Purpose: Give operators an opt-in stream without changing trace collection, persistence, or buffer behavior.
Output: A focused trace contract suite plus runtime initialization, non-recursive emission, and UI command logic.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@DESIGN.md
@UIDESIGN.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_state.lua
@src/scripts/boop/boop_bootstrap.lua
@src/scripts/boop/boop_util.lua
@src/scripts/boop/boop_ui.lua
@tests/boop_trace_spec.lua
</context>

<scope_exception>
The trace contract is split between Plans 03-18 and 03-19 because synchronized version metadata leaves room for at most two behavior files in each package-affecting task. Plan 03-18 establishes state/emission/command behavior; Plan 03-19 wires the alias and operator documentation.
</scope_exception>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify session-only exact-once live trace behavior</name>
  <files>tests/boop_trace_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - tests/boop_trace_spec.lua
    - tests/support/boop_host_busted_helper.lua
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_state.lua
    - src/scripts/boop/boop_util.lua
    - src/scripts/boop/boop_ui.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Fresh runtime state has `trace.live == false`."
    - "The regression named `G-03-6 package reload reset executes before bootstrap early return` sets `boop.bootstrapped == true`, `trace.live == true`, and a pre-existing trace buffer, executes the real `src/scripts/boop/boop_bootstrap.lua` seam, then proves live is false while the same buffer table and entries remain intact."
    - "`traceCommand('live', 'on')` and `off` toggle runtime state and emit status, but never invoke config/database save and never change `traceEnabled`."
    - "Collection off plus live on produces no appended entry and no live output."
    - "Collection on plus live on appends a timestamped line and prints that exact appended line once; two log calls print two corresponding entries with no duplicates."
    - "Collection on plus live off appends normally without live output."
    - "`show` renders the existing buffer contract, `clear` clears the buffer without toggling live, and live output does not recursively append itself."
    - "Bare trace status reports collection and live modes; invalid live values show updated usage."
  </behavior>
  <action>
Extend `tests/boop_trace_spec.lua` with one cohesive G-03-6 contract. Capture config-save calls, trace buffer state, and INFO output separately. Compare each live output's timestamped payload directly to the newly appended buffer entry so formatting and exact-once behavior are proven without depending on the current clock. Assert buffer length after emission to catch recursion.

Add a same-session package-reload regression that uses `BOOP_REPO_ROOT` to execute the actual `src/scripts/boop/boop_bootstrap.lua` file, not a direct `boop.state.init()` substitute. Seed a trace buffer with sentinel entries, retain its table identity, set live on and `boop.bootstrapped` true, execute the seam, and assert live is off while both the original table identity and its ordered contents are unchanged. Save and restore the prior `boop.bootstrapped` value so the lifecycle fixture cannot leak into later examples.

Keep D-03's detailed trace diagnostics while preserving D-01/D-02 runtime ownership isolation and SAFE-02 default-safety expectations. The test must prove live mode is not a config field and cannot activate collection.

Before committing, bump all synchronized package fields from 0.1.434 to 0.1.435. Stage only the four listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `test(03-18): specify live trace session semantics`. Do not push.
  </action>
  <acceptance_criteria>
    - "The suite fails against the pre-feature implementation for absent live state/commands, not because of harness errors."
    - "Reset, non-persistence, collection independence, exact-once output, and buffer parity all have direct assertions."
    - "All synchronized version fields read 0.1.435 and release gates pass."
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'output=$(env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_trace_spec.lua 2&gt;&amp;1); rc=$?; printf "%s\n" "$output"; test "$rc" -ne 0; printf "%s\n" "$output" | rg -q "G-03-6|trace live"; python3 tools/check_release_gates.py'</automated>
  </verify>
  <done>The red trace suite precisely defines the session lifecycle and emission contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Initialize and unconditionally reset live trace state without losing the buffer</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_bootstrap.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_state.lua
    - src/scripts/boop/boop_bootstrap.lua
    - src/scripts/boop/scripts.json
    - tests/boop_trace_spec.lua
    - tests/boop_state_contract_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Runtime domain defaults include `trace = { buffer = {}, live = false }`."
    - "Every execution of the packaged bootstrap script resets live to false before the bootstrap function call, even when `boop.bootstrapped == true` makes that function return immediately."
    - "The unconditional reset mutates only `trace.live`; it preserves the existing trace table, buffer table identity, and buffered entries."
    - "Repeated ordinary runtime ensure calls do not turn live mode off mid-session."
  </behavior>
  <action>
Add `live = false` to the runtime trace domain default. In top-level `boop_bootstrap.lua`, obtain the ensured runtime state and set only `state.trace.live` to false before calling `boop.bootstrap()`. This statement is the unconditional package-load boundary: it must execute whenever Muddler loads the final bootstrap script, including same-session reloads where `boop.bootstrap()` immediately returns because `boop.bootstrapped` is already true. Preserve the existing trace table and buffer rather than replacing either one. Do not place the reset in `boop.runtime.ensureState()`, because ordinary runtime access must retain an explicit in-session live-on choice.

Run the real reload/bootstrap G-03-6 example plus the existing state contract tests. Before committing, bump all synchronized package fields from 0.1.435 to 0.1.436. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `feat(03-18): initialize session live-trace state`. Do not push.
  </action>
  <acceptance_criteria>
    - "Fresh state and the real same-session package-reload seam are live-off."
    - "Reload with an already-true bootstrap guard preserves the exact buffer object and entries."
    - "Ordinary ensure calls preserve an explicit in-session live-on choice."
    - "All synchronized version fields read 0.1.436 and focused release gates pass."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_state_contract_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua --filter="G-03-6 package reload reset" tests/boop_trace_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua &amp;&amp; luac -p src/scripts/boop/boop_bootstrap.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>The live flag has a durable runtime default and an unconditional package-load reset that survives the bootstrap early-return path without buffer loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stream accepted trace appends and expose session-only command state</name>
  <files>src/scripts/boop/boop_util.lua, src/scripts/boop/boop_ui.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - src/scripts/boop/boop_util.lua
    - src/scripts/boop/boop_ui.lua
    - src/scripts/boop/boop_state.lua
    - tests/boop_trace_spec.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "The existing collection gate remains first; disabled collection neither appends nor emits."
    - "After an enabled trace entry is formatted, appended, and trimmed, live mode prints `trace live: ` followed by that exact entry once through a path that never calls trace logging."
    - "The UI live toggle changes only `boop.state.trace.live`; it does not call `saveConfigValue`, modify defaults, or change collection."
    - "Enabling live while collection is off explicitly says collection remains off."
    - "Bare status and usage distinguish collection from live mode; show and clear retain existing semantics."
  </behavior>
  <action>
In `boop_util.lua`, leave the `config.traceEnabled` gate, timestamp formatting, append, and 100-entry trimming behavior intact. After a successful append, if session live mode is true, emit exactly one `trace live: {entry}` INFO line through a direct non-tracing output path. Ensure the output helper cannot call `trace.log`, so the stream cannot recurse or alter the buffer.

In `boop_ui.lua`, add an in-memory `setTraceLiveEnabled` path and extend `traceCommand` to accept `live on|off`. Never call `saveConfigValue`, add a persisted default, or change `config.traceEnabled` for live mode. When live is enabled while collection is off, acknowledge live mode and state that collection remains off. Update bare trace status and usage text while leaving `on|off`, `show [n]`, `clear`, buffer slicing, and clear semantics unchanged.

Run the full trace spec to green, parse both Lua files, and run `muddle`. Before committing, bump all synchronized package fields from 0.1.436 to 0.1.437. Stage only the five listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before commit, and commit as `feat(03-18): stream appended traces without recursion`. Do not push.
  </action>
  <acceptance_criteria>
    - "Each accepted append emits exactly once when live is on and never emits when collection or live mode is off."
    - "Live mode remains session-only and collection-independent."
    - "Show, clear, buffer trimming, and existing trace command behavior remain green."
    - "All synchronized version fields read 0.1.437; focused tests, syntax, release gates, and muddle pass."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_trace_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_util.lua &amp;&amp; luac -p src/scripts/boop/boop_ui.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>The live stream is exact-once, non-recursive, session-only, and independent from trace collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Internal trace append → Mudlet output | Diagnostic data crosses from bounded state into an immediate operator-visible stream. |
| Operator trace command → session/config state | A session toggle must not mutate persisted configuration or enable collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-18-01 | Information Disclosure | live trace output | medium | mitigate | Require explicit per-session opt-in and reset it off at top-level package bootstrap execution before any guarded lifecycle return. |
| T-03-18-02 | Denial of Service | recursive trace output | high | mitigate | Emit only after append through a direct output path that cannot call trace logging. |
| T-03-18-03 | Tampering | config persistence | medium | mitigate | Toggle only runtime state and test that save/default paths are untouched. |
| T-03-18-04 | Repudiation | duplicate/mismatched stream entries | low | mitigate | Compare each emitted timestamped payload to its exact appended buffer entry and assert one-to-one counts. |
| T-03-18-SC | Tampering | package installs | low | accept | No npm, pip, or cargo package install is introduced by this plan. |
</threat_model>

<verification>
Run the focused trace and state host Busted tests, including the real same-session bootstrap seam, parse all changed Lua files, run `python3 tools/check_release_gates.py`, and run `muddle`.
</verification>

<success_criteria>
- Live tracing resets off at the unconditional package-load boundary even when the guarded bootstrap lifecycle returns early, and is never persisted.
- Live mode never enables collection.
- Every newly accepted timestamped entry streams once without recursion.
- Existing buffer, show, clear, and collection behavior remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-18-SUMMARY.md` when done.
</output>
