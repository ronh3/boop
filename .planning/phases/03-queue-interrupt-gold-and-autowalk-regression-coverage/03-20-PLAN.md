---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 20
type: execute
wave: 20
depends_on: [03-19]
files_modified:
  - tests/boop_event_transitions_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements: [SAFE-02, SAFE-04, WALK-01, WALK-02]
gap_closure: true
gap_ids: [G-03-7]
must_haves:
  truths:
    - "One copied Inv response and one copied Room response settle the current response fence exactly once in either arrival order."
    - "A room-ID-less Room List remains fail-closed across a moved Room.Info boundary: old-generation evidence may be latched but cannot mutate targets, gold, walk, or combat after movement."
    - "Accepted room evidence creates one immutable source-authority value `{applicationId, roomId, observationGeneration}`; the accepted-room application, every runtime context derived from it, and every room-owned effect carry that exact value."
    - "Target, gold, walk, tick, and downstream room-owned work validate their captured source authority; stale work never substitutes the destination room's current authority."
    - "Movement invalidates stale applications and local attack intent without sending a global queue clear or disturbing unrelated blocker/queue ownership."
  artifacts:
    - "tests/boop_event_transitions_spec.lua contains order-independent fence, moved-boundary, copied-payload, and exact-once application regressions for G-03-7."
    - "src/scripts/boop/boop_runtime.lua owns per-fence Inv/Room latches, exact room-application identity, immutable source-authority capture/validation, and authority-bearing runtime contexts/effects."
    - "src/scripts/boop/boop_events.lua defers accepted-room consumers through the generation-owned application boundary and passes the captured source authority into room-owned tick work."
  key_links:
    - "Char.Items.List Inv/Room -> per-fence copied latches -> exactly one completed transition."
    - "Completed transition -> immutable `{applicationId, roomId, observationGeneration}` -> accepted-room application -> runtime context/effects."
    - "Moved Room.Info -> invalidate application/timer/local intent -> captured old authority fails validation without consulting or adopting destination authority."
---

<objective>
Close the room-evidence half of G-03-7 by making response fences order-independent and moving accepted-room consumers behind an exact-generation application boundary.

Purpose: Preserve D-12, D-14, D-15, and D-16 fail-closed room authority while preventing a room-ID-less List from binding to persistent prior-room identity or synchronously escaping into target/combat work.
Output: Focused RED-to-GREEN regression coverage, per-fence latches, and one immutable, invalidatable source-authority seam for Plan 03-21 to carry through every automatic dispatch path.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-live-room-evidence-wakeup-regression.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-16-SUMMARY.md
@src/scripts/boop/boop_runtime.lua
@src/scripts/boop/boop_events.lua
@tests/boop_event_transitions_spec.lua

<interfaces>
- `boop.runtime.beginRoomResponseFence(reason, opts)` currently creates FIFO fences, including the Plan 03-16 room-only revalidation variant.
- `boop.runtime.observeRoomItemsList(location, items)` currently serializes `await_inv` then `await_room`; replace that phase assumption with independent copied latches while preserving FIFO invalidation and room-only behavior.
- `boop.onRoomItemsList()` currently applies accepted target/gold/walk/tick effects synchronously.
- `boop.onRoomInfo()` is the movement identity boundary and already starts a fresh observation generation.
- `boop.runtime.context()` currently snapshots the destination's current state without recording which accepted room application authorized the work.
- `boop.runtime.tickStep()` creates target/combat effects and `boop.runtime.applyEffects()` applies them without immutable source authority.
- `boop.runtime.clearAttackIntent(reason, opts)` clears only boop-local attack/prequeue/alias metadata and does not mutate the native server queue.
</interfaces>
</context>

<scope_exception>
This heavy gap slice is one tightly coupled test-first task so its RED state is not committed separately and the package receives exactly one synchronized version bump and one package-affecting commit.
</scope_exception>

<tasks>

<task type="auto">
  <name>Task 1: Latch unordered responses, consume one generation-owned room application, and bump the synchronized package version</name>
  <files>tests/boop_event_transitions_spec.lua, src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/debug/phase-03-live-room-evidence-wakeup-regression.md
    - tests/boop_event_transitions_spec.lua
    - src/scripts/boop/boop_runtime.lua
    - src/scripts/boop/boop_events.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Inv then Room and Room then Inv, with exactly one response of each type, each complete one current fence and schedule one application without requiring a duplicate Room response."
    - "Each latch stores a deep copy; later mutation of GMCP tables or returned snapshots cannot alter accepted inventory or room payloads."
    - "Duplicate halves do not replace a latch, complete twice, schedule another application, update inventory twice, or run target/gold/walk/tick twice."
    - "A post-Inv/pre-Info Room List for room 4255 may produce only a pending generation-45 application; moved Room.Info for 4249 invalidates it before any target/gold/walk/tick consumer runs."
    - "The invalidated generation-45 callback is a zero-effect no-op; generation 46 settles from its own one-response-per-half pair in either order."
    - "The generation-45 application, its runtime context, and every effect derived from it carry the same immutable `{applicationId, roomId='4255', observationGeneration=45}` source authority before and after Room.Info advances to room 4249/generation 46."
    - "A stale application/effect is checked with its captured generation-45 authority; validation never reads generation 46 and relabels old work as destination-owned."
    - "Claiming an accepted application is exact-once but retains its source authority as the current observation's accepted authority for downstream validation until movement or a newer accepted application invalidates/replaces it."
    - "Room-only settled-Add revalidation from Plan 03-16 still requires only its Room half and remains exact-operation, exact-room, and exact-generation owned."
    - "If the zero-delay scheduling boundary is unavailable, accepted evidence remains held and traced/warned rather than being applied synchronously."
  </behavior>
  <action>
First update the existing `room-response-fence` contexts in `tests/boop_event_transitions_spec.lua`. Replace the assertion that blesses strict Inv-then-Room behavior with table-driven Inv→Room and Room→Inv cases, and add a named `G-03-7 post-Inv pre-Info application invalidation` chronology using rooms 4255 and 4249. Capture timer callbacks explicitly. Assert exact target-update, gold, walk-settlement, tick, send, timer, fence-depth, application-id, owner, and generation counts. Run the focused spec and require failures only at the missing latch/application assertions; do not commit the RED state.

In `boop_runtime.lua`, retain FIFO fence identity but give each ordinary fence independent `invSeen`, `roomSeen`, copied `invItems`, copied `roomItems`, and one completion/consumption marker. Accept the first valid half in either order, reject duplicate replacement, and finalize only when the required halves for that fence are present. Invalidated fences remain zero-effect drains, and Plan 03-16 `roomOnly` fences continue to require only the Room half. Never infer a room for a List from a later mutable GMCP table.

Define the source-authority contract once in `boop_runtime.lua` as an immutable value with exactly `applicationId`, `roomId`, and `observationGeneration`. Add one room-application record under the existing targeting/room-observation ownership, with that source authority, copied items, pending timer identity, and claimed/consumed/valid state. Never mutate or repurpose an authority table after creation; copy the three scalar fields when crossing a boundary. Expose narrow runtime operations to create, invalidate, claim/consume, and validate that exact record. Claiming the application must require the same current room ID, observation generation, settled observation, active application id, and current `gmcp.Room.Info.num`; it marks the callback consumed exactly once but retains a copied `acceptedSourceAuthority` on the settled observation so downstream effects from that application remain valid. A moved Room.Info invalidates both pending application authority and prior accepted authority; a later accepted application replaces `acceptedSourceAuthority` with its own three-field value. Validation must accept the captured authority as its required argument and compare those three source fields to `acceptedSourceAuthority`, the settled observation, and current `gmcp.Room.Info`; it must never manufacture an authority from whatever room is current when a delayed callback runs.

Extend `boop.runtime.context(sourceAuthority)` so a room-owned context carries a copy of the exact captured authority, and stamp every target, combat-plan, walk-advance, and gold-flush effect produced from that context with the same source authority. `applyEffects` must forward the effect's captured value to downstream consumers rather than re-reading current room authority. Effects without room provenance, such as the deliberate flee path, remain distinguishable as non-room effects; Plan 03-21 preserves those callers explicitly.

In `boop_events.lua`, keep inventory rebuilding tied to the accepted Inv half, but route the Room half's `noteRoomGmcpObserved`, `targets.updateRoomItems`, gold detection, walker settlement, and tick through one captured zero-delay application callback. This is an event-loop ordering boundary, not a sleep or elapsed-time correctness mechanism. The callback must claim/revalidate the exact application and create its runtime context from the application's captured source authority before any consumer runs. Pass that same authority into room-owned tick work. On moved Room.Info, invalidate the pending application before other room-change work, cancel its timer for hygiene, and clear only boop-local attack intent. Emit no native queue clear and preserve unrelated owners and queue metadata.

Use order-independent latches per the diagnosed live evidence even though the older RESEARCH text assumed ordered observation; this newer diagnosis supersedes that assumption while preserving D-14 complete same-room identity. Preserve D-01, D-02, D-03, D-04, D-09, D-10, D-11, D-12, D-13, D-14, D-15, and D-16. Do not add non-zero timing delays, polling, persistent GMCP fallbacks, or global shared-queue cleanup.

After the focused suite is GREEN, read the currently synchronized package version, increment its patch component once, and update all four checkpoints together: `mfile.version`, `mfile.title` as `boop Hunter <version>`, `boop.version` in `src/scripts/boop/boop_init.lua`, and the CODEX current synchronized package-version checkpoint. Stage only the six listed files, inspect `git diff --cached --name-only`, run `python3 tools/check_release_gates.py` immediately before the single package-affecting commit, and commit as `fix(03-20): make room fences order independent`. Do not push.
  </action>
  <acceptance_criteria>
    - "Both one-response-per-half orders settle once; the old strict-order expectation is removed."
    - "Moved Room.Info invalidates the exact old application before target, gold, walk, or tick application."
    - "Accepted applications, runtime contexts, and room-owned effects expose the same immutable three-field source authority, and stale validation cannot substitute destination authority."
    - "Room-only gold revalidation, FIFO stale drains, copied payloads, and aggregate blockers remain green."
    - "No non-zero delay or queue-wide cleanup is introduced."
    - "All four version checkpoints are equal and monotonically greater than the task-start value; release gates pass immediately before the single commit."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_event_transitions_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_runtime.lua &amp;&amp; luac -p src/scripts/boop/boop_events.lua &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-7 room evidence is order-independent, copied, exact-generation owned, and unable to apply after a moved-room boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Asynchronous GMCP Lists → response fence | Inv and Room responses may arrive in either order, duplicate, or belong to an invalidated epoch. |
| Accepted room payload → target/gold/walk/tick application | A room-ID-less payload must not produce effects after room identity changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-20-01 | Spoofing | Room List identity | high | mitigate | Bind copied payloads to exact fence/application ids and revalidate room plus generation at consume time. |
| T-03-20-02 | Tampering | mutable GMCP payload tables | high | mitigate | Deep-copy each first latch and reject duplicate replacement. |
| T-03-20-03 | Elevation of Privilege | stale application callback | high | mitigate | Invalidate on moved Room.Info and validate the callback's captured three-field authority; never substitute the destination's current authority. |
| T-03-20-04 | Denial of Service | Room-before-Inv order | medium | mitigate | Retain the Room half until Inv arrives and complete exactly once without requiring another List. |
| T-03-20-SC | Tampering | package installs | low | accept | No npm, pip, cargo, LuaRocks, or Mudlet package install is introduced. |
</threat_model>

<live_uat_evidence>
After Plan 03-22 and final exact-SHA CI, trace evidence must show one natural room fence settling without `ql`, `ih`, status, or a second Room response. The 4255→4249 chronology must show the generation-45 application invalidated before any consumer and generation 46 settling from its own evidence. Record the complete captured source authority `{applicationId, roomId, observationGeneration}`, response order, validator input, and zero stale side-effect counts; the stale validator input must remain room 4255/generation 45 after room 4249/generation 46 is current.
</live_uat_evidence>

<verification>
Run the focused event-transition host spec, Lua syntax checks, diff hygiene, release gates, and Muddler build. The parent later runs the complete packaged real-Mudlet suite and records the live evidence above at immutable final HEAD.
</verification>

<success_criteria>
- A current fence accepts exactly one Inv and one Room response in either order.
- Room-ID-less evidence cannot cross a moved Room.Info boundary.
- Accepted consumers run only through one exact room-application record and carry its immutable source authority into runtime contexts/effects.
- Stale work is rejected using its captured source authority, never a newly read destination authority.
- Stale invalidation preserves unrelated shared queue and blocker ownership.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-20-SUMMARY.md` when done.
</output>
