---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 30
type: execute
wave: 30
depends_on:
  - 03-29
files_modified:
  - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
autonomous: false
gap_closure: true
gap_ids:
  - G-03-21
  - G-03-22
  - G-03-23
  - G-03-24
  - G-03-25
  - G-03-26
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "Live Tests 7-12 run against exact installed package 0.1.473 produced by Plan 03-29."
    - "Test 7 confirms standard queue semantics remain queue addclearfull freestand BOOP_ATTACK and that pre-existing manual queue work is intentionally removed before boop becomes pending."
    - "Test 7 cannot pass without actual live ready-but-silent native loss; unavailable evidence leaves Test 7/G-03-21 NOT RUN/pending."
    - "Test 7 observes exact denial and named success candidates before their immediately following authoritative prompt; that prompt reconciles one uncontaminated terminal whether ready or not."
    - "When Test 7's following prompt has no valid candidate, non-ready leaves queued with no timer and only the first later ready prompt starts bounded silent grace."
    - "Any later outbound outside the exact standard owner, whether manual/unowned or another boop owner, makes generic success/denial ambiguous while ready-started bounded silence still ends."
    - "Test 11 records each logical rage dispatch's exact ordered transformed wire commands and final observed owned sequence; no unsplit logical rage string is treated as wire evidence."
    - "Test 10 proves a pending standard holds boop SETTARGET, SETALIAS, new-standard, rage, and direct target-changing sends through no-clear departure quarantine until old result/grace terminal."
    - "Test 10 separately proves exactly one intentional clearqueue all for present/unknown forbidden-target revocation before exact local terminal."
    - "Unrelated work is seeded after standard ADDCLEARFULL when testing target policy: it survives departure and is intentionally removed by forbidden-target clear."
    - "Queued-alias timing and ALIAS CLEAR behavior are recorded only as benign non-gating protocol evidence."
    - "UAT/gap status changes only from explicit user observations; this plan makes no source change and no package bump."
    - "Parent push and wait_for_exact_ci occur only after 03-30-SUMMARY, UAT commit, STATE/ROADMAP/phase completion, and every other mutation."
  artifacts:
    - path: .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
      provides: "Observation-derived Tests 7-12 and G-03-21..G-03-26 results plus non-gating alias protocol evidence."
  key_links:
    - from: .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-29-SUMMARY.md
      to: "installed live package"
      via: "exact 0.1.473 identity and automated/build evidence"
    - from: "Test 7 ADDCLEARFULL observation"
      to: G-03-21
      via: "pre-existing work disappears, every candidate terminalizes at its following prompt regardless of readiness, and only candidate-free readiness starts grace"
    - from: "Test 11 rage observation"
      to: G-03-25
      via: "assist/Psion-expanded ordered wires and final owned sequence, not the unsplit logical action, govern generic outcome causality"
    - from: "Test 10 evidence class"
      to: G-03-24
      via: "post-ADDCLEARFULL work survives serialized departure quarantine but is deliberately cleared for present/unknown forbidden revocation"
    - from: "Tests 7-12 observations"
      to: "UAT gap statuses"
      via: "one-to-one explicit user evidence only"
    - from: "final completed phase HEAD"
      to: "main.yml exact-SHA authority"
      via: "parent push then tools/wait_for_exact_ci.sh after all repository mutations"
---

<objective>
Run the non-autonomous live Mudlet checkpoint for Tests 7-12 against exact package 0.1.473, record only user-observed evidence, capture queued-alias timing as a non-gating note, and hand final immutable HEAD to the parent.

Purpose: Native ADDCLEARFULL effects, readiness-independent following-prompt authority, canonical transformed rage wires, genuine silent loss, all-owner command causality, fixed-alias retarget serialization, target clearing, and live rendering require real Achaea/Mudlet authority. Automated success alone cannot resolve these gaps.

Output: Exact package preflight, six PASS/ISSUE/NOT RUN evidence blocks, benign alias protocol note, scoped UAT update, no package bump, and correctly ordered final exact-SHA CI handoff.

Tasks in this plan edit no source, manifest, package metadata, built artifact, existing SUMMARY, STATE, ROADMAP, REQUIREMENTS, or additional plan. The execution/phase workflow may create 03-30-SUMMARY and completion artifacts afterward; the parent waits for all of them before push/CI.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-25-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-26-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-27-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-28-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-29-SUMMARY.md
@mfile
@src/scripts/boop/boop_init.lua

Tests 7-12 are acceptance authority. Normal standard dispatch itself uses ADDCLEARFULL and intentionally clears all older native queue work. Test 10 therefore seeds unrelated work only after the standard replacement when distinguishing no-clear departure quarantine from explicit forbidden-target clearing. Correctness comes from holding fixed BOOP_ATTACK rebinding and target-changing boop sends until old terminal; alias resolution timing remains informational only.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Artifact | Result |
|---|---|
| Package preflight | Exact installed 0.1.473 identity and automated release evidence. |
| Tests 7-12 | Six labeled PASS/ISSUE/NOT RUN blocks with concrete lifecycle evidence. |
| ADDCLEARFULL authority | Live confirmation that pre-existing manual queue work is removed. |
| Prompt reconciliation | Exact success/denial candidate terminal at the immediately following prompt, including not-ready prompts. |
| Outbound causality | Manual and differently owned boop traffic after ADDCLEARFULL invalidate generic attribution. |
| Rage wire authority | Ordered transformed expected/observed wires and final owned sequence for live rage outcomes. |
| Silent-loss authority | Genuine ready-but-silent evidence or explicit pending status. |
| Target authority | Serialized fixed-alias departure quarantine and separate forbidden-target-clear observations. |
| Alias protocol note | Snapshot/late-resolution and ALIAS CLEAR observation, non-gating. |
| UAT update | One-to-one observation-derived test/gap status only. |
| Final handoff | Parent waits for all phase mutations, then pushes and waits exact SHA. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify exact package 0.1.473 before live testing</name>
  <files>None (read-only preflight)</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-29-SUMMARY.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - AGENTS.md
    - CODEX.md
    - mfile
    - src/scripts/boop/boop_init.lua
  </read_first>
  <action>
Identify Plan 03-29's exact 0.1.473 commit. Confirm mfile.version, mfile.title, boop.version, and CODEX.md all report 0.1.473; existing six-gap focused/full host suites passed unchanged; Muddler completed; fresh artifact identity matches; and any complete local real-Mudlet result is recorded. If local real Mudlet was unavailable, retain final exact-SHA CI as pending and do not push an intermediate SHA.

Tell the user exactly which `boop Hunter 0.1.473` package to install/reload and require `boop status` to report 0.1.473 before Test 7. Do not rebuild, edit, commit, push, or run wait_for_exact_ci.
  </action>
  <acceptance_criteria>
    - "Plan 03-29 commit and exact package 0.1.473 are identified."
    - "All four metadata checkpoints, artifact, and automated/build evidence agree."
    - "The user confirms installed 0.1.473 before Test 7."
    - "No repository or external state is changed by preflight."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_prequeue_spec.lua tests/boop_event_transitions_spec.lua tests/boop_targets_spec.lua tests/boop_gold_retry_spec.lua tests/boop_interrupt_spec.lua tests/boop_rage_ingestion_spec.lua tests/boop_rage_contract_spec.lua tests/boop_trace_spec.lua tests/boop_ui_registry_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>Exact installed candidate 0.1.473 is ready for live Tests 7-12 without an intermediate push or repository mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run live Tests 7-12 and the non-gating alias protocol observation</name>
  <files>None (human live verification; no repository mutation)</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-29-SUMMARY.md
  </read_first>
  <what-built>Plans 03-25 through 03-29 produced exact standard outcomes on preserved ADDCLEARFULL semantics, bounded gold quarantine, immediate leap denial, global rage/Triumph recovery, and exact lower-noise trace terminals at version 0.1.473.</what-built>
  <action>
Pause repository work. Guide the user through 03-UAT.md Tests 7-12 against installed 0.1.473, then one isolated benign alias timing observation with boop disabled and native queues empty. Require concrete owner/generation/ordered-wire/final-sequence/candidate/prompt/native evidence and never infer pass from missing output.
  </action>
  <how-to-verify>
1. Test 7 / G-03-21 — ADDCLEARFULL standard outcomes and genuine silent loss:
   - While standard readiness is unavailable, add one harmless manual native queue entry using locally confirmed HELP QUEUE syntax. Let boop stage its standard and capture exact `queue addclearfull freestand BOOP_ATTACK`. Confirm the earlier manual entry is absent because ADDCLEARFULL replaced the whole native queue. This collateral clearing is expected. If checking with a manual queue-list command adds an outbound request, use this run only to prove replacement, then start a fresh clean dispatch for outcome attribution.
   - On a clean dispatch, capture one authoritative success candidate before its immediately following authoritative prompt: matching `Balance Used` or `Equilibrium Used`, or an existing standard attack-success/result line. Arrange or identify that following prompt as not-ready. Confirm no lifecycle mutation occurs on the line itself; that not-ready prompt still reconciles exactly one `executed` terminal for the owner/generation, and a later ready prompt or old timer is a no-op.
   - On another clean dispatch, capture a paralysis/prone/web/impale/unavailable-arms denial before an immediately following not-ready authoritative prompt. Confirm the line is buffered, that not-ready prompt reconciles one `denied` terminal, later prompt/timer callbacks add no duplicate, no storm occurs, and one retry appears only after matching recovery and later readiness.
   - On separate dispatches, put outbound traffic after the owned ADDCLEARFULL and before the generic candidate/prompt. First use a manual command. Also exercise a tick/path that would send boop rage or another direct target-changing command and confirm the pending-standard barrier holds it; if any differently owned boop outbound is actually observed because of an already-armed race, confirm it contaminates the candidate exactly like manual traffic. In every contaminated chronology the following prompt must diagnose ambiguity and must not terminalize/retry the standard from that line.
   - Capture a candidate-free non-ready authoritative prompt for a queued standard. Confirm it leaves the generation queued and arms no timer. Then capture the first later ready prompt with no valid candidate; confirm grace starts only there, expires once, and emits at most one ADDCLEARFULL replacement. A contaminated candidate counts as no valid candidate, so any ready-started grace still ends on its original schedule without restart or extension.
   - Paste owner/generation, fixed alias action/target, room, expected/observed ADDCLEARFULL, removed manual entry, all outbound owner/generation sequences, candidate kind/line, immediately following prompt and ready state, terminal timing, candidate-free non-ready observation, grace start/end, and replacement count.
   - If genuine live silent loss cannot be produced, report NOT RUN for Test 7/G-03-21 even when replacement and denial cases pass.

2. Test 8 / G-03-22 — Immediate causal leap denial:
   - Arrange one boop leap rejected by the exact leg line with no intervening manual send. Confirm immediate command_failed terminal and immediate eligible diag/attack.
   - Wait beyond the old timeout and change rooms; confirm no duplicate terminal or state change.
   - If safe, send an intervening manual command and produce identical denial while leap is pending; confirm ambiguity leaves leap for bounded timeout.
   - Paste owner/generation, outbound sequences/window, exact denial, terminal, follow-up, and stale callback evidence.

3. Test 9 / G-03-23 — Pack quarantine:
   - Displace an inventory-owned put with diag, allow its single replay, and withhold result through fresh timeout.
   - Confirm one warning/release and continued combat/queue/walk with no automatic put.
   - Observe complete freestand-ready/result window plus grace, then a causally newer complete inventory List proving sovereigns and one later safe opportunity.
   - Exercise/observe a late old result or invocation after newer evidence; confirm diagnosis, no newer-state mutation, snapshot invalidation, and requirement for another newer complete List.
   - Paste old/new owner/generation, native put counts, prompts, grace, inventory generations, and subsystem release.

4. Test 10 / G-03-24 — Two target invalidation policies:
   - Departure branch: let boop bind fixed BOOP_ATTACK and stage a standard, observe its ADDCLEARFULL, then add benign unrelated native queue work. Prove target death, Item.Remove departure, or room movement while the old invocation remains pending. Confirm no explicit clearqueue all and a short local retarget quarantine tied to the old owner/generation.
   - Let a replacement be discovered and provoke paths that would normally emit SETTARGET, SETALIAS, another standard, rage, or direct target-changing work. Confirm every attempt is held: the fixed alias remains on the departed target and no new-target command is sent while the old generation is nonterminal. Let the old invocation reach its ready/result prompt and fail harmlessly at most once against the absent old target, or withhold a result until ready-only grace expires. Confirm the old generation terminal appears first; only afterward may normal gates emit replacement SETTARGET, SETALIAS, and ADDCLEARFULL. The post-standard unrelated queue entry survives this no-clear branch.
   - Present/unknown forbidden branch: stage a fresh standard, observe ADDCLEARFULL, then add benign unrelated work. Blacklist, explicitly retarget, or revoke eligibility while old-target presence is true or unknown. Confirm exactly one `clearqueue all` is sent before exact local terminal, the trace states intentional collateral clearing, the post-standard unrelated work is lost as expected, and zero forbidden attacks execute. Replacement may proceed only after that terminal through normal gates.
   - Paste evidence class, old/replacement owner and generation, old/replacement target, fixed alias value over time, attempted and emitted SETTARGET/SETALIAS/ADDCLEARFULL/rage/direct commands, old result or grace terminal, clear count, collateral trace, forbidden/new-target attack counts, and before/after queue evidence.

5. Test 11 / G-03-25 — Global rage and Triumph:
   - Capture a normal rage dispatch and, with assist enabled, an assist-plus-rage dispatch. When the installed class is Psion, also capture assist plus `psi transcend shatter` plus rage expansion; otherwise record that live Psion expansion is NOT RUN while retaining its automated authority. For each logical dispatch, confirm trace records only the exact transformed wire commands in send order, matching observed sysDataSendRequest sequences, and the final owned wire sequence—never the unsplit logical rage string.
   - Capture matching global cooldown denial after a clean final owned wire and confirm one causal terminal plus zero further boop rage sends until exact Available abilities recovery.
   - Produce the same denial after a later manual or differently owned boop command. Confirm that sequence is newer than the rage dispatch's final owned wire, so ambiguity leaves boop unchanged.
   - Confirm only listed abilities recover. Observe Triumph use or exact expiry, plus insufficient/reconnect/timeout where practical, and ordinary post-expiry rage.
   - Paste owner/generation/ability/target/room, logical action, ordered expected/observed wire lists, final owned wire sequence, contaminating sequence where applicable, exact lines, command counts, listed readiness, Triumph generation/terminal, and stale evidence.

6. Test 12 / G-03-26 — Exact useful live trace:
   - Run successful diag, successful ts, causally denied leap, and one timeout.
   - Confirm exactly one retained/live enter-terminal pair per owner/generation/reason.
   - Hunt through clear rooms/kills; routine repetition must not obscure lifecycle lines, while `boop trace show 100` retains/count-preserves forensics.
   - Paste pair counts, live routine counts, and retained samples.

7. Non-gating queued-alias protocol observation:
   - Disable boop and empty native queues. Confirm HELP ALIAS and HELP QUEUE syntax locally.
   - Use an alias name no longer than twelve characters and a harmless read-only command approved by the user. Queue it while the relevant balance is unavailable, then ALIAS CLEAR it before readiness. Observe whether queued execution is inert or uses the prior payload.
   - If safe, repeat after rebinding to a different harmless read-only command to distinguish snapshot-at-queue from resolve-at-execution behavior.
   - Clean up and record exact commands/output. This observation cannot gate any gap, change Test 10 acceptance, authorize source edits, or replace the fixed-alias pending-generation mutation barrier; it may inform only later optimization.

For every UAT test return installed version 0.1.473 and PASS, ISSUE, or NOT RUN plus requested concrete evidence. Redact unrelated private chat while preserving lifecycle facts.
  </how-to-verify>
  <acceptance_criteria>
    - "All six tests have explicit result/evidence blocks against installed 0.1.473."
    - "Test 7 proves ADDCLEARFULL replacement and is PASS only with actual live silent-loss evidence."
    - "Test 7 proves success and denial each terminalize at their immediately following not-ready prompt, while candidate-free non-ready prompts arm no grace."
    - "Test 7 proves ambiguity after every later outbound outside the exact standard owner without unbounded silent hold."
    - "Test 11 records ordered transformed rage wires and final owned sequence, with clean versus contaminated generic-denial behavior; the unsplit logical string is not wire evidence."
    - "Test 10 proves fixed-alias departure quarantine through old terminal and intentional-clear present/unknown forbidden behavior using unrelated work added after standard replacement."
    - "Owner/generation/ordered-wire/final-sequence/candidate/prompt/native-command facts support every claimed pass."
    - "Alias timing is recorded only as non-gating protocol evidence."
    - "No repository change occurs during the checkpoint."
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_release_gates.py</automated>
    <human-check>User supplies six labeled Test 7-12 blocks for installed 0.1.473 with concrete evidence, plus the separate non-gating alias observation.</human-check>
  </verify>
  <resume-signal>Paste Test 7 through Test 12 as PASS, ISSUE, or NOT RUN with evidence, followed by the non-gating alias protocol observation. Test 7 approval without actual silent-loss evidence is insufficient.</resume-signal>
  <done>Live authority exists for every executed test, unresolved cases remain explicit, ADDCLEARFULL and both target policies are observed, and alias timing remains non-gating.</done>
</task>

<task type="auto">
  <name>Task 3: Record observation-only UAT results and defer final CI until all mutations finish</name>
  <files>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-29-SUMMARY.md
    - AGENTS.md
    - CODEX.md
  </read_first>
  <action>
Map observations one-to-one: Test 7/G-03-21, Test 8/G-03-22, Test 9/G-03-23, Test 10/G-03-24, Test 11/G-03-25, and Test 12/G-03-26. Update only 03-UAT.md.

Set pass only from explicit adequate user evidence. Set issue for observed failure. Leave pending/NOT RUN for missing, ambiguous, wrong-version, or insufficient evidence. ADDCLEARFULL and denial success without genuine ready-but-silent native loss leaves Test 7/G-03-21 pending.

For Test 7 record that normal standard ADDCLEARFULL intentionally removed older native queue work; do not characterize this as preservation. Record each exact success/denial candidate before its immediately following authoritative prompt, that prompt's ready state, and whether reconciliation accepted or rejected it. A valid candidate must terminalize at that prompt even when not ready. Record separately that a candidate-free non-ready prompt left the generation queued with no timer and that only the first later ready prompt began grace. Treat every outbound sequence after ADDCLEARFULL from outside the exact standard owner—manual or another boop owner—as contamination. Record that normal boop rage/direct/target-changing attempts were held while the standard remained pending.

For Test 11 record the logical rage identity separately from its wire evidence. Store the exact ordered expected and observed transformed wire commands, their sysDataSendRequest sequences, and final owned wire sequence for ordinary and assist-enabled rage, plus Psion prefix expansion when live-testable. Never record the unsplit logical action as a wire command. Attribute a generic denial only after a complete clean final-wire chronology; record any newer manual or differently owned boop sequence as contamination and nonmutation.

For Test 10 require unrelated work to have been added after ADDCLEARFULL, then apply the split truth. Departure/death/movement sends no explicit clear, preserves that native work, and holds every boop target/alias/standard/rage/direct replacement through the old generation's result or ready-grace terminal; only then may replacement SETTARGET/SETALIAS/ADDCLEARFULL occur. Blacklist/retarget/eligibility revocation with presence true/unknown sends one explicit clear before exact terminal, forbids the old attack, and intentionally removes the seeded work.

Preserve prior reports, evidence, root causes, and history. Add installed version, owner/generation, outbound/native commands, terminal facts, and user attribution. Record alias timing in a clearly labeled protocol_observation field that cannot affect status. Do not infer root cause or implement/propose source changes.

Update each gap only from its matching observation. A proven PASS may resolve it; ISSUE stays pending with evidence; NOT RUN stays pending. Edit no source, summary, state, roadmap, requirement, metadata, or other plan. Commit the UAT change as planning-only with no package bump. Do not push or run wait_for_exact_ci.

After task completion, execution creates 03-30-SUMMARY and the phase workflow may update STATE, ROADMAP, and completion artifacts. Parent must wait until those and every other mutation finish, then push immutable final HEAD and run tools/wait_for_exact_ci.sh. Any later mutation invalidates CI evidence and requires another final push/wait.
  </action>
  <acceptance_criteria>
    - "Tests/gaps update one-to-one only from explicit user observations."
    - "G-03-21 remains pending without genuine ready-but-silent native loss."
    - "UAT accurately records not-ready prompt terminals, candidate-free non-ready no-timer behavior, all-later-outbound contamination, standard ADDCLEARFULL collateral, and both target-policy outcomes."
    - "UAT records ordered transformed rage wires/final sequence separately from the unsplit logical action and distinguishes clean from contaminated generic denial."
    - "Departure evidence records the fixed-alias mutation barrier through old terminal before replacement targeting."
    - "Alias protocol evidence remains distinct and non-gating."
    - "No speculative source or unrelated planning change occurs."
    - "The UAT commit is planning-only and package metadata remains 0.1.473."
    - "Final CI is delayed until 03-30-SUMMARY, UAT commit, phase completion, and all mutations exist."
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_release_gates.py &amp;&amp; git diff --check -- .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md</automated>
  </verify>
  <done>03-UAT.md faithfully records live authority, unresolved evidence remains pending, package version stays 0.1.473, and final immutable-HEAD CI is correctly sequenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 03-29 artifact to installed Mudlet | Wrong installed version invalidates observations. |
| ADDCLEARFULL to queue observation | Normal standard dispatch intentionally removes older native work. |
| Generic output to UAT result | Manual and boop commands can produce identical denial text. |
| Candidate line to prompt | A pre-prompt line is evidence only until the immediately following prompt reconciles it, regardless of readiness. |
| Logical rage to wire evidence | Assist/Psion expansion makes an unsplit action different from the actual ordered sends. |
| Silent-loss production | Failure to reproduce cannot become a pass. |
| Target evidence to queue policy | Departure quarantine and present/unknown forbidden revocation intentionally differ. |
| Fixed alias to replacement sends | Premature SETTARGET/SETALIAS/rage/direct work can redirect or race old native invocation. |
| Alias protocol to correctness | Undocumented timing is observational only. |
| Final mutations to CI | Exact headSha does not exist until every phase artifact is complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-30-01 | Spoofing | installed package | high | mitigate | Require exact status/version 0.1.473 and Plan 03-29 evidence. |
| T-03-30-02 | Repudiation | ADDCLEARFULL behavior | high | mitigate | Capture manual-before, exact replacement command, and absent old queue entry. |
| T-03-30-03 | Spoofing | generic outcome | high | mitigate | Capture every outbound owner/sequence and following-prompt nonmutation after any foreign-owner send. |
| T-03-30-10 | Spoofing | logical rage string | high | mitigate | Capture transformed expected/observed wire order and final sequence; never use unsplit action as wire evidence. |
| T-03-30-11 | Denial of Service | not-ready prompt | high | mitigate | Require valid candidate terminal immediately; arm grace only for a candidate-free later ready prompt. |
| T-03-30-04 | Tampering | G-03-21 result | high | mitigate | Keep pending unless actual live silent loss is observed. |
| T-03-30-05 | Elevation of Privilege | forbidden target | high | mitigate | Require one explicit clear and zero forbidden attacks. |
| T-03-30-06 | Tampering | departure collateral | high | mitigate | Seed work after ADDCLEARFULL and require no target-loss clear. |
| T-03-30-09 | Elevation of Privilege | alias rebind race | high | mitigate | Require old terminal before any replacement SETTARGET/SETALIAS/standard/rage/direct emission. |
| T-03-30-07 | Elevation of Privilege | alias note | high | mitigate | Non-gating evidence only; no source/acceptance mutation. |
| T-03-30-08 | Repudiation | exact-SHA CI | high | mitigate | Parent waits for summary, UAT, phase completion, and every mutation. |
| T-03-30-SC | Tampering | dependency surface | low | accept | No dependency/package installation is performed. |
</threat_model>

<verification>
1. Confirm exact installed 0.1.473.
2. Run Tests 7-12 with concrete owner/generation/ordered-wire/final-sequence/candidate/prompt/native evidence.
3. Keep G-03-21 pending without actual ready-but-silent loss.
4. Record normal ADDCLEARFULL collateral, not-ready prompt terminals, candidate-free readiness gating, all-foreign-owner contamination, canonical rage wire lists, and fixed-alias target quarantine with post-replacement unrelated work.
5. Record alias timing only as non-gating protocol evidence.
6. Update only UAT and do not bump package metadata.
7. Wait for 03-30-SUMMARY, UAT commit, STATE/ROADMAP/phase completion, and all mutations.
8. Parent then pushes immutable final HEAD and runs tools/wait_for_exact_ci.sh; any later mutation invalidates it.
</verification>

<multi_source_coverage_audit>
## Source coverage

| Source | Item | Coverage |
|---|---|---|
| GOAL | Authoritative live closure | Tests 7-12 and observation-only UAT updates. |
| UAT | G-03-21..G-03-26 | One-to-one checkpoint and update mapping. |
| REQ | SAFE-02, SAFE-04 | Exact owner/generation/ordered-wire/final-sequence/candidate/prompt/native evidence and fixed-alias serialization. |
| REQ | WALK-01, WALK-02, WALK-03 | Standard/gold/interrupt/target/trace release evidence. |
| Adjusted approach | ADDCLEARFULL | Standard replacement is destructive, authoritative, and explicitly observed. |
| Native protocol | Alias timing | Separate benign observation, never correctness dependency. |
| AGENTS/CODEX | Final CI | Parent waits until every mutation is complete. |

No source change, package bump, architecture rewrite, new implementation plan, Phase 4/5 expansion, or intermediate CI push is authorized.
</multi_source_coverage_audit>

<success_criteria>
- Exact installed 0.1.473 precedes testing.
- Tests 7-12 have observation-backed results or remain pending.
- Standard ADDCLEARFULL semantics, readiness-independent prompt reconciliation, canonical rage wire causality, all-owner contamination, and both target policies are recorded accurately.
- Alias timing remains non-gating and no package mutation occurs.
- Parent final exact-SHA CI is sequenced after every repository mutation.
</success_criteria>

<output>
Create .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-30-SUMMARY.md when done. After every SUMMARY/STATE/ROADMAP/phase mutation is complete, hand final HEAD to the parent for push and tools/wait_for_exact_ci.sh.
</output>
