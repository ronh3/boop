---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 40
type: execute
wave: 36
depends_on:
  - 03-39
files_modified:
  - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
autonomous: false
gap_closure: true
gap_ids:
  - G-03-27
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-02
assumption_delta_decision:
  primary_noun: owner-keyed interrupt generation
  decision: no-change
  rationale: this plan validates the admission and supersession contract from Plan 03-39 without adding commands or changing runtime behavior
must_haves:
  truths:
    - A valid leap immediately supersedes a pending diagnose and completes from the resulting room change without waiting for the old diagnose timeout.
    - There is no owner-free window in which attack, gold, target, Rage, or walker work can run between the diag terminal and leap ownership.
    - Late diagnose result, prompt, and timeout evidence cannot terminalize or otherwise mutate the leap generation.
  artifacts:
    - path: .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
      provides: Installed-runtime Supplemental Check B authority for G-03-27.
    - path: .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-40-SUMMARY.md
      provides: Exact live hierarchy outcome and handoff to the remaining Phase 3 UAT plans.
  key_links:
    - from: pending diagnose
      to: valid leap
      via: One superseded diag terminal immediately followed by the incoming leap owner and queue wire.
    - from: old diagnose callbacks
      to: current leap state
      via: Old-generation no-op guards observed beyond the original diagnose timeout.
---

<objective>
Prove the priority-aware interrupt hierarchy in live Mudlet with a safe valid movement path before resuming the remaining Phase 3 UAT sequence.

Purpose: Close G-03-27 at the external Mudlet/Achaea queue, prompt, GMCP, and room boundaries.
Output: Supplemental Check B evidence in `03-UAT.md`; no runtime mutation.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-39-SUMMARY.md
@tests/boop_interrupt_spec.lua
@tests/boop_diag_spec.lua
</context>

## Artifacts this phase produces

- No runtime symbol or package file.
- Current installed-runtime Supplemental Check B evidence for diag-to-leap supersession and stale old evidence; same/lower-tier admission remains deterministic automated authority.
- `03-40-SUMMARY.md` with the exact pass/issue result and next-plan handoff.

<tasks>

<task type="auto">
  <name>Task 1: Gate live testing on focused hierarchy regressions and package identity</name>
  <files>None; read-only preflight</files>
  <read_first>
    - `03-39-SUMMARY.md` for the implemented version, tier registry, supersession ordering, and test results.
    - `03-UAT.md` Supplemental Check B and G-03-27.
    - `tests/boop_interrupt_spec.lua` and `tests/boop_diag_spec.lua` for exact valid-leap-over-diag and stale-evidence cases.
    - `CODEX.md` for synchronized version and release gates.
  </read_first>
  <action>
Confirm all four package authorities equal the version produced by Plan 03-39, expected 0.1.483, and package paths are clean. Run focused interrupt, diagnose, timeout, safety, state-contract, walk, gold, event, and tick suites. Require named cases for diag-to-leap supersession, utility-to-diag supersession, lower/same-tier rejection, one queue replacement, no transition-window automation, and stale result/prompt/timeout/room callbacks. Run syntax/JSON, release, and Muddler gates. Stop before live UAT on any failure and do not modify source or UAT during preflight.
  </action>
  <acceptance_criteria>
    - The installed candidate version and all four repository checkpoints match.
    - Every hierarchy and stale-callback branch has a named passing regression.
    - Adjacent automation suites, release gates, and Muddler pass before live work.
    - No repository file changes during preflight.
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_interrupt_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua tests/boop_safety_spec.lua tests/boop_state_contract_spec.lua tests/boop_walk_spec.lua tests/boop_gold_retry_spec.lua tests/boop_event_transitions_spec.lua tests/boop_tick_spec.lua &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>The package and deterministic hierarchy model qualify for live Supplemental Check B.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run valid leap-over-diagnose supersession in Mudlet</name>
  <files>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md</files>
  <read_first>
    - `03-UAT.md` Supplemental Check B for its easy live sequence.
    - `03-39-SUMMARY.md` for exact trace names and expected terminal reason.
    - `CODEX.md` for installed version and trace commands.
  </read_first>
  <action>
Install/reload the Plan 03-39 package and choose a room with a known valid leap exit. Turn boop on, clear trace, enable collection and live trace, then queue `diag` and immediately issue `leap <valid-direction>` before diagnose completes. Capture one diag terminal with `superseded_by:leap`, the incoming leap owner/queue wire, the real moved Room.Info, and one leap room-change terminal. There must be no attack, Rage, target replacement, gold, or walker send between the old terminal and incoming ownership, and the leap must not wait for the old diagnose timeout.

Remain in a safe state beyond the old diagnose timeout and capture that any late diagnose result, prompt, or timer creates no second terminal and does not alter current interrupt state. Do not use a nonexistent exit or the unavailable leg-denial setup as a substitute for the valid room-change path. If command timing prevents capture, repeat from a fresh trace rather than inferring passage. Same-tier and lower-tier rejection are covered by Task 1's deterministic regressions and do not require a timing-sensitive manual attempt.
  </action>
  <what-built>The Plan 03-39 package declares explicit interrupt tiers and atomically supersedes lower-priority owners while quarantining old-generation evidence.</what-built>
  <how-to-verify>
    1. Confirm the installed version produced by Plan 03-39 and choose a valid leap exit.
    2. Run `boop trace clear`, `boop trace on`, and `boop trace live on`.
    3. Queue `diag`, immediately issue `leap <valid-direction>`, and paste the complete native output plus live trace through the room-change terminal.
    4. Wait beyond the old diag timeout and paste any resulting trace; there must be no second old-generation terminal.
    5. Report `pass`, `issue`, or `not run`, including any timing or room limitation.
  </how-to-verify>
  <acceptance_criteria>
    - One diag generation terminalizes with `superseded_by:leap` and one leap generation enters immediately.
    - Valid room movement completes leap before the old diag timeout.
    - No automation effect appears between owners or while leap remains active.
    - Old callbacks remain inert beyond timeout.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_release_gates.py</automated>
    <human-check>Inspect owner/generation ordering, native clear/send counts, room-change terminal, absence of transition-window automation, and silence of late old-generation callbacks.</human-check>
  </verify>
  <resume-signal>Paste the complete native output and live trace and label Supplemental Check B `pass`, `issue`, or `not run`.</resume-signal>
  <done>Live Mudlet either proves G-03-27 or identifies the first external boundary that still violates the hierarchy.</done>
</task>

<task type="auto">
  <name>Task 3: Record G-03-27 authority and resume the existing UAT chain</name>
  <files>.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md</files>
  <read_first>
    - Complete Task 2 output and result label.
    - `03-UAT.md` Supplemental Check B and G-03-27 current fields.
    - `03-39-SUMMARY.md` installed version and expected trace schema.
  </read_first>
  <action>
Update only Supplemental Check B and G-03-27 in `03-UAT.md`. Record the actual installed version, old and incoming owner/generation, superseded terminal reason, queue wire counts, room-change terminal, automation count, and stale callback observations. Mark pass only from complete raw live evidence; otherwise retain issue or not-run at the first failed boundary. Record same/lower-tier behavior from the automated preflight rather than requiring a manual timing race. Commit only UAT evidence and `03-40-SUMMARY.md` under the executor workflow, with no version bump and no push. Do not edit source, VERIFICATION, STATE, ROADMAP, or REQUIREMENTS and do not claim Phase 3 passage.
  </action>
  <acceptance_criteria>
    - Supplemental Check B and G-03-27 have one honest current result tied to exact live evidence.
    - Historical report and source diagnosis remain intact.
    - No runtime or phase-level status changes occur.
    - The summary routes next to Plan 03-35 and the remaining UAT chain.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_release_gates.py &amp;&amp; git diff --check -- .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md</automated>
  </verify>
  <done>Priority hierarchy authority is recorded and remaining Phase 3 UAT may resume without conflating this result with overall phase passage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human command timing to queued operations | The intended diag must still be active when leap is requested. |
| Live trace to lifecycle authority | Missing lines or trace truncation could hide an owner-free window or duplicate terminal. |
| Valid room movement to completion | A nonexistent exit tests timeout, not emergency supersession and room-change completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-40-01 | Spoofing | Active diag prerequisite | high | mitigate | Capture diag enter before leap request in the same live trace. |
| T-03-40-02 | Repudiation | Atomic owner transition | critical | mitigate | Preserve complete old-terminal/new-enter chronology and zero automation sends. |
| T-03-40-03 | Denial of Service | Invalid leap setup | medium | mitigate | Require a known valid exit and real moved Room.Info; record unavailable setup honestly. |
| T-03-40-04 | Tampering | Stale callback isolation | critical | mitigate | Observe beyond old timeout and retain exact terminal counts. |
| T-03-40-SC | Tampering | Package identity | high | mitigate | Confirm synchronized installed version and pass release/Muddler preflight. |
</threat_model>

<verification>
- Automated hierarchy and adjacent automation suites pass before live work.
- Live trace proves one continuous owner transition and one valid room-change terminal.
- Old callbacks are observed beyond timeout and remain inert.
- UAT evidence changes do not self-promote Phase 3.
</verification>

<success_criteria>
- A pending diagnose cannot force a valid leap or fly to wait for timeout.
- The supersession window never authorizes ordinary automation.
- Same/lower-tier requests are enforced by deterministic regressions without making live UAT timing-dependent.
- Remaining Phase 3 UAT is unblocked after G-03-27 is recorded.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-40-SUMMARY.md` when done.
</output>
