---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "07"
type: execute
wave: 7
depends_on:
  - "03-06"
files_modified:
  - src/scripts/boop/boop_walk.lua
  - src/scripts/boop/boop_events.lua
  - tests/boop_walk_spec.lua
  - tests/boop_event_transitions_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - WALK-01
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "Per D-21, stop emits external stop only for a boop-owned run; attached stop detaches and leaves the external run active."
    - "Per D-22, stop invalidates generation/reservation before any external side effect, so late callbacks emit and mutate nothing."
    - "Per D-23, restart has no inherited room, settlement, blocker, refresh, timer, or queued-move evidence."
    - "Per D-24, operator output distinctly reports owned stop versus attached detach."
    - "Arrival, finish, Room.Info, and complete-list callbacks act only on the current run/room generation."
  artifacts:
    - path: "src/scripts/boop/boop_walk.lua"
      provides: "Ownership-aware start/stop/restart and current-generation arrival/finish handling"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Current-generation Room.Info/List/arrived/finished adapter wiring"
    - path: "tests/boop_walk_spec.lua"
      provides: "Owned stop, attached detach, restart, output, and external-loss boundary tests"
    - path: "tests/boop_event_transitions_spec.lua"
      provides: "Room/target/gold/arrival/stop/restart interleaving tests"
  key_links:
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_walk.lua"
      via: "current-generation onRoomChange/onArrived/onFinished evaluation"
      pattern: "(onRoomChange|onArrived|onFinished)"
    - from: "src/scripts/boop/boop_walk.lua"
      to: "demonnicAutoWalker"
      via: "owned-only demonwalker.stop and current-generation demonwalker.move"
      pattern: "demonwalker.(stop|move)"
---

<objective>
Complete D-21 through D-24 by wiring ownership-aware stop/restart and current-generation arrival/room events around the Plan 03-06 walker core.

Purpose: Stopping or detaching must invalidate boop work before external effects and preserve independently owned walker runs.
Output: Owned stop/attached detach behavior, fresh restart state, current-generation event integration, and exact event/output regression coverage.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@UIDESIGN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-05-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-06-SUMMARY.md

<interfaces>
Created production symbols and fields — exhaustive:
- No new production constant, local function, public API, owner form, state/record/snapshot field, timer/retry field, or blocker code.
- New stable transition reason values passed to existing `boop.walk.invalidateCurrentGeneration(reason)`: `operator_stop_owned`, `operator_detach_attached`, `external_finished`, `external_lost`, `room_changed`, `restart`, and `settlement_refresh_exhausted`.
- New exact operator outcome text: `walk stopped -- boop-owned demonwalker run ended` and `walk detached -- external demonwalker run remains active`.

Existing symbols modified:
- `boop.walk.start`, `boop.walk.stop`, `boop.walk.onFinished`, `boop.walk.onArrived`, `boop.walk.onRoomSettled`, `boop.walk.onRoomChange`, `boop.walk.maybeAdvance`, and `boop.walk.move`.
- Locals `armArrivalFallback` and `resetRuntimeFlags`.
- `boop.onRoomInfo`, `boop.onRoomItemsList`, `boop.onWalkArrived`, and `boop.onWalkFinished`.
- Plan 03-06 state fields/APIs/reason labels and `walk:<generation>` owner are consumed unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin owned stop, attached detach, restart, and event ordering</name>
  <files>tests/boop_walk_spec.lua, tests/boop_event_transitions_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_walk_spec.lua` — modified stop/detach/restart, owner clear/update, and stale callback assertions.
    - `tests/boop_event_transitions_spec.lua` — modified Room.Info/List/arrived/finished and cross-domain ordering assertions.
    - `src/scripts/boop/boop_walk.lua` — Plan 03-06 generation, reservation, exact owner, stop, room, arrival, and finish APIs under test.
    - `src/scripts/boop/boop_events.lua` — current walker Room.Info/List/arrived/finished adapter wiring under test.
    - `src/scripts/boop/boop_runtime.lua` — exact-owner blocker and shared room-observation APIs consumed by the lifecycle.
    - `tests/support/boop_test_helper.lua` — deterministic timer, walker, event, and owner fixtures.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-21 through D-24 plus D-14/D-16 room evidence.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — walk, settlement, duplicate, external-loss, reservation, and owner-transition rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Owned-vs-attached start pattern`, `Stop integration contract`, `tests/boop_walk_spec.lua`, `tests/boop_event_transitions_spec.lua`, and `Prove cleanup/authorization precedes the first side effect`.
  </read_first>
  <behavior>
    - "Owned stop raises one stop event after invalidation."
    - "Attached stop raises zero stop events and leaves external enabled state unchanged."
    - "Stop/restart old callbacks emit and mutate zero."
    - "Blocked manual move reports primary reason/no queue and changes no movement state."
    - "New run requires fresh Room.Info and complete list."
  </behavior>
  <acceptance_criteria>
    - At the first external stop event, the old run's emitter/refresh timers are canceled, reservation is invalid, and exact owner `walk:<old generation>` is already cleared.
    - Owned stop raises exactly one `demonwalker.stop`; attached detach raises zero stop events and leaves `demonwalker.enabled` unchanged.
    - Stop→old callback and stop→restart→old callback each raise zero move events and leave the new generation, room generation, reservation ID, owner, and timers unchanged.
    - Restart sets one `walk:<new generation>` owner to `walk_room_unsettled`; only fresh current Room.Info plus complete current room items may clear it or create one new reservation.
    - Current Room.Info after a reserved/emitted move updates the same owner from `walk_move_pending` to `walk_room_unsettled`; complete evidence with no reservation clears exactly that owner.
    - External finish/loss invalidates callbacks before clearing or updating the captured owner, and package loss calls install/update zero times.
    - Output contains exactly `walk stopped -- boop-owned demonwalker run ended` for owned stop or `walk detached -- external demonwalker run remains active` for detach.
    - One patch increment synchronizes all four version checkpoints, and focused Lua syntax plus release gates succeed.
  </acceptance_criteria>
  <action>
Add exact state snapshots at the first external event to prove generation/reservation/timers are invalidated and `walk:<generation>` is cleared first. Cover owned stop, attached detach, external finish/loss, stop→old callback, stop→restart→old callback, and restart→fresh Room.Info/List with exact move/stop/install/update/timer/output counts and the two exact outcome strings from `<interfaces>`. Assert the real owner transition sequence `walk_room_unsettled` → clear on complete evidence → `walk_move_pending` on reservation/emission → `walk_room_unsettled` on confirmed new Room.Info, with package loss updating the same owner to `walker_unavailable`. Interleave target removal, gold terminal, arrived, manual request, Room.Info/List, stop, restart, and stale callbacks while preserving target-removal queue-drift behavior. Denied manual movement must report the runtime-selected primary reason plus no-queue confirmation and leave generation/reservation/timers unchanged. Synchronize the next patch version before commit; do not edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_walk_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>D-21–D-24 and room/arrival ordering are RED with exact event, output, ownership, and stale-generation assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ownership-aware stop/restart and current-generation event wiring</name>
  <files>src/scripts/boop/boop_walk.lua, src/scripts/boop/boop_events.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_walk.lua` — modified current-generation start/stop/room/arrival/finish and exact owner lifecycle.
    - `src/scripts/boop/boop_events.lua` — modified Room.Info/List/arrived/finished adapters.
    - `src/scripts/boop/boop_runtime.lua` — Plan 03-06 reservation/defaults, exact-owner blocker APIs, and shared room snapshots.
    - `tests/boop_walk_spec.lua` — Task 1 ownership, output, reservation, and stale-callback RED contracts.
    - `tests/boop_event_transitions_spec.lua` — Task 1 Room.Info/List and cross-domain RED contracts.
    - `tests/support/boop_test_helper.lua` — timer/walker/event fixture behavior.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-21 through D-24 plus current-room evidence decisions.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — walk/event ordering and exact-owner lifecycle invariants.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `External event registration pattern`, `Owned-vs-attached start pattern`, `Stop integration contract`, `Room transition ordering pattern`, and `Walker boundary`.
  </read_first>
  <behavior>
    - "Stop invalidates before stop/detach."
    - "Only boop-owned stop raises demonwalker.stop."
    - "Restart resets every room/reservation/timer observation."
    - "Room/arrival/finish events ignore stale generations."
  </behavior>
  <acceptance_criteria>
    - `start` allocates a fresh generation/reservation context and sets `walk:<generation>` to `walk_room_unsettled` until current Room.Info plus complete current items arrive.
    - Room.Info/List/arrived/finished handlers compare current run and room generations before mutation; old callbacks produce zero state, output, or events.
    - Confirmed new Room.Info after a move retains the current exact owner but changes its code to `walk_room_unsettled`; complete evidence clears it only when no reservation is active.
    - `stop` and external finish call `invalidateCurrentGeneration` before clearing exactly `walk:<captured generation>`; attached detach performs the same local invalidation/clear without external stop.
    - Mid-run package loss invalidates queued movement, updates the active owner to `walker_unavailable`, warns/traces, and makes zero install/update calls.
    - Owned stop raises exactly one `demonwalker.stop`; attached detach raises zero and the two exact operator outcome strings are emitted on their respective paths.
    - Focused walker/event specs, Lua syntax, and the release gate succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Modify only the existing symbols in `<interfaces>` and use the exact transition reasons/outcome text listed there. `start` allocates a fresh generation for owned and attached runs, clears prior room/reservation/refresh/emitter state, sets exact owner `walk:<generation>` to `walk_room_unsettled`, and requires current GMCP evidence. `stop` captures ownership and owner key, calls `invalidateCurrentGeneration` before clearing that exact owner or performing any external action, raises `demonwalker.stop` only for owned runs, detaches only for attached runs, and emits the distinct exact outcome. Route Room.Info, complete room List, arrived, and finished handlers through current run/room generation: current Room.Info after a reservation updates the same owner to `walk_room_unsettled`; complete current evidence clears it only when no move is reserved; old callbacks return without state/output/events. External finish invalidates callbacks before exact-owner clear. Mid-run package loss uses `external_lost`, invalidates the queued reservation, updates the same active owner to `walker_unavailable`, fails closed, and never installs or updates. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_walk.lua src/scripts/boop/boop_events.lua tests/boop_walk_spec.lua tests/boop_event_transitions_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run walker/event specs and then the full suite inside real Mudlet; use exact corresponding CI when the local AppImage is absent.</authoritative>
  </verify>
  <done>D-21–D-24 and WALK-01/02/03 pass with owned stop, attached detach, fresh restart, and stale event no-ops.</done>
</task>

</tasks>

## Artifacts this plan creates

- Only the exact transition reason values and two output strings listed in `<interfaces>` are new; there are no new production symbols or fields.
- Existing files only; no new command, dependency, schema, manifest, external route ownership, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stop/restart → deferred callbacks | Old callbacks may act on a fresh run. |
| boop ownership → external stop event | Attached runs must remain under their original owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-WALK-STOP | Spoofing / Tampering | stop/restart callbacks | high | mitigate | Invalidate first and assert zero old-generation effects. |
| T-03-WALK-OWNER | Elevation of Privilege | owned vs attached stop | medium | mitigate | Capture ownership and exact external stop counts/output. |
</threat_model>

<verification>
- Syntax/release gates after each task.
- Real Mudlet walker/event/full suite or exact CI is authoritative.
- Documentation/help synchronization is assigned to Plans 03-08 and 03-09.
</verification>

<success_criteria>
- D-21 through D-24 are implemented and observable.
- WALK-01/02/03 event integration cannot emit from stale generations.
- Every commit synchronizes version fields and avoids built artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-07-SUMMARY.md` when done.
</output>
