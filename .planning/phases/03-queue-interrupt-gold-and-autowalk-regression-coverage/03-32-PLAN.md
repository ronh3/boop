---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 32
type: execute
wave: 32
depends_on:
  - 03-31
files_modified:
  - src/scripts/boop/boop_events.lua
  - tests/boop_gold_retry_spec.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
gap_closure: true
gap_ids:
  - G-03-23
requirements:
  - SAFE-04
  - WALK-02
  - WALK-03
must_haves:
  truths:
    - "Every autonomous inventory/room refresh uses the documented empty-string body and Mudlet's required IRE/MCCP flush."
    - "Whitespace-only transport flushes cannot contaminate standard or rage command attribution, while real manual/different-owner commands still do."
    - "Inv and Room responses bind only to the active room/generation fence."
    - "A full-queue pickup does not expire merely because it is still waiting for the balances represented by that queue."
    - "One integrated request→Inv/Room response→full get→confirmed pickup→freestand put chronology consumes quarantine exactly once."
    - "This plan produces one green package commit at synchronized version 0.1.475."
  artifacts:
    - path: src/scripts/boop/boop_events.lua
      provides: "Framed/flushed GMCP requests, transport-flush classification, and execution-aware pickup result timing."
    - path: tests/boop_gold_retry_spec.lua
      provides: "Integrated protocol and get-confirm-put chronology with stale room/generation and duplicate defenses."
    - path: mfile
      provides: "Package title/version 0.1.475 synchronized with runtime and CODEX checkpoints."
  key_links:
    - from: "requestRoomItemsForFence"
      to: "gmcp.Char.Items.List handlers"
      via: "Char.Items.Inv/Room empty-string requests, per-request flush, and exact fence identity"
    - from: "queue add full get sovereigns"
      to: "pickup terminal"
      via: "result deadline begins only after an authoritative full-ready execution opportunity"
    - from: "confirmed pickup"
      to: "pack quarantine"
      via: "consume-before-dispatch and one independent freestand put per D-10/D-12A"
assumption_delta_decision:
  primary_noun: "owner-keyed operation/blocker generation"
  decision: "no-change"
  rationale: "this gap closure repairs established multi-owner lifecycle behavior and does not generalize identity or walker optionality"
---

<objective>
Close G-03-23 by implementing the official Achaea/Mudlet GMCP request/flush contract and proving the later full-queue pickup through one real event-handler chronology.

Purpose: Gold and room settlement must recover autonomously without manual `ql`, `ih`, status, injected production state, wrong-room evidence, premature timeout, or duplicate put behavior per D-09 through D-16.

Output: Framed/flushed requests, transport-safe outbound observation, execution-aware pickup timing, integrated regression coverage, and synchronized package 0.1.475.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-26-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-31-SUMMARY.md
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_runtime.lua
@src/triggers/boop/Gold/triggers.json
@src/triggers/boop/Gold/Gold_Get_Success.lua
@tests/boop_gold_retry_spec.lua
@tests/support/boop_test_helper.lua

Official authority checked during planning:
- https://nexus.ironrealms.com/GMCP documents `Char.Items.Inv ""` and `Char.Items.Room ""`.
- https://wiki.mudlet.org/w/Special:MyLanguage/Manual:Lua_Functions documents `send(" ")` after each `sendGMCP` call on IRE games with MCCP.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Symbol/file | Result |
|---|---|
| `sendGmcpRequestWithFlush` in `boop_events.lua` | Private helper emitting one exact GMCP request and its required whitespace transport flush. |
| Existing `armGoldPendingTimeout` | Pickup branch becomes execution-aware; packing and first-terminal generation guards remain intact. |
| Gold operation fields `awaitingQueueExecution` and `executionReadyPrompt` | Exact diagnostic state for a full-queue get waiting to become executable. |
| `tests/boop_gold_retry_spec.lua` | Integrated protocol order, fence, queue delay, trigger completion, quarantine consumption, and put regression. |
| Package metadata | All four version checkpoints become 0.1.475. |
| New files/public API | None. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair autonomous GMCP refresh and prove full-queue get-confirm-put completion</name>
  <files>src/scripts/boop/boop_events.lua, tests/boop_gold_retry_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VERIFICATION.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-26-SUMMARY.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-31-SUMMARY.md
    - src/scripts/boop/boop_events.lua
    - src/scripts/boop/boop_runtime.lua
    - src/triggers/boop/Gold/triggers.json
    - src/triggers/boop/Gold/Gold_Get_Success.lua
    - tests/boop_gold_retry_spec.lua
    - tests/support/boop_test_helper.lua
    - https://nexus.ironrealms.com/GMCP
    - https://wiki.mudlet.org/w/Special:MyLanguage/Manual:Lua_Functions
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <action>
Add a RED integrated case using protocol logs around the real event entry points. It must start from a current Room.Info plus incomplete room items, create a deferred same-room gold operation, and assert exact outbound order: `sendGMCP([[Char.Items.Inv ""]])`, whitespace flush, `sendGMCP([[Char.Items.Room ""]])`, whitespace flush. For a room-only fence assert one Room request and one flush. Feed Inv and Room List payloads through `boop.onRoomItemsList`, not by assigning accepted room/inventory state directly. Assert active fence, room id, observation generation, response order, accepted application, and no stale/wrong-room response can settle or dispatch.

Implement private `sendGmcpRequestWithFlush`. Use the exact empty JSON-string body documented by Iron Realms and call `send(" ")` after each request per Mudlet's IRE/MCCP contract. In `boop.onDataSendRequest`, classify a trimmed-empty send as transport-only before `runtime.observeOutbound`, Rage observation, gold old-put activity, and movement intent. Prove it does not advance the command outbound sequence or contaminate a pending standard; every non-whitespace manual or differently owned command remains observable and contaminating. Do not hide any real command.

Extend the integrated case through D-12A: completed current-room evidence queues exactly `queue add full get sovereigns`. Record that pickup as awaiting full-queue execution and do not start its four-second result deadline at enqueue time. Not-ready prompts leave the exact generation held without consuming retries or timing out. At the first authoritative prompt where full-queue readiness can execute, mark one execution opportunity and arm one generation/dispatch-owned result deadline; duplicate prompts cannot restart it. Existing exact `You scoop up ... gold sovereigns.` trigger/adapter evidence then calls the pickup terminal, transitions ownership to inventory, consumes one eligible old pack quarantine before dispatch, and emits exactly one `queue add freestand put sovereigns in <pack>`. Feed the existing put-success adapter and prove one terminal/release. Apply the same execution-window rule to initial, retry, and displacement-replayed full pickups; packing retains its independent freestand timing.

Regress timeout-before-readiness, success-before-first-ready-prompt, ready-with-no-result timeout, stale timer after success, changed room/generation, reversed or duplicate Lists, duplicate gold lines, unrelated owners, and one get/one put counts. Preserve D-01/D-02 exact owner release, D-09 room-owned acquisition, D-10 inventory-owned packing, D-11 coalescing, D-12 complete stable-room evidence, D-12A exact queue forms, and D-13 through D-16 walker arbitration. Do not rely on direct accepted-state injection for the integrated case and do not add a parallel room coordinator.

After green focused tests, bump once from 0.1.474 to 0.1.475 across all four fields. Stage exactly the five listed files as one green package-affecting commit, inspect staged paths, run release/Muddler gates, commit once, and do not push. The current Gold trigger manifest is verification-only and must remain unchanged unless real captured text contradicts it; no substitute success line may be invented. No docs/help edit is required because the intended get-confirm-put contract is unchanged.
  </action>
  <acceptance_criteria>
    - "Inv and Room requests include `\"\"` and each is immediately flushed once."
    - "Whitespace transport sends never enter command causality; actual manual/different-owner sends still do."
    - "Only active current-room/current-generation List responses complete the fence."
    - "A full-queue get cannot time out before a full-ready execution opportunity."
    - "The integrated handler chronology emits one full get, accepts one exact pickup, consumes quarantine once, emits one freestand put, and terminalizes once."
    - "Duplicate, stale, reversed, wrong-room, and unrelated-owner cases remain safe."
    - "Focused tests, Lua syntax, release gates, and Muddler pass."
    - "One green package commit synchronizes all version checkpoints at 0.1.475 and is not pushed."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_gold_retry_spec.lua tests/boop_event_transitions_spec.lua tests/boop_walk_spec.lua &amp;&amp; luac -p src/scripts/boop/boop_events.lua tests/boop_gold_retry_spec.lua src/scripts/boop/boop_init.lua &amp;&amp; python3 -m json.tool src/triggers/boop/Gold/triggers.json &gt;/dev/null &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
  </verify>
  <done>G-03-23 is automated end to end at package 0.1.475 with exact framed/flushed refresh, current fence authority, execution-aware full pickup, and one quarantine-consuming put.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lua → Mudlet → Achaea GMCP | Request body and MCCP flush determine whether responses arrive. |
| GMCP List → room fence | Server payloads are untrusted until location, room, generation, and fence match. |
| Full queue → result timeout | Enqueue time is not execution authority. |
| Pickup terminal → pack quarantine | Consumption must precede one put dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-32-01 | Tampering | GMCP framing | high | mitigate | Assert exact empty-string bodies and one documented flush per request. |
| T-03-32-02 | Spoofing | List response | high | mitigate | Require active fence, expected location, room id, and generation. |
| T-03-32-03 | Denial of Service | pickup timeout | high | mitigate | Start deadline from first executable full-ready prompt, once. |
| T-03-32-04 | Elevation of Privilege | quarantine put | high | mitigate | Consume eligibility before dispatch and recheck every current gate. |
| T-03-32-05 | Repudiation | transport versus command | medium | mitigate | Log protocol order while excluding only trimmed-empty transport from command causality. |
| T-03-32-SC | Tampering | dependency surface | low | accept | No dependency or package-manager installation is authorized. |
</threat_model>

<verification>
The focused suite must exercise emitted requests and real handlers before live Test 9. Source remains under `src/`; one synchronized version bump, release gate, and Muddler build accompany the implementation commit. Parent push/exact-SHA CI waits until all later live and planning mutations finish.
</verification>

<multi_source_coverage_audit>
## Source coverage

| Source | Item | Coverage |
|---|---|---|
| GOAL | Loot cannot bypass room/safety ownership or stall forever | Exact room fence and bounded executable pickup preserve all owners. |
| REQ | SAFE-04, WALK-02, WALK-03 | Wrong-room, blocked walk, and explicit optional walker behavior remain guarded. |
| RESEARCH | Shared room observation and staged gold | Existing coordinator and get-confirm-put stages are reused. |
| CONTEXT | D-01, D-02, D-08, D-09–D-16 | Exact releases, first terminal, room acquisition, inventory packing, coalescing, settlement, refresh cap, and one move remain binding. |
| VERIFICATION/UAT | G-03-23 | Bare requests and enqueue-time pickup timeout are covered by one integrated chronology. |

No walker installation change, arbitrary temp attack, AOE, `ch`, Phase 4/5 feature, or built-artifact edit is included.
</multi_source_coverage_audit>

<success_criteria>
- The integrated protocol and gold chronology passes without manual GMCP injection.
- Full-queue pickup result timing begins from execution opportunity, not enqueue.
- One green package commit produces 0.1.475 without pushing.
- Live Test 9 remains required before Phase 3 can pass.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-32-SUMMARY.md` after the green execution commit. Do not push.
</output>
