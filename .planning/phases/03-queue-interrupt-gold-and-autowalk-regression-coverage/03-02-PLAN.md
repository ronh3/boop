---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tests/boop_interrupt_spec.lua
  - tests/boop_diag_spec.lua
  - tests/boop_diag_timeout_spec.lua
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - "Per D-05, each queued interrupt holds attacks and prequeue until its exact completion evidence or timeout wins."
    - "Per D-06, timeout releases only the interrupt owner and preserves unrelated holds and queued operator work."
    - "Per D-07, repeating the same or another interrupt while pending sends nothing and does not restart timing."
    - "Per D-08, the first terminal callback releases and reports once; every later callback is a zero-effect no-op."
    - "Per D-05 and D-08, real zero-argument diagnose triggers correlate output through an ordered FIFO whose timed-out head remains as a tombstone, so stale output can never become evidence for a newer generation."
  artifacts:
    - path: "src/scripts/boop/boop_ui.lua"
      provides: "Generation-owned creation, diagnose evidence enqueueing, and idempotent queueInterrupt dispatch"
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Operation-specific prompt/result completion, FIFO/tombstone evidence correlation, and one terminal API"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Zero-argument FIFO-head diagnose result evidence through boop.onDiagReadyLine"
    - path: "src/triggers/boop/Diag/Diag_Result_Detail.lua"
      provides: "Existing read-only zero-argument detailed-result trigger boundary"
    - path: "src/triggers/boop/Diag/Diag_Result_Perfect.lua"
      provides: "Existing read-only zero-argument perfect-result trigger boundary"
    - path: "tests/boop_interrupt_spec.lua"
      provides: "Repeat and prompt/timeout ordering matrix"
    - path: "tests/boop_diag_spec.lua"
      provides: "Normal zero-argument result-then-prompt and FIFO/reset coverage"
    - path: "tests/boop_diag_timeout_spec.lua"
      provides: "Timeout tombstone isolation through the real zero-argument trigger path"
  key_links:
    - from: "src/scripts/boop/boop_ui.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "queueInterrupt creates state.diag.operation and appends one diagnose evidence record before dispatch"
      pattern: "(enqueueDiagEvidence|interrupt:)"
    - from: "src/triggers/boop/Diag/Diag_Result_Detail.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "the existing detailed-result trigger invokes boop.onDiagReadyLine() with zero arguments"
      pattern: "onDiagReadyLine"
    - from: "src/triggers/boop/Diag/Diag_Result_Perfect.lua"
      to: "src/scripts/boop/boop_events.lua"
      via: "the existing perfect-result trigger invokes boop.onDiagReadyLine() with zero arguments"
      pattern: "onDiagReadyLine"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "boop.onDiagReadyLine delegates to markOldestDiagEvidenceResult without inferring a generation from the active operation"
      pattern: "(onDiagReadyLine|markOldestDiagEvidenceResult)"
    - from: "src/scripts/boop/boop_runtime.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "the prompt consumes only the result-seen FIFO head and delegates a matching current diagnose generation to completeInterrupt"
      pattern: "(consumeOldestDiagEvidencePrompt|completeInterrupt)"
---

<objective>
Implement D-05 through D-08 for `diag`, `matic`, `catarin`, `fly`, `ts`, and `leap` as one generation-owned interrupt lane, with production FIFO/tombstone correlation for zero-argument diagnose output.

Purpose: Repeats and stale prompt/result/timeout callbacks must not resend, release a newer operation, or let an old diagnose line mark the active generation.
Output: Exact interrupt operation/evidence state, first-terminal API, isolated timeout behavior, and real trigger-path regression coverage.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md

<interfaces>
Consume Plan 03-01 exact-owner APIs.

Created symbols and values — exhaustive:
- State field `state.diag.generation`, initialized to `0`.
- State field `state.diag.operation`, inactive value `false`; an active record has exact fields `generation`, `name`, `command`, `completionMode`, `resultSeen`, `terminal`, `blockerOwner`, `timeoutTimer`, and `startedAt`.
- State field `state.diag.evidenceQueue`, initialized to `{}` and deep-copied/reset independently; each diagnose dispatch appends one ordered record with numeric `generation` plus `resultSeen = false`, `terminal = false`, and `tombstone = false`.
- Exact `completionMode` values `prompt` and `result_then_prompt`.
- Stable blocker owner form `interrupt:<generation>` and blocker code `interrupt_pending`; affected systems are `combat` and `queue`.
- Public API `boop.runtime.completeInterrupt(generation, terminalReason)`.
- Public API `boop.runtime.enqueueDiagEvidence(generation)`.
- Public zero-argument API `boop.runtime.markOldestDiagEvidenceResult()`.
- Public zero-argument API `boop.runtime.consumeOldestDiagEvidencePrompt()`.
- Stable terminal reason values `prompt_complete`, `diagnose_result_prompt`, and `timeout`.

Existing symbols modified, not created:
- `DOMAIN_DEFAULTS.diag`; its `generation`, `operation`, and `evidenceQueue` defaults are initialized and deep-copied independently on ensure/reset.
- Local `queueInterrupt(label, command, opts)`.
- Public command functions `boop.ui.diag`, `boop.ui.matic`, `boop.ui.catarin`, `boop.ui.fly`, `boop.ui.touchShield`, and `boop.ui.leap`.
- Event result handler `boop.onDiagReadyLine`; it accepts no arguments and delegates to the zero-argument FIFO-head marker.
- Existing trigger scripts `src/triggers/boop/Diag/Diag_Result_Detail.lua` and `src/triggers/boop/Diag/Diag_Result_Perfect.lua`; they remain unmodified and continue to call `boop.onDiagReadyLine()` with zero arguments.
- Existing compatibility fields `state.diag.hold`, `awaitPrompt`, `timeoutTimer`, and `label`; they become derived display fields only.
- Existing `promptStep`, `boop.runtime.step`, and `boop.runtime.applyEffects`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeat and terminal-order interrupt contracts</name>
  <files>tests/boop_interrupt_spec.lua, tests/boop_diag_spec.lua, tests/boop_diag_timeout_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_interrupt_spec.lua` — modified prompt-mode interrupt timer/send contracts and primary callback-capture analog.
    - `tests/boop_diag_spec.lua` — modified diagnose result-then-prompt and default-isolation coverage.
    - `tests/boop_diag_timeout_spec.lua` — modified timeout isolation and stale-generation coverage.
    - `src/scripts/boop/boop_ui.lua` — `queueInterrupt` and public interrupt command entry points under test.
    - `src/scripts/boop/boop_runtime.lua` — `DOMAIN_DEFAULTS.diag`, prompt processing, effects, and `completeInterrupt` implementation seam.
    - `src/scripts/boop/boop_events.lua` — existing zero-argument `boop.onDiagReadyLine` boundary under test.
    - `src/triggers/boop/Diag/Diag_Result_Detail.lua` — read-only real trigger script calling `boop.onDiagReadyLine()` with zero arguments.
    - `src/triggers/boop/Diag/Diag_Result_Perfect.lua` — read-only real trigger script calling `boop.onDiagReadyLine()` with zero arguments.
    - `tests/support/boop_test_helper.lua` — reset and exact-owner fixture behavior.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-05 through D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — row 03-W0-02 and interrupt/repeat ordering rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Interrupt command seam`, `tests/boop_interrupt_spec.lua`, `tests/boop_diag_spec.lua`, `tests/boop_diag_timeout_spec.lua`, and `Capture callbacks as an ordered queue`.
  </read_first>
  <behavior>
    - "Same and different repeats keep one generation, send, owner, and timer."
    - "Prompt-before-result does not complete diag."
    - "Prompt/result then timeout and timeout then prompt/result each terminate once."
    - "The actual zero-argument diagnose trigger path correlates only the FIFO head."
    - "A timed-out diagnose tombstone absorbs its late result plus prompt without mutating the newer operation."
    - "FIFO records and defaults reset independently."
  </behavior>
  <acceptance_criteria>
    - Each prompt-mode interrupt request sends exactly one unchanged `queue addclearfull freestand <command>` command and schedules exactly one timeout; same-command and different-command repeats add zero sends and zero timers.
    - `diag` remains active after a prompt without a diagnose result, then terminates exactly once after a real zero-argument result trigger plus prompt or timeout.
    - Success/prompt→timeout and timeout→success/prompt orders each produce one terminal transition, one owner release, one warning only on timeout, and zero late-callback ticks or state mutation.
    - The exact stale ordering is diagnose N → N timeout → diagnose N+1 starts → N's old result script invokes zero-argument `boop.onDiagReadyLine()` → prompt. Relative to a snapshot taken after N+1 starts, N+1 generation, operation `resultSeen`, exact owner, timer ID, terminal state, send count, tick count, warning/OK/info output counts, and trace count remain unchanged; only N's tombstone is marked and drained.
    - Normal diagnose N result→prompt marks and consumes N's FIFO head, delegates exactly once to `completeInterrupt(N, "diagnose_result_prompt")`, releases only `interrupt:N`, and leaves the evidence queue empty.
    - A duplicate result line cannot scan past a result-seen FIFO head, and a prompt cannot scan past a head without result evidence; ambiguous output is never reassigned to a newer record.
    - Fresh resets materialize `state.diag.generation == 0`, `state.diag.operation == false`, and `state.diag.evidenceQueue == {}`; mutating an operation or queue record in one reset cannot affect the next reset.
    - One patch increment synchronizes `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint; the version release gate succeeds.
  </acceptance_criteria>
  <action>
Capture every scheduled callback by stable timer ID. Table-drive all prompt-mode commands and `diag` separately, asserting exact command text, `send(..., false)` count, owner/generation/operation fields, timer count, warning/terminal/tick counts, and retained unrelated owners for both callback orders. Add reset/deep-copy tests proving `DOMAIN_DEFAULTS.diag.generation`, `DOMAIN_DEFAULTS.diag.operation`, and `DOMAIN_DEFAULTS.diag.evidenceQueue` materialize independently for every fresh state. Add same-request and different-request repeat rows with zero resend and zero timer replacement.

Exercise the actual trigger boundary by `dofile`-executing the existing detail/perfect trigger scripts, which call zero-argument `boop.onDiagReadyLine()`. Cover normal N result→prompt and the exact stale order N timeout → N+1 dispatch → old N result trigger → prompt. Snapshot N+1 immediately after dispatch and assert its generation, operation `resultSeen`, owner, timer, terminal state, sends, ticks, utility output, and trace counts do not change across the stale result/prompt. Assert the allowed queue-only mutation: N's terminal tombstone becomes result-seen and is then removed while N+1's record remains unresolved. Add head-blocking cases proving duplicate/missing/ambiguous lines and prompts never scan forward. Remove every test helper or call shape that passes a diagnose generation to the result handler. Assert timeout sends no server queue removal. Before commit, increment one patch version and synchronize all required version fields plus CODEX. Do not edit either trigger script or generated artifacts.
  </action>
  <verify>
    <automated>luac -p tests/boop_interrupt_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>The D-05–D-08 ordering and repeat matrix is RED for missing generation/terminal ownership and FIFO/tombstone correlation through the real zero-argument trigger path, with exact side-effect counts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the generation-owned interrupt lane</name>
  <files>src/scripts/boop/boop_ui.lua, src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, tests/boop_diag_spec.lua, tests/boop_diag_timeout_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_ui.lua` — modified `queueInterrupt` and interrupt command handlers.
    - `src/scripts/boop/boop_runtime.lua` — modified `DOMAIN_DEFAULTS.diag`, prompt step, effects, and terminal API.
    - `src/scripts/boop/boop_events.lua` — modified `boop.onDiagReadyLine` result-evidence entry point.
    - `tests/boop_diag_spec.lua` — modified focused reset/deep-copy and diagnose operation assertions.
    - `tests/boop_interrupt_spec.lua` — Task 1 prompt-mode RED contracts.
    - `tests/boop_diag_timeout_spec.lua` — modified exact result-after-timeout and old-result/prompt-after-new-generation contracts.
    - `src/triggers/boop/Diag/Diag_Result_Detail.lua` — read-only real zero-argument trigger boundary covered by the stale-result test.
    - `src/triggers/boop/Diag/Diag_Result_Perfect.lua` — read-only real zero-argument trigger boundary covered by the normal completion test.
    - `tests/support/boop_test_helper.lua` — reset behavior used to prove independent defaults.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-05 through D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — interrupt/repeat ordering rows and exact side-effect requirements.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — sections `Pattern 2: Generation-Owned Operation with First-Terminal-Wins` and `First-Terminal-Wins Operation`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Namespace and state-default pattern` and `Interrupt command seam`.
  </read_first>
  <behavior>
    - "A fresh request creates one interrupt owner and timer."
    - "Every terminal path delegates to completeInterrupt."
    - "Repeats preserve the active operation and report pending state."
    - "Timeout clears only local intent, timer, and exact owner."
    - "Each diagnose dispatch appends one FIFO evidence record."
    - "A zero-argument diagnose result marks only the FIFO head and never infers the active generation."
    - "Timeout retains a terminal tombstone so late result plus prompt is absorbed before any newer evidence."
  </behavior>
  <acceptance_criteria>
    - `DOMAIN_DEFAULTS.diag` explicitly defines `generation = 0`, `operation = false`, and `evidenceQueue = {}`; `ensureState()` deep-copies all three and helper reset restores independent values after mutation.
    - A fresh dispatch increments generation once, creates one exact `interrupt:<generation>` owner with `interrupt_pending`, sends once, and captures one timeout timer.
    - A diagnose dispatch calls `enqueueDiagEvidence(generation)` exactly once before sending and appends `{generation = generation, resultSeen = false, terminal = false, tombstone = false}` without replacing prior records.
    - `completeInterrupt(generation, terminalReason)` returns true only for the current nonterminal operation and marks terminal before effects. On diagnose timeout it marks that generation's retained evidence record `terminal = true` and `tombstone = true`, then clears only its exact owner/timer/derived display state; every late terminal call returns false with zero mutation.
    - Zero-argument `markOldestDiagEvidenceResult()` inspects only `evidenceQueue[1]`. If the head is unresolved, it marks only that record `resultSeen = true` and mirrors `operation.resultSeen = true` only when the head generation exactly matches the current nonterminal diagnose operation. Missing queues, duplicate lines against a result-seen head, and older terminal heads never advance to a newer record or mutate owner/timer/tick/output state.
    - Zero-argument `consumeOldestDiagEvidencePrompt()` inspects only `evidenceQueue[1]`. A head without result evidence is retained with zero terminal effects. A result-seen terminal tombstone is removed with no owner, timer, tick, warning/OK/info, completion trace, or active-operation mutation. A result-seen head matching the current nonterminal diagnose operation is removed and delegates exactly once to `completeInterrupt(generation, "diagnose_result_prompt")`.
    - `boop.onDiagReadyLine()` accepts no arguments and only delegates to `boop.runtime.markOldestDiagEvidenceResult()`; both existing trigger scripts remain unchanged zero-argument callers.
    - Prompt completion is used for `matic`, `catarin`, `fly`, `ts`, and `leap`; for `diag`, prompt processing delegates only through `consumeOldestDiagEvidencePrompt()`. No result or prompt path directly clears an owner.
    - In `tests/boop_diag_timeout_spec.lua`, the actual zero-argument trigger path in order N timeout → N+1 starts → old result → prompt marks/drains only N's tombstone and leaves N+1 generation, `resultSeen`, owner, timer, terminal state, sends, ticks, utility output, and trace counts unchanged.
    - Timeout emits one warning/trace, sends no server queue-removal command, and preserves unrelated owner records and queued operator work.
    - Focused interrupt/diag/timeout specs, `luac -p`, and the release gate succeed after one synchronized patch increment across all version checkpoints.
  </acceptance_criteria>
  <action>
Implement the exact created inventory in `<interfaces>`. Add `generation = 0`, `operation = false`, and `evidenceQueue = {}` to existing `DOMAIN_DEFAULTS.diag`, rely on `ensureState()` deep-copy semantics, and make reset/default-isolation assertions in `tests/boop_diag_spec.lua` pass. Make existing `queueInterrupt` allocate `state.diag.generation`, construct `state.diag.operation`, set owner `interrupt:<generation>`, and reject any request while the operation record is active. For `completionMode == "result_then_prompt"`, call `boop.runtime.enqueueDiagEvidence(generation)` once before dispatch; prompt-mode commands do not append diagnose evidence. `enqueueDiagEvidence` always appends and never removes, replaces, or scans prior records.

Implement `boop.runtime.markOldestDiagEvidenceResult()` as a zero-argument queue-head operation: inspect only `state.diag.evidenceQueue[1]`, return false for no head or an already result-seen head, otherwise mark that exact record. Mirror `state.diag.operation.resultSeen` only after comparing the record generation to the current nonterminal `diag` operation in `result_then_prompt` mode. Never choose or relabel a record from `state.diag.operation`, and never scan past the FIFO head. Keep `boop.onDiagReadyLine()` zero-argument and make it only delegate to this runtime API; do not edit the two existing trigger scripts.

Implement `boop.runtime.consumeOldestDiagEvidencePrompt()` as the only diagnose prompt consumer. Inspect only the queue head and leave a head without result evidence untouched. Remove a result-seen head exactly once. If it is terminal/tombstoned, return after that queue-only drain. If its generation matches the current nonterminal diagnose operation, delegate to `boop.runtime.completeInterrupt(generation, "diagnose_result_prompt")`; otherwise return without mutating the active operation. Prompt-mode interrupts continue to call the terminal API with `prompt_complete`. Preserve existing prompt-gag handling, but tombstone drain must schedule no tick and emit no terminal/warning/trace output.

Make `boop.runtime.completeInterrupt` compare generation and terminal state, set terminal before effects, kill only the captured timer for hygiene, clear only the operation owner, clear the operation/derived display fields, trace owner/generation/reason, and return whether it won. When timeout wins for a diagnose operation, locate that generation's evidence record, set `terminal` and `tombstone` true, and retain it in FIFO order before releasing the operation. A result line is never reassigned away from the FIFO head, even when the head is terminal or output is ambiguous/missing.

In `tests/boop_diag_timeout_spec.lua`, capture timeout callbacks by stable timer ID, execute the real trigger script with no arguments, and use the actual `boop.onPrompt()` path. Add the exact N timeout → N+1 starts → old result → prompt ordering and prove N+1's generation, operation `resultSeen`, owner, timer, terminal state, output, sends, and tick counts are unchanged while N's tombstone alone is marked/drained. Keep normal N result→prompt success plus FIFO/reset independence in `tests/boop_diag_spec.lua`. Timeout warns once and performs no server-side queue removal; fresh dispatch retains existing `queue addclearfull freestand <command>` behavior. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_ui.lua src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua tests/boop_interrupt_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run focused Mudlet/Busted behavior for interrupt, diagnose, and diagnose-timeout specs, including normal N result→prompt through an existing zero-argument trigger and exact N timeout → N+1 starts → old result trigger → prompt tombstone absorption.</authoritative>
  </verify>
  <done>Interrupts are idempotent first-terminal operations, and zero-argument diagnose output is FIFO/tombstone-correlated so stale result/prompt evidence cannot mutate or clear a newer hold.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol list; no additional interrupt constant, function, API, owner, field, completion mode, or terminal reason may be introduced without adding it there.
- Existing files only; no new command, alias, dependency, schema, manifest, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator alias → Achaea queue | Repeats can duplicate commands or replace timing state. |
| Prompt/result/timer → operation state | Callback order is nondeterministic and may be stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-INT-REPEAT | Denial of Service / Tampering | `queueInterrupt` | high | mitigate | One active record and exact resend/timer count tests. |
| T-03-INT-STALE | Spoofing / Tampering | zero-argument diagnose result/prompt correlation | high | mitigate | FIFO-head-only result marking, timeout tombstones, generation compare before completion, and the real N timeout → N+1 → stale result → prompt trigger-path test. |
| T-03-INT-QUEUE | Tampering | timeout cleanup | medium | mitigate | Local-only cleanup and zero server-removal assertions. |
</threat_model>

<verification>
- Focused syntax/release gates run after each task.
- Real Mudlet/Busted or exact corresponding CI is required for behavior.
- Host/static checks never substitute for the authoritative runtime.
- Diagnose ordering proves normal zero-argument result→prompt completion, FIFO/reset independence, and N timeout → N+1 → old result → prompt tombstone absorption with no N+1 mutation.
</verification>

<success_criteria>
- D-05, D-06, D-07, and D-08 are implemented with exact callback/send counts.
- SAFE-02 attacks/prequeue remain held by every unrelated owner.
- Every commit-bearing task synchronizes version fields and avoids built artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-02-SUMMARY.md` when done.
</output>
