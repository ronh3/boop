---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tests/boop_interrupt_spec.lua
  - tests/boop_diag_spec.lua
  - tests/boop_diag_timeout_spec.lua
  - tests/boop_prequeue_spec.lua
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - "Per D-05, each queued interrupt holds attacks and prequeue until its exact completion evidence or timeout wins."
    - "Per D-06, timeout releases only the interrupt owner and preserves unrelated holds and queued operator work."
    - "Per D-07, repeating the same or another interrupt while pending sends nothing and does not restart timing."
    - "Per D-08, the first terminal callback releases and reports once; every later callback is a zero-effect no-op."
  artifacts:
    - path: "src/scripts/boop/boop_ui.lua"
      provides: "Generation-owned creation and idempotent queueInterrupt dispatch"
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Operation-specific prompt/result completion through one terminal API"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Current-generation diagnose result evidence through boop.onDiagReadyLine"
    - path: "tests/boop_interrupt_spec.lua"
      provides: "Repeat and prompt/timeout ordering matrix"
    - path: "tests/boop_diag_spec.lua"
      provides: "Diagnose result-then-prompt ordering matrix"
    - path: "tests/boop_diag_timeout_spec.lua"
      provides: "Timeout isolation and stale-generation coverage"
    - path: "tests/boop_prequeue_spec.lua"
      provides: "No prequeue escape while another owner remains"
  key_links:
    - from: "src/scripts/boop/boop_ui.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "state.diag.operation generation and interrupt:<generation> owner"
      pattern: "(interrupt:|completeInterrupt)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "boop.onDiagReadyLine marks resultSeen only on the matching active diagnose generation; its matching prompt delegates terminal release to completeInterrupt"
      pattern: "(onDiagReadyLine|resultSeen|completeInterrupt)"
---

<objective>
Implement D-05 through D-08 for `diag`, `matic`, `catarin`, `fly`, `ts`, and `leap` as one generation-owned interrupt lane.

Purpose: Repeats and stale prompt/result/timeout callbacks must not resend, release a newer operation, or bypass an unrelated attack/prequeue hold.
Output: Exact interrupt operation state, first-terminal API, isolated timeout behavior, and callback-order regression coverage.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md

<interfaces>
Consume Plan 03-01 exact-owner APIs.

Created symbols and values — exhaustive:
- State field `state.diag.generation`, initialized to `0`.
- State field `state.diag.operation`, inactive value `false`; an active record has exact fields `generation`, `name`, `command`, `completionMode`, `resultSeen`, `terminal`, `blockerOwner`, `timeoutTimer`, and `startedAt`.
- Exact `completionMode` values `prompt` and `result_then_prompt`.
- Stable blocker owner form `interrupt:<generation>` and blocker code `interrupt_pending`; affected systems are `combat` and `queue`.
- Public API `boop.runtime.completeInterrupt(generation, terminalReason)`.
- Stable terminal reason values `prompt_complete`, `diagnose_result_prompt`, and `timeout`.

Existing symbols modified, not created:
- `DOMAIN_DEFAULTS.diag`; its `generation` and `operation` defaults are initialized and deep-copied independently on ensure/reset.
- Local `queueInterrupt(label, command, opts)`.
- Public command functions `boop.ui.diag`, `boop.ui.matic`, `boop.ui.catarin`, `boop.ui.fly`, `boop.ui.touchShield`, and `boop.ui.leap`.
- Event result handler `boop.onDiagReadyLine`; it accepts or captures the diagnose generation, marks only the matching current nonterminal operation's `resultSeen`, and never releases an owner itself.
- Existing compatibility fields `state.diag.hold`, `awaitPrompt`, `timeoutTimer`, and `label`; they become derived display fields only.
- Existing `promptStep`, `boop.runtime.step`, and `boop.runtime.applyEffects`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeat and terminal-order interrupt contracts</name>
  <files>tests/boop_interrupt_spec.lua, tests/boop_diag_spec.lua, tests/boop_diag_timeout_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_interrupt_spec.lua` — modified prompt-mode interrupt timer/send contracts and primary callback-capture analog.
    - `tests/boop_diag_spec.lua` — modified diagnose result-then-prompt and default-isolation coverage.
    - `tests/boop_diag_timeout_spec.lua` — modified timeout isolation and stale-generation coverage.
    - `src/scripts/boop/boop_ui.lua` — `queueInterrupt` and public interrupt command entry points under test.
    - `src/scripts/boop/boop_runtime.lua` — `DOMAIN_DEFAULTS.diag`, prompt processing, effects, and `completeInterrupt` implementation seam.
    - `tests/support/boop_test_helper.lua` — reset and exact-owner fixture behavior.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-05 through D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — row 03-W0-02 and interrupt/repeat ordering rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Interrupt command seam`, `tests/boop_interrupt_spec.lua`, `tests/boop_diag_spec.lua`, `tests/boop_diag_timeout_spec.lua`, and `Capture callbacks as an ordered queue`.
  </read_first>
  <behavior>
    - "Same and different repeats keep one generation, send, owner, and timer."
    - "Prompt-before-result does not complete diag."
    - "Prompt/result then timeout and timeout then prompt/result each terminate once."
    - "Old generation callbacks cannot mutate a newer operation."
  </behavior>
  <acceptance_criteria>
    - Each prompt-mode interrupt request sends exactly one unchanged `queue addclearfull freestand <command>` command and schedules exactly one timeout; same-command and different-command repeats add zero sends and zero timers.
    - `diag` remains active after a prompt without a diagnose result, then terminates exactly once after result plus prompt or timeout.
    - Success/prompt→timeout and timeout→success/prompt orders each produce one terminal transition, one owner release, one warning only on timeout, and zero late-callback ticks or state mutation.
    - A captured old-generation callback leaves a newer operation's generation, owner, command, timer ID, and terminal state unchanged.
    - Fresh resets materialize `state.diag.generation == 0` and `state.diag.operation == false`, and mutating one reset's operation record cannot affect the next reset.
    - One patch increment synchronizes `mfile.version`, `mfile.title`, `boop.version`, and the CODEX checkpoint; the version release gate succeeds.
  </acceptance_criteria>
  <action>
Capture every scheduled callback by stable timer ID. Table-drive all prompt-mode commands and `diag` separately, asserting exact command text, `send(..., false)` count, owner/generation/operation fields, timer count, warning/terminal/tick counts, and retained unrelated owners for both callback orders. Add reset/deep-copy tests proving `DOMAIN_DEFAULTS.diag.generation` and `DOMAIN_DEFAULTS.diag.operation` materialize independently for every fresh state. Add same-request and different-request repeat rows with zero resend and zero timer replacement. Assert timeout sends no server queue removal and late callbacks produce no state, output, tick, or timer mutation. Before commit, increment one patch version and synchronize all required version fields plus CODEX. Do not edit generated artifacts.
  </action>
  <verify>
    <automated>luac -p tests/boop_interrupt_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>The D-05–D-08 ordering and repeat matrix is RED for missing generation/terminal ownership, with exact side-effect counts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the generation-owned interrupt lane</name>
  <files>src/scripts/boop/boop_ui.lua, src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, tests/boop_diag_spec.lua, tests/boop_diag_timeout_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_ui.lua` — modified `queueInterrupt` and interrupt command handlers.
    - `src/scripts/boop/boop_runtime.lua` — modified `DOMAIN_DEFAULTS.diag`, prompt step, effects, and terminal API.
    - `src/scripts/boop/boop_events.lua` — modified `boop.onDiagReadyLine` result-evidence entry point.
    - `tests/boop_diag_spec.lua` — modified focused reset/deep-copy and diagnose operation assertions.
    - `tests/boop_interrupt_spec.lua` — Task 1 prompt-mode RED contracts.
    - `tests/boop_diag_timeout_spec.lua` — modified exact result-after-timeout and old-result/prompt-after-new-generation contracts.
    - `tests/support/boop_test_helper.lua` — reset behavior used to prove independent defaults.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-05 through D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — interrupt/repeat ordering rows and exact side-effect requirements.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — sections `Pattern 2: Generation-Owned Operation with First-Terminal-Wins` and `First-Terminal-Wins Operation`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Namespace and state-default pattern` and `Interrupt command seam`.
  </read_first>
  <behavior>
    - "A fresh request creates one interrupt owner and timer."
    - "Every terminal path delegates to completeInterrupt."
    - "Repeats preserve the active operation and report pending state."
    - "Timeout clears only local intent, timer, and exact owner."
    - "A diagnose result marks resultSeen only for its matching active generation, and only that generation's later prompt may complete it."
  </behavior>
  <acceptance_criteria>
    - `DOMAIN_DEFAULTS.diag` explicitly defines `generation = 0` and `operation = false`; `ensureState()` deep-copies both and helper reset restores independent values after mutation.
    - A fresh dispatch increments generation once, creates one exact `interrupt:<generation>` owner with `interrupt_pending`, sends once, and captures one timeout timer.
    - `completeInterrupt(generation, terminalReason)` returns true only for the current nonterminal operation, marks terminal before effects, clears only its exact owner/timer/derived display state, and returns false with zero mutation for every late call.
    - `boop.onDiagReadyLine` marks `resultSeen = true` only when the supplied/captured generation matches the current active nonterminal `diag` operation with `completionMode == "result_then_prompt"`; it does not clear the owner, kill the timer, tick, or emit terminal output.
    - Prompt completion is used for `matic`, `catarin`, `fly`, `ts`, and `leap`; for `diag`, a matching prompt after current-generation result evidence delegates exactly once to `completeInterrupt(generation, "diagnose_result_prompt")`.
    - In `tests/boop_diag_timeout_spec.lua`, a result arriving after timeout is a zero-effect no-op, and an older generation's result then prompt invoked after a newer diagnose generation begins cannot set the newer operation's `resultSeen`, clear `interrupt:<new-generation>`, kill its timer, tick, warn, or emit completion output.
    - Timeout emits one warning/trace, sends no server queue-removal command, and preserves unrelated owner records and queued operator work.
    - Focused interrupt/diag/timeout specs, `luac -p`, and the release gate succeed after one synchronized patch increment across all version checkpoints.
  </acceptance_criteria>
  <action>
Implement the exact created inventory in `<interfaces>`. Add `generation = 0` and `operation = false` to existing `DOMAIN_DEFAULTS.diag`, rely on `ensureState()` deep-copy semantics, and make reset/default-isolation assertions in `tests/boop_diag_spec.lua` pass. Make existing `queueInterrupt` allocate `state.diag.generation`, construct `state.diag.operation`, set owner `interrupt:<generation>`, and reject any request while the operation record is active. `boop.runtime.completeInterrupt` must compare generation and terminal state, set terminal before effects, kill only the captured timer for hygiene, clear only the operation owner, clear the operation/derived display fields, trace owner/generation/reason, and return whether it won.

Modify `boop.onDiagReadyLine` in `boop_events.lua` to bind result evidence to a generation before mutation. It may use an explicitly supplied generation for captured-callback tests and otherwise capture the current active operation's generation at entry; it returns without effects unless that generation still identifies the current nonterminal `diag` operation in `result_then_prompt` mode. On a match, set only that operation record's `resultSeen = true` and trace the generation. The matching prompt path must carry the same generation and call `boop.runtime.completeInterrupt(generation, "diagnose_result_prompt")`; prompt-mode interrupts continue to call the terminal API with `prompt_complete`. No result or prompt path may directly clear an owner.

In `tests/boop_diag_timeout_spec.lua`, capture result, prompt, and timeout callbacks with their generation. Add one exact ordering where timeout wins and a later result produces zero mutation/effects. Add a second ordering that starts diagnose generation N, captures its result and prompt callbacks, lets N terminate, starts generation N+1, then invokes N's result and prompt and proves N+1's `resultSeen`, owner, timer, operation fields, output, sends, and tick counts are unchanged. Keep the normal current-generation result-then-prompt success case in `tests/boop_diag_spec.lua`. Timeout warns once and performs no server-side queue removal; fresh dispatch retains existing `queue addclearfull freestand <command>` behavior. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_ui.lua src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua tests/boop_interrupt_spec.lua tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run focused Mudlet/Busted behavior for interrupt, diagnose, and diagnose-timeout specs, including result after timeout and generation N result/prompt after generation N+1 starts.</authoritative>
  </verify>
  <done>Interrupts are idempotent first-terminal operations, and diagnose result/prompt evidence is generation-bound so stale callbacks cannot mutate or clear a newer hold.</done>
</task>

<task type="auto">
  <name>Task 3: Prove timeout isolation at prequeue boundaries</name>
  <files>tests/boop_prequeue_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_prequeue_spec.lua` — modified captured-prequeue and owner-clear ordering coverage.
    - `src/scripts/boop/boop_runtime.lua` — aggregate `shouldHold` and interrupt terminal behavior under test.
    - `src/scripts/boop/boop_events.lua` — prequeue scheduling/callback effect boundary.
    - `src/scripts/boop/boop_ui.lua` — fresh interrupt dispatch and timeout terminal source.
    - `tests/boop_interrupt_spec.lua` — command/timer generation contract from Task 1.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-01, D-02, D-06, and D-08.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — row 03-W0-02 and interrupt owner ordering.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `tests/boop_prequeue_spec.lua`, `Coordinator hold-before-effect pattern`, `Ownership identity`, and `Prove cleanup/authorization precedes the first side effect`.
  </read_first>
  <behavior>
    - "Clearing interrupt owner leaves prequeue at zero while another combat/queue owner remains."
    - "Captured prequeue callbacks recheck aggregate owners."
    - "Fresh interrupt dispatch after local timeout retains ADDCLEARFULL behavior."
  </behavior>
  <acceptance_criteria>
    - Interrupt-first and unrelated-owner-first clear orders both keep standard/rage sends, alias changes, prequeue scheduling, and captured callback execution at zero while one combat/queue owner remains.
    - A prequeue callback captured before an owner is added rechecks aggregate ownership at invocation and consumes no queued state while held.
    - Clearing the final affecting owner permits exactly one normal reevaluation, with no duplicate prequeue timer or tick.
    - A fresh post-timeout interrupt uses the unchanged `queue addclearfull freestand <command>` dispatch and creates one new generation/timer.
    - `luac -p tests/boop_prequeue_spec.lua` and the release gate succeed after one synchronized patch increment across all version checkpoints.
  </acceptance_criteria>
  <action>
Seed interrupt plus unrelated owners, clear them in both orders, and assert exact standard/rage sends, alias changes, timers, ticks, and retained owner records. Capture a prequeue callback before adding an owner and prove the callback becomes a no-op at invocation. After timeout and final unrelated-owner release, assert one fresh evaluation and the unchanged fresh interrupt queue command. Synchronize the next patch version before commit; do not introduce a new hold command or edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_prequeue_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run interrupt, diag, timeout, and prequeue specs plus the full suite in real Mudlet; use GitHub Actions when the local AppImage is absent.</authoritative>
  </verify>
  <done>SAFE-02 prequeue cannot escape through interrupt timeout or one-owner release.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol list; no additional interrupt constant, function, API, owner, field, completion mode, or terminal reason may be introduced without adding it there.
- Existing files only; no new command, alias, dependency, schema, manifest, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator alias → Achaea queue | Repeats can duplicate commands or replace timing state. |
| Prompt/result/timer → operation state | Callback order is nondeterministic and may be stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-INT-REPEAT | Denial of Service / Tampering | `queueInterrupt` | high | mitigate | One active record and exact resend/timer count tests. |
| T-03-INT-STALE | Spoofing / Tampering | `completeInterrupt` | high | mitigate | Generation/terminal compare before effects and both-order tests. |
| T-03-INT-QUEUE | Tampering | timeout cleanup | medium | mitigate | Local-only cleanup and zero server-removal assertions. |
</threat_model>

<verification>
- Focused syntax/release gates run after each task.
- Real Mudlet/Busted or exact corresponding CI is required for behavior.
- Host/static checks never substitute for the authoritative runtime.
- Diagnose ordering proves result-after-timeout and old result/prompt-after-new-generation callbacks are zero-effect no-ops.
</verification>

<success_criteria>
- D-05, D-06, D-07, and D-08 are implemented with exact callback/send counts.
- SAFE-02 attacks/prequeue remain held by every unrelated owner.
- Every commit-bearing task synchronizes version fields and avoids built artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-02-SUMMARY.md` when done.
</output>
