---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: 24
type: execute
wave: 24
depends_on:
  - 03-23
files_modified:
  - tests/boop_diag_spec.lua
  - tests/boop_diag_timeout_spec.lua
  - tests/boop_gold_retry_spec.lua
  - src/scripts/boop/boop_ui.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-04
  - WALK-02
gap_closure: true
gap_ids:
  - G-03-8
must_haves:
  truths:
    - "Diag sends valid `clearqueue all` followed by `queue addclearfull freestand diagnose`, and never sends bare `queue clear`."
    - "Before diag replaces native queues, it transfers any sent pickup or pack dispatch into exact interrupt-owned replay state."
    - "Pickup before and after diag uses `queue add full get sovereigns`; packing remains on `freestand`."
    - "Prompt/result completion and timeout completion are both exact, first-terminal interrupt releases; each causes at most one normal tick and one eligible gold replay."
    - "A real diag collision completes pickup and pack from explicit inventory evidence without a room refresh, duplicate get/put, abandoned gold, or unrelated Lua-state clearing."
    - "If the fresh timeout on the replayed get or put fires before explicit evidence, the gold owner remains held and actionable with no duplicate replay; movement cancels pickup once and explicit disable cancels pack once."
    - "Live evidence proves both timeout/release orderings and the complete diag-to-gold recovery path under Mudlet's native queue behavior."
  artifacts:
    - path: src/scripts/boop/boop_ui.lua
      provides: "Diag interrupt integration that transfers displaced gold ownership and uses only valid native queue syntax."
    - path: tests/boop_diag_spec.lua
      provides: "Real prompt/result diag collision coverage against the native queue model."
    - path: tests/boop_diag_timeout_spec.lua
      provides: "Exact timeout terminal handling and one-tick replay coverage."
    - path: tests/boop_gold_retry_spec.lua
      provides: "Integrated pickup-to-pack completion plus fresh replay-timeout and explicit-invalidation coverage through the real diag command path."
  key_links:
    - from: src/scripts/boop/boop_ui.lua
      to: src/scripts/boop/boop_events.lua
      via: "Diag marks the exact sent gold phase displaced before issuing clearqueue all."
      pattern: "displaceGold"
    - from: src/scripts/boop/boop_ui.lua
      to: src/scripts/boop/boop_runtime.lua
      via: "Prompt/result or timeout completes the exact interrupt owner before the ordinary tick re-evaluates replay."
      pattern: "completeInterrupt"
    - from: tests/boop_gold_retry_spec.lua
      to: src/scripts/boop/boop_events.lua
      via: "The real diag path proves a fresh replay timeout is nonterminal and only explicit stage-valid invalidation releases the gold owner."
      pattern: "pickup_pending|pack_pending"
    - from: tests/boop_diag_spec.lua
      to: tests/support/boop_test_helper.lua
      via: "Real diag sends are applied to the native queue model and checked for syntax errors and global replacement."
      pattern: "clearqueue all|addclearfull"
---

<objective>
Wire diag's destructive native queue replacement to the gold displacement contract and prove exact completion/replay plus fail-closed fresh-timeout recovery through prompt/result and timeout paths.

Purpose: Finish G-03-8 end to end by replacing invalid native queue syntax with `clearqueue all` and making diag explicitly responsible for any pickup or pack command it displaces, while ensuring elapsed time alone cannot abandon replayed gold and preserving first-terminal interrupt/fail-closed queue semantics per D-01 through D-12A.
Output: Valid diag dispatch, exact timeout/prompt release behavior, integrated native-queue/fresh-timeout/invalidation regressions, and actionable live-UAT evidence instructions.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
@.planning/debug/phase-03-diag-gold-queue-collision.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-23-SUMMARY.md
@tests/support/boop_test_helper.lua
@tests/boop_diag_spec.lua
@tests/boop_diag_timeout_spec.lua
@tests/boop_gold_retry_spec.lua
@src/scripts/boop/boop_ui.lua
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_runtime.lua
</context>

<scope_exception>
This is one heavy integration task because real diag dispatch, exact terminal release, the native queue model, and pickup-to-pack completion must be observed in one causal test path. The task keeps its RED tests uncommitted until the production wiring and synchronized package bump are ready for one release-gated commit.
</scope_exception>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate diag with exact gold displacement and completion, then bump the synchronized package version</name>
  <files>tests/boop_diag_spec.lua, tests/boop_diag_timeout_spec.lua, tests/boop_gold_retry_spec.lua, src/scripts/boop/boop_ui.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-UAT.md
    - .planning/debug/phase-03-diag-gold-queue-collision.md
    - .planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-23-SUMMARY.md
    - tests/support/boop_test_helper.lua
    - tests/boop_interrupt_spec.lua
    - tests/boop_diag_spec.lua
    - tests/boop_diag_timeout_spec.lua
    - tests/boop_gold_retry_spec.lua
    - src/scripts/boop/boop_ui.lua
    - src/scripts/boop/boop_events.lua
    - src/scripts/boop/boop_runtime.lua
    - mfile
    - src/scripts/boop/boop_init.lua
    - CODEX.md
  </read_first>
  <behavior>
    - "Diag emits exactly `clearqueue all` followed by `queue addclearfull freestand diagnose`; the native queue model records no invalid queue command."
    - "When a `queue add full get sovereigns` pickup is already present, real diag marks that exact dispatch displaced before `clearqueue all` removes it."
    - "When pack is already present, real diag preserves its inventory-owned operation and replays put after exact release without requiring a room response."
    - "A matching prompt/result completes the exact interrupt once, the existing completion flow performs one normal tick, and one displaced gold command re-enters the queue."
    - "A diag timeout that wins the first-terminal race completes the exact interrupt and performs one normal tick; a later prompt/result cannot cause a second replay."
    - "For each terminal path, invoking the old gold timeout before release or after release/replay cannot abandon or duplicate gold."
    - "For replayed pickup under each diag terminal path, invoking the fresh replay timeout with no evidence keeps the exact owner/generation/phase/room evidence held, emits one actionable warning/status, and repeated ticks/callbacks send no second get."
    - "Moving rooms after that pickup timeout releases the gold owner exactly once; repeated movement and every old/fresh callback remain no-ops."
    - "For replayed pack under each diag terminal path, invoking the fresh replay timeout with no evidence keeps the exact owner/generation/phase/inventory evidence held, emits one actionable warning/status, and repeated ticks/callbacks send no second put."
    - "Calling the real `boop.ui.setEnabled(false)` path after that pack timeout releases the gold owner exactly once; repeated disable/flee/failure callbacks cannot release it again."
    - "Replayed pickup advances on explicit get evidence to one pack command, and explicit put evidence completes with no residual in-flight operation."
  </behavior>
  <action>
First update the three focused specs to use Plan 03-23's native queue model for actual sends. In `tests/boop_diag_spec.lua`, assert the exact ordered pair `clearqueue all` then `queue addclearfull freestand diagnose`, absence of queue validation errors, global native replacement, exact interrupt completion through the real prompt/result trigger, and one post-release tick. In `tests/boop_diag_timeout_spec.lua`, assert timeout wins only when it is the first exact terminal signal, releases the same owner, runs one normal tick, and makes a later prompt/result idempotent. In `tests/boop_gold_retry_spec.lua`, drive real `boop diag` collisions for both `full` pickup and `freestand` pack dispatches and both old-gold-timeout/release orderings. For both prompt/result and diag-timeout terminal winners, capture the fresh timer armed by the one replay, fire it without result evidence, and assert the operation remains held in Plan 03-23's explicit-evidence wait with one warning/status update, no `pending_timeout` terminal, no second get/put, and no new timer across repeated ticks/callbacks. Then move rooms through real `onRoomInfo` for pickup and call real `boop.ui.setEnabled(false)` for pack; instrument owner clearing/terminal trace and assert exactly one terminal release despite repeated invalidation and stale callbacks. Retain controls that provide explicit get-then-put evidence through final completion. Confirm RED before production changes and do not commit the RED state.

Then update `src/scripts/boop/boop_ui.lua` queue-interrupt dispatch. After the exact interrupt generation/owner and blocker exist but immediately before the first destructive queue send, call the Plan 03-23 gold-displacement entry point with that exact owner and a diag reason. Replace invalid bare `queue clear` with valid `clearqueue all`, then retain the existing `queue addclearfull freestand diagnose` command. Both commands have global-clear semantics, so ownership transfer must happen before `clearqueue all`; do not globally reset unrelated Lua runtime queue, walk, target, or attack ownership.

Keep prompt/result completion on the existing exact first-terminal route per D-05 through D-08. On the timeout callback, call `completeInterrupt` for the captured exact generation and owner; only when it returns true, invoke one ordinary `boop.tick()` so displaced gold can be re-evaluated after release. If prompt/result already won, the timeout performs neither release nor tick; if timeout won, a later trigger performs neither. Consume Plan 03-23's fresh replay-timeout state without adding another tick or replay. Preserve the existing operator invalidation routes: moved Room.Info terminally invalidates room-owned pickup, while `boop.ui.setEnabled(false)` and auto-flee terminally invalidate inventory-owned pack through `clearGoldQueueIntent`. Ensure each route clears the exact owner once and that the one-time warning/status tells the operator which route is available. Preserve owner aggregation/priority/status contracts D-01 through D-04 and gold room/inventory/queue-type authority D-09 through D-12A. Do not lengthen interrupt or gold timeouts, introduce sleeps, infer completion from elapsed time, or weaken the D-13 through D-24 walk/manual/stop safety contracts.

No command help or README wording change is needed: the public claim that diag clears queued work and queues diagnose remains accurate, while this task corrects the rejected clear command and repairs internal ownership.

Once all focused tests pass, read the synchronized current version, increment its patch component exactly once, and set the same new value in `mfile.version`, `mfile.title` as `boop Hunter NEW_VERSION`, `src/scripts/boop/boop_init.lua` `boop.version`, and the CODEX.md synchronized package-version checkpoint. Stage exactly the seven files listed in this task, inspect the staged paths, run `python3 tools/check_release_gates.py` immediately before committing, and create one package-affecting commit containing tests, implementation, and the synchronized bump. Do not edit built artifacts and do not push.
  </action>
  <acceptance_criteria>
    - "Real diag transfers an active pickup or pack dispatch to the exact interrupt owner before sending the first destructive command."
    - "Diag sends exactly `clearqueue all` followed by `queue addclearfull freestand diagnose`; it never sends bare `queue clear`, and the native queue model reports no validation error."
    - "A displaced pickup replays exactly one `queue add full get sovereigns`; a displaced pack replays exactly one `queue add freestand put sovereigns in <pack>`."
    - "Prompt/result and timeout are mutually idempotent first-terminal paths: the winner releases the exact interrupt and runs one normal tick, while every later terminal signal is a no-op."
    - "Both old gold-timeout orderings preserve the operation and permit at most one post-release replay."
    - "A fresh replay timeout remains nonterminal and observable, sends no duplicate across repeated ticks/callbacks, and retains exact owner/generation/phase evidence."
    - "Moved Room.Info releases timed-out replayed pickup once; real `boop.ui.setEnabled(false)` releases timed-out replayed pack once; repeated invalidation and stale callbacks cannot release twice."
    - "Explicit pickup evidence advances to one pack command and explicit put evidence completes with no residual gold or interrupt owner and no room refresh."
    - "All seven listed files are the only staged paths; all four version checkpoints are equal and monotonically greater than the task-start value; focused Busted, Lua syntax, diff hygiene, release gates, and direct `muddle` pass before the single commit."
  </acceptance_criteria>
  <verify>
    <automated>env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_diag_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_diag_timeout_spec.lua &amp;&amp; env BOOP_REPO_ROOT="$PWD" TESTS_DIRECTORY="$PWD/tests" busted --helper=tests/support/boop_host_busted_helper.lua tests/boop_gold_retry_spec.lua &amp;&amp; luac -p tests/boop_diag_spec.lua tests/boop_diag_timeout_spec.lua tests/boop_gold_retry_spec.lua src/scripts/boop/boop_ui.lua src/scripts/boop/boop_init.lua &amp;&amp; git diff --check &amp;&amp; python3 tools/check_release_gates.py &amp;&amp; muddle</automated>
    <human-check>
      In a real Mudlet profile with boop enabled, turn auto gold on and create a room where pickup queues `queue add full get sovereigns`. Immediately run `boop diag`; capture the native queue output plus `boop status`, then let the matching diagnose result/prompt complete the interrupt. Confirm diag uses `clearqueue all`, there is no `Unknown queue specified`, the displaced `full` get is replayed once without `ql`, `ih`, or another room response, explicit inventory pickup causes one `freestand` put, and explicit pack evidence completes the operation. Repeat with diagnose allowed to time out before its result/prompt and confirm one replay; then repeat while invoking the old gold timeout on each side of interrupt release. For pickup and pack, deliberately withhold result evidence after replay until the fresh replay timer fires: status must retain the same gold owner and show the stage-specific recovery, the warning/trace must print once, and waiting or running status/tick-producing commands must not queue a second get/put. Move rooms to invalidate pickup and verify one owner release. For pack, run `boop off` to exercise `boop.ui.setEnabled(false)` and verify one owner release; repeat the invalidation and late callbacks to prove no second terminal. Capture owner, generation, phase, native queue contents, replay count, fresh-timeout status/warning, explicit invalidation, and final no-operation state for 03-UAT.md.
    </human-check>
  </verify>
  <done>Real diag transfers pickup and pack dispatch ownership before native replacement, sends valid `clearqueue all` followed by addclearfull with no invalid bare clear, completes exactly once through either prompt/result or timeout, replays `full` pickup or `freestand` pack once through a normal tick in both callback orderings, keeps a fresh replay timeout nonterminal and actionable without duplicate replay, releases pickup/pack exactly once through movement/disable, and completes gold from explicit evidence; all four version fields match at one higher patch; focused tests, release gates, and package build pass; live evidence is ready to mark G-03-8 passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lua interrupt coordinator → Mudlet native queue | Diag intentionally replaces native queued commands and must account for work removed by that operation. |
| Prompt/result and timer → interrupt terminal state | Competing terminal signals must not release twice or trigger duplicate gold replay. |
| Inventory evidence → pickup/pack completion | Only explicit current evidence may advance or complete the preserved gold operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-24-01 | Tampering | `boop_ui.lua` diag queue replacement | high | mitigate | Transfer the exact sent gold dispatch before `clearqueue all` and replace rejected bare queue-clear syntax. |
| T-03-24-02 | Elevation of Privilege | prompt/result and timeout callbacks | high | mitigate | Gate release and the follow-up tick on exact `completeInterrupt` success; test both terminal winners. |
| T-03-24-03 | Denial of Service | gold replay after diag | high | mitigate | Run one ordinary tick only after exact release and verify one pickup/pack replay through explicit completion. |
| T-03-24-04 | Spoofing | inventory completion evidence | medium | mitigate | Retain generation/owner checks and require explicit pickup/pack evidence before phase advancement. |
| T-03-24-05 | Denial of Service | fresh replay timeout | high | mitigate | Keep the owner nonterminal with one actionable warning/status, prohibit duplicate replay, and prove stage-valid movement/disable invalidation releases once. |
</threat_model>

<verification>
- Focused Lua specs prove valid native syntax, real diag displacement, exact prompt/result and timeout completion, both old-timer orderings, nonterminal fresh replay timeouts, no duplicate replay, exact movement/disable invalidation, and full pickup-to-pack completion.
- Syntax checks cover every modified Lua file; `python3 tools/check_release_gates.py` enforces the synchronized monotonic version and package invariants.
- `muddle` proves the package builds from source.
- The parent session must run the complete Mudlet/AppImage suite and, after all phase mutations reach immutable final HEAD, push and run `tools/wait_for_exact_ci.sh` as required by AGENTS.md.
- Live UAT evidence must record valid native queue output, exact interrupt owner/generation, preserved gold phase/evidence, both old-timeout/release orderings, one replay, the fresh replay timeout retaining the owner with no duplicate, one stage-specific warning/status, movement/disable invalidation with one terminal release, explicit get/put advancement, and no unrelated Lua-state clearing.
</verification>

<multi_source_coverage_audit>
## GOAL Coverage

| Source item | Status | Plan coverage |
|-------------|--------|---------------|
| Close failed diagnosed G-03-7 without stale prior-room dispatch | COVERED | 03-20 establishes order-independent evidence/application; 03-21 generation-owns irreversible dispatch; 03-22 proves live manual-to-auto wake-up. |
| Close failed diagnosed G-03-8 without abandoned gold | COVERED | 03-23 establishes native queue/displacement ownership plus nonterminal fresh-timeout recovery; 03-24 integrates real diag completion, one replay, and exact movement/disable invalidation. |

## REQ Coverage

| Requirement | Status | Plan coverage |
|-------------|--------|---------------|
| SAFE-02 | COVERED | 03-20, 03-21, 03-23, and 03-24 preserve fail-closed current evidence and exact ownership. |
| SAFE-04 | COVERED | 03-20, 03-21, 03-23, and 03-24 prevent stale timer/generation/owner work from dispatching or terminally abandoning replayed gold. |
| WALK-01 | COVERED | 03-20 and 03-22 accept both complete response orders and wake a settled manual walk on the real auto command. |
| WALK-02 | COVERED | 03-20 through 03-24 preserve current-room/current-owner gates and do not clear unrelated shared queue work. |
| WALK-03 | COVERED | 03-22 preserves one-move dispatch while proving the manual-to-auto transition. |

## RESEARCH Coverage

| Research item | Status | Plan coverage |
|---------------|--------|---------------|
| Exact owner/generation authority at asynchronous boundaries | COVERED | 03-20 and 03-21 own room application/dispatch; 03-23 and 03-24 own displacement, old/fresh timers, explicit-evidence wait, invalidation, and interrupt completion. |
| `Room.Items.List` lacks room identity and must be fenced fail-closed | COVERED | 03-20 uses independent latches plus current Room.Info identity before application. |
| Native global queue clearing has no stable displaced-entry index | COVERED | 03-23 models both `clearqueue all` and addclearfull and records explicit operation-level displacement; 03-24 wires diag before the first destructive send. |
| Earlier ordered-response assumption contradicted by diagnosed live evidence | COVERED | 03-20 supersedes the ordered phase gate with order-independent per-fence latches and tests both orders. |
| Package legitimacy | COVERED | No new package-manager dependency or install is planned. |

## CONTEXT Decision Coverage

| Decisions | Status | Plan coverage |
|-----------|--------|---------------|
| D-01, D-02, D-03, D-04 | COVERED | 03-20 through 03-24 preserve owner aggregation, priority, derived compatibility state, and status semantics. |
| D-05, D-06, D-07, D-08 | COVERED | 03-23 preserves exact interrupt authority; 03-24 tests prompt/result versus timeout first-terminal idempotence. |
| D-09, D-10, D-11, D-12, D-12A | COVERED | 03-23 preserves pickup room authority, pack inventory authority, duplicates, settled evidence, and phase-specific queue types across fresh replay timeout; 03-24 proves explicit completion and stage-valid invalidation. |
| D-13, D-14, D-15, D-16 | COVERED | 03-20 and 03-22 preserve current Room.Info plus complete List, capped refresh behavior, and one-move dispatch. |
| D-17, D-18, D-19, D-20 | COVERED | 03-21 prevents stale action sends; 03-22 uses the exact manual-to-auto command and retains manual-move idempotence. |
| D-21, D-22, D-23, D-24 | COVERED | 03-20/03-21 invalidate only generation-owned room intent; 03-23/03-24 avoid broad shared-state clearing and preserve stop ownership/feedback. |

## UAT Gap Coverage

| Gap | Status | Plan coverage |
|-----|--------|---------------|
| G-03-7 | COVERED | 03-20, 03-21, 03-22 |
| G-03-8 | COVERED | 03-23, 03-24 |

## Exclusions

| Source item | Status | Reason |
|-------------|--------|--------|
| Deferred source-artifact ideas | EXCLUDED | Deferred items remain outside this diagnosed two-gap closure and have no task, artifact, or requirement coverage here. |
</multi_source_coverage_audit>

<success_criteria>
- G-03-8 is closed in the real diag path for pickup and pack under native queue replacement.
- Bare queue-clear syntax is absent from diag; valid `clearqueue all` precedes the retained addclearfull command.
- Initial and replayed pickup uses `full`; packing remains on `freestand`.
- Prompt/result and timeout are exact, mutually idempotent terminal paths, each allowing at most one normal-tick replay.
- Both stale gold timeout orderings are regression-covered without sleeps or longer timeouts.
- A replay's fresh timeout cannot terminally complete from elapsed time, cannot trigger another replay, and leaves visible stage-specific recovery.
- Movement invalidates replayed pickup once; explicit disable/flee invalidates replayed pack once; success/failure evidence remains authoritative.
- Live UAT can mark the gap passed from concrete native queue, owner/generation, replay, and explicit-completion evidence.
- Package version fields are monotonically synchronized in the task's single implementation commit.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-24-SUMMARY.md` when done.
</output>
