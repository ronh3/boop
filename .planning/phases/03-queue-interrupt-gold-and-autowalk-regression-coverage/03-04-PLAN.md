---
phase: 03-queue-interrupt-gold-and-autowalk-regression-coverage
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - tests/boop_runtime_spec.lua
  - tests/boop_gold_spec.lua
  - tests/boop_gold_retry_spec.lua
  - src/scripts/boop/boop_runtime.lua
  - src/scripts/boop/boop_events.lua
  - src/scripts/boop/boop_util.lua
  - mfile
  - src/scripts/boop/boop_init.lua
  - CODEX.md
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "Per D-09, detection, pickup, and pickup retries are owned by one exact room ID and room generation; room change invalidates them."
    - "Per D-10, confirmed pickup transfers to inventory-owned packing, which may complete or retry after movement."
    - "Per D-11, duplicate text/Add/List signals in one room coalesce into one lifecycle and one get."
    - "Per D-12, missing or unsettled evidence sends no loot command until current stable room evidence still shows gold."
    - "Gold sends get, then waits for confirmed pickup, then sends put; it never chains get/put with an attack."
  artifacts:
    - path: "src/scripts/boop/boop_runtime.lua"
      provides: "Canonical gold generation/operation defaults and gold flush effect support"
    - path: "src/scripts/boop/boop_events.lua"
      provides: "Room-owned acquisition, inventory-owned packing, exact owner authorization, retries, and terminals"
    - path: "src/scripts/boop/boop_util.lua"
      provides: "Combat dispatch without gold command prefixing"
    - path: "tests/boop_gold_spec.lua"
      provides: "Evidence, duplicate, transfer, command-order, and owner authorization contracts"
    - path: "tests/boop_gold_retry_spec.lua"
      provides: "Room/generation-guarded retry and timeout contracts"
  key_links:
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_runtime.lua"
      via: "roomObservation snapshot plus exact gold:<generation> owner exclusion"
      pattern: "(currentGoldOperation|goldDispatchAuthorized|gold:)"
    - from: "src/scripts/boop/boop_events.lua"
      to: "src/scripts/boop/boop_util.lua"
      via: "independent staged gold queue commands; combat dispatch no longer embeds loot"
      pattern: "(queueGoldCommand|executeAction)"
---

<objective>
Build the staged gold core required by D-09 through D-12 without room/event/tick cross-lifecycle expansion.

Purpose: Gold commands must remain bound to current evidence, transfer ownership only after confirmed pickup, and never share an attack command chain.
Output: Canonical gold operation, exact helper/API inventory, staged get-confirm-put behavior, retry/timeout guards, and focused gold specs.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CODEX.md
@README.md
@DESIGN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-01-SUMMARY.md
@.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-03-SUMMARY.md

<interfaces>
Created symbols and values — exhaustive:
- Local constant `GOLD_PHASE` with exact entries `DEFERRED_ROOM="deferred_room"`, `PICKUP_PENDING="pickup_pending"`, and `PACK_PENDING="pack_pending"`.
- State field `state.gold.generation`, initialized to `0`.
- State field `state.gold.operation`, inactive value `false`; active records have exact fields `generation`, `phase`, `terminal`, `blockerOwner`, `source`, `roomId`, `roomGeneration`, `goldItemId`, `packTarget`, `getRetries`, `putRetries`, `flushTimer`, and `timeoutTimer`.
- Stable owner form `gold:<generation>`.
- Exact blocker codes `gold_deferred_room`, `gold_pickup_pending`, and `gold_pack_pending`.
- New local functions `currentGoldOperation(generation, expectedPhase)`, `goldDispatchAuthorized(operation)`, `startGoldOperation(source, observation, packTarget)`, `transferGoldToPacking(generation)`, and `completeGoldOperation(generation, terminalReason)`.
- Stable terminal reason values `room_changed`, `get_success_no_pack`, `put_success`, `get_retry_exhausted`, `put_retry_exhausted`, `pending_timeout`, and `disabled`.

Existing symbols modified, not created:
- `DOMAIN_DEFAULTS.gold`; its `generation` and `operation` defaults are initialized and deep-copied independently on ensure/reset.
- Existing constant `AUTO_GOLD_FLUSH_SECONDS`, existing constant `GOLD_READY_QUEUE`, and existing local `queueGoldCommand`.
- Public `boop.clearGoldQueueIntent`, `boop.markGoldQueueIntent`, `boop.flushPendingGold`, `boop.maybeFlushPendingGold`, `boop.onGoldGetSuccess`, `boop.onGoldPutSuccess`, and `boop.onGoldCommandFailure`.
- Locals `stopGoldPendingTimeout`, `armGoldPendingTimeout`, `queueGoldCommands`, `clearPendingGoldDrop`, `maybeFlushPendingGold`, `onGoldDetected`, `retryGoldGet`, and `retryGoldPut`.
- `boop.runtime.step`/local `tickStep`, `boop.runtime.applyEffects`, and `boop.executeAction`.
- Existing top-level compatibility fields `autoGrabPending`, `autoGrabPendingAt`, `autoGrabTimer`, `getPending`, `putPending`, `getRetries`, `putRetries`, `packTarget`, and `pendingTimer`; they become derived-only or are removed after focused specs migrate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staged gold evidence, transfer, and retry contracts</name>
  <files>tests/boop_runtime_spec.lua, tests/boop_gold_spec.lua, tests/boop_gold_retry_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `tests/boop_runtime_spec.lua` — modified gold default/reset/deep-copy assertions.
    - `tests/boop_gold_spec.lua` — modified staged get-confirm-put, evidence, duplicate, transfer, and owner tests.
    - `tests/boop_gold_retry_spec.lua` — modified retry/timeout generation and wrong-room tests.
    - `src/scripts/boop/boop_runtime.lua` — `DOMAIN_DEFAULTS.gold`, tick/effect, and exact-owner authorization state under test.
    - `src/scripts/boop/boop_events.lua` — existing gold detection, command, retry, success, failure, and timeout seams.
    - `src/scripts/boop/boop_util.lua` — existing combat/gold chaining seam that later implementation removes.
    - `tests/support/boop_test_helper.lua` — reset, room-observation, and owner fixture APIs.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-09 through D-12.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — gold, evidence, timeout, and owner-authorization rows.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-RESEARCH.md` — section `Pattern 4: Two-Stage Gold Ownership`.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Current gold lifecycle seam to replace`, `tests/boop_gold_spec.lua`, `tests/boop_gold_retry_spec.lua`, and `Capture side effects to assert exact count and order`.
  </read_first>
  <behavior>
    - "Text/Add before a complete current list sends zero commands; current list with gold sends one get."
    - "Duplicate signals preserve generation/timers and send one get."
    - "Room change before success cancels pickup; success before room change preserves pack."
    - "Old timeout/new generation and retry after room change are zero-effect."
    - "Gold owner alone authorizes its current stage; any unrelated owner blocks without consuming retries."
  </behavior>
  <acceptance_criteria>
    - Fresh reset materializes `state.gold.generation == 0` and `state.gold.operation == false`; mutating a nested active operation after one reset does not affect a later reset.
    - Text/Add before complete current-room evidence sends zero commands; current evidence with sovereigns sends exactly one `queue add freestand get sovereigns` with echo `false`.
    - Duplicate text/Add/List signals preserve generation and timer IDs and produce zero additional get commands.
    - Room-change-before-success terminates room-owned pickup; success-before-room-change emits one later put when packing is configured and preserves inventory-owned packing after movement.
    - Initial get, initial put, get retry, and put retry each bypass only `gold:<generation>`; any listed unrelated owner produces zero sends and zero retry consumption.
    - Old timeout/new generation and retry-after-room-change callbacks produce zero sends and leave the current operation snapshot byte-for-byte equivalent.
    - One patch increment synchronizes all four version checkpoints, and focused Lua syntax plus release gates succeed.
  </acceptance_criteria>
  <action>
Add reset/deep-copy assertions for the exact gold defaults and operation fields. Replace simultaneous/chained expectations with ordered send capture: get only, confirmed get, put only, terminal, then reevaluation. Cover every gold/evidence/timeout ordering in 03-VALIDATION, exact command strings/echo flags, pack-enabled/disabled branches, duplicate text/Add/List signals, wrong-room callbacks, and inventory pack retry after movement. For get, put, and each retry, test exact self-owner exclusion and unrelated `room:observation`, `flee:active`, `pull:<generation>`, and `interrupt:<generation>` owners with zero sends and unchanged retries. Synchronize the next patch version before commit; never edit built output.
  </action>
  <verify>
    <automated>luac -p tests/boop_runtime_spec.lua tests/boop_gold_spec.lua tests/boop_gold_retry_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
  </verify>
  <done>D-09–D-12 focused gold contracts are RED with exact owner, room, generation, phase, retry, timer, and command-order assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement room-owned pickup and inventory-owned packing</name>
  <files>src/scripts/boop/boop_runtime.lua, src/scripts/boop/boop_events.lua, src/scripts/boop/boop_util.lua, tests/boop_runtime_spec.lua, tests/boop_gold_spec.lua, mfile, src/scripts/boop/boop_init.lua, CODEX.md</files>
  <read_first>
    - `src/scripts/boop/boop_runtime.lua` — modified `DOMAIN_DEFAULTS.gold`, tick/effect, and operation state.
    - `src/scripts/boop/boop_events.lua` — modified staged gold lifecycle and command boundaries.
    - `src/scripts/boop/boop_util.lua` — modified combat dispatcher; remove gold prefixing while preserving attack dispatch.
    - `tests/boop_runtime_spec.lua` — modified focused gold default/reset/deep-copy assertions.
    - `tests/boop_gold_spec.lua` — modified focused staged lifecycle assertions.
    - `tests/boop_gold_retry_spec.lua` — Task 1 retry/timeout RED contracts.
    - `tests/support/boop_test_helper.lua` — reset, exact-owner, and room-observation fixtures.
    - `mfile` — modified package version/title metadata.
    - `src/scripts/boop/boop_init.lua` — modified `boop.version`.
    - `CODEX.md` — modified synchronized package-version checkpoint.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-CONTEXT.md` — D-09 through D-12.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-VALIDATION.md` — gold ordering and exact-owner authorization invariants.
    - `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-PATTERNS.md` — sections `Namespace and state-default pattern`, `Current gold lifecycle seam to replace`, `Gold chaining seam to remove`, `Gold ownership transfer`, and `Shared room observation`.
  </read_first>
  <behavior>
    - "Every gold side effect rechecks generation, phase, terminal, owner, and relevant room evidence immediately before send."
    - "Confirmed pickup atomically transfers to PACK_PENDING."
    - "Room change clears only DEFERRED_ROOM/PICKUP_PENDING."
    - "Combat execution sends only its supplied combat action."
  </behavior>
  <acceptance_criteria>
    - Existing `DOMAIN_DEFAULTS.gold` explicitly defines `generation = 0` and `operation = false`; `ensureState()` and helper reset deep-copy them independently, as proven by focused runtime/gold assertions.
    - `startGoldOperation` creates one same-room lifecycle, uses `DEFERRED_ROOM` without sending when evidence is incomplete, and requests room items no more than once.
    - `PICKUP_PENDING` queues only get; confirmed pickup atomically enters `PACK_PENDING` before queuing only put, or terminates with `get_success_no_pack`.
    - Every initial/retry/timeout/success/failure side effect compares generation, phase, terminal, exact owner, and relevant room evidence immediately before send or mutation.
    - Room change clears only `DEFERRED_ROOM` and `PICKUP_PENDING`; `PACK_PENDING` remains valid across movement.
    - `boop.executeAction` emits only the supplied combat action and contains no gold get/put prefix construction or mark-intent call.
    - Focused runtime/gold/retry specs, Lua syntax, and the release gate succeed after one synchronized version patch increment across all four checkpoints.
  </acceptance_criteria>
  <action>
Implement the exact created inventory in `<interfaces>`. Add `generation = 0` and `operation = false` to existing `DOMAIN_DEFAULTS.gold`; preserve `ensureState()` deep-copy semantics and make focused runtime/gold reset-isolation assertions pass. `startGoldOperation` coalesces same-room evidence; incomplete evidence creates `DEFERRED_ROOM`, requests the shared list once, and sends nothing. Current complete evidence that still includes sovereigns enters `PICKUP_PENDING`, sets `gold:<generation>` across gold/combat/queue/walk, and queues only `get sovereigns`. `goldDispatchAuthorized` must use exact owner exclusion and reject every unrelated owner. `transferGoldToPacking` drops room ownership fields atomically and either completes without pack or enters `PACK_PENDING` before queueing only `put sovereigns in <pack>`. All initial/retry/timeout/success/failure paths use `currentGoldOperation` and the final authorization check. Remove gold-prefix construction and mark-intent calls from `boop.executeAction`; it dispatches only combat after aggregate holds clear. Keep pack validation unchanged for Phase 04. Synchronize the next patch version before commit.
  </action>
  <verify>
    <automated>luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua src/scripts/boop/boop_util.lua tests/boop_runtime_spec.lua tests/boop_gold_spec.lua tests/boop_gold_retry_spec.lua &amp;&amp; python3 tools/check_release_gates.py</automated>
    <authoritative>Run focused gold specs and then the full Mudlet suite; use GitHub Actions when the local AppImage is absent.</authoritative>
  </verify>
  <done>Gold core passes focused D-09–D-12 contracts with staged commands and exact room/generation ownership.</done>
</task>

</tasks>

## Artifacts this plan creates

- Exact created inventory is the `<interfaces>` created-symbol list; no additional gold constant, function, owner, field, phase, timer/retry field, blocker code, or terminal reason may be introduced without adding it there.
- Existing files only; no new command, dependency, schema, manifest, or built artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GMCP/text gold evidence → Achaea queue | Partial, duplicate, or stale evidence can send wrong-room loot commands. |
| Pickup success → inventory pack | Ownership transfer must be atomic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-GOLD-ROOM | Spoofing / Tampering | detection/send/retry | high | mitigate | Room ID/generation/list and final owner checks. |
| T-03-GOLD-STALE | Tampering | timeout/retry callback | high | mitigate | Generation/phase/terminal comparison and zero-effect tests. |
| T-03-GOLD-DUP | Denial of Service | duplicate evidence | medium | mitigate | Same-room coalescing with exact one-get assertion. |
</threat_model>

<verification>
- Focused gold syntax/release checks after each task.
- Real Mudlet focused/full suite or matching CI is authoritative.
- Room/event/tick cross-lifecycle integration is intentionally assigned to Plan 03-05.
</verification>

<success_criteria>
- D-09 through D-12 are implemented in focused gold behavior.
- SAFE-04 prevents early, duplicate, wrong-room, stale, and chained loot commands.
- Every commit synchronizes version metadata and avoids built artifacts.
</success_criteria>

<output>
Create `.planning/phases/03-queue-interrupt-gold-and-autowalk-regression-coverage/03-04-SUMMARY.md` when done.
</output>
