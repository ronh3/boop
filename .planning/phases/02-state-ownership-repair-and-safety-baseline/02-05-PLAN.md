---
phase: 02-state-ownership-repair-and-safety-baseline
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/scripts/boop/boop_walk.lua
  - tools/check_release_gates.py
autonomous: true
requirements: ["STATE-01", "STATE-02"]
must_haves:
  truths:
    - "Walk advancement reads canonical owned state and blocker snapshots."
    - "Unsafe advancement is blocked for target, flee, GMCP, pull, room-settling, diag, and gold blockers."
    - "The state-drift gate rejects reintroduced flat walk state in the migrated file."
  artifacts:
    - "src/scripts/boop/boop_walk.lua uses state.walk/state.diag/state.combat/state.gold/state.targeting."
    - "tools/check_release_gates.py watches boop_walk.lua with zero flat-state allowance after migration."
  key_links:
    - "boop.walk.blockedReason() -> boop.runtime.blockerSnapshot()/shouldHold('walk')."
    - "boop.walk.maybeAdvance() -> raiseEvent only when owned state says the room is safe to leave."
    - "tools/check_release_gates.py -> src/scripts/boop/boop_walk.lua flat-state scanner."
---

<objective>
Migrate the walk blocker path to owned state and enforce the migration with the state-drift gate.

Purpose: Phase 02 owns unsafe advancement blockers, not full walker route behavior. The walk module must stop reading removed flat fields before Phase 03 builds deeper walk coverage.
Output: Owned-domain walk blocker reads and a zero-flat-access state-drift baseline for `boop_walk.lua`.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-01-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
@src/scripts/boop/boop_walk.lua
@src/scripts/boop/boop_runtime.lua
@tools/check_release_gates.py
@tests/boop_walk_spec.lua
</context>

## Artifacts This Phase Produces

- `boop.state.combat.blocker` owned blocker snapshot with stable `code`, `label`, `systems`, `waitsFor`, and observed-state fields.
- `boop.runtime.blockerSnapshot()`, `boop.runtime.setBlocker(...)`, `boop.runtime.clearBlocker(...)`, `boop.runtime.shouldHold(system)`, `boop.runtime.clearAttackIntent(reason)`, and `boop.runtime.clearAutomationIntent(reason, opts)`.
- GMCP recovery blockers for `gmcp_ire_missing`, room/target blockers for `missing_room`, `room_partial`, and `target_lost`, and pull blockers for `pull_away` and `pull_timeout_away`.
- Owned-domain walk state in `src/scripts/boop/boop_walk.lua` with `tools/check_release_gates.py` allowing zero flat-state access for migrated Phase 02 files.
- Canonical status/dashboard/trace rendering for blocker code, short label, affected systems, and awaited resume state.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate walk state reads and unsafe advancement holds</name>
  <read_first>
    @src/scripts/boop/boop_walk.lua
    @src/scripts/boop/boop_runtime.lua
    @tests/boop_walk_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>src/scripts/boop/boop_walk.lua</files>
  <behavior>
    - The walk module reads and writes `boop.state.walk.active`, `owned`, `roomSettled`, `moveQueued`, `arrivalRoom`, and `arrivalTimer`.
    - Blocker checks use `boop.state.diag.hold`, `boop.state.combat.fleeing`, `boop.state.combat.pullState`, `boop.state.gold.*`, `boop.state.targeting.currentTargetId`, and `boop.runtime.shouldHold("walk")`.
    - Existing `boop.walk.start`, `stop`, `status`, `onRoomSettled`, `maybeAdvance`, and `move` command behavior remains recognizable.
  </behavior>
  <action>Refactor `src/scripts/boop/boop_walk.lua` so its local state helper returns the owned `state.walk` table plus explicit owned domain locals rather than treating `boop.state` as a flat walk state. Update arrival timers, active/owned flags, room-settled flags, move-queued flags, and arrival room values to use `state.walk.*`. Update `blockedReason()` and `maybeAdvance()` to consult `boop.runtime.blockerSnapshot()`/`boop.runtime.shouldHold("walk")` and owned `diag`, `combat`, `gold`, and `targeting` state. Preserve D-43, D-44, D-45, and D-46 boundaries by limiting changes to blocker reads and unsafe advancement prevention.</action>
  <acceptance_criteria>
    - `tests/boop_walk_spec.lua` passes its owned blocker setup and no-advance assertions.
    - `boop.walk.status()` reports active/owned/settled state from `state.walk`.
    - No external walker install/status behavior changes unless needed to display an existing blocker reason.
  </acceptance_criteria>
  <verify>
    <automated>luac -p src/scripts/boop/boop_walk.lua tests/boop_walk_spec.lua &amp;&amp; TESTS_DIRECTORY="$PWD/tests" busted tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>`boop_walk.lua` uses owned domains for Phase 02 blocker decisions and prevents unsafe advancement.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the state-drift gate for the migrated walk file</name>
  <read_first>
    @tools/check_release_gates.py
    @src/scripts/boop/boop_walk.lua
    @.planning/phases/01-release-gates-and-state-contracts/01-01-SUMMARY.md
    @.planning/phases/01-release-gates-and-state-contracts/01-05-SUMMARY.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>tools/check_release_gates.py</files>
  <action>Update `KNOWN_FLAT_STATE_ACCESS` in `tools/check_release_gates.py` so `src/scripts/boop/boop_walk.lua` remains listed with an empty allowed map after migration. Keep other file baselines aligned with the actual migrations already completed in Plan 02-04. This implements D-02 and D-03 for the walk migration and preserves the Phase 01 pattern that migrated files stay watched with zero allowed flat access.</action>
  <acceptance_criteria>
    - `python3 tools/check_release_gates.py --check state-drift` passes with `boop_walk.lua` watched as a zero-flat-access file.
    - A new flat state access in `boop_walk.lua` would fail the scanner under the existing release gate behavior.
    - The update does not broaden the allowed baseline for any file.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile tools/check_release_gates.py &amp;&amp; TESTS_DIRECTORY="$PWD/tests" busted tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>The release gate enforces the walk ownership migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime blocker to walker | Owned safety state decides whether external walker movement may advance. |
| Source scanner to CI | Static release gate enforces the reviewed state ownership baseline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Tampering | src/scripts/boop/boop_walk.lua | high | mitigate | Read canonical blocker and owned domains before raising any walk move event. |
| T-02-16 | Denial of Service | src/scripts/boop/boop_walk.lua | medium | mitigate | Preserve existing active/settled/move-queued state machine while preventing repeated unsafe advancement. |
| T-02-17 | Tampering | tools/check_release_gates.py | medium | mitigate | Keep `boop_walk.lua` in the state-drift baseline with zero allowed flat-state access. |
</threat_model>

<verification>
Run Lua syntax for walk tests/source, `TESTS_DIRECTORY="$PWD/tests" busted tests/boop_walk_spec.lua`, and `python3 tools/check_release_gates.py --check state-drift`.

After Wave 3 completes, run the full Mudlet suite command:
`muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`

Do not edit built artifacts or version fields.
</verification>

<success_criteria>
STATE-01 and STATE-02 are satisfied for walk blockers: the module uses owned domains and cannot advance walking while canonical blockers say the room is unsafe.
</success_criteria>

<output>
Create `.planning/phases/02-state-ownership-repair-and-safety-baseline/02-05-SUMMARY.md` when done. Do not commit or push; the orchestrator owns version synchronization, release gates, commit, and push. Before any future commit or push, run `python3 tools/check_release_gates.py --check versions`.
</output>
