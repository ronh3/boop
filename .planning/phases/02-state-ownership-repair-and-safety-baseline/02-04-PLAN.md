---
phase: 02-state-ownership-repair-and-safety-baseline
plan: 04
type: execute
wave: 2
depends_on: ["02-03"]
files_modified:
  - src/scripts/boop/boop_safety.lua
  - src/scripts/boop/boop_events.lua
  - src/scripts/boop/boop_targets.lua
  - src/scripts/boop/boop_attacks.lua
  - tools/check_release_gates.py
autonomous: true
requirements: ["STATE-01", "SAFE-01", "SAFE-03"]
must_haves:
  truths:
    - "Auto-flee clears all automation intent before escape movement."
    - "Target disappearance clears stale attack state and retargets only from valid room targets."
    - "Touched attack and target paths read/write Phase 02 state through owned domains."
  artifacts:
    - "src/scripts/boop/boop_safety.lua calls the canonical cleanup helper before flee sends."
    - "src/scripts/boop/boop_events.lua handles target-loss cleanup and pull exceptions."
    - "src/scripts/boop/boop_targets.lua exposes owned target-call cleanup where needed."
    - "src/scripts/boop/boop_attacks.lua removes Phase 02-owned no-context flat fallbacks."
    - "tools/check_release_gates.py reflects any migrated-file state-drift baseline changes."
  key_links:
    - "boop.safety.flee() -> boop.runtime.clearAutomationIntent() before boop.executeAction()."
    - "boop.onRoomItemsRemove() -> boop.runtime.clearAttackIntent() before retarget/tick."
    - "boop_attacks.lua -> boop.runtime.context()/owned domains for target/rage/inventory fallback state."
---

<objective>
Repair auto-flee and target-loss cleanup around the canonical blocker/cleanup API, then tighten migrated state ownership.

Purpose: SAFE-01 and SAFE-03 are the highest-risk behavioral outcomes in Phase 02. Cleanup must precede side effects, and stale targets must not leave queued attacks behind.
Output: Flee cleanup, target-loss/pull handling, owned attack fallback reads, and state-drift baseline updates.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
@src/scripts/boop/boop_safety.lua
@src/scripts/boop/boop_events.lua
@src/scripts/boop/boop_targets.lua
@src/scripts/boop/boop_attacks.lua
@tools/check_release_gates.py
@tests/boop_safety_spec.lua
@tests/boop_event_transitions_spec.lua
@tests/boop_pull_spec.lua
</context>

## Artifacts This Phase Produces

- `boop.state.combat.blocker` owned blocker snapshot with stable `code`, `label`, `systems`, `waitsFor`, and observed-state fields.
- `boop.runtime.blockerSnapshot()`, `boop.runtime.setBlocker(...)`, `boop.runtime.clearBlocker(...)`, `boop.runtime.shouldHold(system)`, `boop.runtime.clearAttackIntent(reason)`, and `boop.runtime.clearAutomationIntent(reason, opts)`.
- GMCP recovery blockers for `gmcp_ire_missing`, room/target blockers for `missing_room`, `room_partial`, and `target_lost`, and pull blockers for `pull_away` and `pull_timeout_away`.
- Owned-domain walk state in `src/scripts/boop/boop_walk.lua` with `tools/check_release_gates.py` allowing zero flat-state access for migrated Phase 02 files.
- Canonical status/dashboard/trace rendering for blocker code, short label, affected systems, and awaited resume state.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move auto-flee cleanup before escape movement</name>
  <read_first>
    @src/scripts/boop/boop_safety.lua
    @src/scripts/boop/boop_runtime.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
    @tests/boop_safety_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>src/scripts/boop/boop_safety.lua</files>
  <behavior>
    - `boop.safety.flee()` clears queue, prequeue, walk, gold, target-call, standard, rage, and attack intent before any send.
    - Flee disables hunting and does not re-enable it automatically.
    - Flee emits one concise summary line and leaves detail to trace/status.
  </behavior>
  <action>Verify from `02-03-SUMMARY.md` that `boop.runtime.clearAutomationIntent(reason, opts)` and `boop.runtime.clearAttackIntent(reason)` were implemented in `src/scripts/boop/boop_runtime.lua`; if either helper is absent or lacks the Plan 02-03 owned-field behavior, stop and repair Plan 02-03 rather than adding helper implementation in this plan. Update `boop.safety.flee()` to call `boop.runtime.clearAutomationIntent("flee", { includeWalk = true, includeGold = true, includeAttack = true })` before reading the flee direction or sending the escape command. Ensure the consumed helper cancels `state.queue.prequeueTimer`, `state.queue.prequeuedStandard`, `state.queue.aliasAction`/dirty intent as appropriate, `state.walk.active`/`owned`/`roomSettled`/`moveQueued`/`arrivalRoom`/`arrivalTimer`, `state.gold.autoGrabPending`/`autoGrabPendingAt`/`autoGrabTimer`/`getPending`/`putPending`/`pendingTimer`, `state.targeting.calledTargetId` and related call fields, `state.combat.attacking`, and queued attack/rage intent represented by runtime-owned queue and combat fields. Preserve explicit operator re-enable behavior and one compact output line per D-18, D-19, D-20, D-25, and D-26.</action>
  <acceptance_criteria>
    - The first captured send in `tests/boop_safety_spec.lua` occurs after all seeded automation intent fields are cleared.
    - `boop.config.enabled` remains false after flee and is not automatically toggled back on.
    - Trace/status detail is available through blocker/cleanup state without repeated live output.
    - `src/scripts/boop/boop_runtime.lua` is not modified by this plan; runtime helper behavior remains owned by Plan 02-03.
  </acceptance_criteria>
  <verify>
    <automated>luac -p src/scripts/boop/boop_safety.lua tests/boop_safety_spec.lua && TESTS_DIRECTORY="$PWD/tests" busted tests/boop_safety_spec.lua && python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Auto-flee cleanup satisfies SAFE-01 ordering and preserves operator-controlled re-enable behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Centralize target-loss cleanup and pull exception handling</name>
  <read_first>
    @src/scripts/boop/boop_events.lua
    @src/scripts/boop/boop_targets.lua
    @src/scripts/boop/boop_runtime.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
    @tests/boop_event_transitions_spec.lua
    @tests/boop_pull_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>src/scripts/boop/boop_events.lua, src/scripts/boop/boop_targets.lua</files>
  <behavior>
    - `boop.onRoomItemsRemove()` uses the canonical cleanup helper when the current target disappears.
    - Same-tick retargeting happens only when current room denizen data is valid.
    - Active pull/room transitions preserve only the state required to resolve or hold pull lifecycle, then apply normal cleanup when pull ends.
  </behavior>
  <action>Verify from `02-03-SUMMARY.md` that `boop.runtime.clearAttackIntent("target_lost")` clears current target fields plus queued attack intent as specified by Plan 02-03; if not, stop and repair Plan 02-03 rather than editing runtime in this plan. Modify `boop.onRoomItemsRemove()` so current target disappearance calls `boop.runtime.clearAttackIntent("target_lost")`, emits a single `target_lost` warning, and sets a blocker when room item data is partial. Retarget in the same event only if `state.targeting.denizens` reflects trustworthy current-room targets; otherwise wait for prompt plus GMCP room/target state. Preserve active pull exception behavior while `state.combat.pullState.active` or resolving phases own the temporary away/return lifecycle, and after return or timeout call normal target-loss cleanup if the target is still absent. Use `boop.targets.clearTargetCall("target lost")` only if additional target-call side effects beyond the runtime field cleanup are still required, and satisfy D-21, D-22, D-23, D-24, D-27, D-35, D-36, and D-37.</action>
  <acceptance_criteria>
    - Target-loss tests assert stale queue/prequeue/alias/attack intent is cleared before any new target is set.
    - Pull tests assert away/return lifecycle remains held with structured pull blockers and clears stale target state after lifecycle resolution.
    - No command-fragment validation behavior is changed in this task, honoring D-38.
    - `src/scripts/boop/boop_runtime.lua` is not modified by this plan; missing cleanup helper behavior is treated as a Plan 02-03 defect.
  </acceptance_criteria>
  <verify>
    <automated>luac -p src/scripts/boop/boop_events.lua src/scripts/boop/boop_targets.lua tests/boop_event_transitions_spec.lua tests/boop_pull_spec.lua && TESTS_DIRECTORY="$PWD/tests" busted tests/boop_event_transitions_spec.lua tests/boop_pull_spec.lua && python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Target disappearance and pull exceptions satisfy SAFE-03 without unsafe retargeting from partial room data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove Phase 02-owned attack fallback flat-state access</name>
  <read_first>
    @src/scripts/boop/boop_attacks.lua
    @src/scripts/boop/boop_runtime.lua
    @tools/check_release_gates.py
    @tests/boop_attacks_spec.lua
    @tests/boop_runtime_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>src/scripts/boop/boop_attacks.lua, tools/check_release_gates.py</files>
  <behavior>
    - No-context attack helpers use `boop.runtime.context()` or owned domains for class/spec, current target, shield, rage readiness, inventory, and opener trace state.
    - Migrated attack fallback fields are removed from `KNOWN_FLAT_STATE_ACCESS`.
    - Existing attack profile behavior remains intact.
  </behavior>
  <action>In `src/scripts/boop/boop_attacks.lua`, remove Phase 02-owned flat fallback reads for class/spec, current target id, target shield, rage readiness, wielded items, and opener trace state by reading runtime context or `boop.state.combat`, `boop.state.targeting`, `boop.state.rage`, and `boop.state.inventory`. Keep profile selection behavior and existing public function names stable. Update `tools/check_release_gates.py` so any migrated attack fallback keys no longer remain as allowed drift counts; leave only genuinely untouched non-Phase-02 entries if source inspection proves they are outside this task. This implements D-01, D-02, D-03, D-05, and D-06 for the touched attack paths.</action>
  <acceptance_criteria>
    - `python3 tools/check_release_gates.py --check state-drift` passes after baseline review.
    - Attack specs and runtime specs still cover current-target HP, shield, rage, inventory, and opener behavior through owned state/context.
    - No broad attack-system refactor or new rage mode is introduced.
  </acceptance_criteria>
  <verify>
    <automated>luac -p src/scripts/boop/boop_attacks.lua tests/boop_attacks_spec.lua tests/boop_runtime_spec.lua && TESTS_DIRECTORY="$PWD/tests" busted tests/boop_attacks_spec.lua tests/boop_runtime_spec.lua && python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Phase 02-owned attack fallback state reads are migrated and enforced by the release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safety state to command send | Runtime state controls whether flee movement is sent with stale automation still active. |
| Room item GMCP to targeting | GMCP room item deltas decide whether a target still exists and whether retargeting is safe. |
| Combat planner to queue | Attack planning can write queued game commands if stale target state leaks through. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | src/scripts/boop/boop_safety.lua | high | mitigate | Clear all automation intent before flee movement and assert send ordering. |
| T-02-12 | Tampering | src/scripts/boop/boop_events.lua | high | mitigate | Clear stale attack intent when the current target disappears and retarget only from valid current-room targets. |
| T-02-13 | Tampering | src/scripts/boop/boop_attacks.lua | medium | mitigate | Remove Phase 02-owned flat fallback reads and update the state-drift baseline. |
| T-02-14 | Denial of Service | src/scripts/boop/boop_events.lua | medium | mitigate | Keep pull timeout-away held until return-to-origin and trustworthy state, with structured blocker output. |
</threat_model>

<verification>
Run Lua syntax checks, affected Busted specs with `TESTS_DIRECTORY="$PWD/tests"`, and `python3 tools/check_release_gates.py --check state-drift` after every task. After Wave 2 completes, run the full Mudlet suite command exactly: `muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`. This plan must not edit built artifacts, version fields, or `src/scripts/boop/boop_runtime.lua`.
</verification>

<success_criteria>
SAFE-01 and SAFE-03 are satisfied: flee cleanup precedes escape movement, target loss clears stale intent, pull exceptions are narrow, and touched attack paths use owned domains.
</success_criteria>

<output>
Create `.planning/phases/02-state-ownership-repair-and-safety-baseline/02-04-SUMMARY.md` when done. Do not commit or push; the orchestrator owns version synchronization, release gates, commit, and push. Before any future commit or push, run `python3 tools/check_release_gates.py --check versions`.
</output>
