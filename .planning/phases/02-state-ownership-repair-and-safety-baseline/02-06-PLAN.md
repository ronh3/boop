---
phase: 02-state-ownership-repair-and-safety-baseline
plan: 06
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified:
  - src/scripts/boop/boop_ui.lua
  - src/scripts/boop/boop_ui_registry.lua
  - README.md
  - UIDESIGN.md
autonomous: true
requirements: ["STATE-02", "STATE-03"]
must_haves:
  truths:
    - "Status, dashboard, debug, and trace surfaces report the same canonical blocker values."
    - "Operator-facing output uses compact stable code-plus-label wording."
    - "Docs/help are updated only for real command-surface changes made in this phase."
  artifacts:
    - "src/scripts/boop/boop_ui.lua renders blocker code, label, affected systems, and waits-for state from runtime."
    - "src/scripts/boop/boop_ui_registry.lua help entries mention blocker/status fields if the command help changes."
    - "README.md and UIDESIGN.md reflect changed operator-visible status behavior when needed."
  key_links:
    - "src/scripts/boop/boop_ui.lua -> boop.runtime.blockerSnapshot() for home/status/control/config/debug summaries."
    - "tests/boop_ui_spec.lua -> src/scripts/boop/boop_ui.lua expected blocker strings."
    - "tests/boop_trace_spec.lua -> runtime trace entries for normalized blocker transitions."
---

<objective>
Render canonical blocker/status data through trace, status, dashboard, and focused operator docs.

Purpose: STATE-03 is only true when every diagnostic surface describes the same owned state. This plan removes ad hoc UI blocker inference and keeps operator-facing help coherent.
Output: UI/status/debug rendering, help/docs sync for changed status fields, and passing trace/UI specs.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-02-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
@src/scripts/boop/boop_ui.lua
@src/scripts/boop/boop_ui_registry.lua
@README.md
@UIDESIGN.md
@tests/boop_trace_spec.lua
@tests/boop_ui_spec.lua
</context>

## Artifacts This Phase Produces

- `boop.state.combat.blocker` owned blocker snapshot with stable `code`, `label`, `systems`, `waitsFor`, and observed-state fields.
- `boop.runtime.blockerSnapshot()`, `boop.runtime.setBlocker(...)`, `boop.runtime.clearBlocker(...)`, `boop.runtime.shouldHold(system)`, `boop.runtime.clearAttackIntent(reason)`, and `boop.runtime.clearAutomationIntent(reason, opts)`.
- GMCP recovery blockers for `gmcp_ire_missing`, room/target blockers for `missing_room`, `room_partial`, and `target_lost`, and pull blockers for `pull_away` and `pull_timeout_away`.
- Owned-domain walk state in `src/scripts/boop/boop_walk.lua` with `tools/check_release_gates.py` allowing zero flat-state access for migrated Phase 02 files.
- Canonical status/dashboard/trace rendering for blocker code, short label, affected systems, and awaited resume state.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ad hoc UI blockers with the runtime snapshot</name>
  <read_first>
    @src/scripts/boop/boop_ui.lua
    @src/scripts/boop/boop_runtime.lua
    @tests/boop_ui_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>src/scripts/boop/boop_ui.lua</files>
  <behavior>
    - `boop.ui.statusLine()`, `boop.ui.status()`, home dashboard, control/config summaries, party summary where applicable, and debug snapshot render the runtime blocker snapshot.
    - Output includes stable `code -- label`, affected systems, and waits-for state.
    - Existing compact boop aesthetic and `[OK]`, `[INFO]`, `[WARN]`, `[ERR]` feedback style remain intact.
  </behavior>
  <action>Replace the local `currentBlocker()` inference in `src/scripts/boop/boop_ui.lua` with a small formatter that reads `boop.runtime.blockerSnapshot()` and falls back to existing disabled/diag/gold/leader-call checks only when no canonical blocker is active. Render blocker values as stable `code -- label` text plus compact affected-system and waits-for fragments in `boop.ui.statusLine()`, `boop.ui.status()`, home, control/config section summaries, party summary if it already prints blocker state, and debug output. Keep the renderer concise and aligned with UIDESIGN, satisfying D-28, D-30, D-31, D-33, and D-34.</action>
  <acceptance_criteria>
    - `tests/boop_ui_spec.lua` passes for home/status/config/debug blocker strings.
    - UI output for the same seeded blocker uses identical code and label across all affected surfaces.
    - No new dashboard section or menu system is introduced.
  </acceptance_criteria>
  <verify>
    <automated>luac -p src/scripts/boop/boop_ui.lua tests/boop_ui_spec.lua &amp;&amp; TESTS_DIRECTORY="$PWD/tests" busted tests/boop_ui_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>UI/status/dashboard surfaces read the canonical blocker snapshot for STATE-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ensure trace and help/docs match the changed status surface</name>
  <read_first>
    @src/scripts/boop/boop_ui.lua
    @src/scripts/boop/boop_ui_registry.lua
    @README.md
    @UIDESIGN.md
    @tests/boop_trace_spec.lua
    @tests/boop_ui_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>src/scripts/boop/boop_ui.lua, src/scripts/boop/boop_ui_registry.lua, README.md, UIDESIGN.md</files>
  <behavior>
    - Trace entries for blocker enter/exit, target-loss cleanup, flee cleanup, pull holds, GMCP recovery, and retarget decisions use normalized owned-state values.
    - Command help and docs mention changed blocker/status fields only where the operator can see them.
    - README-wide release documentation remains Phase 06 scope unless directly affected by this status output.
  </behavior>
  <action>Audit trace calls added by prior plans and ensure status-visible transitions log normalized owned values, not raw GMCP dumps, per D-29 and D-30. Update `src/scripts/boop/boop_ui_registry.lua` help text if existing help topics describe `boop status`, trace, or debug output and now need the stable code-plus-label wording. Update `README.md` and `UIDESIGN.md` only for command-surface changes actually made in this phase, per D-32, keeping the operator-facing docs concise and leaving unrelated release documentation untouched.</action>
  <acceptance_criteria>
    - `tests/boop_trace_spec.lua` passes for canonical blocker, cleanup, recovery, and retarget trace messages.
    - Help/docs contain no stale wording that contradicts the new `boop status` or dashboard blocker format.
    - Docs changes do not claim full live reconnect validation or full walker route coverage in Phase 02.
  </acceptance_criteria>
  <verify>
    <automated>luac -p src/scripts/boop/boop_ui.lua src/scripts/boop/boop_ui_registry.lua tests/boop_trace_spec.lua tests/boop_ui_spec.lua &amp;&amp; TESTS_DIRECTORY="$PWD/tests" busted tests/boop_ui_spec.lua &amp;&amp; TESTS_DIRECTORY="$PWD/tests" busted tests/boop_trace_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Trace/status/dashboard and focused docs describe the same canonical owned-state values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime state to operator output | Owned blocker state becomes user-facing status, dashboard, and trace text. |
| Documentation to operator action | Help/docs guide the operator's response to safety blockers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Repudiation | src/scripts/boop/boop_ui.lua | medium | mitigate | Render blocker code, label, affected systems, and waits-for state from the runtime snapshot across status/dashboard/debug surfaces. |
| T-02-19 | Information Disclosure | src/scripts/boop/boop_ui.lua | low | accept | Output contains local safety state and target ids already used by boop; no secrets are introduced. |
| T-02-20 | Spoofing | README.md, UIDESIGN.md | low | mitigate | Keep docs/help aligned so operators do not mistake a held automation state for enabled safe hunting. |
</threat_model>

<verification>
Run Lua syntax for UI/registry/spec files, `TESTS_DIRECTORY="$PWD/tests" busted tests/boop_ui_spec.lua`, `TESTS_DIRECTORY="$PWD/tests" busted tests/boop_trace_spec.lua`, and `python3 tools/check_release_gates.py --check state-drift`.

After Wave 4 completes, run the full Mudlet suite command:
`muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`

Plan 02-07 still owns final release gates.
</verification>

<success_criteria>
STATE-03 is satisfied: trace, status, dashboard, and debug surfaces report canonical owned blocker/status values, and command help/docs match any changed operator-visible fields.
</success_criteria>

<output>
Create `.planning/phases/02-state-ownership-repair-and-safety-baseline/02-06-SUMMARY.md` when done. Do not commit or push; the orchestrator owns version synchronization, release gates, commit, and push. Before any future commit or push, run `python3 tools/check_release_gates.py --check versions`.
</output>
