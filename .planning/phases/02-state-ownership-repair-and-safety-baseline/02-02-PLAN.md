---
phase: 02-state-ownership-repair-and-safety-baseline
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/boop_event_transitions_spec.lua
  - tests/boop_pull_spec.lua
  - tests/boop_trace_spec.lua
  - tests/boop_ui_spec.lua
autonomous: true
requirements: ["STATE-02", "STATE-03", "SAFE-03"]
must_haves:
  truths:
    - "GMCP recovery and partial room/target updates create visible owned blockers."
    - "Target disappearance clears stale attack intent and only retargets from valid current-room targets."
    - "Trace, status, and dashboard assertions read the same canonical blocker values."
  artifacts:
    - "tests/boop_event_transitions_spec.lua covers GMCP blocker/backoff and target-loss cleanup."
    - "tests/boop_pull_spec.lua covers active-pull target-loss exception and return/timeout cleanup."
    - "tests/boop_trace_spec.lua covers blocker, cleanup, recovery, and retarget trace lines."
    - "tests/boop_ui_spec.lua covers stable code-plus-label blocker rendering."
  key_links:
    - "tests/boop_event_transitions_spec.lua -> src/scripts/boop/boop_events.lua for GMCP and target removal transitions."
    - "tests/boop_pull_spec.lua -> src/scripts/boop/boop_ui.lua pull lifecycle and src/scripts/boop/boop_events.lua room return handling."
    - "tests/boop_trace_spec.lua and tests/boop_ui_spec.lua -> runtime blocker snapshot consumed by status/dashboard/trace."
---

<objective>
Create Wave 0 event, pull, trace, and UI test contracts for missing-state blockers, target-loss cleanup, and canonical status surfaces.

Purpose: STATE-02, STATE-03, and SAFE-03 can regress silently if event transitions, trace, and UI each infer state separately. These tests pin one owned source of truth before implementation.
Output: Focused Busted specs for GMCP recovery, target disappearance, pull exceptions, trace lines, and compact blocker/status rendering.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
@.planning/phases/01-release-gates-and-state-contracts/01-05-SUMMARY.md
@tests/boop_event_transitions_spec.lua
@tests/boop_pull_spec.lua
@tests/boop_trace_spec.lua
@tests/boop_ui_spec.lua
</context>

## Artifacts This Phase Produces

- `boop.state.combat.blocker` owned blocker snapshot with stable `code`, `label`, `systems`, `waitsFor`, and observed-state fields.
- `boop.runtime.blockerSnapshot()`, `boop.runtime.setBlocker(...)`, `boop.runtime.clearBlocker(...)`, `boop.runtime.shouldHold(system)`, `boop.runtime.clearAttackIntent(reason)`, and `boop.runtime.clearAutomationIntent(reason, opts)`.
- GMCP recovery blockers for `gmcp_ire_missing`, room/target blockers for `missing_room`, `room_partial`, and `target_lost`, and pull blockers for `pull_away` and `pull_timeout_away`.
- Owned-domain walk state in `src/scripts/boop/boop_walk.lua` with `tools/check_release_gates.py` allowing zero flat-state access for migrated Phase 02 files.
- Canonical status/dashboard/trace rendering for blocker code, short label, affected systems, and awaited resume state.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GMCP, target-loss, and pull lifecycle tests</name>
  <read_first>
    @tests/boop_event_transitions_spec.lua
    @tests/boop_pull_spec.lua
    @src/scripts/boop/boop_events.lua
    @src/scripts/boop/boop_ui.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>tests/boop_event_transitions_spec.lua, tests/boop_pull_spec.lua</files>
  <behavior>
    - Missing `gmcp.IRE`, `gmcp.IRE.Target`, or `gmcp.IRE.Display` creates one owned `gmcp_ire_missing` blocker, calls `boop.requestCoreSupports()`, and holds target-dependent automation.
    - Blockers clear only after both prompt observation and the relevant GMCP state arrive.
    - Target disappearance clears current target and attack intent, preserves active pull exceptions, and retargets only from valid current-room denizens.
  </behavior>
  <action>Extend `tests/boop_event_transitions_spec.lua` with synthetic GMCP recovery tests for missing IRE support, immediate retry, throttled repeat behavior, prompt-plus-GMCP clearing, partial room/target blocker entry, target-loss cleanup, and valid same-tick retargeting. Extend `tests/boop_pull_spec.lua` so active pull/room transitions preserve the narrow target-loss exception and pull timeout while away leaves automation held until return-to-origin plus trustworthy state. Cite and implement expectations for D-21, D-22, D-23, D-24, D-27, D-35, D-36, D-37, D-38, D-39, D-40, D-41, and D-42.</action>
  <acceptance_criteria>
    - Tests assert `boop.requestCoreSupports()` is called and automation is held when IRE GMCP support is missing.
    - Tests assert target-loss cleanup clears `currentTargetId`, `targetName`, target-call intent, prequeued standard state, queued alias dirty state, and pending attack intent before retargeting.
    - Pull tests distinguish active pull exceptions from normal target-loss cleanup and assert no stale pull blocker remains after lifecycle resolution.
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_event_transitions_spec.lua tests/boop_pull_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>GMCP recovery, target-loss cleanup, and pull lifecycle expectations are covered before production event changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trace and UI canonical blocker tests</name>
  <read_first>
    @tests/boop_trace_spec.lua
    @tests/boop_ui_spec.lua
    @src/scripts/boop/boop_ui.lua
    @src/scripts/boop/boop_util.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>tests/boop_trace_spec.lua, tests/boop_ui_spec.lua</files>
  <behavior>
    - Trace records blocker enter/exit, target-loss cleanup, flee cleanup, pull holds, GMCP recovery, and retarget decisions using normalized owned-state values.
    - `boop status`, home, control/config summaries, and debug output show stable `code -- label`, affected systems, and resume wait state from one canonical snapshot.
    - Live output remains transition-only and rate-limited.
  </behavior>
  <action>Add assertions in `tests/boop_trace_spec.lua` for blocker enter/exit, target-loss cleanup, flee cleanup, pull hold, GMCP recovery, and valid retarget trace messages. Update `tests/boop_ui_spec.lua` expected status/dashboard/debug strings to a stable code-plus-label format and add affected-system and waits-for assertions. Cover D-10, D-14, D-15, D-16, D-28, D-29, D-30, D-31, D-32, D-33, and D-34 without adding a parallel UI blocker source.</action>
  <acceptance_criteria>
    - Trace tests assert normalized values from `boop.state.combat.blocker`, `state.targeting`, `state.walk`, `state.gold`, `state.diag`, `state.queue`, and `state.gag` rather than raw GMCP tables.
    - UI tests assert the same blocker code and label appear in status and dashboard surfaces.
    - Tests preserve existing rich/plain output harness style and do not introduce visible explanatory prose outside command output expectations.
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_trace_spec.lua tests/boop_ui_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Trace and UI specs enforce canonical blocker/status rendering for STATE-03 before UI implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GMCP to event handlers | Missing or partial GMCP state enters boop automation through Mudlet events. |
| Runtime state to operator UI | Safety blockers are rendered to the operator through status, dashboard, and trace surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | tests/boop_event_transitions_spec.lua | high | mitigate | Add tests that target-dependent automation is held when GMCP/room/target state is incomplete. |
| T-02-05 | Tampering | tests/boop_event_transitions_spec.lua | high | mitigate | Add tests that stale target removal clears attack intent before any valid retarget. |
| T-02-06 | Repudiation | tests/boop_trace_spec.lua | medium | mitigate | Add trace assertions for blocker transitions and cleanup decisions so safety behavior is auditable. |
| T-02-07 | Information Disclosure | tests/boop_ui_spec.lua | low | accept | Status/trace show local runtime state only; no secrets or remote auth data exist in Phase 02 scope. |
</threat_model>

<verification>
Plan-level verification is syntax plus the existing state-drift gate. Full Mudlet Busted execution is required by the final phase validation plan after production implementation satisfies these Wave 0 contracts.
</verification>

<success_criteria>
Wave 0 tests define GMCP recovery, target-loss, pull, trace, and UI contracts for STATE-02, STATE-03, and SAFE-03 without editing production package behavior or built artifacts.
</success_criteria>

<output>
Create `.planning/phases/02-state-ownership-repair-and-safety-baseline/02-02-SUMMARY.md` when done. Do not commit or push; the orchestrator owns version synchronization, release gates, commit, and push. Before any future commit or push, run `python3 tools/check_release_gates.py --check versions`.
</output>
