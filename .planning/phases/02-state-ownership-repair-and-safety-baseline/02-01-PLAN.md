---
phase: 02-state-ownership-repair-and-safety-baseline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/support/boop_test_helper.lua
  - tests/boop_runtime_spec.lua
  - tests/boop_safety_spec.lua
  - tests/boop_walk_spec.lua
autonomous: true
requirements: ["STATE-01", "STATE-02", "SAFE-01"]
must_haves:
  truths:
    - "Phase 02 safety tests seed and assert owned runtime domains only."
    - "Runtime blockers prevent attack, queue, gold, and walk automation while required game state is incomplete."
    - "Auto-flee cleanup is asserted before any escape command is sent."
  artifacts:
    - "tests/support/boop_test_helper.lua owns Phase 02 helper setup without flat state keys."
    - "tests/boop_runtime_spec.lua covers blocker snapshot and hold behavior."
    - "tests/boop_safety_spec.lua covers cleanup-before-flee-send ordering."
    - "tests/boop_walk_spec.lua covers owned blocker reads and unsafe walk advancement holds."
  key_links:
    - "tests/support/boop_test_helper.lua -> src/scripts/boop/boop_runtime.lua via boop.state.init() and owned domains."
    - "tests/boop_runtime_spec.lua -> boop.runtime.step() via blocker-held effect assertions."
    - "tests/boop_safety_spec.lua -> boop.safety.flee() via send ordering assertions."
    - "tests/boop_walk_spec.lua -> boop.walk.blockedReason()/maybeAdvance() via owned state setup."
---

<objective>
Create Wave 0 test contracts for the owned blocker model, auto-flee cleanup ordering, and walk advancement holds before production runtime edits.

Purpose: Phase 02 changes safety-critical automation. These tests make the intended fail-closed behavior executable before implementation and cite the locked decisions they protect.
Output: Focused Busted specs and helper setup that downstream implementation plans must satisfy.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
@.planning/phases/01-release-gates-and-state-contracts/01-03-SUMMARY.md
@tests/support/boop_test_helper.lua
@tests/boop_runtime_spec.lua
@tests/boop_safety_spec.lua
@tests/boop_walk_spec.lua
</context>

## Artifacts This Phase Produces

- `boop.state.combat.blocker` owned blocker snapshot with stable `code`, `label`, `systems`, `waitsFor`, and observed-state fields.
- `boop.runtime.blockerSnapshot()`, `boop.runtime.setBlocker(...)`, `boop.runtime.clearBlocker(...)`, `boop.runtime.shouldHold(system)`, `boop.runtime.clearAttackIntent(reason)`, and `boop.runtime.clearAutomationIntent(reason, opts)`.
- GMCP recovery blockers for `gmcp_ire_missing`, room/target blockers for `missing_room`, `room_partial`, and `target_lost`, and pull blockers for `pull_away` and `pull_timeout_away`.
- Owned-domain walk state in `src/scripts/boop/boop_walk.lua` with `tools/check_release_gates.py` allowing zero flat-state access for migrated Phase 02 files.
- Canonical status/dashboard/trace rendering for blocker code, short label, affected systems, and awaited resume state.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime and flee safety contract tests</name>
  <read_first>
    @tests/support/boop_test_helper.lua
    @tests/boop_runtime_spec.lua
    @tests/boop_safety_spec.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>tests/support/boop_test_helper.lua, tests/boop_runtime_spec.lua, tests/boop_safety_spec.lua</files>
  <behavior>
    - `helper.reset()` and any new helper setup seed `boop.state.combat`, `targeting`, `queue`, `gold`, `walk`, `diag`, `trace`, `rage`, `inventory`, `ih`, and `gag` domains only.
    - `boop.runtime.step({ type = "tick" })` emits no target, combat, gold, or walk effects when the owned blocker snapshot affects those systems.
    - `boop.safety.flee()` clears queue, prequeue, walk, gold, target-call, standard, rage, and attack intent before the first `send("wake", false)` call.
  </behavior>
  <action>Create focused Busted assertions for owned-state strictness per D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08. Extend `tests/boop_runtime_spec.lua` with blocker default/context/hold assertions against the planned `boop.state.combat.blocker` and `boop.runtime.blockerSnapshot()` contract. Extend `tests/boop_safety_spec.lua` with a send-order recorder that seeds `state.queue.prequeuedStandard`, `state.queue.aliasAction`, `state.targeting.calledTargetId`, `state.walk.active`, `state.walk.moveQueued`, `state.gold.autoGrabPending`, `state.gold.getPending`, `state.gold.putPending`, `state.combat.attacking`, and rage/attack intent fields, then asserts cleanup happens before any flee command per D-18, D-19, D-20, D-25, and D-26. Add helper methods only when they reduce repeated owned-domain setup; do not add obsolete flat keys.</action>
  <acceptance_criteria>
    - Runtime tests fail against the current implementation because the canonical blocker API and owned blocker state do not yet exist, but Lua syntax passes.
    - Safety tests fail against the current implementation unless cleanup happens before the first escape send.
    - New tests and helper changes contain owned-domain paths such as `boop.state.queue.prequeuedStandard` and `boop.state.walk.active`, with no setup through removed flat compatibility keys.
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/support/boop_test_helper.lua tests/boop_runtime_spec.lua tests/boop_safety_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Wave 0 runtime and flee safety contracts are present, syntax-valid, and specific enough for the implementation plans to satisfy without interpretation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace walk placeholder with owned blocker tests</name>
  <read_first>
    @tests/boop_walk_spec.lua
    @tests/boop_tick_spec.lua
    @tests/boop_event_transitions_spec.lua
    @src/scripts/boop/boop_walk.lua
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
  </read_first>
  <files>tests/boop_walk_spec.lua</files>
  <behavior>
    - `boop.walk.blockedReason()` reads `state.walk`, `state.diag`, `state.combat`, `state.gold`, `state.targeting`, and the canonical runtime blocker instead of flat fields.
    - `boop.walk.maybeAdvance()` does not raise `boop.walk.move` while a target, flee, GMCP, pull, room-settling, diag, or gold blocker is active.
    - Full route start/stop/move semantics are not expanded in this phase.
  </behavior>
  <action>Replace the placeholder in `tests/boop_walk_spec.lua` with focused setup using `helper.reset()`, stubs for `raiseEvent` and `tempTimer`, and owned-domain seeds for `state.walk.active`, `state.walk.roomSettled`, `state.walk.moveQueued`, `state.combat.fleeing`, `state.combat.pullState`, `state.diag.hold`, `state.gold.autoGrabPending`, and `state.targeting.currentTargetId`. Assert unsafe advancement is held for target, flee, GMCP, and pull blockers per D-09, D-11, D-12, D-13, D-17, D-43, D-44, D-45, and D-46 while preserving the Phase 02 boundary against broader walker behavior.</action>
  <acceptance_criteria>
    - `tests/boop_walk_spec.lua` no longer contains only the disabled placeholder body.
    - Tests assert no `raiseEvent("boop.walk.move", ...)` happens while any Phase 02 blocker affects walking.
    - Tests use owned domains and do not require `demonnicAutoWalker` install/status changes.
  </acceptance_criteria>
  <verify>
    <automated>luac -p tests/boop_walk_spec.lua &amp;&amp; python3 tools/check_release_gates.py --check state-drift</automated>
  </verify>
  <done>Walk blocker behavior has executable Wave 0 coverage without broadening into the Phase 03 route behavior suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GMCP to runtime tests | Synthetic GMCP/test state crosses into automation decisions. |
| Runtime state to Mudlet sends | Owned state determines whether command side effects are allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | tests/boop_runtime_spec.lua | high | mitigate | Add blocker-held assertions before production edits so stale or incomplete state cannot produce target/combat/gold/walk effects. |
| T-02-02 | Tampering | tests/boop_safety_spec.lua | high | mitigate | Assert all automation intent is cleared before flee movement is sent. |
| T-02-03 | Denial of Service | tests/boop_walk_spec.lua | medium | mitigate | Assert repeated unsafe walk attempts remain held without changing external walker install/status behavior. |
</threat_model>

<verification>
Plan-level verification is syntax plus the existing state-drift gate. Full Mudlet Busted execution is required by the final phase validation plan after production implementation satisfies these Wave 0 contracts.
</verification>

<success_criteria>
Wave 0 tests define the runtime blocker, flee cleanup, and walk hold contracts for STATE-01, STATE-02, and SAFE-01 without editing production package behavior or built artifacts.
</success_criteria>

<output>
Create `.planning/phases/02-state-ownership-repair-and-safety-baseline/02-01-SUMMARY.md` when done. Do not commit or push; the orchestrator owns version synchronization, release gates, commit, and push. Before any future commit or push, run `python3 tools/check_release_gates.py --check versions`.
</output>
