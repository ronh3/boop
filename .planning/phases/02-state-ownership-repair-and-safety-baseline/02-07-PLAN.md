---
phase: 02-state-ownership-repair-and-safety-baseline
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified: []
autonomous: false
requirements: ["STATE-01", "STATE-02", "STATE-03", "SAFE-01", "SAFE-03"]
must_haves:
  truths:
    - "All Phase 02 owned-state, blocker, flee, target-loss, walk, trace, status, and dashboard tests are green."
    - "Release gates for versions, manifests, and state-drift are green before any orchestrator commit or push."
    - "A focused human checkpoint confirms compact blocker/status readability."
  artifacts:
    - "Automated evidence from release gates and canonical Mudlet/Busted command."
    - "Human checkpoint result recorded in the plan summary."
  key_links:
    - "tools/check_release_gates.py -> version/manifest/state-drift discipline."
    - "tests/*_spec.lua -> Phase 02 behavior coverage from 02-VALIDATION.md."
    - "boop status/dashboard output -> human readability checkpoint."
---

<objective>
Validate Phase 02 end-to-end and pause for one focused human readability check of compact blocker/status output.

Purpose: Automated tests cover reason-code behavior, but D-34 requires one operator-facing review to confirm the concise blocker/status surface is understandable.
Output: Final automated verification evidence and a human checkpoint result for Phase 02.
</objective>

<execution_context>
@/var/home/ron/.codex/gsd-core/workflows/execute-plan.md
@/var/home/ron/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-PATTERNS.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-01-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-02-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-03-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-04-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-05-SUMMARY.md
@.planning/phases/02-state-ownership-repair-and-safety-baseline/02-06-SUMMARY.md
</context>

## Artifacts This Phase Produces

- `boop.state.combat.blocker` owned blocker snapshot with stable `code`, `label`, `systems`, `waitsFor`, and observed-state fields.
- `boop.runtime.blockerSnapshot()`, `boop.runtime.setBlocker(...)`, `boop.runtime.clearBlocker(...)`, `boop.runtime.shouldHold(system)`, `boop.runtime.clearAttackIntent(reason)`, and `boop.runtime.clearAutomationIntent(reason, opts)`.
- GMCP recovery blockers for `gmcp_ire_missing`, room/target blockers for `missing_room`, `room_partial`, and `target_lost`, and pull blockers for `pull_away` and `pull_timeout_away`.
- Owned-domain walk state in `src/scripts/boop/boop_walk.lua` with `tools/check_release_gates.py` allowing zero flat-state access for migrated Phase 02 files.
- Canonical status/dashboard/trace rendering for blocker code, short label, affected systems, and awaited resume state.

<source_audit>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 02 | Runtime, safety, trace, status, and dashboard agree on canonical owned state and fail closed when game state is incomplete. | 02-01..02-07 | COVERED | Tests, implementation, UI, docs, and validation plans cover the full phase goal. |
| REQ | STATE-01 | Owned domains for room, target, pull, walk, gold, diag, flee, queue, inventory, trace, rage, IH, and gag. | 02-01, 02-03, 02-04, 02-05, 02-07 | COVERED | Runtime domains, attack fallbacks, walk migration, helper tests, and state-drift gate. |
| REQ | STATE-02 | Visible blockers/warnings for GMCP reconnect, missing IRE, partial room/target updates, and unsafe hidden state. | 02-01, 02-02, 02-03, 02-05, 02-06, 02-07 | COVERED | Canonical blocker API, event recovery, walk holds, and UI/status output. |
| REQ | STATE-03 | Trace/status/dashboard report canonical values. | 02-02, 02-06, 02-07 | COVERED | Trace/UI tests and renderer changes. |
| REQ | SAFE-01 | Auto-flee cancels/blocks queue, prequeue, walk, gold, and attack intent before escape movement. | 02-01, 02-04, 02-07 | COVERED | Safety tests and cleanup-before-send implementation. |
| REQ | SAFE-03 | Current target disappearance clears queued attack state and retargets only valid room targets. | 02-02, 02-04, 02-07 | COVERED | Event/pull tests and target-loss cleanup implementation. |
| RESEARCH | R-01 | Add small canonical blocker/cleanup API over owned state. | 02-01, 02-03 | COVERED | `boop.state.combat.blocker` and runtime APIs. |
| RESEARCH | R-02 | Migrate walk blocker reads without full walker rewrite. | 02-01, 02-05 | COVERED | Focused tests and walk migration. |
| RESEARCH | R-03 | Tighten state-drift gate for migrated files. | 02-04, 02-05 | COVERED | Baseline updates after attack/walk migrations. |
| RESEARCH | R-04 | Synthetic GMCP recovery tests are enough; live reconnect belongs to Phase 06. | 02-02, 02-03, 02-07 | COVERED | Synthetic tests and final validation exclude live reconnect as a blocker. |
| CONTEXT | D-01..D-46 | Locked decisions from 02-CONTEXT.md. | 02-01..02-07 | COVERED | Decision IDs are cited in task actions across the plan set. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated Phase 02 gates</name>
  <read_first>
    @CODEX.md
    @tools/check_release_gates.py
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-RESEARCH.md
  </read_first>
  <files>None</files>
  <action>Run the final static release gates and syntax checks for all Phase 02 touched Lua/test files. Run the canonical Mudlet/Busted command exactly: `muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror`. If the local AppImage or Lua 5.1-compatible Busted environment is unavailable, record the exact blocker in the summary and treat GitHub Actions Mudlet/Busted as the required full-suite evidence before phase verification. Always include `python3 tools/check_release_gates.py --check versions --check manifests --check state-drift` to account for the repo's version discipline before any orchestrator commit or push.</action>
  <acceptance_criteria>
    - `python3 tools/check_release_gates.py --check versions --check manifests --check state-drift` passes.
    - Lua syntax passes for all modified `src/scripts/boop/*.lua` and `tests/*.lua` files.
    - Full Mudlet/Busted evidence from the exact full-suite command is green locally or explicitly recorded as requiring CI due to local environment limitation.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_release_gates.py --check versions --check manifests --check state-drift && luac -p src/scripts/boop/boop_runtime.lua src/scripts/boop/boop_events.lua src/scripts/boop/boop_safety.lua src/scripts/boop/boop_walk.lua src/scripts/boop/boop_targets.lua src/scripts/boop/boop_attacks.lua src/scripts/boop/boop_ui.lua src/scripts/boop/boop_ui_registry.lua tests/support/boop_test_helper.lua tests/boop_runtime_spec.lua tests/boop_safety_spec.lua tests/boop_walk_spec.lua tests/boop_event_transitions_spec.lua tests/boop_pull_spec.lua tests/boop_trace_spec.lua tests/boop_ui_spec.lua && muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror</automated>
  </verify>
  <done>Final automated static gates and syntax checks are green, with full-suite evidence recorded or explicitly delegated to CI if local Mudlet is unavailable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-check compact blocker/status readability</name>
  <read_first>
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-CONTEXT.md
    @.planning/phases/02-state-ownership-repair-and-safety-baseline/02-VALIDATION.md
    @src/scripts/boop/boop_ui.lua
    @tests/boop_ui_spec.lua
  </read_first>
  <files>None</files>
  <action>Pause after automated verification and ask the operator to review the compact blocker/status behavior specified by D-34. Do not make source edits inside this checkpoint; record approval or concrete output issues in the plan summary for gap closure.</action>
  <what-built>Phase 02 canonical blocker/status behavior: owned-state blockers, fail-closed GMCP/target/walk holds, flee cleanup, target-loss cleanup, and compact trace/status/dashboard rendering.</what-built>
  <how-to-verify>
    1. In Mudlet or from the test output, create or inspect a synthetic blocker such as `gmcp_ire_missing`, `target_lost`, or `pull_timeout_away`.
    2. Confirm live output shows one concise `[WARN]` transition line and does not repeat while the blocker reason is unchanged.
    3. Run `boop status` and open the main `boop` dashboard; confirm both show the same `code -- label`, affected systems, and waits-for state.
    4. Run `boop trace show 20`; confirm trace includes blocker enter/exit or cleanup detail using normalized owned-state values.
  </how-to-verify>
  <resume-signal>Type "approved" if the compact blocker/status behavior is readable, or describe the specific output issue to fix.</resume-signal>
  <acceptance_criteria>
    - Human review confirms the compact blocker/status surface is understandable.
    - Any readability issue is recorded for gap closure instead of being silently accepted.
    - Live reconnect validation remains Phase 06 evidence and is not required for Phase 02 approval.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_release_gates.py --check versions --check manifests --check state-drift</automated>
  </verify>
  <done>Focused D-34 human readability checkpoint is approved or produces a concrete gap closure item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree to release gate | Static release checks decide whether the tree can be committed or pushed by the orchestrator. |
| Automated evidence to human approval | Tests verify behavior, and the operator validates readability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Tampering | tools/check_release_gates.py | high | mitigate | Run versions, manifests, and state-drift checks before any orchestrator commit/push. |
| T-02-22 | Repudiation | Phase 02 verification evidence | medium | mitigate | Record full-suite status or local environment blocker in the summary. |
| T-02-23 | Denial of Service | human checkpoint | low | mitigate | Use one focused readability checkpoint, with automated reason-code coverage already in tests. |
</threat_model>

<verification>
Final phase verification must include `python3 tools/check_release_gates.py --check versions --check manifests --check state-drift`. The full Mudlet suite command is `muddle && AUTORUN_BUSTED_TESTS=true TESTS_DIRECTORY="$PWD/tests" QUIT_MUDLET_AFTER_TESTS=true PRETEST_PACKAGE="$PWD/build/boop Hunter.mpackage" /tmp/Mudlet.AppImage --profile "GithubTests" --mirror` when the local AppImage environment is available.
</verification>

<success_criteria>
All Phase 02 requirements are covered by passing static gates, passing focused specs or recorded full-suite evidence, and one approved compact blocker/status human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/02-state-ownership-repair-and-safety-baseline/02-07-SUMMARY.md` when done. Do not commit or push; the orchestrator owns version synchronization, release gates, commit, and push. Before any future commit or push, run `python3 tools/check_release_gates.py --check versions`.
</output>
